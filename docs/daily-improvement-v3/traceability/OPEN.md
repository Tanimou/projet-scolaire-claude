# Traceability — OPEN items only

> **This is the file the routine reads every run.** It contains only rows whose status is `open`, `in-progress`,
> `blocked` or `deferred-with-reason`. Closed rows are archived under `traceability/CLOSED-*.md` and are **never**
> re-read during selection — that re-reading is what made every run slower than the last.
>
> Partitioned 2026-08-08 by the throughput change (S1). Nothing was deleted — every row lives in exactly one file.

**When you close a finding:** move its row out of this file and append it to the matching `CLOSED-<layer>.md`,
with its evidence. Never delete a row.


## 1. Layer 0 — Trust and production foundation

| Finding | Epic | Story | Status | Test | Evidence | Gate |
|---|---|---|---|---|---|---|
| PF-03 `db push --accept-data-loss`, no migration history | V3-E01→E02 | S-E02-1 | `in-progress` | `apps/api/src/shared/migrations/migration-state.spec.ts` — **10/10 passed locally** (`pnpm exec jest src/shared/migrations`) | **Ledger established and executed, not asserted.** `apps/api/prisma/migrations/0_baseline/migration.sql` (1849 lines) generated with `migrate diff --from-empty`; applied to scratch DB `v3_e02_deploy_check` in the running `pilotage_postgres`, after which `migrate diff --from-url … --exit-code` returned **0 (schemas identical, 54 tables)** and `migrate status` printed *"Database schema is up to date!"*; a second run reported *"No pending migrations to apply"* (idempotent). Negative path executed too: against the un-baselined dev DB the new `infra/docker/migrate-entrypoint.sh` **refused with exit 1 and wrote nothing**. No executable path invokes `db push` any more (`infra/docker-compose.yml`, `scripts/dev.sh`, `infra/pilotage.sh` — remaining matches are comments only). **Updated 2026-08-07 (run 19) — the local half is now proven THROUGH the artefact, not around it.** The earlier evidence applied `0_baseline` to a scratch database by hand; run 19 reset the real local database and rebuilt it **through the migrator service**: `docker compose --env-file .env -f infra/docker-compose.yml --profile app up -d` applied `0_baseline`, a second run printed *"No pending migrations to apply"* (idempotent), `prisma migrate diff --from-url <local> --to-schema-datamodel --exit-code` returned **0, "No difference detected"**, and the database now holds **55 tables** (54 + the ledger). That path had **never** executed before, because of `PF-84`: `.dockerignore` excluded `infra/docker`, so the migrator exited 2 without ever reaching its own logic. After the fix it **refused the un-baselined database correctly** (exit 1, state `UNBASELINED_NONEMPTY`) before the reset. The restore side of the same guarantee is now rehearsed too — `S-E02-3`, `VAL-03`. **Residual, re-scoped 2026-08-06 (run 19).** The old residual read *"baselining the hosted database is an operator action"*. Under Step −1 that is no longer work this routine owes: the hosted VPS is an audit fixture, not a deployment target, so there is no hosted database to baseline. What remains is a **capability** gap rather than an operator errand — nothing yet PREVENTS a `db push` database reappearing at a new address, which is the class `S-E02-5` owns; the local instance of that drift was measured this run (24 DDL statements) and then resolved by construction by the reset. `docs/runbooks/baseline-hosted-database.md` stays as the procedure for any future deployment. The seed half of PF-03 is `S-E02-4` | G-MIGRATION |
| PF-56 observability/SLO/restore unproven | V3-E02 | **S-E02-13** *(metrics)*, **S-E02-14** *(traces)*, **S-E02-15** *(web)*, **S-E02-17** *(queue third)* | `in-progress` | `apps/api/src/shared/quality/observability-gate.spec.ts` — **33/33**; `apps/api/src/modules/metrics/metrics.spec.ts` — **13/13**; `apps/worker/src/shared/observability/metrics-server.spec.ts` — **6/6**, on a **real socket** | **The finding said "optional rather than proven active". Measured, the profile could not start at all — and that measurement is the run's main result.** `infra/docker-compose.yml` has declared an `obs` profile (Prometheus/Grafana/Loki) since it was written; **all three** of its bind-mount sources (`./grafana/prometheus.yml`, `./grafana/dashboards`, `./grafana/provisioning`) **did not exist in the repository** — 3 of the 8 relative bind-mount sources in that file, and all 3 this profile. Docker creates a *directory* for a missing bind-mount source, so Prometheus would have received a directory where it expects its config file; and no application registered a single metric, so a working Prometheus would have scraped nothing. R-26's sixth instance in seven runs. **What landed:** a Prometheus registry and `GET /metrics` on the API (default process metrics + an HTTP latency histogram and request counter, the two an SLO actually needs), the same exposition on the worker's existing release-manifest socket, the three missing config files written for real (scrape config, provisioned datasource, provisioned 4-panel SLO dashboard), and `scripts/observability-check.js` as **stage 9** of `ci-gate.sh` and a step in `ci.yml`. **The cardinality rule is the security rule:** `/metrics` is unauthenticated by construction, so series are labelled by the **matched route template** and never by the resolved URL — a test drives the label function with a request carrying both and asserts the id does not reach the exposition (G-TENANT), and the gate fails if nginx ever publishes the path. **The check caught its own first defect:** merging compose files with `Object.assign` let `docker-compose.prod.yml`'s *partial* `api` override replace the base definition wholesale, and it reported "listening port cannot be determined" for both applications on its first run; it now merges the way compose merges. **Executed in both directions** — see the run log. **Not closed, and the residual is most of the finding.** *(Updated by run 16: the **trace third is now closed** — `S-E02-14` / `PF-78`. Both Nest applications emit redacted spans, proven by an executed request rather than by a configuration read. What follows is the rest.)* Queue depth / failure / DLQ are not exposed, and no official BullMQ instrumentation exists, so job processing is untraced as well as unmeasured; **no alert rule or SLO threshold is defined** — that is a product decision about what "good" means; `apps/web` is covered by neither surface (`PF-79`); and the restore third stays blocked on **D-01**. Above all, this does **not** start Prometheus and watch a scrape succeed — the profile is proven *coherent and complete*, not *ingesting* **Updated 2026-08-07 (run 24) — the queue third is now EXECUTED against a running container, and that is the first time any observability claim in this row has been proven that way.** `S-E02-17` registers `pilotage_queue_depth{queue,state}`, `pilotage_queue_jobs_total{queue,job,outcome}`, `pilotage_queue_job_duration_seconds{queue,job}` and `pilotage_queue_depth_collection_failures_total{queue}` on the worker registry, adds Grafana panels 5–8, and extends `scripts/observability-check.js` (ci-gate stage 9) so instrumented queues ≡ registered queues. Full `scripts/ci-gate.sh`: **`GATE: PASS`, exit 0, 15/15 stages** (R-23 — the verdict line, not a stage selection), with `test-ratchet` reporting **no drift** on api *and* worker (the `PF-80` check). **Step 2 measured something the rows above could not have known:** `pilotage_worker` was running an image built **2026-06-06**, two months older than `S-E02-10`/`13`/`14`/`15`, whose `dist/` contained neither `shared/release/` nor `shared/observability/` — so `127.0.0.1:4001` refused connections and **0** series existed in the *running* stack while every row above read green. That is not a code defect (`EXPECTED_GIT_SHA` is unset, which by `S-E02-10`’s deliberate design yields `unverified` and blocks nothing) but it is a fact about this routine’s evidence base, recorded as **`PF-111`**. The worker was therefore rebuilt and recreated, per Step −1. **What the rebuild then proved, by execution rather than by unit test:** the container boots healthy, `QueueDepthCollector` logs « Profondeur de file instrumentée pour 3 file(s) : exports, notifications-email, imports », and a real scrape returns **24 depth samples** — 3 queues × **8** BullMQ states, because the implementation measured the state list instead of trusting this story’s six; `prioritized` and `waiting-children` are real states a six-state list would have silently dropped. A probe job was then enqueued with `attempts: 1` and a payload carrying `tenantId: "probe-tenant"`, `exportJobId: "nonexistent-probe-id"` and `requestedBy: "routine-run-24"`; it failed in the processor, and the exposition moved to `pilotage_queue_jobs_total{queue="exports",job="<other>",outcome="failed_terminal"} 1`, `pilotage_queue_job_duration_seconds_count{...} 1`, and `pilotage_queue_depth{queue="exports",state="failed"}` **0 → 1**. **G-TENANT is therefore discharged against the live artefact, not only in a spec:** the job *name* was `probe-terminal-failure`, which is not allow-listed, and it rendered as the `<other>` sentinel; none of the three identifiers appears anywhere in the 12 742-byte exposition (`grep -c` = **0**). Probe removed afterwards; the stack was left **up and healthy**. **Not closed, and the residual is why the PR was NOT auto-merged.** The verify panel confirmed three defects *inside the mechanism this slice makes blocking*: **`PF-104`** — a job that fails by *stalling* emits `stalled`, never `failed`, so an OOM-killed or SIGKILLed worker under-reports precisely when the system is broken; **`PF-105`** — check 9’s rule 6 compares a constant against labels rendered *from* that constant, i.e. a guard that cannot fail; **`PF-106`** — no test executes the `@OnWorkerEvent` seam or `onModuleInit`, **re-scoped by this run’s evidence** from “the capability may be dead” to “the capability is live, proven once by hand, and nothing re-proves it”. Plus **`PF-107`**/**`PF-108`** (the outage signal is plotted on no panel; a shared-resource gauge is `sum`med across replicas, so `--scale worker=2` doubles every line), **`PF-109`** and **`PF-110`**. Alert rules and SLO thresholds remain **undefined** — a product decision, and reason enough for PF-56 to stay open regardless. And nothing was *ingested*: no Prometheus scrape, no Grafana render — the `obs` profile was not started this run **Updated 2026-08-08 (run 26) — it is ingested now, and that sentence is the one this row has carried since run 15.** Every observability claim above stopped at *emitted and coherent*, because starting the profile was believed to be forbidden; under Step −1 it is not, so `docker compose --env-file .env -f infra/docker-compose.yml --profile obs up -d` was run. **All four Prometheus scrape targets report `up`** — `pilotage-api` (`api:4000`), `pilotage-worker` (`worker:4001`), `pilotage-web` (`web:3001`) and `prometheus` itself — with no `lastError` on any of them, which is the first time in this programme that any of the three expositions has been read by something other than the check script that spawned it. Dashboard expressions were then resolved **against the real TSDB rather than against the JSON**: `max by (queue, state) (pilotage_queue_depth)` returned all 24 series, `sum by (queue) (rate(pilotage_queue_depth_collection_failures_total[5m]))` returned one series per queue, and `pilotage_queue_stalled_total` returned the sample the SIGKILL drill produced. **Grafana rendered too**: `GET /api/dashboards/uid/pilotage-slo` returns « Pilotage — SLO » with **nine** panels including the new *Jobs bloqués (stalled) par file*, and the provisioned datasource `pilotage-prometheus` resolves to `http://prometheus:9090`. **Still open, and the reason is unchanged:** alert rules and SLO thresholds are undefined, which is a product decision this routine does not take. **Newly honest limits:** nothing asserts a *panel renders data* (the Grafana API confirms provisioning, not a rendered graph), Loki ingests nothing application-side, and Jaeger was started but no span arrival was asserted — the tracing evidence remains *emitted*, not *ingested* | VAL-09, G-TENANT |
| PF-77 routine's single-writer lock expires mid-run; a concurrent tick reaps it and resets the tree | V3-E02 *(routine)* | — *(no story: the fix is in the routine's own files, outside this repository)* | `open` | n/a — measured from `routine-lock.sh`'s own run log | **Observed live on this run, and it nearly cost the slice.** The lock was acquired at 20:03 with `GATE=OK`; V3's Step 4 asks for a heartbeat around the *build* and nothing asks for one during **Step 3**, which has run over an hour in every run since run 9. At **21:09** a second V3 tick found the heartbeat older than `STALE_MIN` (60 min), **reaped the lock**, took it, and released it at 21:11 — so from 21:09 this run was still writing to the checkout while holding nothing, which is the pile-up the lock exists to prevent. `heartbeat` printing `no lock held` is the only signal, and by then the guarantee has already lapsed. Recovering by re-running `gate` made it worse: the salvage path `git stash`ed every tracked modification and reset the branch to `origin/main`. The work survived **only** because that salvage stash exists (`git stash pop` restored all 9 tracked files; the new files survived by being untracked). Recorded as **R-27** | G-DNC |
| PF-80 sprint workflow reports `landed: true` on a change that breaks a spec outside the directory it verified | V3-E02 *(routine)* | — *(story to be written)* | `open` | n/a — measured from run 17's Step 4 | Discovered by `S-E02-15`, and it is the reason that slice's ledger is trustworthy rather than a defect in it. Widening `TRACED_SERVICES` in `packages/contracts` broke `apps/api/src/shared/tracing/tracing.spec.ts`, which pinned the old two-entry list. The sprint ran the specs it authored (`src/shared/quality` — green) and returned `landed: true`; `node scripts/test-ratchet.js api` in the routine's Step 4 reported **1 NEW test failure**. The general shape: **a shared-contract edit has a blast radius the editing agent cannot see from the directory it is working in.** Two consequences, both already acted on this run — the routine must run the **whole** gate before merging and report its verdict line (**R-23**), never a stage selection; and the workflow's readiness step should run the ratchet, which is a change to `bmad/workflows/sprint.workflow.js` and belongs in its own slice | G-DNC |
| PF-01 `demo` tenant fallback | V3-E01 | S-E01-1 | `open` | — | — | G-TENANT |
| PF-02 RLS claimed, not implemented | V3-E01 | **`S-E01-2`** *(moitié (b) close ; moitié (a) intacte)* | `in-progress` | `apps/api/src/shared/prisma/prisma.service.spec.ts` — faux client sans base ni client généré | **La moitié (b) — le tenant interpolé dans le texte SQL — est supprimée ; la moitié (a) — « RLS annoncée, pas implémentée » — reste VRAIE et c'est pourquoi la ligne n'est pas fermée.** `prisma.service.ts:29` lisait `$executeRawUnsafe("SET LOCAL app.current_tenant_id = '" + tenantId + "'")` ; le contexte part désormais en **paramètre lié** via `SELECT set_config('app.current_tenant_id', $1, true) AS applied` (template balisé `$queryRaw`), un id non-UUID canonique est **refusé avant `$transaction`** (`TenantContextError` exportée ; non-string refusé avant la regex, aucun `trim()`/casse/retrait — refus, pas assainissement), et la valeur renvoyée par `set_config` est **relue** puis comparée en `===` à l'id demandé, sinon la transaction est abandonnée et `fn` n'est jamais appelé. **Ce qui reste faux après ce diff, mesuré et non déduit :** zéro `ENABLE ROW LEVEL SECURITY`, zéro `CREATE POLICY` dans tout le dépôt (`0_baseline` compris) et **zéro appelant** de `withTenant` — un grep ne rend que sa propre définition. Le helper est donc la **couture d'application future**, pas un mécanisme d'isolation actif, et son docblock le dit maintenant explicitement (les deux propositions de l'ancien commentaire étaient mesurablement fausses). `ADR-032` est écrit en `accepted (partial)` : D1 paramètre lié, D2 refus fail-closed avant transaction, D3 relecture, D4 aucun contournement (DNC-10) sont **décidés et exécutés** ; la moitié policy RLS + prédicat applicatif reste **proposée**. `ADR-002` est annoté en tête et au-dessus de sa §Decision plutôt que réécrit. **Deux pièges sont consignés dans `ADR-032` comme prérequis DURS de la story RLS**, parce qu'ils sont les deux façons dont cette story livre du vert en ne protégeant rien : (1) le **propriétaire des tables contourne RLS** sans `FORCE ROW LEVEL SECURITY` — avec le montage Prisma courant le rôle applicatif possède les tables, donc policies + tests verts + isolation **nulle** ; (2) `current_setting('app.current_tenant_id')::uuid` **sans son second argument `missing_ok`** lève `42704` sur toute connexion sans contexte (et `22P02` sur chaîne vide), ce qui casse migrations, seeds, health checks et tous les jobs dès le premier jour. Fermeture de la ligne = quand une policy existe, s'applique au rôle que l'API utilise réellement, et qu'un test cross-tenant le prouve | G-TENANT, G-DNC |
| PF-18 student client aliased to parent | V3-E01 | S-E01-4 | `open` | — | — | G-AUTHZ |
| PF-08 global custom roles (cross-tenant) | V3-E05 | **S-E05-13** *(renumbered from `S-E05-1`, 2026-08-10)* | `open` | — | **Id collision, resolved in favour of the operator override.** The 2026-08-10 override named `S-E05-1` as the CSV-neutraliser slice (`PF-168`), which has now landed under that id. This row had never been enumerated or implemented under `S-E05-1`, so nothing was overwritten — but two ids meaning two things is how a later run implements the wrong slice, so it is renumbered here and in [`docs/spec/features/v3-e05/PROGRESS.md`](../../spec/features/v3-e05/PROGRESS.md). `S-E05-12` was the highest id in use; `13` is the next free one | G-TENANT |
| PF-178 **P1** *(product decision, raised by `S-E05-2`)* — a correct ceiling refuses a `school_admin` the `teacher`, `parent` and `student` roles, and nothing replaces that path | V3-E05 → **`ADR-015`** / `open-decisions.md` | — *(story to be written — `S-E05-2b`)* | `open` — **measured, then shipped fail-closed on purpose** | `users.service.spec.ts` asserts the refusal; no test asserts a replacement path, because none exists | **This is the ceiling working, and it is still a real product gap.** Measured against `permissions.constants.ts`: `school_admin` holds **75** codes; `teacher` (34) exceeds it by **6** (`grades.write`, `grades.revise`, `attendance.write`, `lessons.write`, `lessons.delete`, `exports.execute.teacher`), `parent` (20) by **3** (`guardianships.claim`, `exports.execute.parent`, `remediation.book`), `student` (7) by **5** (the five `*.read.self`). `seed.ts:216-220` materialises those into real `rolePermission` rows, so the DB roles genuinely carry them. Net: **a `school_admin` can now assign none of the four other seeded system roles** — `super_admin` correctly (that is `PF-156`'s point) and `teacher`/`parent`/`student` as collateral, on the admin onboarding path. **The ceiling was deliberately not weakened to preserve the old behaviour**, because the old behaviour *is* the P0: the same mechanism that let an admin hand out `teacher` let them hand themselves `grades.revise`. The answer is a delegation model — an explicit assignable-roles grant, or a grantor-relative realm-role ladder permitting `school_admin → teacher\|parent\|student` while refusing `teacher → school_admin` — which also closes `PF-09`'s residual. **Needs a human ruling before implementation**; recorded in `open-decisions.md` as `D-12` | G-AUTHZ |
| PF-10 coefficient matrix foreign-tenant write | V3-E05 | S-E05-3 | `open` | — | — | G-TENANT |
| PF-11 notification dedup not tenant-scoped | V3-E05 | S-E05-4 | `open` | — | — | G-TENANT |
| PF-07 attendance reads without ABAC | V3-E05 | S-E05-5 | `open` | — | — | G-AUTHZ |
| PF-51 unvalidated PATCH / query params / enum | V3-E05 | S-E05-6 | `open` | — | — | G-AUTHZ |
| PF-46 public unthrottled registration — **throttling third CLOSED**, `emailVerified` third still open | V3-E05 | S-E05-7 | `in-progress` | `register-throttle.supertest.spec.ts` "429s, and `createUser` was called exactly TIER1_MAX times" · `public-endpoint-throttle.spec.ts` · `register-throttle.guard.spec.ts` (56/56 green, jest run observed) | `POST /auth/register-parent` now refuses above a two-tier fixed-window bound **before the pipes and therefore before every Keycloak call**. Proved over a booted Nest app, not over a hand-built limiter: the (TIER1_MAX+1)th request is 429 **and `keycloak.createUser.mock.calls.length` is unchanged**. **NOT closed** — see the narrowing note below |
| PF-25 wrong password → "MFA required"; `mfaEnabled` hard-coded | V3-E05 | S-E05-8 | `open` | — | — | G-AUTHZ |
| PF-26 logout/session.error/phantom routes | V3-E05 | S-E05-9 | `open` | — | — | G-AUTHZ |
| PF-52 unused `hasPermission`, `users.suspend` unimplemented | V3-E05 | S-E05-10 | `open` | — | — | G-AUTHZ |
| PF-53 non-atomic invite/permission rewrite, catalogue drift | V3-E05 | S-E05-11 | `open` | — | — | G-AUDIT |
| PF-04 incompatible counts across portals | V3-E03 | S-E03-1..n | `open` | — | — | G-TRUTH, G-PORTAL |
| PF-05 parent grades page returns zero | V3-E03 | S-E03-2 | `open` | — | — | G-TRUTH, G-PORTAL |
| PF-12 parent child/enrollment self-contradiction | V3-E03 | S-E03-3 | `open` | — | — | G-TRUTH |
| PF-15 stale active academic year | V3-E03 | S-E03-4 | `open` | — | — | G-TRUTH |
| PF-20 dashboard vs queue/rule totals | V3-E03 | S-E03-5 | `open` | — | — | G-TRUTH |
| PF-24 snapshot queue has no consumer | V3-E03 | S-E03-6 | `open` | — | — | G-TRUTH |
| PF-36 teacher count variance 43/46/48, 25/26 | V3-E03 | S-E03-7 | `open` | — | — | G-TRUTH |
| PF-40 async contradictory interim KPIs | V3-E03 | S-E03-8 | `open` | — | — | G-TRUTH |
| PF-50 unpaginated/fan-out hotspots | V3-E03 | S-E03-9 | `open` | — | — | G-TRUTH |
| PF-31 missing audit rows, hard-coded actor role, empty chain fields | V3-E04 | **S-E04-1** *(provenance seam + ADR)*, **S-E04-3** *(client IP/UA)*, **S-E04-6** *(five families in-transaction)*, **S-E04-7** *(remaining sites + blocking gate)*, **S-E04-8** *(hash chain)* | `in-progress` — **the actor-role literals are closed by `S-E04-1` (run 29); nothing else is** | `apps/api/src/shared/audit/provenance.spec.ts`, `provenance-callsites.spec.ts`, `shared/quality/audit-provenance-gate.spec.ts` — executed, with a negative control that restored the collapsed files to `HEAD` and observed **14 red / 101 green** | **Run 29 (`S-E04-1`) closed the 8 `actorRole: 'school_admin'` literals, the 9 `portal: 'admin'` write literals and two anonymous inline derivations the intake had not measured, onto one `deriveAuditProvenance(jwt)` in `apps/api/src/shared/audit/`, with `ADR-036` pinning the proxy-hop decision. It did NOT close the column: `PF-121` (`packages/imports-core`), `PF-122` (`child-claims.service.ts` parametrises both fields and writes `actorRole: 'admin'`, not a realm role) and `PF-123` (`assessments.controller.ts` writes neither field) are all inside or adjacent to the same surface and all invisible to the new gate — `AC-1`'s "exactly one file decides an actor role" was narrowed in the same land pass rather than ticked. Transactionality (`S-E04-6`), IP/UA (`S-E04-3`), the vocabulary (`S-E04-4`) and the chain (`S-E04-8`) are untouched.** *(Original run-28 measurement, unchanged:)* **Reproduces in full, and is now quantified rather than restated.** Live local database: `select count(*), count(hash), count(prev_hash), count(ip_address), count(user_agent) from audit_log` → **54 \| 0 \| 0 \| 0 \| 0** — chain and provenance columns populated on **zero** rows (`M-1`…`M-3`); `actor_role` is `school_admin` on **53 of 54** (`M-4`). **Where the finding's wording is wrong, and it matters for scoping:** *"hard-coded actor role"* is true of **8 of 28** `apps/api` sites, not all (`M-6`) — the other 20 already derive it, so a sweep assuming 28 would rewrite 20 correct sites. Sites: `identity/invite.controller.ts:155`, `identity/roles.controller.ts:135`/`:185`/`:219`, `imports/imports.service.ts:440`, `integrations/integrations.service.ts:639`, `school-structure/academic-years.controller.ts:266`, `school-structure/subjects.controller.ts:227`. **Where the finding is *understated*:** the write surface is **30**, not 28 — `packages/imports-core/src/engine.ts:197`/`:288` write two more (`M-7`), and being outside `apps/api/src` they **cannot import** an `apps/api/src/shared/audit/` home, so the kit states plainly that they inherit neither the chain nor IP/UA in this epic. **AC-2 reproduces at two levels:** only **6 of 28** sites write inside a `$transaction` (`M-32`), and `identity/roles.controller.ts` `create()` writes the role then a *separate* audit row with no transaction while `update()` opens one and writes the audit row *outside* it (`M-9`); more sharply, **role grant/revoke — AC-2's first named family — writes no audit row at all** (`identity/users.service.ts:57`, `:74`), as do `modules/schools/` and `modules/enrollments/` (**0** `auditLog` references each, `M-33`) — those are *absent*, not misplaced. AC-2's finance clause is **vacuous today**: no finance module exists (`M-30`). **The inherited decision, now scoped by measurement** (`docs/spec/features/v3-e06/PROGRESS.md:370`): `trust proxy`/`trustProxy` has **0 occurrences** across `apps/api/src` + `infra/` and `main.ts:37` is a bare `NestFactory.create`, so `req.ip` is the socket peer and would store the web container's address identically for every actor forever, while blanket `X-Forwarded-For` trust is **strictly worse than blank** (client-forgeable). Resolved in the kit as a pinned-hop-count ADR ordered **first**, with the hash chain ordered **last** — a chain over a literal `actorRole` and a proxy IP is a cryptographically verifiable record of falsehoods. Pre-V3 backfill stays impossible (risk `A-01`); the genesis gap is documented, never fabricated | G-AUDIT |
| PF-145 **P1** — only `admin` can be authenticated by the e2e harness; teacher, parent and student all fail `otp_required` | V3-E05 *(raised by run 32)* | — *(story to be written)* | `open` | n/a — measured from the auth log during the `PF-135` run | With `PF-144` fixed, three of the four `authenticate *` setups still fail, and **not** on a selector: `[auth][cause] CredentialsLoginError: otp_required`, raised for `teacher`, `parent` and `student` but not for `admin`. So even a repaired harness can only ever cover **one of four portals** — precisely the coverage `G-PORTAL` asks for, and the reason `ADR-023`'s authenticated layer exists. Two readings, and the ledger should not pick one by guessing: either the local realm carries `CONFIGURE_TOTP` as a required action on those three seeded users (a seed/realm defect — `S-E06-1`'s `ActivationHint` says exactly that `CONFIGURE_TOTP` is added for `school_admin`/`teacher`), or the flow genuinely needs a second factor these fixtures cannot supply (a harness gap needing a TOTP-capable fixture). **Measure which before writing the story** — `kcadm get users -r pilotage-scolaire --fields username,requiredActions` settles it in one command. Distinct from `PF-25`, which is about a *wrong password* reporting « MFA required » | G-AUTHZ, G-PORTAL |
| PF-146 **P2** — the e2e reachability guard turns « cannot tell » into a green skip, and that is how `PF-144` hid | V3-E02 *(gate power)* | — *(story to be written)* | `open` | n/a — reproduced twice during run 32 | `auth.setup.ts`'s `originReachable()` aborts its probe after **4 s** and treats the abort as *« no stack booted → green skip, no false red »*. Against `next dev` a cold route compiles in **5–17 s** (measured this run: `/teacher/login` 5.9 s, `/student/login` 5.3 s, `/admin/dashboard` **16.6 s**), so a perfectly healthy stack reads as *down* and the whole authenticated layer skips green. That is the **DNC-08 posture inverted** — an unclassifiable state reported as a pass rather than as a problem — and it is the mechanism that let `PF-144` sit undetected for as long as it did. The intent (never a false red when the stack is genuinely absent) is right; the implementation cannot distinguish *absent* from *slow*. Fix: raise the probe timeout well above a cold compile, and make **timeout** distinct from **ECONNREFUSED** — a refused connection skips, a timeout fails loudly. Same family as `PF-105`: a check whose green means nothing | G-DNC |
| PF-148 **P2** — the audit page's focus indicator is 1.42:1, and this slice adds three more controls that use it | V3-E04 | **`S-E04-7`** *(or a V3-E02 a11y slice)* | `open` — raised by run 33's a11y lens | — *(none — `apps/web` still has no component unit runner, `PF-135`)* | `focus-visible:outline-none` + `focus-visible:ring-blue-400/40` composites to **#bfdbfd — 1.42:1** against the adjacent surface, well below SC 1.4.11's 3:1, so there is effectively **no perceivable keyboard focus state**. **The reviewing agent called this "a straight regression" on the two filter controls; that is measured false and the correction matters for severity** — the two pre-existing selects keep their own `focus:ring-blue-400/30` **and** a `focus:border-blue-400` border change, and this diff does not touch them. What is true: the slice adds **three new** interactive controls (`AuditTable.tsx:280`, `AuditPageFilters.tsx:318` and `:351`) that instantiate the same low-alpha house ring, and the pattern exists in only **3** files repo-wide, so it is narrow enough to fix in one pass. The sharp end is `AuditTable.tsx:280`: that chevron is the **only keyboard route to the detail drawer this slice created**, so the slice adds a keyboard path and then makes its focus state almost invisible. Fix: raise the ring alpha and pair it with a border/offset change, as the untouched selects already do | G-DNC |
| PF-150 **P2** — the epic's remaining order is asserted in prose, in one story's report, and nowhere a run can read it | V3-E04 | **`S-E04-7`** *(re-pointed 2026-08-09 — `S-E04-6` did not encode it)* | `open` — raised by run 33 | — | The sequencing ruling that `S-E04-8` (hash chain) stays **last** — because a chain computed over provenance that is not yet true would be a cryptographically verifiable record of falsehoods — lives only in run reports and story prose. `dependency-map.md` does not encode it, so the ordering survives on the next run remembering it. Record it as an explicit `blockedBy` on `S-E04-8` rather than as a paragraph. **⚠️ Id collision, noted 2026-08-09, not resolved here:** `docs/spec/features/v3-e04/PROGRESS.md` uses **`PF-150`** for a different finding — the `portal = ''` gap at `analytics.service.ts:3654`. Two registers, one number: the `PF-120` shape recurring, this time for finding ids rather than measurement ids. Whoever renumbers must do it in both files at once | G-DNC |
| PF-153 **P2** *(latent — becomes live with `ADR-013`)* | V3-E04 → **V3-E05** | **`V3-E05` / `ADR-013`** | `open` — raised by `S-E04-6` (run 34) | `apps/api/src/modules/identity/users.service.spec.ts` has G-TENANT negatives for a foreign `userId` and a foreign `userRoleId` — **and none for a foreign role**, which is the gap | `users.service.ts:85` — `assignRole`'s `prisma.role.findUnique({ where: { id: roleId } })` is unfiltered and **stays** unfiltered. `Role` has no `tenantId` (`schema.prisma:900`); tenancy runs `schoolId → School.tenantId`, and `schoolId: null` means global — so adding a tenant filter to a global catalogue, inside an audit slice, would be a visibility change dressed as a fix. **`ADR-035` D9 originally asserted that `S-E04-6` closes this; the ADR text was corrected in the land pass to record the deferral**, with `Role.schoolId` named as the tenancy path. An ADR recording a check that does not exist is worse than one recording the gap, because the next slice reads it as done. Latent because `roles.controller.ts:120` creates every custom role with `schoolId: null`; live the moment `ADR-013`'s school-scoped roles land — at which point `GET /roles` (`roles.controller.ts:70`, unfiltered, returns every role with its full permission-code set) is a cross-tenant catalogue disclosure and `POST /users/:id/roles` a cross-tenant grant. Fix: `role.schoolId === null \|\| role.school.tenantId === tenantId`, plus the foreign-role negative | G-TENANT |
| PF-154 **P2** *(pre-existing)* | V3-E04 | **`S-E04-7`** or **`V3-E05`** | `open` — raised by `S-E04-6`'s test-architect lens | — | `enrollments.controller.ts` `transfer` — the capacity pre-check (`target._count.enrollments >= target.maxStudents`) runs **outside** the transaction, so a concurrent enrolment can fill the target section between the check and `tx.enrollment.create`. Not introduced by `S-E04-6` and not widened by it; registered because that slice is what made the two writes atomic, and therefore what makes the remaining race legible. **Why it matters at this severity:** it is the one live path where the second entity write fails after the first succeeded, and without atomicity a child is transferred **out** of their class and **into nothing** — enrolled nowhere, children's data. Atomicity now holds; the race still produces a spurious 500. Fix: a conditional `create` or a unique constraint — **not** a longer transaction | G-TENANT |
| TOOL-13 **P1** — the drift gate's whole end-to-end block, including *"the unmodified repository PASSES"*, silently becomes `describe.skip` when no database answers, and the ratchet reports that as GREEN | V3-E02 | **`TOOL-13`** — `docs/spec/features/v3-e02/stories/TOOL-13.md` (spec landed #228, implemented run 45) | **`closed`** — 2026-08-12 (run 45, `ci/2026-08-12-v3-e02-tool13-skip-ratchet`). **Measured by the routine, not by an agent:** the two spec files report `{"total":147,"passed":142,"failed":0,"pending":5}` (51.9 s, exit 0) and that real report has `numFailedTests: 0` — which `HEAD`s ratchet calls `✓ no drift.` The same report fed to `scripts/lib/ratchet-core.js` reduces to `skipped = {"src/shared/quality/schema-drift-gate.spec.ts": 5}` — the exact five that never ran. A baseline two lower yields `skipRises [{from:3,to:5}]`; a **matching** baseline yields `[]` and `missingSuites []` (the control, without which the rise case is satisfied by an always-fail comparator). **AC-5s mutant measured on that same report:** a `[skipped]`-only set counts **0** against `NOT_EXECUTED_STATUSES` **5**, and the statuses actually present are `["passed","pending"]` — jest 29.7.0 never emits `skipped` for `describe.skip`, so the story §1.1 measurement is confirmed rather than inherited. **Proven end-to-end by `ci-gate.sh` itself**, on both apps: `⚠ test-ratchet[api]: this baseline records no skipped counts. The skip ratchet is INACTIVE for this app` and `✓ test-ratchet[worker]: no drift (skipped-count ratchet INACTIVE — baseline has no "skipped" block).` — AC-9s qualified verdict verbatim, so the green is never unqualified. ⚠️ **Ships DISARMED by design:** `apps.<app>.skipped` is deliberately absent because it may only be written from a complete run, which this slice was forbidden to produce (`TOOL-16(b)`). **Arming it is an operator step:** `node scripts/test-ratchet.js <app> --update` from a complete run | `apps/api/src/shared/quality/schema-drift-gate.spec.ts` — `describeWithDb = reachable ? describe : describe.skip` | Pre-existing, **not introduced by `TOOL-10`**, and stated because the preflight now sits upstream of it: `probeServer()` at module scope decides whether the end-to-end block runs, and `test-ratchet.js` compares **failures against a baseline, never test counts**, so tests that stop existing are invisible to it. The consequence is the one direction a gate must never fail in: if the preflight ever answered `refused` on a healthy server, the cases that prove the gate is *not red on correct code* would vanish and the ratchet would report green. `TOOL-10` mitigates the specific risk (three states, only `ECONNREFUSED` short-circuits, and a real `net.createServer()` listener proves the positive `open` case) but does not remove the structural one. Fix direction: have the ratchet fail when a suite's *skipped* count rises above its baseline, so a disappearing case is a red like any other. **Closing it requires a reachable PostgreSQL on `127.0.0.1:5433`** — the same precondition `S-E01-2b` needs | G-DNC, G-MIGRATION |
| TOOL-11 **P2** — `exec()`'s new cross-server guard throws, and `exec()` is reached from a `finally`, so a future caller could end a run with no verdict at all | V3-E02 | **`TOOL-13`** *(batched §5)* | **`closed`** — 2026-08-12 (run 45, `ci/2026-08-12-v3-e02-tool13-skip-ratchet`). `exec()`s cross-server guard now **returns** `{ok:false, detail}` carrying the same refusal text instead of throwing, so a throw can no longer escape a `finally` past `report(state)` and end the run with no verdict (`DNC-08`). Driven through the exported `openSqlRoutes` seam rather than asserted from source, with a `finally`-shaped reproduction whose sentinel proves the block completes, and a **same-server negative control** without which the case would pass against an `exec` that refuses everything. The pre-existing `AC-P7` case that asserted `.toThrow` moved with it — the one deletion in that file, flagged in advance by this runs pre-mortem | — | `scripts/schema-drift-check.js`'s `exec()` gained a guard refusing a URL whose host:port differs from the one the routes were opened and probed for — correct in itself, and it exists so the memoised preflight can never answer for the wrong server. But its own docstring two lines above still reads *"Returns `{ ok, detail }` rather than throwing"*, and `exec()` is reached from `dropScratch()` ← `cleanup()` ← the `finally` of `check()`, and from the `SIGINT`/`SIGTERM`/`uncaughtException` handlers. A throw on that path escapes past `report(state)` and ends the run **with no verdict** — `DNC-08` committed by the anti-`DNC-08` machinery. **Unreachable today** and that was checked rather than assumed: `deriveMaintenanceUrl` and `buildScratchUrl` replace only the database path segment, and a spec case pins the latter to exactly that. Recorded rather than widened into this diff because the repair is a one-line change of shape (`return { ok: false, detail: … }`) that belongs with a case driving the cleanup path, not bolted onto a slice about latency | G-DNC |
| TOOL-12 **P2** — routes A and B carry no spawn bound, so the `indeterminate` branch — the one that most resembles a real CI stall — descends an unbounded ladder on any host that ships a PostgreSQL client | V3-E02 | **`TOOL-13`** *(batched §6)* | **`closed`** — 2026-08-12 (run 45, `ci/2026-08-12-v3-e02-tool13-skip-ratchet`). `PSQL_HOST_TIMEOUT_MS = 120000` (route B) and `PRISMA_RUN_TIMEOUT_MS = 300000` (route A), both named, both exported, both **data-plane** sized per `DOCKER_EXEC_TIMEOUT_MS` own rationale, and pinned as *different* from the control-plane bounds so collapsing them later is a visible edit. **Non-vacuity verified against the shipped file with a paren balancer before being trusted:** `run(psql` and `run(cli.command` are **1 site each at `HEAD`, neither carrying `timeoutMs`; 1 site each now, both carrying it** — genuinely red-before / green-after, asserted on comment-blanked source so a bound named only in a comment cannot satisfy it | — | `TOOL-10` bounded the two `docker` spawns and the `pnpm` fallback; `run('psql', …)` and `run(cli.command, …)` still pass no `timeoutMs`. Measured with the same paren-balancing extractor the spec uses: `run('docker'` → 2 sites, both bounded; `run('psql'` → 1 site, unbounded; `run(cli.command` → 1 site, unbounded. It does not bite **here** only because `psql` is `ENOENT` on this Windows host, which is why the blackhole run still finished in 7 603 ms — on `ubuntu-latest`, where `ci.yml` runs and a PostgreSQL client ships, `psql`'s default `connect_timeout` is unset and the OS TCP timeout is ~130 s. So the fast path (`refused`) is now bounded on every platform, and the `indeterminate` path is bounded only on this one. Fix direction: the same `options.timeoutMs` already in `run()`, applied to routes A and B with data-plane-sized numbers (see `DOCKER_EXEC_TIMEOUT_MS`'s rationale — a bound that kills a legitimate `CREATE DATABASE` is a false red, not a safeguard) | G-DNC |
| TOOL-16 **P1** — the ratchet reported `<suite failed to load>` and threw the cause away *(half **(a)** CLOSED; half **(b)** OPEN)* | V3-E02 | **`TOOL-13`** *(batched §3.4 — (a) only)* | **(a) `closed`** 2026-08-12 (run 45) · **(b) `open`** | `test-ratchet.spec.ts` case 11 — cause non-empty, contains the fixture reason, carries no ANSI, truncated with a stated marker; the `suite.failureMessage` mutant asserted to fail | **(a)** `scripts/test-ratchet.js` synthesised the sentinel and discarded jest own explanation, so an operator got a symptom and no cause. Now read from **`suite.message`** — the story correction is right and was re-verified: `failureMessage` **does not exist** in jest 29.7.0 `--json` output, so implementing against it would have shipped a fix that is green and prints nothing. ANSI-stripped, truncated behind `LOAD_FAILURE_CAUSE_LINES` with a stated marker, printed for **every** load-failed suite. **It paid for itself immediately:** the two `ci-gate.sh` runs of this very slice each hit a load failure, and both were diagnosed in minutes from the printed cause — under `HEAD` both would have read `<suite failed to load>` and nothing else. **(b)** the underlying suite non-determinism remains open — see `TOOL-17`, which is its measured mechanism | G-DNC |
| TOOL-17 **P1** — a spec that writes a probe file into the REAL working tree races every spec that walks-and-reads that directory, so the api suite fails to load a *different* suite on each run | V3-E02 | **`TOOL-17`** — `docs/spec/features/v3-e02/stories/TOOL-17.md` *(storified and shipped run 46, 2026-08-13. The hermetic-scanner design call `TOOL-15` is parked on was **not** taken: this is the tolerate-the-race half, not the remove-the-race half)* | `closed` 2026-08-13 (run 46) — **the walked-read half only**; three residuals carried to `TOOL-17(b)`, and `TOOL-15` / `TOOL-16(b)` stay open. **A FOURTH residual was measured after the fact by this run own gate and is `TOOL-18`:** the same race lives in the CHECK SCRIPTS, it turned run 1 of `ci-gate.sh` red on this very branch, and the tolerant seam shipped here is deliberately the WRONG fix for it | `scripts/lib/walk-read.js` (the one four-step `catch`) + `apps/api/src/shared/quality/walk-read-gate.spec.ts` — **17 cases**, incl. AC-5 (the PRE-SLICE construction throwing `ENOENT` on the vanished path) and a non-`ENOENT` errno aborting the whole map; **nine** walked-read sites converted across the five victims, each asserting `map.size + skipped.length === walked.length` and `skipped.length <= MAX_VANISHED_FILES` (5). ⚠️ **typechecked, NOT executed** — no jest run in run 46. **Residuals:** a sixth victim at `apps/api/src/shared/audit/write-audit.spec.ts:416`; the FR-4 leak (`MAP.get('<literal>') ?? ''` is now served out of a tolerant map, so a skipped **named** file becomes `''`); and the shared cap of 5 applied to `portal-landing-gate`'s 10-file `apps/web/tests` corpus | **This is `TOOL-15` generalised from one spec pair to a class, and it is the measured mechanism behind `TOOL-16(b)`.** Two runs of `node scripts/test-ratchet.js api` on **one unchanged tree** produced **two different failure sets**, each a suite that failed to LOAD with `ENOENT` on a probe file another spec had written into the shared checkout and then deleted: run 1 — `audit-vocabulary-gate.spec.ts` on `apps/api/src/shared/quality/__audit_write_probe.ts` (written by `audit-write-gate.spec.ts:689`); run 2 — `portal-landing-gate.spec.ts` on `apps/web/src/lib/__csv_escape_probe.tsx` (written by `csv-escape-gate.spec.ts`, i.e. **`TOOL-15` own probe**). The window is narrow and scheduling-dependent: `audit-vocabulary-gate.spec.ts` passes **73/73 alone** (32 s) and the two racing specs pass **149/149 together** (63 s) — it only opens under the full parallel suite, which is why it reads as flake rather than as a defect. **The victims are innocent**: both scanners do the correct thing — `walk()` the tree, then `readFileSync` each file — and the file vanishes between the two. **The consequence is the one that matters for a merge gate:** `GATE: FAIL` on a gate-machinery diff is not evidence about that diff until it is reproduced, and `AUTO-LAND` `green` cannot be discharged for this class of diff at all. Fix direction (shared with `TOOL-15`): probe in a scratch tree, or make the walkers tolerate a file that disappears between `walk` and `read` — the second is a two-line change and fixes every current and future victim, the first is the hermetic repair `TOOL-15` is blocked on. **Correction, run 46:** the second was **not** a two-line change. A two-line `try/catch` is precisely the `DNC-08` fix that would have turned five deliberate load-time failures into green ones; making it safe took a 241-line seam, a 550-line proof spec, and an accounting identity plus a skip cap at each of **nine** sites — and it still only reduces the flake rate. The estimate was wrong in the direction that matters, and it is left in place rather than edited so the next estimate of this shape reads it | G-DNC |
| TOOL-19 **P2** — the Docker engine API is wedged, not the CLI, so three runs of "bound the docker command" were aimed at the wrong layer | V3-E02 | — *(recorded with its measurement by run 47; **not** storified — the repair is an operator action on this host, not a code change)* | `open` — **measured 2026-08-13 (run 47)** | n/a — measured directly against the engine over its named pipe, not through a spec | **The Docker ENGINE is wedged, not the docker CLI — three runs bounded the wrong layer.** Runs 44, 45 and 46 each recorded `docker ps` hanging (>150 s, then >12 min, with bash `timeout` failing to kill it) and concluded the defect was the docker CLI on Windows; run 44 added `spawnSync`-level bounds on that basis. Measured a layer down this run: `//./pipe/docker_engine` **accepts a connection**, so the daemon is listening and the pipe exists, but `GET /v1.43/containers/json?all=1` returns **nothing in 15 000 ms and then `ECONNRESET`s**. The CLI is not slow — it is blocked on an engine that never answers, and **no client-side bound can repair that**; it can only make the client give up sooner. `dockerd` and `com.docker.service` are both resident, alongside **11 orphaned `docker` CLI processes dated Aug 10–12** and **5 `docker-ai` processes** (newest 2026-08-13 02:06), the latter notable because this project's standing configuration records `EnableDockerAI=false`. **Consequence:** Step −1 tells the routine to rebuild a stale local stack, and that instruction is currently **not executable on this host** — `docker compose build` cannot reach an engine that will not answer. Everything parked behind "settle the database" (`S-E01-2b` RLS, `TOOL-13`'s drift-gate half, and the live-PostgreSQL path of `TOOL-10`'s preflight, which has still never executed) stays blocked for an **8th consecutive run**. The unblocking action is an **operator restarting the Docker engine**, after which the 11 orphans should be reaped. Two cheap standing checks, so no future run re-measures the CLI: `127.0.0.1:5433` refuses in **5 ms**, and the named-pipe probe above distinguishes *no stack* from *no engine*. | G-DNC |

| PF-181 **P2** — the new 429 renders above a permanently disabled submit button, so a throttled parent reads "retry in a few minutes" and then cannot | V3-E06 | — *(recorded, deliberately not storified this run — routine triage)* | — | `apps/web/src/app/parent/register/ParentRegisterForm.tsx` — the refusal copy this slice ships (`REGISTRATION_THROTTLED_MESSAGE`) instructs the registrant to wait and retry, but the form's submit control does not re-enable after the failed submission, so the instruction is unfollowable in the UI. **`apps/web` = track c's seam**, not track b's — recorded rather than reached across. Surfaced by the escalation panel as `R-6` | G-PORTAL |
| PF-182 **P2** — the in-process bound is per-replica and pre-validation; the edge `limit_req` companion does not exist | V3-E05 | — *(recorded, not storified this run)* | — | Two residuals of `S-E05-7`, both **outside track b's seam** and both written into `ADR-038` (D2, D4) rather than left to be found. (a) The counters are **module-level and therefore per-process**: `infra/docker-compose*.yml` declares no `replicas` today, so the ceiling holds, but `--scale api=N` silently multiplies it by N with no test going red. (b) The guard runs **before the `ValidationPipe`**, so `curl -d '{}'` in a loop spends tier-2 budget while reaching **zero** Keycloak calls — paying the availability cost without the amplification benefit ever being at stake. The real fix for both is an edge `limit_req` in `infra/nginx/conf.d/pilotage.conf` (note: the strict 10 r/m `auth_zone` at `:142` matches the web **page** routes and has never covered `/api/v1/auth/*`, and the local `--profile app` topology has no nginx at all) | G-AUTHZ |

| PF-174 **P1** — `/admin/users` swallows the new 403 in silence *(the silence half is CLOSED; the menu half is decision-blocked)* | V3-E06 | **`S-E06-8`** | `open` — **NARROWED, NOT CLOSED, and booked that way deliberately** | `pnpm typecheck` + `pnpm lint` via `scripts/ci-gate.sh` — see the verdict line in the PR. **No web unit test exists and none is claimed**: `apps/web` has no runner (verified this run — `apps/web/package.json` declares `test:e2e*` Playwright scripts only, and neither `jest` nor `vitest` is a devDependency), so `AC-2`'s rendering, announcement and focus return are **static review only** | **The half that is closed.** `apps/web/src/app/admin/users/actions.ts` had **no `catch`** and `UsersTable.tsx:30-39` was `try/finally` with none either — the only `'use server'` file in the entire web surface with zero catch clauses (measured across every `'use server'` file in `apps/web/src`; `admin/alerts/actions.ts` funnels six actions through one catching `callApi`, `admin/settings/preferences-actions.ts` uses `Promise.allSettled`). Since `S-E05-2` (`#218`) made the privilege ceiling answer **403 for real**, assigning `teacher`/`parent`/`student` produced an **unhandled rejection**: spinner off, menu closed, `router.refresh()` skipped, nothing said. Both actions now return the house `ApiResult`, re-throw the Next navigation signal **first** (so a 401 still redirects to login instead of rendering `NEXT_REDIRECT;…` as a business refusal), and `revalidatePath` only on success. The refusal renders in a dismissible `role="alert"` strip in a `colSpan={4}` row **directly under the offending user**, `aria-describedby`-linked to that row's trigger, with focus returned to the trigger and a request token so a late response cannot paint a refusal on the wrong row. The API's text is **restituted verbatim** — no client re-wording — except `apiErrorMessage`'s `HTTP <status>` fallback, which is not a sentence and is prefixed by a constat true exactly when that fallback fired. The extractor is a **leaf module importing nothing** (`apps/web/src/lib/api-error-message.ts`), because `api-client.ts` imports `next/headers` + `@/auth` and a *value* import of it from a `'use client'` file is **`PF-133`**, a `next build` break neither `tsc` nor `eslint` can see; `ApiError` moved into the leaf and is re-exported, so ~30 server callers and `instanceof` identity are untouched. Being **total** by `typeof`/`Array.isArray`/`in` narrowing with **no `as`**, it converts `privilege-ceiling.ts:147-152`'s written plea that `message` "MUST stay a string" into a structural impossibility on the web side. **The half that is NOT closed, and why it is not a defect in this slice.** The finding's own fix line ends "and stop offering roles the caller cannot grant". That is **refused on purpose**: which roles a `school_admin` may grant is the **open decision `D-12` / `PF-178`**, and filtering in JSX would encode an answer no human has given, in a place no ADR can re-read. The refusal is stated in a code comment at the menu (`UsersTable.tsx`) so the next reader sees a boundary rather than an oversight. **`DNC-09` is therefore NARROWED, not discharged** — the affordance is still offered, but the refusal is now legible at the point of failure. Re-point the menu half at `D-12`; do not close `PF-174` on this evidence | G-PORTAL, G-DNC |

| PF-160 **P3** *(architectural)* | V3-E04 | **`S-E04-7`** | `open` — raised by `S-E04-6`'s ADR-drift lens | — | `apps/api/src/shared/audit/write-audit.ts` — that directory stopped being framework-free. This is the first **production** file there to import `@nestjs/common` (`provenance.ts` and `client-hints.ts` import only `node:*` and siblings), and it embeds user-facing French UI copy (`AUDIT_WRITE_FAILED_MESSAGE`), justified because `SchoolsManager.tsx:32,41,52` renders `res.error` verbatim inside a red inline banner. It also contradicts story §2.1 (*"Body: one `tx.auditLog.create`. Nothing else. **No** `try`/`catch`"*) — it **re-throws**, so DNC-10 is intact and W-2/W-4 cover it, but the deviation must be legible or the next reader diffing spec against code reads it as a bypass. When `S-E04-7` points ~27 more sites at this seam, including worker/queue paths with no HTTP response in scope, a 500 and « L'opération a été annulée » is the wrong shape. Fix: a plain typed `AuditWriteFailedError` in the seam, mapped to HTTP at the controller/filter layer | G-DNC |
| TOOL-01 **P2** *(ledger, not product)* — 26 finding ids are cited by the stories and by the ledger, and declared by the register the contract names as the resolution authority for none of them | V3-E04 *(routine/ledger)* | — *(recorded, deliberately **not** storified this run — routine triage: a discovered P2 is recorded and reviewed at layer close)* | `open` — measured by the routine in Step 2 of run 35, before the sprint ran | n/a — measured with `comm` over the two registers | `docs/daily-improvement-v3/audit-findings-index.md` declares **133** ids, the highest being `PF-133`. The ledger (`traceability/OPEN.md` plus the four `CLOSED-*.md`) cites **26** the index does not: `PF-134`…`PF-146`, `PF-148`…`PF-150`, `PF-153`…`PF-162`. This matters *at this slice specifically* because `S-E04-7`'s AC-3 requires the baseline to resolve each row's finding id **against that index, not against a regex** — the `S-E06-5` lesson, where three live rows cited ids that existed nowhere because the checker validated only the id's *shape*. **The slice itself is unaffected, and that was checked rather than assumed:** `scripts/audit-write-check.js` does read the real index (`:151`, `:459`), and all 17 baseline rows cite `PF-31` or `PF-121`, both declared — so the resolution is genuine, not accidentally-green in a way that would hide the hole. **But the register is now the binding constraint on the mechanism it feeds:** the moment a baseline row is owned by a post-`PF-133` finding it is unwritable, and the check will correctly refuse it — including `PF-163`, which is the most likely next baseline owner if the human ruling drops `invite` back out of the sweep. Recorded under `TOOL-` rather than `PF-` because the defect is in the routine's own bookkeeping, not in the product. Fix: append the 26 declarations to the index — mechanical, and it belongs *before* the next slice needs one rather than after a red gate explains it | G-DNC |
| PF-164 **P2** — the `AuditTransactionClient` brand launders one indirection out, and the new ratchet blesses the unbranded hop | V3-E04 | **`S-E04-8`** *(gate narrowing only — the three parameters are fixed)* | `in-progress` — raised by **all three** members of `S-E04-7`'s escalation panel independently; **the code half was applied by the routine at land, the gate half was not.** All three forwarders now declare `AuditTransactionClient` (`academic-years.controller.ts:303`, `integrations.service.ts:671`, `child-claims.service.ts:778`), each carrying a comment naming this finding and stating why the bare type launders `ADR-035` D1. Applied at land rather than deferred because the architect panel called it blocking-quality, it is three tokens, and deferring an invariant to the next slice is precisely how `PF-31` stayed open across two epics. **Residual, and it is the durable half:** `TRANSACTION_CLIENT_TYPE_NAMES` (`scripts/audit-write-check.js:276`) still whitelists the unbranded name, so a *new* unbranded forwarder would still pass rule B — narrowing it is blocked on `remediation.controller.ts`'s **baselined** forwarders, which legitimately use the bare type. The `@ts-expect-error` forwarder case in `write-audit.spec.ts` is **not** added either, so nothing yet proves this fix can go red. Until both land, the fix is a correction, not a control | `apps/api/src/shared/audit/write-audit.spec.ts` *(pins the brand at the seam; nothing pins it at a forwarder)* | The three private `audit()` forwarders `S-E04-7` introduces or converts declare `tx: Prisma.TransactionClient`, not the branded `AuditTransactionClient`: `child-claims/child-claims.service.ts:778`, `integrations/integrations.service.ts:671`, `school-structure/academic-years.controller.ts:303`. `write-audit.ts`'s own header states the reason this matters verbatim — `Prisma.TransactionClient` is `Omit<PrismaClient, ITXClientDenyList>`, `Omit` *removes* members but does not forbid them, TypeScript is structural, **so a full `PrismaService` IS assignable**. `writeAudit(this.prisma, …)` is a type error; `this.audit(this.prisma, …)` **compiles** — and passes rule B too, because `TRANSACTION_CLIENT_TYPE_NAMES` (`scripts/audit-write-check.js:276`) whitelists the unbranded name. Typecheck green, ratchet green, audit row written outside the transaction: the literal `PF-31` shape, at a second address, re-opened by the slice whose purpose was to close it. **No call site is wrong today — this is a control gap, not a live defect.** It is sharpened by the docblock added at `academic-years.controller.ts:290`, which asserts the opposite in writing (*"the seam's brand accepts a transaction client and rejects a full `PrismaService`, so 'same transaction' is carried by the type across this hop"*) — that sentence is false, and it is the sentence a future author will trust. **Fix:** type the three parameters `AuditTransactionClient` (already exported from `shared/audit/index.ts:44`; a `$transaction` callback param **is** assignable to the brand, so nothing else moves), delete the false docblock clause, add a `// @ts-expect-error` forwarder case to `write-audit.spec.ts`, then narrow `TRANSACTION_CLIENT_TYPE_NAMES` — that last step is a follow-up, because `remediation.controller.ts`'s **baselined** forwarders still use the bare type | G-AUDIT, G-DNC |
| PF-165 **P2** *(latent — becomes live the moment a second tenant exists)* — a Keycloak identity with no local profile is adopted into the **demo** tenant on first login, with permissions derived from its realm roles rather than from its profile | V3-E04 → **V3-E05** | — *(recorded, deliberately **not** storified this run — routine triage: a discovered P2 is recorded and reviewed at layer close)* | `open` — raised by the routine in Step 2 of run 37, then **independently corroborated and sharpened** by `S-E04-9`'s implementation panel, which needed the same facts to falsify AC-4 | — *(no test: `user-sync.service.ts` has no spec; that absence is part of the finding)* | `apps/api/src/shared/auth/user-sync.service.ts:38-56` — `ensureUser` creates the `UserProfile` **lazily, on first login**, and when it finds none it upserts the hard-coded `DEMO_TENANT_SLUG = 'demo'` tenant and creates the profile there, `status: active`, while `effectivePermissions` (`:64-87`) unions `REALM_ROLE_PERMISSIONS[realmRole]` for **every** realm role on the token regardless of what the profile says. **This is the amplifier that gave `PF-163` its severity, and it is reachable without `PF-163`.** Until this run it existed only inside `PF-163`'s prose, with no row of its own — closing `PF-163` would have silently retired it. Three verified facts make it independent: (a) lazy creation means every realm identity that has never logged in is profile-less **by design**, not by failure; (b) `infra/keycloak/realm-export.json` ships three such identities — `admin@`, `teacher@`, `parent@pilotage.local`, enabled — and `apps/api/prisma/seed-demo.ts` contains **0** matching rows (measured with `grep -c`); (c) `ADR-004` puts every tenant in **one** realm, so those identities are reachable from any tenant. **Latent, not live, and the distinction is the demo tenant itself:** today `demo` is the only tenant, so "wrong tenant" has no second value to be wrong about. It becomes a live `ADR-002` breach on the first day a second tenant exists — which is `V3-E05`'s subject, hence the epic move. Fix direction: tenant assignment must come from an invitation record or an explicit realm→tenant mapping, never from a hard-coded slug, and permissions must not be derivable from realm roles alone for a profile the system did not provision | G-TENANT, G-AUTHZ |

| PF-167 **P2** *(residual of `S-E04-9`, stated by it rather than discovered after it)* — the SMTP-failure branch still leaves an enabled, profile-less identity, and it is **deliberately** not compensated | V3-E04 | — *(recorded, deliberately **not** storified this run — routine triage; it is an `apps/web` copy conversation, not a compensation one)* | `open` — declared by `S-E04-9` in code (`invite.controller.ts:191-203`) and in `ADR-035` D15, then registered by the routine at land | `apps/api/src/modules/identity/invite.controller.spec.ts` covers the compensated paths; **no case covers this branch**, because the branch's behaviour is unchanged | `apps/api/src/modules/identity/invite.controller.ts:191-203` — when `sendExecuteActionsEmail` throws, the handler keeps the Keycloak user on purpose and tells the admin the temporary password is usable. That advice is **true**, and deleting the account there would make it false — so `S-E04-9` left it alone and said so in a comment rather than absorbing it silently. **The residual is nonetheless real and is the same orphan class `PF-163` named:** an enabled identity with no `UserProfile`, and step 1 still refuses every retry with the same 409. What differs is only the trigger — an SMTP outage rather than the audit rollback — and therefore the remedy: closing it means changing a user-facing sentence and the `ActivationHint.tsx` copy that narrates it (`PF-17`'s seam), not adding a compensation. Recorded so that `PF-163`'s closure cannot be read as "the invite path can no longer orphan an identity" — it can, by one remaining door, and that door is documented rather than open by accident | G-DNC |
| TOOL-03 **P2** *(gate, not product)* — the audit-write sweep counts **existing** audit writes, so a privileged mutation that writes **none** is invisible to both the sweep and the baseline | V3-E04 *(routine/gate)* | — *(recorded, deliberately **not** storified this run — routine triage)* | `open` — measured by the routine in Step 2 of run 37 | `scripts/audit-write-check.js` + `apps/api/src/shared/quality/audit-write-gate.spec.ts` — both green, and green is exactly the problem | The sweep's own `$doc` (`scripts/audit-write-baseline.json:4`) states its measurement verbatim: *"27 direct audit writes outside the seam across 16 files"*. That population is **sites that already call `auditLog.create` or `writeAudit`**. A privileged mutation with **zero** audit writes contributes nothing to it, is not a candidate for conversion, needs no baseline row, and therefore passes every rule silently — `PF-166` is the live instance, and it was found by reading a controller, not by any gate. **This is the completeness direction the gate does not have:** it proves *every audit write goes through the seam*, never *every privileged mutation writes one*. Recorded under `TOOL-` per the triage rule because the defect is in the routine's own instrument, not in the product; `PF-166` carries the product half. Fix direction: a second rule keyed on the mutation side — enumerate handlers whose route is a non-GET on a privileged resource family and require a `writeAudit` in the same function — with a baseline for the deliberate exemptions, exactly as rule B already does for its own | G-AUDIT, G-DNC |
| PF-169 **P2** — the two CSV writers disagree about what French Excel needs, and both cite French Excel as the reason | V3-E04 | — *(recorded, **not** storified this run — routine triage)* | `open` — measured by the routine at land of `S-E04-11` | — | `apps/web/src/lib/csv.ts:1-15` declares « FR Excel friendly » and uses **`;`** as delimiter with **CRLF** line endings, on the stated ground that *"Excel locale uses comma as decimal separator"*. `apps/worker/.../audit-csv.generator.ts` uses **`,`** with **`\n`**, and its BOM comment (`:127-130`) cites the **same** authority — French Excel — as its rationale. **Both cannot be right.** If the web module's premise holds, the DPO's audit export opens in French Excel as **one column per row**, defeating the very BOM the generator added for that tool. This is not an aesthetic inconsistency: the audit CSV is the regulator-facing artefact, and « it opens correctly in the DPO's Excel » is the property the BOM exists to buy. Deliberately **not** fixed inside `S-E04-11`: changing the delimiter rewrites every byte offset in the file, which is exactly the silent byte change that slice exists to stop — it needs a versioned, announced format change, and it should be settled **once**, for both writers, alongside `PF-168`'s single shared escaper. **Updated 2026-08-10 — `PF-168` is now CLOSED by `S-E05-1`, and this finding was deliberately NOT taken with it. That is a decision, not an oversight, and it is recorded in three places so it cannot be inherited silently:** a code comment on `CSV_SEPARATOR` in `apps/web/src/lib/csv.ts`, the header of `packages/contracts/src/security/csv-injection.ts`, and **`ADR-037` D8 (ii)**. The reasoning `S-E05-1` recorded adds one measured argument this row did not have: folding the two quote sets into a single union regex would force-quote every worker cell containing a `;` — an `audit_log.user_agent` header, say — and so **silently rewrite the regulator’s file** while claiming to unify it. So the split that shipped is deliberate: the **security-bearing** part (trigger set, neutralising character, when to apply them) is now shared and singular in `@pilotage/contracts`; the **dialect** (separator, BOM, terminator) stays per-surface and duplicated. This row owns the dialect half only, and closing it still requires the **versioned, announced** format change with a consumer census that `ADR-037` D6 already defines for the audit export | G-TRUTH, G-DNC |
| PF-170 **P2** — the audit export truncates at 50 000 rows and says nothing, so a partial file is indistinguishable from a complete one | V3-E04 | — *(recorded, **not** storified this run — routine triage)* | `open` — raised by `S-E04-11`'s intake as a follow-up, registered by the routine at land | — | `apps/worker/.../audit-csv.generator.ts` — `prisma.auditLog.findMany({ …, take: 50_000 })`. At the cap the file simply ends. Nothing in the artefact, the `ExportJob` row or `/admin/exports` states that rows were dropped, so **a regulator can receive a partial export believing it complete** — and the truncation silently interacts with the window: it is `orderBy: createdAt desc`, so what is lost is the *oldest* end of the requested period, i.e. precisely the part a retention question asks about. **This is the same `G-TRUTH` property `S-E04-11` defends — a projection must label its scope — on the one axis that slice deliberately did not touch.** Fix direction: the export states its own scope (tenant, resolved window, the timezone that resolved it, row count, explicit `truncated: true/false`), echoed in the admin export UI, so the file is evidence a DPO can sign rather than a dump | G-TRUTH |
| PF-171 **P2** — `School.timezone` is a second unvalidated timezone column, and neither it nor `Tenant.timezone` is validated on write | V3-E04 → **V3-E05** | — *(recorded, **not** storified this run — routine triage)* | `open` — residual of `S-E04-11`, measured by the routine at land | `audit-kpis.spec.ts` / `exports.processor.spec.ts` cover the **read** side only | `apps/api/prisma/schema.prisma:263` (`Tenant.timezone`) and **`:280` (`School.timezone`)** are two independent `String @default("Europe/Paris")` columns — the schema's own comment states that neither is derived from the other. `S-E04-11` discharged `PF-149`'s **second** half (map the error to a deliberate outcome) on both call sites, and left the **first** half — *validate on write* — only partly done: `isKnownTimezone` is exported and available (`window.ts:141`), but **no writer for either column exists anywhere in `apps/api/src`**, so there was no write path to guard without inventing a settings endpoint, which the slice correctly refused to do. The residual is therefore precise: the day a settings screen, import or seed writes **either** column, an unvalidated value becomes a 503 on the analytics read and a terminal `audit_csv` failure. `School.timezone` is the sharper half because it is the *operational* zone and nothing in the audit path reads it yet — so a bad value there fails somewhere else entirely. Fix: validate at whatever write path lands first, and put the guard where a future writer cannot miss it | G-TRUTH |
| PF-172 **P3** — the 503's stable discriminator is exported where its stated consumer cannot import it | V3-E04 | — *(recorded, **not** storified this run — routine triage)* | `open` — raised by `S-E04-11`'s review panel, registered by the routine at land | — | `analytics.service.ts` exports `TENANT_TIMEZONE_UNUSABLE`, and its docblock states the purpose plainly: to let `/admin/audit` tell « analytics is down » from « this tenant's timezone is misconfigured » without parsing a French sentence. But `apps/web` cannot import from `apps/api/src` — the shared home is `packages/contracts`, which both apps already consume. **As written the docblock promises a capability the placement forbids**, which is the `PF-164` shape (a comment asserting an invariant the code does not carry) at a new address. Compounding it, `/admin/audit`'s `safe()` currently collapses every `ApiError` into one amber strip, so even a correctly-placed code would have no consumer yet. Neither half is a live defect — the 503 is correct and the code is inert — so this is P3. Fix: move the constant to `packages/contracts` next to the error it discriminates, then give the strip a branch | G-TRUTH, G-DNC |
| PF-173 **P2** — the CSV ratchet enforces UNIQUENESS of the escaper, not COVERAGE of the surfaces *(live half closed at land; the structural half is what keeps this open)* | **V3-E05** | — *(**half (a) closed by the orchestrator at land**; half (b) recorded and **not** storified this run — routine triage; the brand is a typed-API change across seven call sites, so it wants its own slice)* | `open` — **structural half only.** Raised by `S-E05-1`'s security lens, measured and registered by the land pass of the same slice. **Half (a), the live site, was closed at land rather than deferred:** `ExportReportButton.tsx:68` now wraps `academicYear?.name` in `csvEscape`. Shipping a *known-live* injection to a teacher-facing export while the fix was one call is not a defensible deferral, and triage governs **order, never scope**. **`:124` was deliberately NOT touched and is not part of (a):** it is the download *filename*, not a CSV cell — `csvEscape` is the wrong tool there (quoting a filename fixes nothing; a filename wants path-separator sanitisation, which is a different finding if anyone raises it). The row stays open on (b), and its evidence column below still describes the gate hole that (b) names | `apps/api/src/shared/quality/csv-escape-gate.spec.ts` covers rules A–E, all of which pass on the offending file — **that is the finding** | Two halves of one shape. **(a) The live site.** `apps/web/src/app/teacher/reports/_components/ExportReportButton.tsx:68` builds a line by raw interpolation — `` lines.push(`Année;${academicYear?.name ?? ''}`) `` — so tenant-authored free text reaches a `.csv` a teacher opens in Excel with no `csvEscape` and no quoting. Excel evaluates a formula in **any** column, not only A, so a year name beginning with `=`/`+`/`-`/`@` is live, and an embedded `;` splits the record. `:72-76` and `:97-98` interpolate the numeric KPIs the same way (safe today because they are `fmt1` output, unsafe as a pattern). **Pre-existing — `S-E05-1` did not introduce it** — but that slice's own header claimed the opposite (« teacher/reports … all go through `csvEscape` »), which is why it is registered here and why the claim was narrowed at land in `apps/web/src/lib/csv.ts` and in `scripts/csv-escape-check.js`'s sanctioned-entry reason. **(b) The structural half, which is the reason this is P2 and not P3.** Rules A–E are all conditioned on an escaper *existing*: a surface that hand-joins user data, or calls `csvRow` (a bare `cells.join(CSV_SEPARATOR)` typed `Array<string \| number>`), declares no escaper, assembles no trigger set, and passes every rule while shipping exactly the defect `PF-168` names. The gate would certify `ExportReportButton.tsx` today. **Fix direction, and the repo already owns the pattern:** `ADR-035` / `apps/api/src/shared/audit/write-audit.ts` (`64f64dd`, *« a brand that makes it a type error to leave it »*) solved the identical shape with a **branded type**. Have `csvEscape`/`csvFixed1` return a branded `CsvCell` and let `csvRow`/`buildCsv` accept only `CsvCell[]`, converting « we count escapers » into « you cannot emit an unescaped cell ». That also retires rule C's status as the only executed evidence for the web half. Minimum acceptable alternative if the brand is deferred again: wrap `academicYear?.name` at `:68` and `:123` | G-TRUTH, G-DNC |
| PF-136 **P2** — two consumers disagree on how to merge a mixed-vocabulary row, and the marker is `aria-hidden` | V3-E04 | **S-E04-7** | `open` — raised by the escalation panel and the a11y lens | — | `AuditDetailDrawer.tsx` resolves `auditVocabularyExplanation(action) ?? auditVocabularyExplanation(resourceType)` — action wins — so a row with a **legacy** action and an **unknown** resource type prints « Enregistrée avant l'unification du vocabulaire », claiming historic provenance for a brand-new undeclared code. That is the error class `ADR-037` D4 exists to remove. The table does the cautious thing (`describeNonCanonicalFields` picks `unknown` if any axis is unknown) and the worker has `weakerVocabulary()` for the same merge — three consumers, three rules. Second, same mechanism: the resource-type marker is wrapped `aria-hidden`, so a screen-reader user browsing cell-by-cell hears « Notes » and **nothing** about the value being outside the declared vocabulary — the entire regulatory signal absent from the accessibility tree. Fix: resolve and render **per field**, and lift `weakerVocabulary` into `packages/contracts/src/audit/labels.ts` so the three cannot disagree | G-TRUTH, G-DNC |
| PF-141 **P2** — the table shows the label and drops the raw code, while its own filter still matches the raw column | V3-E04 | **S-E04-7** | `open` | — | The action badge now renders `action.label` and the raw stored code appears **nowhere in the row**, while the action filter is still a free-text `contains` match against the raw column. A user who reads « Suppression d'un rôle » and types it gets zero rows. It also contradicts the invariant the rest of the slice states explicitly — the drawer renders the raw code under the title *precisely* because « le libellé est posé au-dessus de la valeur réelle, il ne la remplace jamais », and the CSV keeps the raw column for the same reason. The table is the one surface that dropped it. Fix: a small monospace raw line under the badge, or resolve the query term through `AUDIT_ACTIONS` into an `OR` of codes | G-TRUTH |
| PF-142 **P2** — no test in the repository verifies the built CJS `packages/contracts/dist` any more | V3-E02 *(gate power, not product)* | — *(story to be written)* | `open` | — *(that is the finding)* | Both `apps/api/jest.config.js` and `apps/worker/jest.config.js` now map `@pilotage/contracts` to `packages/contracts/src`. The rationale is correct and documented — `test-ratchet.js` spawns jest with `cwd: appDir`, bypassing turbo's `test → ^build`, so specs would otherwise read a stale git-ignored `dist/` — but the consequence is that **nothing verifies the artefact Node actually loads**, while `audit-csv.generator.ts` value-imports the resolvers at **module load** and sits in the worker's Nest graph. The stated compensating control (`pnpm --filter @pilotage/contracts build` as a landing prerequisite) is a human habit, and with GitHub Actions billing-locked (`PF-113`) nothing automated backs it. **Discharged for run 32 only, by execution:** the full `ci-gate.sh` runs `pnpm build` (contracts included) ahead of `boot-check`, so this PR's artefact was built and booted. That is one run's evidence, not a gate. Fix: restore one spec that resolves the built package, or make `boot-check` assert the audit resolvers load from `dist/` | G-DNC |
| PF-121 the two audit writes outside `apps/api` still hard-code both provenance fields, on a path with no JWT | V3-E04 | **S-E04-7** *(owner)* | `open` — **and now RATCHETED rather than merely counted.** `S-E04-7` baselines both sites in `scripts/audit-write-baseline.json` under the declared class `cross-package-seam`, citing this finding, with the dependency inversion stated as the reason. A path SKIP in the gate was refused deliberately: a skip is invisible, a baseline row is reviewed and one-way. Note also that these are `tx.auditLog.create` — **off-seam** debt, not **un-transacted** debt, and the gate reports the two differently. Original record: | — *(none — deliberately out of `S-E04-1`'s scope)* | `packages/imports-core/src/engine.ts:201-202` and `:292-293` each write `actorRole: 'school_admin'` + `portal: 'admin'` inside a `tx.auditLog.create`, shared verbatim by the API and by `apps/worker/.../imports.processor.ts`. On the worker path the row is written while draining a BullMQ job — **no authenticated caller is in scope at all** — so `deriveAuditProvenance(jwt)` cannot be applied without capturing provenance at *enqueue* and carrying it in the payload, plus a ruling on what portal a job acted through. Read plainly: after `S-E04-1`, `import.apply` and `import.rollback` rows still say `school_admin`/`admin` whoever ran them. Second reason it does not belong in a no-build slice: `packages/imports-core/dist/` is the runtime `main` | G-AUDIT |
| PF-123 grade publication records no actor role at all | V3-E04 | **S-E04-7** *(owner)*, read-side half `S-E04-2`/`S-E04-5` | `in-progress` — **read half closed by `S-E04-5` (run 33); write half open** | — *(none — a rule that bans literals cannot see an absence)* | `apps/api/src/modules/grades/assessments.controller.ts:290` writes `assessment.publish` with **no `actorRole` and no `portal` key** — `grep -n "actorRole\|portal"` over the file returns **zero** hits. Grade publication is the event the alert engine, the parent dashboard and every notification hang off, and its audit row cannot say who published. Compounded on the read side: `analytics.service.ts:3358-3364` builds the `/admin/audit` portal facet `where: { portal: { not: null } }`, so a `null`-portal row is offered by **no filter value at all** (no « sans portail » option) while `:3246` filters `portal` by exact match — these rows are not merely under-attributed, they are unreachable by any filtered review. Pre-existing; `S-E04-1` did not touch this file **Read half CLOSED by `S-E04-5` (run 33); the write half is untouched and still owned by `S-E04-7`.** The facet now offers an explicit « sans portail » sentinel (`AUDIT_PORTAL_NONE`, asserted non-colliding with the four declared portals) which the server decodes to `where.portal === null` — so a null-portal row is reachable by a real filter value for the first time. Two guards keep the fix honest rather than decorative: the sentinel is **only offered when such a row exists** (a filter that can only ever return nothing is the DNC-09 defect wearing a filter as a hat), and a test asserts that every offered portal **plus** the sentinel accounts for every row, so no residual gap can reopen unnoticed. Measured on the live local database: **0** of 61 rows currently carry `portal IS NULL`, so the read gap is real in code but **latent in data** today — the honest severity is "unreachable-by-review the moment such a row is written", not "rows are unreviewable now". It stops being latent as soon as `assessments.controller.ts:290` writes one, which is exactly the write half `S-E04-7` owns | G-AUDIT |
| PF-17 Maildev/seed leakage | V3-E06 | **S-E06-1** | `in-progress` *(UI half closed)* | `apps/api/src/shared/quality/production-artefact-gate.spec.ts`; `scripts/production-artefact-check.js` — executed, positive **and** negative | **The four surfaces are gone, and something now refuses their return.** All four `http://localhost:1080` Maildev instructions — `/admin/register`, `/teacher/register`, and the invite form's success panel *and* its footer — are replaced by `apps/web/src/components/auth/ActivationHint.tsx`, whose every sentence is checked against `invite.controller.ts`: `UPDATE_PASSWORD` is a required action, `CONFIGURE_TOTP` is added for `school_admin`/`teacher` only, and a failed send raises a `BadRequestException` the form already surfaces. **What it deliberately does not say is the part worth recording** — no link-validity window (Keycloak owns that lifespan and this codebase never sets it) and no resend affordance (no resend endpoint exists). Replacing a false instruction with a confident guess would have been DNC-06 with better styling. Scan executed: **562 files across the three application sources, Tier A clean**. **Not closed, and the residual is data, not code:** the seed author labels **already written to the hosted database** (`Sophie Dupont`, `M. Lefebvre`, `mme.dupont@voltaire.fr`) are matched by a Tier-A rule so they cannot be *re-introduced in source*, but deleting rows on a live deployment is routine STOP condition 3 — an operator action, tracked in `docs/runbooks/provision-demo-tenant.md` §6 | G-DNC |
| PF-54 hard-coded credential/dev URLs | V3-E06 | **S-E06-1** | `in-progress` *(credential half closed; 17 dev URLs inventoried)* | `apps/api/src/shared/config/config-preflight.spec.ts`; `apps/api/src/shared/quality/production-artefact-gate.spec.ts`; full `scripts/ci-gate.sh` | **The API can no longer authenticate as `admin`/`admin`, and the fallback turned out to have been live rather than latent.** `?? 'admin'` (×2), `?? 'http://localhost:8180'` and `?? 'maildev'` are deleted from `KeycloakAdminService`, `JwtStrategy` and `KeycloakModule`; `assertRequiredConfig` runs in `main.ts` **before** `NestFactory.create` — ahead of the migration preflight, so a missing credential does not wait behind a database round trip — names **every** absent variable in one throw, treats `''` and whitespace as absent, and has **no** `NODE_ENV`, `SKIP_*` or `ALLOW_*` escape (DNC-10). It is required in development too, and `infra/docker-compose.yml` now declares the values explicitly instead of the code assuming them: a default nobody wrote down *is* the defect, so the fix is a declaration, not a second default. Prod uses `${VAR:?…}`, which makes **compose** refuse the deploy command in the operator's terminal rather than crash-loop a container at 02:00. **Step 2 measured the premise and it was worse than the finding said** — the prod overlay declared neither admin variable, so the hosted API really did resolve `admin`/`admin`, and it *matched*, because the `keycloak` service's own master credentials are `admin`/`admin` and the prod overlay never overrides them. The two defects concealed each other. That half is **`PF-81`**, an operator rotation this slice deliberately did not perform. **Not closed:** **17** `?? 'scheme://localhost…'` fallbacks across 16 files remain — four more than the routine's own recon found (`queue.module.ts` ×2, the web API proxy route, `parent/register/actions.ts`, `api-client.ts`) — held by a per-file ratchet that fails **above** a count (debt added), **below** it (lower the ceiling) and on any unlisted file with a hit (no escape by omission). Three of them bake `localhost:3000` into **email bodies** and one into the **invitation link** itself, so the residual is user-visible, not cosmetic. And the scan reads **source**: the `NEXT_PUBLIC_*` class is build-time inlined, so this evidence stops one step short of the story's own AC-1 — recorded as **`PF-82`** rather than glossed | G-AUTHZ |
| PF-81 hosted Keycloak master realm runs `admin`/`admin` | V3-E06 *(operator)* | — *(no story: credential rotation, STOP condition 2)* | `open` | n/a — verified by reading `main`, not by authenticating | **Verified, not inferred, and deliberately not fixed.** `git show main:infra/docker-compose.yml` sets `KEYCLOAK_ADMIN: admin` / `KEYCLOAK_ADMIN_PASSWORD: admin` on the `keycloak` service; `git show main:infra/docker-compose.prod.yml`'s `keycloak` block overrides only `KC_HOSTNAME`, `KC_HTTP_RELATIVE_PATH`, `KC_HOSTNAME_STRICT`, `KC_HOSTNAME_BACKCHANNEL_DYNAMIC`, `KC_PROXY`, `KC_HTTP_ENABLED` and the healthcheck — **neither credential**. No attempt was made to authenticate against the hosted realm: the evidence is the declaration, which establishes the defect without touching production. Resolution is ordered and manual — rotate in Keycloak **first**, then set `KEYCLOAK_ADMIN_USER`/`KEYCLOAK_ADMIN_PASSWORD` in `.env.prod`. After `S-E06-1` the deploy refuses without them, so the two can no longer drift apart silently | G-AUTHZ |
| PF-82 the production-artefact scan reads source, not the built artefact | V3-E06 | — *(story to be written)* | `open` | n/a — measured from the scan's own scope | Recorded because `S-E06-1`'s AC-1 says *"production build"* and the delivered scan reads `apps/*/src`. Equivalent for the four Maildev literals; **not** equivalent for the 17 Tier-B fallbacks, because `NEXT_PUBLIC_*` is inlined at build time — an image built without those variables ships `http://localhost:8180` in the client bundle with the source unchanged and the gate green. Closing it is a second pass over `apps/web/.next` and `apps/*/dist`, in the shape `web-artifact-check.js` already established | G-DNC |
| PF-90 stopping a background gate leaves it running, so several full gates run at once against one log | V3-E02 *(routine)* | — *(no story: the fix is in the harness's task handling, outside this repository)* | `open` | n/a — measured from `Get-CimInstance Win32_Process` during run 20 | **Observed live, three times over, and it corrupted this run's own evidence before it was noticed.** A background `bash scripts/ci-gate.sh` was stopped because a lint error made its verdict void; the stop reported success, but on Windows the harness kills the task shell and **not the process tree beneath it**. Relaunching twice left **five** live `ci-gate.sh` processes — one inside `pnpm build`, one inside `test-ratchet.js api` — all redirecting to, and truncating, the same log. The symptom that gave it away was the stage list reading back in an impossible order: `▶ build` present at one moment, absent the next. The `✗ test:api` in that log was produced by a jest run reading a spec file being edited concurrently, i.e. a failure with no defect behind it — exactly the class of false signal that makes a gate untrustworthy in the other direction too. **The lock cannot catch this**: all the processes belong to the same run and hold the same lock, so the single-writer invariant is *satisfied* while the guarantee it exists to provide fails. That makes this a third member of the family with `PF-77` (the lock lapses mid-run) and `PF-80` (the workflow reports `landed: true` on a tree it never checked): the routine's controls describe one writer, its machinery permits several. Mitigated on this run by enumerating `Win32_Process` for `ci-gate.sh`, killing each tree explicitly, and taking only the final uncontended log as the verdict (**R-23**). The durable fix belongs to the harness's task handling, outside this checkout | G-DNC |
| PF-38 legal/pricing/contact/help 404 | V3-E06 | S-E06-4 | `open` *(measured, not fixed)* | — | `/legal/privacy`, `/legal/terms`, `/legal/cookies` and `/legal` (the middleware `PUBLIC_PREFIXES` entry) are now **inventoried** in `scripts/link-integrity-baseline.json` with this id, so the day S-E06-4 ships them the gate **fails** until the entries are removed — the debt is queued rather than silenced. Deliberately **not** fixed here: the content half is blocked on **D-08** and the routine may never author policy text (R-13). `/pricing` and `/contact`, which the finding also names, are split out as **`PF-94`** because they need no policy decision | — |
| PF-39 profile/help 404, cross-role copy leak | V3-E06 | **S-E06-5** | `in-progress` *(dead-link halves closed; copy halves untouched)* | `apps/api/src/shared/quality/link-integrity-gate.spec.ts` — **173/173**; `apps/api/src/shared/quality/portal-landing-gate.spec.ts` — **135/135**; `node scripts/link-integrity-check.js` → `LINK INTEGRITY CHECK: PASS` against the **real 116-route build** | **Both dead-link halves are closed, and the profile half was invisible to every gate until this slice.** `/help` is a real page (`apps/web/src/app/help/page.tsx`) and its baseline row is **gone** — which the gate itself verifies in the hard direction, because a baselined target that resolves again is a **failure** until its row is removed. The profile half is the one the previous row could not see: « Mon profil » is written `` href={`/${portal}/profile`} `` at `TopbarUserMenu.tsx:45` and `components/UserMenu.tsx:55`, a **template literal**, and `LITERAL_LINK` excluded the backtick — so `/admin/profile`, `/teacher/profile` and `/parent/profile` were **0 of 3 alive** while the gate printed `PASS` on every run since `S-E06-3`. That blindness is `PF-97`. Resolved the way the codebase already resolves it: `TopbarUserMenu.tsx:33-36` had **already** written the idiom down for the student portal — *"render only the help + logout entries so the menu never links to a 404"* — so the three dead entries are removed rather than pointed somewhere plausible, and the typed default is now `PORTAL_SETTINGS_HREF` as a `Partial<Record<PortalId, string>>` in `@/lib/portals`, i.e. *no surface → no menu entry* is a compile-time property instead of an `isStudent` runtime branch a static checker cannot evaluate. **Not closed, and the residual is copy, not routes:** the teacher notification-settings page still carries parent copy (« votre enfant ») and teacher « Import grades » still points into the admin portal. Neither is a dead literal link, so no link gate can see either; they need a reader, not a resolver. The profile *capability* — a real « Mon profil » surface for any portal — is deliberately **not** built here and is recorded as **`PF-99`** | G-PORTAL, G-DNC |
| PF-91 nine auth routes the middleware waves through and nothing emits | V3-E05 | — *(story to be written)* | `open` | n/a — measured by `scripts/link-integrity-check.js` on its first execution | **Found by turning a gate on, and it is the largest of the four.** `apps/web/src/middleware.ts:37-49` exempts `/admin/accept-invite`, `/admin/forgot-password`, `/admin/reset-password`, `/teacher/accept-invite`, `/teacher/forgot-password`, `/teacher/reset-password`, `/parent/forgot-password`, `/parent/reset-password` and `/parent/verify-email` from authentication — and **none of the nine is in the emitted route manifest**. The exemption list is a promise the application cannot keep: the middleware carefully waves through a page that does not exist. Every one sits on either the *first* path a new user takes (accept an invitation) or the *recovery* path (forgot the password), which is where a 404 is least recoverable, because there is no authenticated shell to fall back to. It composes badly with `S-E06-1`, which replaced the Maildev instructions with honest activation copy: the copy is now correct and the destination still is not. Inventoried in `scripts/link-integrity-baseline.json` and **not fixed** — building nine authentication surfaces is its own story and belongs to `V3-E05`, not to a navigation-hygiene slice | G-AUTHZ |
| PF-92 `/parent/remediation` has no index while admin and teacher both do | V3-E06 | — *(story to be written)* | `open` | n/a — same execution | **The portal asymmetry G-PORTAL exists to catch, in the direction that hurts the core user.** `/parent/remediation/[planId]` is emitted; `/parent/remediation` is not, while `/admin/remediation` and `/teacher/remediation` both are. The parent is the actor the remediation loop was designed around — alert → diagnosis → bookable resource → measured improvement — so a parent arriving from an alert deep-link is served and a parent who trims the URL, or follows either of the two links in `parent/dashboard/_components/RemediationProgressStrip.tsx:222,233`, is not. The second of those is the *overflow* link (« +N autres »), i.e. it is reached precisely when a child has **more than one** support plan. Inventoried, not fixed | G-PORTAL |
| PF-98 eight affordances point at routes that resolve for **no** value, invisible until the extractor was widened | V3-E06 | — *(story to be written)* | `open` | n/a — measured by the widened `scripts/link-integrity-check.js` on its first execution | **Found by the gate `PF-97` repaired, not by an audit — which is the argument that the widening was worth its 2.2k lines.** Three shapes, all template literals: (a) `/teacher/messaging`, the « Nouvelle annonce » CTA in the « Communiquer avec les familles » card (`app/teacher/classes/[id]/page.tsx:903`), a route never emitted; (b) `/admin/assessments/*` (`app/admin/assessments/page.tsx:184`), a row link built from an assessment id where `/admin/assessments` is emitted and `/admin/assessments/[id]` **is not**; (c) `/admin/guardians/*`, **four** affordances built from a guardian id including a `RowActions` `editHref` (`app/admin/guardians/page.tsx:192,238,239`, `app/admin/enrollments/page.tsx:277`) where `/admin/guardians/[id]` likewise does not exist. Verified against the filesystem, not only the manifest: neither `[id]` directory is present. **The refusal is the part worth keeping.** `/teacher/messaging` was deliberately **not** retargeted to the nearest real surface, `/teacher/messages/new`, because `TeacherMessageComposer` **ignores** `classSectionId` and seeds the class from `classes[0]` — so the one-line "fix" would have silently composed an announcement for the **wrong class**, which is `DNC-06` at a new address and strictly worse than the 404 it replaced. Making it work means teaching the composer to honour the parameter, which is a data-path change and a different theme. Inventoried under the new `deadShapes` ceiling — the same four rules as `dead`, keyed on the shape — and **not fixed**: building an admin guardian detail page and an admin assessment detail page are capability slices | G-PORTAL |
| PF-99 no portal has a « Mon profil » surface | V3-E06 | — *(story to be written)* | `open` | n/a — measured while closing `PF-39`'s dead-link half | Recorded so that removing the three 404-ing menu entries cannot read as *"profile is done"*. `/admin/profile`, `/teacher/profile` and `/parent/profile` were dead and their links are now gone; `/student/profile` never had a link (`PF-57` owns the student portal's breadth). What no portal has is the surface itself — a place a user reads and corrects their own identity data. That is a real capability with a real gate load (it reads and writes person data, so G-TENANT, G-AUTHZ and G-AUDIT all trigger), which is exactly why it is not a tail on a navigation-hygiene slice. Priority **P2** | G-AUTHZ, G-TENANT, G-AUDIT |
| PF-100 there is no executable test of `apps/web/src/middleware.ts` anywhere in the repository | V3-E06 | — *(story to be written)* | `open` | n/a — verified across every repository assertion about the file | **The honest limit on this slice's G-AUTHZ evidence, written down rather than left implied.** Every claim this repository makes about the middleware is **static source-text matching**, including `portal-landing-gate.spec.ts` P-5, which asserts that `PUBLIC_PREFIXES` contains no portal prefix and that both redirect branches textually exist — it never calls the middleware. So an `if` added above the prefix ladder, a reordered `startsWith` chain, or a `config.matcher` edit would leave all 135 cases green while `/admin` served a redirect-to-dashboard to a logged-out visitor. **Mitigating, and stated so the grep is not read as weaker than it is:** the authz branches are byte-unchanged by `S-E06-5`, and `pathname.startsWith('/admin')` already matched the bare root before it, so there is no new authorisation behaviour to have broken. The fix is specified rather than described: `apps/web/tests/unit/middleware.portal-root.spec.ts`, driving the real exported `default` with `auth` mocked, asserting status **and** `Location`, with a positive control per portal and a negative control on the guard itself. Blocked on nothing except that `apps/web` has no unit runner today (Playwright only) — which is itself part of `VAL-08` | G-AUTHZ |
| PF-101 `packages/contracts` still declares three portals, so `apps/web` carries five local `Portal` re-declarations | V3-E06 | — *(story to be written)* | `open` | n/a — read from `packages/contracts/src/enums/index.ts:3` | `PORTALS` names `admin`, `teacher`, `parent` and `REALM_ROLES` has no `student` — both predate the fourth portal (E8-S1). Because the shared vocabulary is wrong, `apps/web` grew **five** local `Portal` unions with **four different memberships**, which is what made `PF-97`'s resolver have to be scope-aware in the first place. `S-E06-5` deliberately did **not** consolidate: its new `PortalId` is local to `apps/web/src/lib/portals.ts` on purpose, because that module is consumed by the **edge** middleware and `packages/contracts` builds to CJS `dist/` shared with the api and worker runtimes. Fixing the shared enum is a cross-package change with a compile-time blast radius across three runtimes, and it belongs in its own reviewable diff. Priority **P3** — nothing is wrong today; the cost is paid in duplication and in every future reader having to ask which union is authoritative | — |
| PF-112 *(was `PF-104`; renumbered 2026-08-08, collision with run 24's stalled-job finding)* the sprint's readiness step cannot see a lint error, so `landed: true` is returned on a tree whose web build is broken | V3-E02 *(routine)* | — *(story to be written — the fix is in `bmad/workflows/sprint.workflow.js`)* | `open` | n/a — measured from run 24's own Step 4 | **Found by running the gate, on a slice whose readiness was green.** `S-E05-12` returned `landed: true` after `pnpm typecheck` **13/13, exit 0** and its two targeted quality suites **214/214** — and the very next `bash scripts/ci-gate.sh` returned `GATE: FAIL (7 stage(s))`. Neither typecheck nor jest can observe an ESLint error, and in this repository that is not a cosmetic gap: `no-control-regex` is an **error**, `next build` runs ESLint, so a lint error is not a lint problem — it **fails the web build**, and with no `.next` emitted the `web artefact` and `link integrity` stages fail behind it. **One rule violation, four red stages, and every signal the agent had was green.** The general shape: *readiness verifies a strict subset of the merge gate, and the subset excludes the stage most likely to be tripped by a brand-new file.* Same family as **`PF-80`** (`landed: true` on a tree the full gate failed) and it is now the second instance, which is what makes it a class rather than an incident — different missing stage, though: `PF-80` wants `scripts/test-ratchet.js` in readiness, this wants `scripts/lint-ratchet.js` (which already fails on **any** ESLint error, so no new check has to be written — only wired). Both are edits to the routine's own workflow file, outside this repository's product code, so neither can be closed by a product slice. Mitigating and stated so this is not read as worse than it is: the routine caught it before merge, exactly as `R-23` requires, and nothing reached `main`. Priority **P2** — it costs a rebuild per occurrence and it erodes trust in `landed: true`, but the merge gate behind it held | G-DNC |
| PF-103 three latent residuals in the new lexer, plus `PORTAL_LANDING` is **not** in fact declared exactly once | V3-E06 | — *(story to be written)* | `open` | n/a — measured on the real tree: 0 surviving `{/*` markers, 0 lost hrefs across 365 files | Four things the verify panel found that are harmless **today** and belong to a blocking CI stage, so they are queued rather than shrugged at. (a) `'}'` is still in `REGEX_MAY_FOLLOW_PUNCTUATION`, so `<X a={b} /> {/* <Link href="/admin/ghost" /> */}` survives the stripper and `/admin/ghost` is emitted as a **live** target — the `</` guard added this run does not reach it, because the `}` is not adjacent. (b) The printed *"168 interpolated"* is not what the gate saw: `templateRows === expanded + shape + unparsed` is a **tautology** over one array partitioned by `kind`, while two `continue`s discard **133 of 301** slash-leading templates uncounted. (c) `keyof typeof C` has no member floor, so a spread or computed-key map silently under-approximates. (d) **`PORTAL_LANDING` is not written once after all** — `PortalLoginForm.tsx:25-30`'s `DEFAULT_LANDING: Record<PortalAccent, string>` is a fifth byte-identical copy of the four landing paths, and the new guard's P-2 assertion passes only because that copy carries a **different identifier**: it measures the *name*, not the invariant. That matters more than ordinary duplication, because it **is** the post-login redirect target for all four portals — move a landing path and the guard stays green while every credentials login without an explicit `callbackUrl` lands on a stale route. `@/lib/portals` is import-free and edge-safe, so that client component can import it as-is; the fix is a deletion plus widening P-2 to *"no file outside `lib/portals.ts` contains two or more of the four literal landing paths"*. Same file as **`PF-102`** — do them together | G-DNC |
| PF-96 `AuditLog` has no foreign key to `Tenant`, so audit rows outlive the tenant they describe | V3-E04 *(raised)* | — *(story to be written)* | `open` | n/a — measured in the live local Postgres while cleaning up `S-E06-6`'s drill | **Found by deleting a tenant and finding its audit trail still there.** `model AuditLog` declares `tenantId String @db.Uuid` with **no `@relation`** (`schema.prisma:1227`), unlike `School`, `UserProfile` and `AcademicYear`, which all carry a cascading relation to `Tenant`. Measured: deleting the drill's two tenants removed all 44 `calendar_event` rows by cascade and **left all 6 `audit_log` rows behind**, pointing at `tenant_id` values that no longer exist. **This is plausibly correct, and that is exactly why it needs an id:** an append-only governance trail arguably *should* survive the subject it records, and a cascade would let a tenant deletion silently erase its own audit history — but **nothing in the repository says so.** There is no comment on the model, no ADR and no retention rule, so the same shape reads as either a deliberate append-only guarantee or a forgotten relation, and the next maintainer's obvious fix is to add the cascade. Consequences today, all local: `/admin/audit` can surface rows whose tenant is gone (untested — that page is `PF-14`'s open crash), and the drill had to clean up by hand rather than by cascade. **Owner is `V3-E04`** (audit trail and governance surfaces), which already owns the chain-field work `PF-31` describes; what this needs is a one-line policy — append-only by design, stated on the model and in an ADR, plus a retention rule — **not** a schema change taken on reflex. Priority **P3**: no data is wrong and nothing leaks across tenants; the defect is that an intentional guarantee is indistinguishable from an oversight | G-AUDIT |
| PF-95 a raw control byte in a source file makes it binary to git, and every existing gate is blind to it | V3-E06 *(raised)* | — *(story to be written)* | `open` | n/a — measured during `S-E06-6`'s verify phase with `file`, `git diff --no-index` and `grep` | **Found by two reviewers running tools out of band, which is precisely why it needs an id.** `apps/api/src/modules/calendar/calendar-seed.service.ts` was written with a **literal U+0000** (offset 3055) as a composite-key separator. Consequences, all confirmed: `git diff --no-index /dev/null <file>` reported `Bin 0 -> 13165 bytes`, so the 333-line file carrying the transaction, the dedup key, the audit row and the tenant scoping would have reached a **mandated P1 human review** as "Binary file not shown"; `.gitattributes` declares `*.ts text eol=lf`, so git was instructed to run its text filter over content it simultaneously detected as binary, and no EOL normalisation or whitespace scan applied; `grep -rn "createMany"` over the module returned **only spec hits** — the real `tx.calendarEvent.createMany` was invisible without `-a`, so every future grep-based audit or ratchet over `apps/api` would have silently excluded the file. It would have been the **only** binary source file among **691** tracked `.ts`/`.tsx`. **The finding is not the byte, it is that nothing could have caught it:** `git diff --check` has no text to scan on a binary file — the hygiene gate is blind *by construction* — a diff-reading reviewer sees a space, and neither `production-artefact-check.js` nor `link-integrity-check.js` covers source-byte hygiene. The same class already bit `S-E06-2`, where a NUL in a comment was caught only because it happened to trip `no-control-regex` in a file eslint was already reading. Fixed in `S-E06-6` (the separator is now `|`) but the **gate is not built**: a ~10-line "tracked `*.ts`/`*.tsx` must contain no `\0`" check belongs in the `scripts/*-check.js` family wired into `ci-gate.sh` and `ci.yml`, and would land green today with a zero-entry baseline. Raised, not fixed | G-DNC |
| PF-57 student portal has no profile/settings | V3-E06 | S-E06-7 | `open` | — | — | — |
| VAL-02 two-tenant adversarial suite | V3-E01 | S-E01-3 | `open` | — | — | G-TENANT |
| VAL-04 Keycloak client/redirect/audience review | V3-E01 | S-E01-4 | `open` | — | — | G-AUTHZ |
| VAL-07 custom-role create/edit/deny scenarios | V3-E05 | S-E05-1 | `open` | — | — | G-AUTHZ |
| PF-63 7 admin-dashboard analytics specs red (stale year fixture) | V3-E03 | — *(story to be written)* | `open` | `apps/api/src/modules/analytics/analytics.service.spec.ts` — 0/7 | Baselined in `scripts/known-test-failures.json`; ratchet blocks any further drift | G-TRUTH |
| PF-64 4 remediation specs red (stale Prisma doubles) | V3-E10 | — *(story to be written)* | `open` | `remediation.service.spec.ts`, `teacher-remediation.service.spec.ts` — 0/4 | Baselined. **One of the four is the catalogue tenant-scoping assertion — a G-TENANT guard that is currently not executing** | G-TENANT |
| PF-65 5 snapshot-recompute specs red (no write captured) | V3-E03 | — *(story to be written)* | `open` | `apps/worker/src/modules/analytics-snapshots/snapshot-recompute.spec.ts` — 0/5 | Baselined. Stale-harness vs inert-pipeline attribution **not established**; if the pipeline is inert this is `PF-24` | G-TRUTH |
| PF-66 2 parent-digest specs red | V3-E11 | — *(story to be written)* | `open` | `digest-email.template.spec.ts`, `parent-digest-cron.spec.ts` — 0/2 | Baselined. The unverified guard is the one preventing **duplicate parent digests** | G-TRUTH |
| PF-67 module-wiring guards assert on source, not on a booted route table | V3-E02 | **S-E02-9** | `closed` | `scripts/boot-check.js` (executed — see the widened row below) + `apps/api/src/shared/quality/boot-gate.spec.ts` 15/15 | **Closed by booting both applications in the gate.** See the widened row immediately below for the executed evidence; this row is retained because it records the finding's original, narrower diagnosis. Historical note: `grades.module.spec.ts` reads `grades.module.ts` as text because importing the module pulls `AuthModule → JwtStrategy → jwks-rsa → jose` (pure ESM) into a CommonJS ts-jest runtime. It therefore catches an unregistered controller but **cannot** catch a controller that is registered yet fails DI at boot. A boot-time route-table snapshot (or an ESM-capable jest project) would close the class properly. Raised by the `PF-62` fix. **Partially mitigated for `GradesModule` only (run 6, 2026-08-02):** every constructor dependency of both remounted controllers was resolved by hand against the module graph — `PrismaService` and `NotificationsService` via `@Global()` modules registered in `app.module.ts`, `UserSyncService` via the imported `AuthModule`'s exports, `TeacherProfileService` via `TeachingModule`'s exports, `GradesService` locally — and no import cycle exists (`GradesModule`'s only importers are `app.module.ts` and `analytics.module.ts`, neither of which it imports). `NotificationsService` is the one that would actually have crashed the API at boot: `GradesModule` never imports `NotificationsModule`, so it resolves *only* because that module is `@Global()` — de-globalising it would break startup with no test catching it. This is evidence for one module produced by **reading**, not an executed boot, so it does not generalise and the finding stays `open` | G-PORTAL |
| PF-111 the local stack’s containers can be arbitrarily older than the code, and nothing on the `up` path says so | V3-E02 *(routine evidence base)* | — *(story to be written)* | `open` | n/a — measured by `docker image inspect` against `HEAD` | **Measured at run 24’s Step 2, and it invalidates less than it looks like but more than nothing.** `pilotage_worker` was running an image built **2026-06-06** — two months older than `S-E02-10`/`13`/`14`/`15` — whose `dist/` held neither `shared/release/` nor `shared/observability/`. So `127.0.0.1:4001` refused connections and **0** metric series existed in the *running* stack, while four traceability rows read green because each was proven against `dist/` on disk and sockets the check scripts spawn themselves. **Not a code defect:** `EXPECTED_GIT_SHA` is unset, which by `S-E02-10`’s deliberate design yields verdict `unverified` and blocks nothing (DNC-08/DNC-10 exemption, documented in `main.ts`). It matters because **Step −1 makes the local Docker stack the target**, so “it works locally” is now a claim about containers, and a two-month-old container makes that claim false in silence. This is `R-05`’s class. Fix candidates, cheapest first: have `scripts/ci-gate.sh` (or a new stage) compare each running container’s image creation time against `HEAD`’s commit time and fail on inversion; or set `EXPECTED_GIT_SHA` in the local `.env` so the existing release preflight refuses to boot a stale artefact — the mechanism already exists and is simply not armed locally | G-DNC |
| PF-113 no service can be scaled, so `PF-108`'s own reproduction command does not exist here | V3-E02 *(follow-up)* | — *(story to be written)* | `open` | n/a — measured by running the command `PF-108` prescribes | **Found by trying to reproduce `PF-108` the way its own text says to.** `docker compose … up -d --scale worker=2 worker` is refused because `worker` declares `container_name: pilotage_worker`, and **16 of the compose file's 21 services** do the same — so `--scale` is unavailable for all of them. `PF-108` remains a real defect (a gauge summed across replicas is wrong wherever replicas exist), but its fix could only be demonstrated by injecting a second `instance` label into the PromQL — `sum by (queue,state)` → **4**, `max by (queue,state)` → **2**, against a real depth of **2** — rather than by running two workers, and that is a weaker demonstration, recorded as such. The wider point is the one worth scheduling: **no application in this repository has ever run at more than one instance**, so every per-process assumption in the observability layer is untested against a replica-bearing shape. The fix is a decision (is horizontal scaling in scope?) before it is a change; `container_name` is deliberate and locally useful. Priority **P3** | G-TRUTH |
| PF-59 Actions account-locked (billing); no CI job starts | V3-E02 | — *(no story: not a code defect)* | `blocked` | n/a — cannot run | Check-run annotation on job `91540293327`: `"The job was not started because your account is locked due to a billing issue."` Content-independent and repo-wide — Dependabot PR #169 and every `main` run since 2026-07-28 fail identically in ~3 s with zero steps executed; job logs return `BlobNotFound` because no job produced any. **Blocks `VAL-01`** and degrades every CI-dependent gate to `evidence: deferred`. Resolution is a billing action by the repo owner | G-DNC |



## 2. Layer 1 — Signature loop

| Finding | Epic | Story | Status | Test | Evidence | Gate |
|---|---|---|---|---|---|---|
| PF-13 gradebook wrong identifier | V3-E07 | S-E07-1 | `open` | — | — | G-PORTAL |
| PF-21 student DOB dropped | V3-E08 | S-E08-1 | `open` | — | — | G-TRUTH |
| PF-22 lesson edit 400 | V3-E08 | S-E08-2 | `open` | — | — | — |
| PF-37 lesson defaults Published | V3-E08 | S-E08-3 | `open` | — | — | G-PORTAL |
| LG-29 lesson visa workflow | V3-E08 | S-E08-4 | `open` | — | — | G-AUDIT |
| PF-30 batch grades N+1 + phantom revisions | V3-E08 | S-E08-5 | `open` | — | — | G-AUDIT |
| PF-23 calendar edit destroys scope/year | V3-E08 | S-E08-6 | `open` | — | — | G-TRUTH |
| PF-42 dead filter, fabricated metrics | V3-E08 | S-E08-7 | `open` | — | — | G-TRUTH |
| PF-06 attendance silent partial save | V3-E09 | S-E09-1 | `open` | — | — | G-AUDIT |
| PF-35 attendance class/history mixing, invalid trend | V3-E09 | S-E09-2 | `open` | — | — | G-TRUTH |
| PF-49 `BEHAVIOR_ALERT` can never fire; bounds drift | V3-E10 | S-E10-1 | `open` | — | — | G-TRUTH |
| PF-28 alert/announcement truncation windows | V3-E10 | S-E10-2 | `open` | — | — | G-TRUTH |
| PF-27 parent closes school remediation plan | V3-E10 | S-E10-3 | `open` | — | — | G-AUTHZ |
| PF-44 meeting "Clôturer" mislabeled | V3-E10 | S-E10-4 | `open` | — | — | — |
| PF-16 announcement audience misclassified, student missed | V3-E11 | S-E11-1 | `open` | — | — | G-PORTAL |
| PF-34 class messaging 404 / zero audience | V3-E11 | S-E11-2 | `open` | — | — | G-PORTAL |
| PF-43 conversation moderation unreachable | V3-E11 | S-E11-3 | `open` | — | — | G-AUTHZ |



## 3. Layers 2–4 — capability build-out

| Finding | Epic | Status | Gate | Note |
|---|---|---|---|---|
| PF-47, LG-12, LG-27, PF-33, PF-41 | V3-E12 | `open` | G-TENANT, G-AUDIT | one state machine; must not reproduce DNC-04/DNC-11 |
| PF-48, PF-57, LG-15, LG-23, LG-24, LG-25 | V3-E13 | `open` | G-AUDIT | documents from one frozen snapshot |
| LG-13, LG-14, LG-28 | V3-E14 | `open` | G-AUDIT, G-TRUTH | period closure gates official output |
| LG-01, LG-02, LG-06 | V3-E15 | `open` | G-TENANT, G-AUDIT | immutable ledger; must not reproduce DNC-01 |
| LG-03, LG-04, LG-05, LG-07, LG-09 | V3-E16 | `open` | G-AUDIT | idempotent callbacks; DNC-12 |
| LG-10, LG-11 | V3-E17 | `open` | G-TENANT | consent + opt-out; must not reproduce DNC-07 |
| LG-16 timetable | V3-E18 | `open` | G-TENANT | first candidate for promotion — real daily gap, no legal exposure |
| LG-17 discipline / incidents | V3-E18 | `open` | G-AUDIT | safeguarding policy review; must not reproduce DNC-03 |
| LG-18 clubs and activities | V3-E18 | `open` | G-TENANT | discovery only |
| LG-19 HR staff registry and contracts | V3-E18 | `open` | G-AUDIT | staff identity modelled separately from user account (avoids DNC-05) |
| LG-20 statutory payroll | V3-E18 | `blocked` | G-AUDIT | **blocked on D-06 legal review**; consider integrating rather than building |
| LG-21 staff timekeeping | V3-E18 | `open` | G-AUDIT | sequenced after LG-19 |
| LG-22 cafeteria / transport / services | V3-E18 | `open` | G-TENANT | must reuse E15 charge + subscription primitives, not fork them |
| LG-08 budget forecast vs actual | V3-E18 | `open` | G-TRUTH | after E16 |
| LG-26 sensitive / health case register | V3-E18 | `blocked` | G-AUTHZ, G-AUDIT | **blocked on D-07 legal + DPO review**; highest-risk data class |



## 3bis. Validation obligations not owned by an L0 story

These are evidence programmes, not code. They attach to the epic that first needs them.

| Obligation | Epic | Status | Gate | Blocked by |
|---|---|---|---|---|
| VAL-05 provider sandbox: delivery, retry, callback, outage, reconciliation | V3-E16 (payments), V3-E17 (messaging) | `blocked` | G-AUDIT | D-03 + sandbox credentials |
| VAL-06 import batch validate → apply → **rollback** with a synthetic batch | V3-E12 | `open` | G-AUDIT, G-TENANT | E12 promotion (needs a batch to exist) |
| VAL-08 full WCAG keyboard / screen-reader / contrast audit | V3-E02 (CI harness), V3-E06 (fixes) | `open` | — | axe/Playwright assets exist but are not run — see VAL-01 |
| PF-129 **P1** *(was `TECH_DEBT`/latent in `audit-findings-index.md`; **this slice makes it LIVE** — escalated, not newly discovered)* — a third `apps/web` server-side fetch to the API bypasses the one provenance helper, and the invariant that forbids it is enforced nowhere | V3-E04 → **V3-E06** *(fix is `apps/web` — **track c's seam**)* | — *(story to be written by track c)* | `open` — **verified by the routine at land of `S-E05-11`**, by reading the single caller rather than by trusting the brief | — *(none — `apps/web` has no component unit runner, `PF-129`/`PF-133`)* | **`PF-129`'s own text predicted this exact event:** *« Latent today — `RegisterController` writes no audit row — which is exactly what makes it dangerous: the day parent self-registration becomes an audited event, the request arrives with no `x-pilotage-*` header, branch 2 fires, and the row records the web container's egress address with no test red anywhere. »* `S-E05-11` is that day. The routine's own briefing for this slice asserted the opposite — « this endpoint is public so the request genuinely IS the operator's browser » — and the implementing agent **falsified it by measurement** rather than obeying it: `apps/web/src/app/parent/register/actions.ts:1,25-29` is `'use server'` and issues a bare `fetch` carrying only `Content-Type` / `Accept`, so it never calls `clientProvenanceHeaders`. Consequence, now shipping: `extractAuditClientHints` takes branch 2, so in production (`TRUST_PROXY_HOPS=2`) the chain is shorter than N and `ipAddress` is honestly **null**, while on the local `--profile app` topology (N = 0) it is the **web container's** egress address — one constant value shared by every registrant, which is `PF-31` reconstituted — and `userAgent` is `undici`'s, not the browser's. **The API side is already correct and needs no change**: the seam never invents, and the day `apps/web` forwards the headers this row starts recording the real operator with no edit to `register.controller.ts`. Fix stays as `PF-129` always stated it: route `registerParentAction` through `clientProvenanceHeaders` (or through `api()`), **and** add the missing web-side quality spec over the invariant — any `fetch(` whose URL contains `API_URL` under `apps/web/src` must be preceded by `clientProvenanceHeaders` in the same lexical scope — with the three current seams as positive controls. Raised to **P1** because it is no longer latent: an audited write is recording a wrong-or-null provenance today | G-AUDIT, G-PORTAL, G-DNC |
| PF-175 **P2** — grants that escalated **before** the ceiling existed pass it unconditionally, because the union that feeds the comparison includes the escalated role | V3-E05 | **`S-E05-2c`** — `docs/spec/features/v3-e05/stories/S-E05-2c.md` | `open` — **narrowed, not closed**, by `#229` (`S-E05-2c`, 2026-08-12): the detector now exists and is proven. Only an execution against a real database can close it | `apps/api/src/shared/auth/legacy-escalation-sweep.spec.ts` — **49/49** at the gate of `#229` (not re-measured this run), `T-0`…`T-15`: detection, fail-closed on every degenerate input (unknown code, missing `permissionCodes`, non-array candidates, empty/`undefined` baseline), determinism and de-duplication, tenancy scope, the CLI proven read-only *by source reading*, and DNC-10 arities `(2, 1, 1)` asserted so an options bag cannot appear later. **Supersedes this row’s previous « no test: the condition is data, not code »** — the *condition* is data, but the *detector* is code and is now tested | The ceiling compares against `effectivePermissions`, which unions **custom-role** permissions into the grantor’s set (`user-sync.service.ts:80-84`, re-verified 2026-08-12). An actor who already holds a role minted through `PF-09` therefore holds those codes *effectively* and can keep minting and assigning them — the guard reads them as legitimately held. **This is inherent to a grantor-relative ceiling and is not a defect in it**; what was missing was the detection sweep. **Shipped by `#229`:** a pure fail-closed module plus a read-only CLI, `pnpm --filter @pilotage/api sweep:legacy-escalation` — exit **0** clean · **1** findings · **2** inconclusive, so a database outage can never read as « propre ». **The SQL previously recorded on this row is invalid and must not be restored.** It names `r."isSystem"` and `rp."roleId"`, but the physical columns are `is_system` and `role_id` (`@map`, `schema.prisma:906/917`), so it dies on « column r.isSystem does not exist » — and an *error* would have been read as a *finding*. It could not have worked as written in any case: the permission **code** lives on `Permission`, not on `role_permission`, so `count(rp.*)` yields no code to compare against the baseline. **Residual — the only thing left on this finding:** the sweep has still **never been executed against a database**. The run that would have done it (2026-08-13) could not: the local Docker daemon was wedged (Docker Desktop processes from 2026-08-08 alive, `compose` Postgres refusing on its mapped port) and the three track worktrees were removed mid-run by the S3 revert (`#227`). Empty result ⇒ downgrade to monitoring; non-empty ⇒ **human triage, never automatic revocation**: `role` carries no grant provenance (`grantedBy` is on `user_role`), so a legitimate grant reports identically to a `PF-09` artefact | G-AUTHZ |
| PF-176 **P3** — an attempted privilege escalation is logged, and logged anonymously | V3-E05 → V3-E04 | — *(recorded, **not** storified this run — routine triage)* | `open` — declared by `S-E05-2` in code and in `ADR-015` rather than discovered after it | `privilege-ceiling.spec.ts` asserts the refusal, not the log | `privilege-ceiling.ts`'s `assertWithinCeiling` emits a `logger.warn` naming the exceeding codes and the set sizes — deliberately, because a refusal writes **no audit row** (nothing happened, and that is the house posture for a pre-transaction refusal) and an attempted escalation on a children's-data platform must not be silent. But the predicate has **no actor context by design**, so the line carries no `jwt.sub`, no `tenantId` and no client hints: it says an escalation was attempted and cannot say by whom. **Adding a third parameter was refused on purpose** — it is the seam through which an options bag, and later a bypass flag, would arrive (DNC-10). Fix direction: move the `warn` out of the predicate to the four call sites, where `jwt.sub`, `tenantId` and the provenance hints are already in hand | G-AUDIT |
| TOOL-05 **P2** *(ledger, not product)* — finding-id allocation is a race, and with three concurrent tracks it is now a race that is regularly lost | V3-E02 *(routine/ledger)* | — *(recorded, **not** storified this run — routine triage)* | `open` — observed live by the routine at land of `S-E05-2` | n/a — observed from two merged PRs declaring the same id | Two runs on 2026-08-11 read the register's maximum as `PF-172` within the same window and both allocated **`PF-173`**: `S-E05-1` (track c, `#215`) and `S-E05-2` (track b, `#218`). Both merged, so `main` briefly declared **two** `PF-173`s; resolved by renumbering the later-landing one to **`PF-178`**, per the rule `PF-150`'s own note already states. **Distinct from `PF-150`:** that was two *registers* disagreeing about one number; this is two *writers* allocating the same number from a correctly-read register. The ledger inbox prevents exactly this for **rows** and does nothing for **ids** — `tracks.md` guarantees no two tracks edit the same code or the same ledger file, and guarantees nothing about an id space that is global, dense, and allocated by reading a maximum. **Frequency is the argument:** three tracks each raising findings collide whenever two land in the same window, which is now the normal case rather than the exception. Fix directions, cheapest first: a **per-track id stripe**; or `routine-lock.sh gate` handing out a reserved block with the track claim; or `v3-reconcile-ledger.js` **refusing to fold** a block whose id already exists and reporting it — the last turns a silent duplicate into a loud one without changing how ids are chosen | G-DNC |
| TOOL-04 **P1** *(gate, not product)* — the fast gate escalates to an api suite that cannot finish, and it escalates precisely when the diff improves a gate | V3-E02 *(routine/gate)* | — *(recorded, **not** storified this run — routine triage; and it is the routine's own instrument, not the product)* | `open` — measured by the routine across **four** ratchet runs at land of `S-E05-2` | `scripts/ci-gate.sh` + `scripts/test-ratchet.js api` — the stage is red, and red for a reason that has nothing to do with any diff | **`ci-gate.sh` picks between two api stages on `GATE_MACHINERY='^(scripts/\|\\.github/\|infra/\|apps/api/src/shared/quality/)'`: a match runs the WHOLE api suite bounded at 2400 s, a non-match runs `--skip src/shared/quality/` bounded at 1200 s. The whole suite does not complete on this machine — it BLOCKS rather than runs.** Measured: (1) contended, timed out at 2400 s — three `test-ratchet.js api` trees were alive at once, two orphaned for 7–8 h, which is `PF-90` live, and the routine enumerated and killed **24** orphaned processes; (2) uncontended, timed out at 2400 s again, 2877 s for the whole gate; (3) unbounded and uncontended, **still running at ~80 min** with the 7 jest workers holding only **7–40 s of CPU each** — blocked, not computing — killed; (4) `--skip src/shared/quality/` → **`1008/1019 passed · 11 failing · 11 known-failing (baseline)`, `no drift`, exit 0** in minutes. The difference between (3) and (4) is exactly `src/shared/quality/**`, whose members are the artefact- and Docker-dependent meta-gates (`boot-gate`, `web-artifact-gate`, `link-integrity-gate`, `schema-drift-gate` all failed in run 2) — and the fast gate deliberately **no longer builds**, so the escalated branch runs artefact-dependent specs in a mode that produces no artefact. **The sharp end is the escalation predicate, not the bound:** touching any file under `apps/api/src/shared/quality/` is exactly what a slice that STRENGTHENS a gate does, so the gate is hardest to pass precisely when the diff improves it — `S-E05-2` hit this because its sprint extended `audit-write-gate.spec.ts`. Third consecutive gate-bound issue after `#216` and `#217`, both of which raised the bound; the measurement above suggests the bound is not the variable. **The blocking spec was NOT isolated and is not guessed at.** Recorded under `TOOL-` per the triage rule | G-DNC |
| PF-177 **P3** — a duplicated permission code answers 400 while naming no code at all | V3-E05 | — *(recorded, **not** storified this run — routine triage)* | `open` — measured by the routine at land | `roles.controller.spec.ts` covers the ceiling, not this arithmetic | `roles.controller.ts` validates the catalogue by **length comparison** — `perms.length !== body.permissionCodes.length` — so `["students.read","students.read"]` fails (one row returned, two codes sent) and the derived `missing` array is computed by `filter`, which finds every code present and yields **`[]`**. The caller receives `400 Permissions inconnues` with an empty list of unknown permissions: a refusal that cannot say what it refused. Pre-existing, unrelated to the ceiling, and unchanged by it; recorded because `S-E05-2` put a second refusal beside it and the two must not be confused when debugging. Fix: compare de-duplicated sets, or resolve `missing` by set difference rather than by count | G-DNC |
| PF-179 **P2** — a 401 during a role write renders `NEXT_REDIRECT;…` to the admin instead of redirecting to login | V3-E06 *(raised by `S-E06-8`; track c seam)* | — *(recorded, **not** storified this run — routine triage)* | `open` — found by `S-E06-8` while writing AC-1, confirmed against the source at land | — *(none — `apps/web` has no component unit runner, `PF-129`/`PF-133`)* | `apps/web/src/app/admin/roles/actions.ts` — none of `createRoleAction` (`:24-31`), `updateRoleAction` (`:44-50`) or `deleteRoleAction` (`:58-64`) re-throws the Next navigation signal. `api()` calls `redirect()` on a 401 (session expired in Keycloak's view, refresh failed); `redirect()` throws an error whose `digest` begins `NEXT_REDIRECT;`. Caught by a blanket `catch`, that digest is returned **as data** and rendered as an inline error — so the admin is shown `NEXT_REDIRECT;replace;/admin/login…` and is **never sent to the login page**. `S-E06-8` fixed exactly this shape on `/admin/users` by routing through `apiResultFromError`, which checks `isNextNavigationSignal` **first**; these three actions predate that converter and hand-roll their catch instead. **Same family as `PF-174` and the inverse failure**: `PF-174` showed nothing when it should have shown a refusal; this shows a framework internal when it should have navigated. Fix: route all three through `apiResultFromError` (they already return the compatible `{ ok, error }` shape), which also picks up `PF-174`'s total extractor and closes `S-E06-8`'s follow-up F2 in the same pass | G-DNC |
| PF-180 **P3** — the notification-preference actions discard the API's message and render `HTTP 403` for every refusal | V3-E06 *(raised by the routine in Step 2 of `S-E06-8`)* | — *(recorded, **not** storified this run — routine triage)* | `open` — measured while counting catch clauses across the web surface | — | `apps/web/src/app/admin/settings/preferences-actions.ts` — all three actions (`updatePreferenceAction:52`, `setChannelForKindsAction`, `setCadenceForKindsAction`) collapse an `ApiError` to `` `HTTP ${err.status}` `` and **never read `body.message`**, so a permission refusal, a validation error and a tenant refusal are indistinguishable from each other and from a proxy failure. Not the `PF-174` silence — a message *is* shown — but it carries no information the user can act on, and the endpoint is reachable from **three** portals (`revalidateSettings()` invalidates `/admin`, `/teacher` and `/parent` settings). Lowest priority of the three raised here because the surface is a preferences toggle, not a privileged mutation. Fix: return `apiErrorMessage(err)` from the leaf module `S-E06-8` created — a one-line change per action, and the same retrofit F2 names for `admin/roles/actions.ts` | G-DNC |
