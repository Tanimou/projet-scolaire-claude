# Traceability archive — L0 (CLOSED)

> Append-only archive of closed findings for L0. **Not read during story selection.**
> Kept in full for audit: every closed row retains its story, test and evidence.
>
> Partitioned 2026-08-08 by the throughput change (S1). Nothing was deleted — every row lives in exactly one file.


## 1. Layer 0 — Trust and production foundation

| Finding | Epic | Story | Status | Test | Evidence | Gate |
|---|---|---|---|---|---|---|
| PF-03 (drift half) nothing KEEPS the ledger honest — `schema.prisma` can be edited without a migration and every gate stays green | V3-E02 | **S-E02-5** | `closed` | `apps/api/src/shared/quality/schema-drift-gate.spec.ts` — **94/94** (`pnpm exec jest src/shared/quality` → **593/593** across 14 quality guards, was 499 across 13); `scripts/schema-drift-check.js` — executed, positive **and** four negative paths | **The comparison is EXECUTED against a real PostgreSQL, and the reflex implementation was measured and rejected before a line was written.** The defect was structural: `ci-gate.sh` runs `prisma generate`, so it generates a client for a schema **no migration produces**, and lint/typecheck/build/boot then all validate against that fiction, while `infra/docker/migrate-entrypoint.sh` runs `migrate deploy` and only `migrate deploy`, so the edit reaches no database ever. `scripts/schema-drift-check.js` creates a disposable scratch database, applies `apps/api/prisma/migrations` to it, diffs **that database** against the datamodel, and drops it on every exit path (`finally`, `SIGINT`, `SIGTERM`, `uncaughtException`, `WITH (FORCE)`, name-pattern-guarded, orphans swept). **Positive path executed:** `SCHEMA DRIFT CHECK: PASS`, **exit 0**, 55 base tables, `migrate diff` **exit 0 "No difference detected"**, ≈17 s, no scratch database left behind. **Negative paths executed:** a temp copy of `schema.prisma` carrying one extra model → **exit 2, `[+] Added tables - DriftProbe`** (the drifted object is NAMED, and Prisma's output is carried verbatim rather than paraphrased); a temp migrations directory holding invalid SQL → `migrate deploy` **exit 1 (P3018)** → verdict `migrate_deploy_failed`; a dead address → **exit 1** naming all three routes tried. **The measurement that shaped the design:** `migrate diff --from-migrations … --exit-code` returns **exit 2 on this repository unchanged**, reporting all five PostgreSQL extensions as `[+] Added` although `0_baseline` creates every one of them — a gate built that way is permanently red on correct code with no route back to green except breaking correct code, so its absence is pinned by test. `--from-url` was also rejected: it puts the password in `argv` (ADR-025 D6), and `--from-schema-datasource` with `DATABASE_URL` in the child env only addresses the same database with no credential in `argv`. **Not claimed:** it proves the ledger *reproduces* and *executes*, not that a migration is **safe**, not against a **non-empty** database, and not against any **deployed** database | G-MIGRATION |
| DNC-08/DNC-10 at the drift gate — a check that cannot run must fail, and nothing may turn it off | V3-E02 | **S-E02-5** | `closed` | `apps/api/src/shared/quality/schema-drift-gate.spec.ts` — **94/94**, of which the bypass matrix is EXECUTED in child processes | **Proven by running the script, not by grepping it.** Against a dead `DATABASE_URL`, the CLI exits **1** and names every route it tried (`prisma db execute`, host `psql`, `docker exec pilotage_postgres psql`) with the actual error from each — it never prints "skipped" and exits 0. The same run with `SKIP_SCHEMA_DRIFT=1`, `ALLOW_SCHEMA_DRIFT=1`, `SCHEMA_DRIFT_CHECK=0`, `FORCE=1`, `CI=false`, `NODE_ENV=production` set **singly and all together** returns the identical exit code and the identical verdict as the clean-environment control. `parseArgs` knows exactly one flag (`--help`) and refuses every other argument with *"This script has no bypass option."*; `DATABASE_URL` is the only environment variable read, and it is monotone in the failing direction — it names WHERE a scratch database is built, never WHETHER the ledger reproduces the schema, because the scratch is created EMPTY and migrated from the migrations on disk wherever it points. `evaluateDrift({})` is `unknown`/exit 1 and NAMES all nine missing evidence fields — the `restore-drill.js` §0a lesson applied from the start rather than after review. `ok` is the only verdict mapping to exit 0. The two path-parameterised seams the spec needs are FUNCTION exports, never CLI flags. **The stage is wired into both harnesses and a test fails if either wiring is removed**, read on comment-stripped executable content (PF-83), with job affinity asserted in `ci.yml` (it must sit in the job that declares the `postgres:15-alpine` service, before `prisma generate`, with no `continue-on-error`) and order asserted in `ci-gate.sh` (after compose-invocation, before `prisma generate`, **outside** the `--quick` guard) | G-DNC |
| PF-55 tests/typecheck unexecutable | V3-E02 | S-E02-2 | `closed` | `scripts/test-ratchet.js` + `scripts/ci-gate.sh` — both suites executed in full | **The suites were executed, which is the whole finding.** Typecheck now passes in all three apps (`tsc --noEmit` exit 0 for api, worker, web — the audit's "cannot resolve Jest types / workspace tsconfig / Next route types" errors no longer reproduce). Both jest suites ran end to end: **api 424/435, worker 146/153 → 18 failing, 568 passing, 588 total across 45 spec files.** Every one of the 18 is enumerated in `scripts/known-test-failures.json` with a cause and an owning finding id (`PF-63`…`PF-66`), satisfying AC-2 ("executed or explicitly listed with a reason and a finding id"). The gate itself is executed, not asserted — **both** ratchet directions were exercised: a deliberately failing probe spec was rejected (`exit 1`, "1 NEW test failure"), and a baseline entry pointed at a passing test was equally rejected (`exit 1`, "1 baseline entry now PASSES"). Probe deleted after the run. **Deferred:** the *hosted* gate — `.github/workflows/ci.yml` is repaired but cannot execute while `PF-59` (Actions billing lock) stands; Playwright/axe stages are still not wired (`VAL-08`) | VAL-01 |
| PF-78 tracing configured end to end, emitted by nothing | V3-E02 | **S-E02-14** | `closed` | `apps/api/src/shared/tracing/tracing.spec.ts` — **17/17**; `apps/api/src/shared/quality/tracing-gate.spec.ts` — **21/21** (`pnpm exec jest src/shared/quality` → **112/112** with the five other quality guards); `scripts/tracing-check.js` — executed, positive **and** three negative paths | **Spans are emitted, and the proof is an executed request rather than a configuration read.** `node scripts/tracing-check.js` spawns a child process per application, loads the **built** tracing module, serves a real request over a real socket and reports what came out: **api 2 spans, worker 2 spans, carrying no identifier**. Before this slice that number was 0 in all three applications. **The finding was larger than recorded, and that is the run's main result:** `OTEL_EXPORTER_OTLP_ENDPOINT` sat on the compose file's *shared* environment anchor, so it reached **five** services — `migrator`, `api`, `worker`, `web`, `seed` — not the three the finding names. Two are one-shot jobs and one is Next.js; none could ever emit. It is now declared on exactly the two services that start an SDK, and the gate fails **in both directions** if that list and `TRACED_SERVICES` diverge. **The sharp check is load order.** OpenTelemetry patches modules as they load, so an SDK started after `require('./app.module')` patches nothing, throws nothing, and yields an application that looks instrumented and emits no request spans. The gate reads the order off the **emitted** `main.js`. That is not theoretical here: the emission probe's own first version required `node:http` before `startTracing()` and produced **0 spans** — the defect reproduced by accident, inside the tool written to detect it. **G-TENANT is the design constraint, not a footnote:** Jaeger is unauthenticated by construction, so redaction is applied at the **exporter**, the one point every span passes regardless of which instrumentation produced it. One copy, in `packages/contracts/src/observability/`, imported by both apps — two copies and it takes one forgetting `db.statement` for tenant-keyed Redis commands to leave the process. Executed in three directions against the **real** script: bootstrap reordered after `app.module` in the emitted `main.js` → **exit 1** naming the app; `web` handed a collector → **exit 1** naming the service; probe returning 0 spans → **exit 1** (observed live, before the probe was fixed). No bypass flag (DNC-10), locked by a test. **Not claimed:** it does not start Jaeger and watch a span arrive — that needs `docker compose --profile obs up`, which the routine forbids — so the pipeline is proven to **emit and redact**, not to **be ingested**. Prisma is deliberately not instrumented (its instrumentation needs a `previewFeatures = ["tracing"]` schema change, i.e. G-MIGRATION in a slice that does not need it), and there is no official BullMQ instrumentation, so job processing is untraced. `apps/web` emits nothing at all — recorded as **`PF-79`** rather than left as a silence | VAL-09, G-TENANT |
| PF-79 `apps/web` covered by no observability surface — no metrics, no traces | V3-E02 | **S-E02-15** | `closed` | `apps/api/src/shared/quality/web-observability-gate.spec.ts` — **41/41**; `pnpm exec jest src/shared/quality` → **218/218** across 8 suites (was 124 before this epic's gate work); `scripts/tracing-check.js` and `scripts/observability-check.js` — executed, positive **and** negative | **The third artefact is observed, and the gate ran the whole thing rather than reading it.** Full `scripts/ci-gate.sh`: **`GATE: PASS`, 12/12 stages, exit 0** — reported as a verdict line, not a selection of stages (**R-23**). The gap was measured before it was filled, and the measurement is the finding: `grep -rniE 'prom-client\|opentelemetry\|/metrics' apps/web/src` returned **0 hits** while `node scripts/observability-check.js` returned **exit 0** — the gate was green on precisely the state this row describes. **What landed:** a Next 15 `apps/web/src/instrumentation.ts` `register()` hook (runtime-guarded, dynamic-import-only, and total — it cannot throw, because a throw there is fatal to Next's bootstrap and would make *optional* observability a boot dependency of the one artefact users touch), starting a tracer provider and serving Prometheus on a **dedicated internal `node:http` socket**. **The socket is the security decision, not a style one, and it corrects the story as written:** the story asked for a Next route handler; `infra/nginx/conf.d/pilotage.conf`'s `location /` proxies everything unmatched to the web upstream, so a `/metrics` route would have been **public on the internet the day it was added** — and the existing nginx rule would not have caught it, because nginx would not have changed. The gate now asserts the metrics path is absent from the Next route inventory. Metric names are deliberately **identical** to the API's behind an `app="web"` label, so one dashboard can answer the only question that matters in an incident — render or API? — and every HTTP panel of `pilotage-slo.json` gained the `app` grouping without which the two would have blended into one line. **G-TENANT holds on two unauthenticated surfaces:** traces through the single `withRedaction` in `@pilotage/contracts` (a guard spec fails if this module defines its own `sanitize*`), and metric labels through a `routeLabelFromSpan` that reads only template attributes — necessary because a span processor sees attributes *before* export redaction. Executed: `web: a real request produced 1 span(s), carrying no identifier` and `the exposition carries a route-template-labelled histogram sample`, with the probe's cuid and `tenantId` absent from both. **Two real defects were found by running it, not by reading it, and both are the reason this row is trustworthy.** (1) The first build **failed**, with 16 `Module not found: Can't resolve 'fs' / 'v8' / 'cluster'` all traced `instrumentation.ts → web-observability.js → prom-client`: `apps/web/src/middleware.ts` exists, so Next compiles the hook for the **edge** runtime too, and the original guard was an early `return` — which webpack cannot see as a boundary, leaving the `import()` statically reachable and resolved for a runtime with no node builtins. Only `if (NEXT_RUNTIME === 'nodejs') { await import(…) }` gives `DefinePlugin` a branch that dead-code elimination can remove. The **form** of the guard was load-bearing, correct-looking and read by nothing — R-26's eighth instance — so it is now a test, proven in the negative (**1 failed / 40 passed** on the early-return form). (2) `apps/api/src/shared/tracing/tracing.spec.ts` pinned `TRACED_SERVICES` at exactly two services; widening the contract broke it and the ratchet caught it as a **NEW** failure. Updated to three **because the capability was added and is proven by execution**, not to make a red test green — the substantive both-directions check lives in `tracing-check.js`. **Not claimed, and this is most of what is left:** nothing was *ingested* — no Prometheus scrape, no Jaeger span arrival, no booted Next — because that needs `docker compose --profile obs up`, which the routine forbids; **Next's own span emission is inferred** from its built-in OpenTelemetry support rather than executed, since booting Next needs a database and a Keycloak session; span **events** (`exception.*`) are not redacted; `OTEL_TRACES_SAMPLER_ARG` is deliberately **not** honoured on web, because head sampling would drop spans before the metrics processor and bias p95 by an unknown factor; and metrics are per-process, so a multi-worker deployment reports one worker's numbers | VAL-09, G-TENANT |
| PF-03 (seed half) demo seed runs on production by default | V3-E02 | S-E02-4 | `closed` | `apps/api/prisma/seed-guard.spec.ts` — **34/34 passed locally** (`pnpm exec jest prisma/seed-guard.spec.ts`) | **La garde est exécutée, pas affirmée, et dans les deux sens.** Step 2 a trouvé le défaut plus large que la story : **1 seul des 7 scripts** portait un contrôle (`seed-demo.ts`), `infra/docker-compose.prod.yml` le neutralisait en forçant `NODE_ENV: development` **sur le seul service `seed`** (tous les autres reçoivent `production`) avec un commentaire disant explicitement pourquoi, et `scripts/deploy-prod.sh` enchaînait les 7 seeds **par défaut** (`--no-seed` était l'opt-out). `ALLOW_SEED` n'existait nulle part. Corrigé par une garde partagée à **deux signaux indépendants** (`NODE_ENV` réglé par service vs `DEPLOY_ENV` réglé au niveau stack), câblée dans les 7 scripts avant tout `new PrismaClient()`. **Chemin négatif exécuté :** les 7 scripts lancés réellement avec `DEPLOY_ENV=production` **et le jeton correct** sortent tous en **exit 1** `[refused-production]` — le jeton n'est donc pas un contournement (DNC-10). La forme historique exacte (`NODE_ENV=development` + `DEPLOY_ENV=production`) sort en 1 `[refused-conflicting-signals]`, sans opt-in en 1 `[refused-no-opt-in]`, avec `ALLOW_SEED=true` en 1 `[refused-bad-token]`. **Chemin positif exécuté aussi** — une garde qui refuse tout casserait R-12 : en `DEPLOY_ENV=demo` + jeton exact, le script **franchit** la garde et échoue plus loin sur `PrismaClientInitializationError: Can't reach database server`, pas sur la garde. `docker compose config` confirme le défaut fermé sur la stack hébergée (`DEPLOY_ENV: production`, `ALLOW_SEED: ""`). **Non traité, dit franchement :** les données de démo **déjà écrites** en production ne sont pas retirées — c'est une action destructive sur données hébergées (condition d'arrêt n°3), documentée dans `docs/runbooks/provision-demo-tenant.md` §6 | G-MIGRATION |
| PF-69 `apps/api/prisma/` échappe au typecheck **et** au lint | V3-E02 | S-E02-7 | `closed` | `apps/api/src/shared/quality/lint-gate.spec.ts` — 26/26 (bloc « api prisma/ is inside both gates ») | **Les deux gates couvrent désormais le répertoire, et les deux ont été exécutés.** Typecheck : `tsc --noEmit -p prisma/tsconfig.json` ajouté au script `typecheck` de l'api, et les deux casts `TS2571` corrigés en reprenant l'idiome déjà présent dans `seed-keycloak-users.ts:91` (`const json = (await res.json()) as { access_token: string }`) plutôt qu'en inventant un troisième style — `pnpm typecheck --force` : **13/13, 0 en cache**. Lint : le glob `eslint "{src,test}/**/*.ts"` devient `eslint .`, `prisma/**/*.ts` est linté avec `no-console` relâché (c'est la sortie attendue d'un script de seed). Les ~2 900 lignes qui écrivent tout le jeu de démo — la chaîne derrière `PF-17` — ne sont plus invisibles aux deux gates | — |
| PF-70 le stage `lint` de `ci-gate.sh` n'a jamais passé | V3-E02 | S-E02-7 | `closed` | `apps/api/src/shared/quality/lint-gate.spec.ts` — **26/26** (`pnpm exec jest src/shared/quality`) | **Le stage est exécuté, dans les deux sens.** Step 2 a trouvé le défaut plus large que la fiche : PF-70 décrivait « 4 tâches vertes par cache, une rouge ». Mesuré paquet par paquet, **7 des 8 paquets lintables sortaient en exit 2** ; seul `apps/web` sortait 0, et uniquement parce que `next lint` force silencieusement le mode eslintrc — donc le gate ne lintait rien d'autre que web. Corrigé par une migration flat-config **totale** : `packages/eslint-config` réécrit en tableaux plats (`base`/`node`/`react`), un `eslint.config.js` dans chacun des 8 paquets, les 3 `.eslintrc*` supprimés, `apps/web` passé au CLI ESLint via `FlatCompat`. **Chemin positif exécuté :** `pnpm lint --force` → **13/13 tâches, 0 en cache** (le `--force` est le point : aucune tâche ne pouvait rapporter un ✓ périmé). **Chemin négatif exécuté :** une sonde `no-useless-escape` introduite dans `@pilotage/i18n` fait sortir le stage en **exit 1**, au niveau paquet *et* à travers turbo ; sonde supprimée, retour à 0. **Aucun gate affaibli** — les 6 erreurs réelles ont été **corrigées** (2 × `no-useless-escape`, `no-empty-object-type`, une directive `eslint-disable` nommant une règle non chargée, `no-require-imports`), pas désactivées ; le seul `eslint-disable-next-line` ajouté porte sa raison. Première mesure honnête du reste : **996 warnings**, enregistrés comme `PF-71` | G-DNC |
| PF-71 996 warnings de lint, invisibles jusqu'ici | V3-E02 | S-E02-8 | `closed` | `apps/api/src/shared/quality/lint-ratchet.spec.ts` — **20/20** (46/46 avec `lint-gate.spec.ts`, `pnpm exec jest src/shared/quality`) | **996 → 44, et le nombre est désormais contraint dans les deux sens.** `scripts/lint-ratchet.js` + `scripts/lint-warning-baseline.json` posent un plafond par paquet : dépasser échoue, **rester en dessous échoue aussi** (le plafond ne peut que baisser), une erreur ESLint échoue toujours, et un paquet lintable absent du baseline échoue — c'est cette dernière règle qui empêche de rejouer PF-69/PF-70 à une nouvelle adresse. Câblé dans `ci-gate.sh` (stage 2) **et** dans `ci.yml`, avec un test qui vérifie que les deux ne divergent pas. **Exécuté dans les deux sens :** sonde `no-unused-vars` dans `@pilotage/i18n` (plafond 0) → **exit 1** en nommant la règle ; sonde retirée → exit 0 ; plafond artificiellement relevé → **exit 1** « BELOW the ceiling » ; paquet retiré du baseline → **exit 1** « absent from the baseline ». **La correction majeure porte sur la fiche elle-même :** PF-71 affirmait « 952 des 996 sont auto-corrigeables », donc « la première coupe est presque gratuite ». **C'est faux et dangereux.** 243 d'entre eux (api 218, worker 25) sont `consistent-type-imports` dans les deux seuls paquets qui héritent de `emitDecoratorMetadata` — leur autofix casse l'injection de dépendances NestJS **sans qu'aucun gate ne le voie** (voir `PF-67`). Coupe réellement livrée : 694 `import/order` auto-corrigés, 10 directives `eslint-disable` inutiles supprimées, 5 `consistent-type-imports` corrigés là où c'est sûr (web, imports-core), et 243 **règle retirée** là où elle est incorrecte — pas « corrigés ». **Reste 44**, chacun nommé et possédé dans le baseline (api 9, web 34, ui 1) ; un test refuse un plafond non nul dont la note est un `TODO` | G-DNC |
| PF-14 audit page crash, reports 404 | V3-E04 | **S-E04-2** *(story ids now follow `docs/spec/features/v3-e04/tasks.md`, authored run 28)* | **`closed`** *(run 30 — both halves)* | `scripts/link-integrity-check.js` — **executed, positive AND negative** (`PASS` with the row retired; `FAIL — DEAD LINK — /admin/reports` with the entry re-inserted, then reverted). The render itself is **manual** evidence, labelled as such: `apps/web` has no unit runner | **SETTLED BY EXECUTION, in the direction the static read had ruled out.** *Crash half — **reproduces**.* The authenticated render was finally performed (run 30, 2026-08-08, local stack, `mme.dupont@voltaire.fr` / `school_admin`): `/admin/audit` returned **HTTP 500** and fell through to `/admin/error.tsx` (« Une erreur est survenue », `Référence : 2236692779`). Console: `Failed to load resource: … 500`, `{digest: 2236692779, … An error occurred in the Server Components render}`. Server log, same digest: **`Attempted to call humanizeResourceType() from the server but humanizeResourceType is on the client.`** So the kit's own hypothesis was **half wrong on the load-bearing half**: *importing* a value from a `'use client'` module is legal in Next 15; ***calling*** it from a server component is not — the bundler substitutes a client reference and invoking it throws. `page.tsx:93`/`:98` called both inside an `Array.map`, so the crash was **unconditional on every authenticated load**, with no data dependency. Third time a « did not reproduce statically » has been falsified by a running stack (`R-30`) — the kit was right to refuse to close this in either direction without the render. *Fixed* by extracting both functions and their label maps into a new **non-`'use client'`** module `apps/web/src/app/admin/audit/audit-labels.ts`, imported by all four consumers; `AuditPageFilters.tsx` no longer exports them, because a re-export would have preserved the identical trap for the next server caller. *Reports half — closed by repointing, which also closed `PF-119`:* `sidebar-items.ts:175` now points at `/admin/analytics` under `label: 'Analytique'`, so a dead menu entry and an orphaned real page were fixed by one line; `grep -rn "/admin/reports" apps/web/src` returns **nothing**, and the baseline row is **retired** (the ratchet lowered by one), not re-reasoned. **Residual, stated plainly:** the *second* render proving the fix is **`pending-verification`** — the `web` image could not be rebuilt (see `PF-126`), so the running container still serves the pre-fix bundle. Prior text kept below for provenance. **Split by measurement into a half that reproduces and a half that does not, and the second is the more useful result.** *Reports half — reproduces:* there is no `apps/web/src/app/admin/reports` directory, and the route is published live from `apps/web/src/components/shell/sidebar-items.ts:175`; live probe `curl http://localhost:3000/admin/reports` → **307** to `/admin/login?callbackUrl=%2Fadmin%2Freports`, i.e. the middleware intercepts and the 404 is only reachable post-login. *Crash half — **did NOT reproduce statically**, and was **not** settled either way:* `apps/web/src/app/admin/audit/page.tsx` is a server component carrying `export const dynamic = 'force-dynamic'`, wraps both fetches in a `safe()` that swallows `ApiError`, and imports `humanizePortal`/`humanizeResourceType` as **values** from a `'use client'` module — legal in Next 15, though it forces them into the client bundle. The live probe also 307s, so **the authenticated render was never observed**. The kit records this as an open measurement **in both directions** and names the single way to settle it (an authenticated render as `mme.dupont@voltaire.fr`, console captured) — `S-E04-2`'s headline AC. **Nothing claims the page renders; nothing claims it crashes.** The slice also inherits **`PF-119`**, raised this run: `/admin/analytics` is a real page with **no** sidebar entry while the dead `/admin/reports` entry sits beside it under the same `BarChart3` icon — so `AC-5` may be satisfied by *repointing* the entry rather than building a second analytics page, and `S-E04-2` must decide that on evidence | G-AUDIT |
| PF-119 a real admin page is reachable from nowhere while the entry beside it points at a page that does not exist | V3-E06 *(raised)* → V3-E04 | **S-E04-2** *(decided and closed, run 30)* | **`closed`** | `scripts/link-integrity-check.js` *(positive + negative, executed)* — but see the asymmetry below: this gate **cannot** be the test for the orphan half, and no gate covers it yet | **Closed by the repoint, not by a new page.** `ux.md` §8's preferred option was taken: the entry became `{ key: 'analytics', icon: BarChart3, label: 'Analytique', href: '/admin/analytics' }`, so `/admin/analytics` is reachable from the admin sidebar **for the first time** and the dead target is gone — two defects, one line. **The structural point survives closure and is now a standing gap:** the link gate checks that published hrefs resolve, never that resolvable routes are published, so it went **green** the whole time this page was orphaned and would go green again tomorrow. Raised as **`PF-127`** so the asymmetry is owned by an id rather than by this row's prose. Prior evidence: Raised by run 28's `epic-spec` sprint while scoping `AC-5`, re-verified by the land pass: `apps/web/src/app/admin/analytics/page.tsx` **exists** and `grep analytics apps/web/src/components/shell/sidebar-items.ts` returns **0** hits, while `:175` publishes `/admin/reports`, which has no directory. Both carry `BarChart3`. **The structural point:** the link gate checks that published hrefs resolve, not that resolvable routes are published, so an orphaned route is invisible to it **by construction** — the same asymmetry `PF-93` exposed for portal roots | — |
| PF-120 the V3-E04 spec-kit shipped two colliding `M-n` measurement registers | V3-E04 | — *(land pass, run 28)* | `closed` | — *(no test — a documentation defect)* | **Found by verifying the sprint's numbers instead of trusting them (`R-30`), and closed in the same run.** `data-model.md` §0 and `PROGRESS.md` were written by different agents and each opened its own `M-1…M-n` register; they agree through `M-6` and diverge after — `M-7` = *"write sites outside `apps/api/src`"* (**2**) in one file and *"sites hard-coding `portal: 'admin'`"* (**9**) in the other, `M-24` = *RLS occurrences* (**0**) in one and *"third vocabulary consumer"* in the other. **Every individual measurement was accurate** — nine were independently re-verified at the land pass, including all four collided ids — so nothing false was published; what was published was an **ambiguous citation key** in a kit written to be cited by eight downstream slices. Fixed by declaring `data-model.md` §0 the register of record (it alone carries a `Source` column), re-verifying and promoting the three `PROGRESS.md`-only measurements as `M-32`…`M-34`, and renumbering `PROGRESS.md` onto the canonical ids while **naming** the collision rather than overwriting it. **The irony is recorded, not smoothed:** the same kit refuses `ADR-029` *"because it is the first free file"* precisely to avoid recreating `PF-110`, then committed the identical error one section earlier for measurement ids. **Untouched half:** the sprint has no mechanism to give an id namespace an owner when several agents write into it at once | G-DNC |
| PF-83 `boot-gate.spec.ts` ordering assertion could be turned red by a comment | V3-E02 | S-E06-1 *(discovered and closed in the same run)* | `closed` | `apps/api/src/shared/quality/boot-gate.spec.ts` — **18/18** (was 15; the three new cases are the directional proof) | **Found by the gate, on a workflow whose step order nobody had touched.** The assertion compared `indexOf('scripts/boot-check.js')` with `indexOf('pnpm build')` over raw file text; `S-E06-1` added a comment naming that script above the build step, and the guard read the prose — `bootAt`(4409) `< buildAt`(5543), reported by `test-ratchet.js api` as **1 NEW test failure**. **The repair is the point:** rewording the comment would have turned the test green while leaving a guard any future comment can break, so the assertion now runs over executable content with `#` comments blanked and byte offsets preserved (R-26 rule (a), applied to a guard rather than to a config). **Proven in three directions against the real files:** the false positive reproduced (raw text → the comment wins; stripped → the real order stands) for `ci.yml` *and* `ci-gate.sh`, plus a genuinely misordered fixture the stripped form still catches — without that third case the fix could have been an assertion that can no longer fail | G-DNC |
| PF-84 the safe migrator has never been able to start (`.dockerignore` excluded `infra/docker`) | V3-E02 | S-E02-3 *(discovered and closed earlier in run 19; guarded by this slice)* | `closed` | `apps/api/src/shared/quality/restore-drill-gate.spec.ts` — the `PF-84` describe block, **13 cases** (3 plain + a 6-shape `it.each` exclusion matrix + a 1-entry `it.each(appPaths)` + 3 plain — recounted, not asserted) | **Found by running the migrator, not by reading it.** `ls /app/infra` inside the built image lists `docker-compose.yml`, `keycloak/`, `nginx/`, `postgres/`, `pilotage.sh` — and **no `docker/`** — so `command: ["sh", "/app/infra/docker/migrate-entrypoint.sh"]` ended in `sh: can't open`, **exit 2**, on every stack ever started: the safe migrator that replaced `prisma db push --accept-data-loss` had never once run. The `.dockerignore` line was deleted, the image rebuilt, and the migrator then refused the un-baselined database **correctly** (exit 1, `UNBASELINED_NONEMPTY`) where it previously never reached its own logic. **The guard encodes the general rule, not the address:** every `/app/…` path in a compose `command:`/`entrypoint:` must exist in the repository **and** survive `.dockerignore`. It parses all three YAML forms (exec list, shell string, folded block — `minio-init` uses the third), evaluates patterns with last-match-wins including `!` negation and `**` globs, **reads executable content only** so the fix's own five-line explanation cannot break it (`PF-83`'s lesson), and is **proven in the negative** on a synthetic pattern list, because a matcher that always returned false would pass every positive assertion and cover nothing. The guard **never mutates** `.dockerignore` — it reads the file as it stands; the file itself *is* edited by this slice (the `infra/docker` line removed, an eleven-line rationale added in its place). **Known limit, stated:** the same defect class applies to compose `volumes:` bind-mount sources and Dockerfile `COPY` sources — out of scope here rather than implied covered | G-MIGRATION |
| PF-85 the sprint workflow honours the operator's story id, then briefs the agents on a different story | V3-E02 *(routine)* | — *(fixed in `bmad/workflows/sprint.workflow.js` earlier in run 19)* | `closed` | n/a — measured from run 19's own agent briefs | Recorded here for the bidirectional chain; the fix lives in the routine's workflow file and was made earlier in run 19, **not** by this slice. `PF-60` resolved `mode`/`epic`/`slice` as `ARG_* || intake.*` but left `intent` — the field every downstream agent actually reads — untouched, so the identifiers said `V3-E02`/`S-E02-3` while the instruction body described `S-E06-2` (CSP, branding sanitisation). Second half of `PF-60`, not a regression: the partition the partial fix drew (identifiers vs body) is exactly where the leak was | G-DNC |
| PF-86 the documented way to start the stack starts a **different** stack | V3-E02 | **S-E02-16** | `closed` *(re-scoped `TECH_DEBT` → `BROKEN_RUNTIME`)* | `apps/api/src/shared/quality/compose-invocation-gate.spec.ts` — **34/34** (`pnpm --filter @pilotage/api exec jest src/shared/quality/compose-invocation-gate`) | **Closed, and re-scoped on measurement rather than on judgement — the finding was understated.** It was filed as a documentation defect, closing *"neither is a runtime defect in the application"*. The counter-evidence sits inside `infra/docker-compose.yml` alone and needs no untracked file: `KC_HOSTNAME` and `KEYCLOAK_PUBLIC_URL` hard-coded `http://localhost:8180` — **the api uses the latter as its expected token issuer** — while `keycloak.ports` defaulted to `8080` and the web's `NEXT_PUBLIC_KEYCLOAK_URL` defaulted to `8080` too. On the documented path Keycloak published on 8080, announced an issuer reachable on **no** port, sent the browser to 8080, and the api rejected every resulting token. **Login was broken by construction**, and appeared to work only because one machine's gitignored `.env` said `8180` on a code path where compose never read it. **Four corrections, because one would not have held.** (1) Thirteen published host ports lose `${VAR:-…}` for `${VAR:?…}`: compose **refuses** in the operator's terminal and names the remedy — the `S-E06-1` argument about `?? 'admin'` applied one layer out, *a default nobody wrote down is the defect*. (2) A host port is written **once**: `KEYCLOAK_PUBLIC_URL`, `KC_HOSTNAME` and both `NEXT_PUBLIC_KEYCLOAK_URL` derive from `${KEYCLOAK_PORT}`, `WEB_PUBLIC_URL` from `${WEB_PORT}` — rule 1 alone would still have shipped the wrong issuer at `KEYCLOAK_PORT=9999`. (3) Every profile activates its own dependencies. (4) `.env.example` stops describing a **third** incompatible stack (it alone said 5432/8080 while `apps/api/.env.example`, the seeds, the restore runbook and the drill all said 5433/8180). ADR-026 records why `infra/.env` and `--project-directory ..` were rejected. **Executed in both directions.** The real script against the restored pre-slice compose file → **exit 1, 4 problems**, one per rule family (C1 silent default, C3 hard-coded issuer, C4 ×2 invalid profiles); restored → exit 0, `git diff` clean. The stage replaced with `true` in **both** `ci-gate.sh` and `ci.yml` → **2 failed / 32 passed**, one per disconnected file. Three **live** docker probes run inside the script itself: without `--env-file` compose refuses (exit 1, naming the missing variable), with `--env-file .env` it accepts, and `--profile seed` alone is a valid project. **And the stack was recreated through the corrected command and left healthy**: `keycloak`/`api`/`worker`/`web` force-recreated, migrator idempotent (*"No pending migrations to apply"*), eight containers healthy on the ports the root `.env` declares, `GET /healthz` **200**, web `/` **200**, and the **running** Keycloak's discovery document reporting `issuer: http://localhost:8180/realms/pilotage-scolaire` — the port that is in fact published. **The gate caught its own first defect, which is the best evidence it reads something real:** its first execution died on `bad indentation of a mapping entry` — the `:?` message this slice had just written contained a `": "`, which YAML reads as a mapping separator, so the fix broke the file it was fixing. **Not claimed:** this does not prove a full login journey (that is `V3-E05`); what is proven is that the announced issuer and the published port agree on a running container. `infra/docker-compose.prod.yml` is deliberately outside the C5 scope — own env file, and an audit fixture per Step −1 — so **its** port coherence is unexamined. Discovered en route: **`PF-89`** | G-DNC, G-MIGRATION |
| PF-89 `--profile prod` alone is an invalid Compose project | V3-E02 | **S-E02-16** *(discovered and closed in the same run)* | `closed` | `compose-invocation-gate.spec.ts` — the `nginx`/`web` case, plus rule C4 over the real file | **Found by the gate written to close `PF-86`, on its first execution — not by reading.** `nginx` (profile `prod`) `depends_on` `web` and `api` (profile `app`), and Compose refuses the **entire** project in that situation rather than degrading it, so `docker compose --profile prod up -d` — in the compose header and in `DEPLOYMENT.md`'s profile table — died on *"service nginx depends on undefined service web"*, confirmed live before the fix. Same shape as the `seed`/`api` instance recorded inside `PF-86`, at a second address: **two of the file's five profiles were unrunnable exactly as documented**, and nothing in the repository could say so. Recorded separately because it was found by an executed gate rather than an audit — it is the evidence that rule C4 generalises rather than describing one mistake. Closed by giving `migrator` `["app","seed","prod"]` and `api`/`worker`/`web` `["app","prod"]`. `worker` gaining `prod` goes one step past bare validity (nothing in `prod` depends on it) and is deliberate, and said so in ADR-026: the profile claims to start "le mode prod", and a prod stack without its background worker is not that stack — the same defect as `PF-86` at an address the validity rule alone would not reach | G-DNC |
| PF-87 the operator override arrived as `String.prototype.slice`, so a missing value never read as missing | V3-E02 *(routine)* | — *(fixed in `bmad/workflows/sprint.workflow.js` earlier in run 19)* | `closed` | n/a — diagnosed from an agent's own report | Recorded here for the chain; fixed earlier in run 19, **not** by this slice. `args` arrives as a JSON **string**, so `args.mode`/`args.epic` were `undefined` while **`args.slice` resolved to the built-in method** and was therefore truthy — the one field carrying the story's identity became the literal `function slice() { [native code] }`, and three consecutive sprints were briefed on `S-E06-2` while the routine had selected `S-E02-3`. Almost certainly the true mechanism behind `PF-60`, which was diagnosed and fixed as a precedence bug. The general shape is the keeper: **a missing value that collides with a built-in method name never reads as missing.** This slice is the first to have been briefed correctly on `S-E02-3` as a result | G-DNC |
| PF-45 CSP disabled, branding injected into `<style>` | V3-E06 | **S-E06-2** | `closed` | `apps/api/src/shared/quality/csp-gate.spec.ts` — **40/40**; `apps/api/src/modules/school-structure/branding-security.spec.ts` — **66/66** (`pnpm exec jest src/shared/quality src/modules/school-structure` → **448/448** across 13 suites, was 331/331 across 10); `scripts/csp-check.js` — executed, positive **and** negative, plus a **live probe against the running stack** | **The finding was two defects; measured, it was three, and the severest was not the one recorded.** A2 §11 calls the branding path *"a stored CSS-injection path"*. It is **stored XSS**: `BrandingStyle` interpolated three tenant-controlled strings into a server-rendered `<style>` through `dangerouslySetInnerHTML`, which React does not escape, and the HTML parser closes a `<style>` on `</style` alone — `red}</style><script>alert(1)</script>` is **37 characters** against the DTO's `@MaxLength(60)`. Served on every authenticated page of all four portals. **Third defect, not in the audit — `PF-88`:** `PATCH /schools/:id/branding` resolved its target with `findUnique({ where: { id } })` and never consulted the caller's tenant, so `branding.write` in *any* instance wrote *any* school. Composed with the sink, that is a **cross-tenant stored XSS**. Found at Step 2 by reading the write path the story was about to validate. **Reproduced live before anything changed, on the running artefact rather than in source:** `curl` against the local stack returned `/admin/login` **200 with no CSP header at all**, and `/healthz` with every other helmet header (COOP, CORP, HSTS, …) and **no CSP** — helmet ran; the one directive that turns an injection into a non-event was the one switched off. `node scripts/csp-check.js` against the **pre-fix build artefact**: **`CSP CHECK: FAIL`, exit 1**, naming all four defects. **What landed:** one shared grammar in `packages/contracts/src/security/` — safe **by construction, not by enumeration** (it recognises a closed grammar and returns `null` for everything else; the colour-function argument set contains no `(` or `)`, which puts `url()`, `var()` and `expression()` out of reach *by composition* rather than by blacklist) — imported by the api DTO at write time **and** by the render sink; a per-response nonce CSP built in `middleware.ts`; `default-src 'none'` on the api; tenant-scoped branding writes; and `scripts/csp-check.js` as **stage 12** of `ci-gate.sh` and a step in `ci.yml`. **Both halves are independently necessary, which is why there is one copy and not two:** write-time validation says nothing about rows already in the database, and the seed scripts write `branding` through Prisma without ever traversing the DTO. Proven on the real database, not argued — a hostile value was written directly into the local `branding` table with `psql` (bypassing the DTO exactly as the seed does), read back out, and fed through the built module: stored `"red}</style><script>alert(1)</script>"` → emitted `":root{}"`, containing neither `<` nor `script`. Row restored afterwards. **`style-src-attr 'unsafe-inline'` is declared explicitly and is the narrowest exception available:** nonces do not apply to `style=""` attributes, which React and Radix produce everywhere, and the only alternative is `'unsafe-inline'` on `style-src` *entire* — which would re-open this very sink. **The run's main result is what booting it found — recorded as `R-28`.** With the policy in place, `csp-check.js` green, the guard spec 40/40 and the full gate 14/14, `/admin/login` was still **dead**: served `x-nextjs-cache: HIT` / `x-nextjs-prerender: 1` from the build-time cache with **21 `<script>` tags and 0 carrying a nonce** (15 external, 6 inline RSC payload). `'strict-dynamic'` makes CSP3 browsers ignore `'self'`, so all 21 would be blocked; and dropping `'strict-dynamic'` does not rescue it — the 15 external would load, the 6 inline would not, so the page renders and does not respond, which is worse because it looks alive. A header's correctness is a property of the header **and the document it is served with**, and every cheap check only sees the header. Fixed at the **class**: `export const dynamic = 'force-dynamic'` at the root layout, not enumerated across the 14 static routes, because the fifteenth page someone adds would fail silently with a symptom that does not look like CSP. `csp-check.js` now refuses any artefact where a prerendered document carries a `<script>` — **proven in the negative against the real artefact, where it named all 14**. Static pages went 14 → **2/2** and the second gate run passed 14/14. **Executed live after the rebuild, on all four portals:** `/admin/login`, `/teacher/login`, `/parent/login`, `/student/login` each return **cache MISS**, ~24 script tags, **every one carrying the response nonce** (was 0), and 5 consecutive requests produced **5 distinct nonces** — a reused nonce is `'unsafe-inline'` in more characters. `--probe` reports *HTTP 200, ENFORCING, 13 directives*. **Negative paths executed against the guard**, one failure each, each naming the right thing: CSP re-disabled in `main.ts` → red **despite a comment quoting the forbidden string** (PF-83's rule doing its job); raw interpolation restored → red; the `ci-gate.sh` stage replaced with `true` → red; `'unsafe-inline'` added to `style-src` in the loaded artefact → red. **Not claimed:** no browser was driven, so "no console CSP violation on the four portals' main journeys" (AC-3) is proven for the login pages by nonce coverage and cache-miss, **not** by an authenticated end-to-end journey — that needs the Playwright harness `VAL-08` still owns; and `report-only` was never exercised on a real deployment because the routine's target has none | G-AUTHZ |
| PF-88 `PATCH /schools/:id/branding` writes any school in any tenant | V3-E06 | **S-E06-2** *(discovered and closed in the same run)* | `closed` | `branding-security.spec.ts` — describe *"portée locataire (G-TENANT, PF-88)"*, 3 cases | **G-TENANT evidence, and the positive path is half of it.** `update(tenantId, schoolId, patch)` resolves with `findFirst({ where: { id, tenantId } })` — the constraint is **inside the query**, not a read-then-compare, so no code path reads another tenant's row before deciding; the controller takes the tenant from `ensureUser(jwt)` and never from the path. A foreign-tenant write raises `NotFoundException` (404, not 403 — a 403 would confirm the id exists) and `branding.upsert` is asserted **not called**. The allowed write is asserted to succeed, because a guard that refuses everything would satisfy the negative test and break the product (R-12). A third case pins the arity at 3, so restoring `update(schoolId, patch)` is named rather than silently reintroduced. **Not claimed:** the denial is proven against a Prisma double reproducing `findFirst` semantics, not against a live two-tenant request — that adversarial suite is `VAL-02`, owned by `V3-E01` | G-TENANT |
| PF-19 `/admin/classes/new` crashes | V3-E06 | **S-E06-3** | `closed` | `apps/api/src/shared/quality/link-integrity-gate.spec.ts` — **46 cases** (`pnpm --filter @pilotage/api exec jest src/shared/quality` → **499/499** across 13 suites, was 448/448); `scripts/link-integrity-check.js` — executed, positive **and** negative, against the real repository and the real build | **The route now exists, and — the part that matters — something can now say so.** `/admin/classes/new` was linked twice from `/admin/classes` (the `PageHeader` action and the `EmptyState` action, the only affordance a school with zero classes ever sees) and was never emitted. `apps/web/src/app/admin/classes/new/{page.tsx,ClassForm.tsx}` is the fix: a server half that fetches years and cycles and applies the **list page's own** mapping so the selects cannot offer a value `POST /classes` would reject, over a `'use client'` form island, on the existing `createClass` server action and the existing `@RequiresPermission('classes.write')` controller — **no new endpoint, no new permission, no schema change, and not one of the controller's four refusals (foreign-tenant year/level 404, cross-school 400, archived-year 400, duplicate 409) weakened**; they are surfaced in the form instead of swallowed. **The finding is a class, not an instance, and that is why a route-existence check could not close it.** `/admin/classes/new` *resolved* — Next matched it against `/admin/classes/[id]` with `id = "new"` — so a gate asking *"is this link in the route table?"* answers **yes** while the page crashes. `scripts/link-integrity-check.js` therefore classifies four verdicts rather than a boolean: `exact` and `catch-all` are alive, `dead` fails unless baselined with a reason **and** an owning finding id, and **`dynamic-capture` fails unconditionally and is never baselineable**. The `catch-all` carve-out is load-bearing rather than a nicety: consuming literal segments is the *entire* function of `/api/proxy/[...path]`, through which every client fetch in the application reaches the API, so conflating the two would have made the gate permanently red on correct code with no route back to green except breaking the BFF. It reads the **emitted** `.next/app-path-routes-manifest.json`, never the `app/` tree — `PF-82` is exactly the defect of a sibling gate reading source, and it is not reproduced here. **Executed in both directions against the real script, by the routine and not only by the spec.** Positive: `109 routes · 114 literal targets · 90 alive · 24 dead (24 baselined) · 0 captures`, `LINK INTEGRITY CHECK: PASS`. Negative, by deleting `/admin/classes/new` from the emitted manifest to restore the pre-slice inventory: **exit 1**, `DYNAMIC CAPTURE — /admin/classes/new resolves ONLY because the dynamic segment of /admin/classes/[id] swallowed a literal segment`, citing **both** call sites (`page.tsx:155`, `:221`); and `--update` on that same state **refused to write**, leaving the baseline byte-identical — a capture cannot be baselined away. Second negative, on the real sources: a fresh `href="/admin/this-route-does-not-exist"` → **exit 1**, `DEAD LINK`, naming the file and line; reverted, back to exit 0. **The ratchet fails in four directions, and the two easy to forget are the point:** a baselined target that is **alive** again fails (the ceiling only comes down), and a baselined target nothing links to any more fails (a ceiling nobody stands under silently re-authorises the target the day someone links it). No environment variable is read anywhere in the file and the only verb is `--update` (DNC-10), locked by a test that **executes** the script with every plausible escape variable set rather than grepping for them. Wired as **stage 13** of `ci-gate.sh` and a step in `ci.yml`, with a guard that reads comment-stripped executable content so a comment can neither create nor destroy the wiring (`PF-83`'s lesson). **The run's main result is what turning it on found:** the story had measured 15 dead targets; the gate's first execution returned **24**, and four new findings came out of the same pass — `PF-91` (nine auth routes the middleware waves through and nothing emits), `PF-92`, `PF-93`, `PF-94`. **Not claimed:** this is a **static** gate. It resolves *literal* link targets against the route inventory; it does not drive a browser, so AC-2/AC-3 — an authenticated per-role crawl asserting zero runtime 404s and zero error boundaries — is **not** delivered and still belongs to `VAL-08`. Template-literal hrefs are deliberately out of scope, which means the sibling defects the story cites (teacher/parent profile 404s, teacher class-messaging 404) are invisible to it. And no browser rendered the new form: it is proven to build, to be emitted, to be authenticated and to post to a contract it cannot violate — not to have created a class end to end | G-PORTAL, G-DNC |
| PF-93 the four bare portal roots have no index route | V3-E06 | **S-E06-5** | `closed` | `apps/api/src/shared/quality/portal-landing-gate.spec.ts` — **135/135** (`pnpm --filter @pilotage/api exec jest src/shared/quality/portal-landing-gate`); `node scripts/web-artifact-check.js --update` → the hand-edited baseline came back **byte-unchanged** against the real build; `node scripts/link-integrity-check.js` → **PASS** | **The four roots resolve, and the landing target is written exactly once.** `apps/web/src/app/{admin,teacher,parent,student}/page.tsx` are `force-dynamic` server redirects; `PORTAL_LANDING` was **moved** out of `middleware.ts` into `apps/web/src/lib/portals.ts` and is now read by the middleware **and** the four roots, so the value the roots send a visitor to is the same value a fresh login is sent to — `S-E02-16` rule 2 (*a value is written exactly once*) applied to a route rather than to a port. The module is deliberately import-free, reads no `process.env` and carries no `server-only`, because the **edge** middleware pulls it into its bundle; `packages/contracts` was measured and rejected (CJS `dist/`, shared with api/worker runtimes that have no use for a Next route), and the residual vocabulary split — `PORTALS` there still declares **three** portals, predating the student portal — is recorded as **`PF-101`** rather than fixed in passing. `PORTAL_REQUIRED_ROLES` is now `Record<PortalId, readonly string[]>`, so a fifth portal is a **compile error** until the table gains its row. **G-AUTHZ, and the honest version of it:** each new root sits inside the middleware's protected zone — `pathname.startsWith('/admin')` already matched the bare root **before** this slice, so an unauthenticated visitor was already redirected to `/admin/login?callbackUrl=/admin` and a wrong-role visitor to `/admin/login?error=wrong_portal`; the authz branches are **byte-unchanged** by this diff. So there is no *new* authorisation behaviour, only newly-reachable routes behind an unchanged wall. Every redirect key is a **literal** lookup — no `searchParams`, no `headers()`, no referrer — so none of the seven new routes adds an open-redirect surface. **The false-positive half is closed differently from the real half, which was the point of the follow-up this row asked for:** `/api/auth` and `/favicon` are now `"class": "prefix-constant"` and stop counting as dead debt, and the class is **refused with a file:line** the moment any reference to the value sits on an href-like line — so it cannot become a quieter baseline. **Not claimed:** G-AUTHZ is proven by **regex over `middleware.ts`'s source**, not by calling it. There is no executable test of `apps/web/src/middleware.ts` anywhere in this repository, so an `if` inserted above the prefix ladder would leave all 135 cases green — recorded as **`PF-100`**, with the test shape specified | **A real gap and a deliberate false-positive class arrived in the same row, and the row says so rather than reading as clean debt.** `/admin`, `/teacher`, `/parent` and `/student` are route *prefixes* (ADR-003) with pages beneath them and nothing at the top, so the most guessable URL in each portal 404s for an authenticated user — verified against `middleware.ts:130-135`, which resolves the portal, finds no auth route, and proceeds to a path the router cannot serve. They entered the inventory because `extractLiteralLinks` treats **any** literal string starting with `/` as a candidate, with no `href`/`Link` context — which also dragged in `/api/auth` and `/favicon`, two `PUBLIC_PREFIXES` constants that are prefixes by construction and are **not** defects. The loose definition is kept on purpose: it is what surfaced the four real ones, and narrowing it to `href=` would also have hidden `PF-91`, which lives in the same allowlist. The follow-up is a `class: "prefix-constant"` tag so the ceiling can reach zero honestly instead of by exclusion | — |
| PF-94 `/pricing` and `/contact` dead in the public landing footer | V3-E06 | **S-E06-5** | `closed` | `apps/api/src/shared/quality/portal-landing-gate.spec.ts` — **135/135**, including the *"the check is not vacuous: the same patterns DO match invented copy"* control; `node scripts/link-integrity-check.js` → **PASS** against the real build | **Both pages exist, and neither of them invents anything — which was the hard part, not the routing.** `/pricing` and `/contact` (plus `/help`) are real pages over a shared `PublicInfoPage`, each `force-dynamic` so no prerendered document can carry an unnonced `<script>` under the `S-E06-2` nonce CSP. **`/pricing` carries no price at all**: decision **D-05** (commercial packaging) is open, so there is no reviewed tier, amount, currency or billing period to publish, and inventing one would be the same class of overreach as authoring policy text (**R-13**) — a sentence an automated routine shipped that the product cannot honour. It states what is true today and routes the question to the one channel that exists, and each of its claims is checkable: *"aucun paiement n'est demandé ni traité dans l'application"* holds because there is **no** payment integration anywhere in the tree (a guard asserts this, so the sentence becomes false the day one is added); *"un compte famille se crée directement en ligne"* holds because `/parent/register` is an emitted self-service route while `AUTH_ROUTES_BY_PORTAL.student` deliberately has no register entry. The contact channel is read from the **existing single** `SUPPORT_EMAIL` in `apps/web/src/lib/support-contact.ts` — no second copy, no invented address, no phone number, no opening hours and **no response-time promise**. **DNC-09 is refused explicitly:** none of the three pages says « bientôt » or « en cours de finalisation », because holding copy reads as an *unbuilt product* and ages into a broken promise; the wording is present-tense about *this page*, never a schedule. **Not claimed:** no browser rendered any of the three, so every front-end and accessibility claim is static — four measured a11y defects (landmark nesting inside `<main>`, CTA gradient contrast at **2.77:1** against SC 1.4.3, focus rings at 1.51–1.68:1 against SC 1.4.11, and a 320 px reflow clip) are recorded with their measured passing replacements and deliberately left to `VAL-08` rather than widening this diff | `apps/web/src/app/page.tsx:771,788` — the **unauthenticated** marketing surface, the first page a prospective school sees, linking to two routes that were never built. Same family as `PF-38` and `PF-39`: the product advertises a completeness it does not have, on the surface where credibility is decided. Split out from `PF-38` rather than folded into it because the distinction is actionable — unlike `/legal/*` these are **not** blocked on **D-08**, no policy text is required to write a contact page, so they are the cheapest rows in the ceiling to retire | — |
| PF-97 the link gate was blind to every template-literal href, so « Mon profil » 404'd on every authenticated page of three portals while the gate printed `PASS` | V3-E06 | **S-E06-5** *(discovered and closed in the same run)* | `closed` | `apps/api/src/shared/quality/link-integrity-gate.spec.ts` — **173/173** (167 pre-existing + 6 new, over throw-away `mkdtempSync` trees, each paired with its control); `node scripts/link-integrity-check.js` — executed, positive **and** negative | **Found by reading the gate before trusting its output, which is the only reason this slice is not a green gate over a hole.** `LITERAL_LINK = /(['"])(\/[^'"`\n\r]*)\1/g` matched quoted strings only and its character class **excluded the backtick** — so every template-literal href in `apps/web` was invisible to the stage that exists to make navigation quality a build invariant. 18 sites, 6 portal-interpolated. The extractor now resolves an interpolation over the union declared **in that file's scope**, and the scope-awareness is load-bearing rather than tidy: `Portal` is declared **five times in `apps/web` with four different unions** (4-portal in `auth.ts`/`PortalShell.tsx`, 3-portal in `NotificationCenter.tsx`/`PortalErrorState.tsx`, 2-portal in `PortalCalendarView.tsx`, plus `MeetingRequestPortal = teacher|admin`), so a resolver matching the type by name globally would have manufactured phantom dead rows for `/student/notifications` and turned `main` **red on correct code** — the `R-30` trap, and an invented red is as bad as the silent drop this finding is about. An interpolation it cannot resolve is **reported**, never dropped. **Two blockers were found by measuring the new lexer against a guard-reverted copy, and both were `PF-97`'s own shape reappearing inside the function written to close it:** `'<'` is a member of `REGEX_MAY_FOLLOW_PUNCTUATION`, so the `/` of a JSX **closing tag** was read as a regex opener and the scanner ran to the next `/` on the line — `</div> // href="/admin/classes/[id]"` came back **verbatim** (comment never blanked) and produced an *unbaselineable* `DYNAMIC CAPTURE`, i.e. a gate red with no route back to green; and `</span>` followed by a template href on one line returned **0 literals and 0 template rows**, a silent drop. Fixed at one root cause with the **narrow** option — a `regexMayStartAt` refusing the regex branch only when `src[i-1] === '<'`, so `a < /re/` keeps its old reading and the change is strictly a subset. **What the widening then found is the evidence it works:** `/teacher/messaging`, `/admin/guardians/*` and `/admin/assessments/*` — eight affordances nothing could previously see. **Residual, stated:** three latent defects remain in the new ~700-line hand-rolled lexer, measured as harmless on today's tree (0 surviving `{/*` markers, 0 lost hrefs across 365 files) but recorded as **`PF-103`** because this is a **blocking** stage; and nothing records why `ts.createSourceFile` was rejected in favour of hand-rolled lexing, which is owed | G-DNC |
| PF-102 post-authentication open redirect on all four portal login forms | V3-E05 | **S-E05-12** | `closed` | `apps/api/src/shared/quality/open-redirect-gate.spec.ts` — **214/214** with `portal-landing-gate.spec.ts` in the same run (`npx jest src/shared/quality/open-redirect-gate.spec.ts portal-landing-gate.spec.ts`, 44.9 s); the 37-row accept/reject matrix (29 reject + 8 accept) driven through a differential `new URL(result, base).origin` oracle; `pnpm typecheck` **13/13, exit 0**; `git diff --check` clean | **Validated once, at the read site — and the fix the ledger recommended was measured before it was written, then rejected.** `apps/web/src/lib/safe-callback-url.ts` (100 L, 5 executable, pure, import-free, edge-safe — the `lib/portals.ts` pattern verbatim, so no ADR is owed) is called once at `PortalLoginForm.tsx:74`, so **both** sinks inherit one same-origin binding: `router.push` on the credentials branch and `signIn(…, { callbackUrl })` on the SSO one. A per-sink guard would be a rule a third sink can forget. **The briefed rule is exploitable:** `raw.startsWith('/') && raw[1] !== '/' && raw[1] !== '\\'`, resolved against Node's WHATWG `URL` with base `https://pilotage.example/parent/login`, passes **four** inputs that still navigate off-origin — `/<TAB>/evil.example`, `/<LF>/evil.example`, `/<CR>/evil.example` (→ `https://evil.example/`) and `/<CR><LF>/evil` (→ `https://evil/`) — because the parser strips tab/LF/CR from **anywhere** before deciding the origin. The shipped rule adds a fifth clause, **C0 rejected anywhere**, and performs **no normalisation at all**, so the bytes validated are the bytes navigated (the parser-differential class is closed by construction, not by a longer deny-list). **Fails-before proven two ways** inside the run: the pre-slice expression is reported as an offender by the same scan that walks the tree, and the briefed rule fails on exactly R25/R26/R27. Whole-tree structural scan: 366 files, 55 sink call sites, bare-identifier inventory exactly 3, **0 offenders**. `DEFAULT_LANDING` deleted, retiring the fifth copy of the four landing paths onto `PORTAL_LANDING` (`PF-103`'s duplication note). **Not claimed:** next-auth's own `/api/auth/signin/*?callbackUrl=` is a second public entrance whose same-origin clamp is header-derived (`trustHost: true` + nginx `server_name _`) — recorded, deliberately not asserted; the scan is lexical and single-file and misses the inline read at the sink, two-argument `push(x, opts)`, `signIn(…, { callbackUrl })` and `packages/ui/src`; and the fix is in code, not on the deployment. Six residuals with owners in `docs/spec/features/v3-e05/PROGRESS.md`. **Verdict line executed by the routine rather than left to the sprint (R-23): `bash scripts/ci-gate.sh` → `GATE: PASS`, exit 0, every stage, one `pnpm build` (8/8 tasks, 27 m 13 s).** It took **two** passes, and pass 1 is the part worth reading: `GATE: FAIL (7 stage(s))`, all seven reducing to **one** root cause plus two load artefacts. `no-control-regex` is an ESLint **error** in this repository and `next build` runs ESLint, so the three control-character regexes the slice shipped (`safe-callback-url.ts:67`, `open-redirect-gate.spec.ts:554,556`) did not warn — they failed `lint`, `lint:warnings` **and `@pilotage/web#build`**, and with no `.next` emitted, `web artefact` and `link integrity` fell over behind them. `boot` (180 s) and `tracing` (`ETIMEDOUT`) failed on timeouts behind a 19-minute build and both returned `PASS` when re-run standalone on the byte-identical tree — measured rather than assumed, because *"probably flaky"* is how a real defect gets waved through. Fixed at the right layer with no gate weakened: the C0 clause became a **code-unit scan** in both files rather than an `eslint-disable` (the matrix still returns **214/214**, which is what makes the two forms provably equivalent), and the `import/order` warning that put `apps/web` at 35 against a ceiling of 34 was **reordered, not re-baselined** (`lint-ratchet: 44/44, no drift`). The premise correction above is unaffected by the rewrite. **Discovered by that failure: `PF-112`** *(renumbered from `PF-104` on 2026-08-08 by `S-E02-18` — that id had two different findings; run 24's stalled-job one keeps it, see row 114)* — readiness cannot see a lint error, so the sprint returned `landed: true` on a tree whose web build was broken | G-AUTHZ |
| PF-29 holiday import: no confirm, stale year | V3-E06 | **S-E06-6** | `closed` | `apps/api/src/modules/calendar/calendar-seed-holidays.spec.ts` — **23 new cases** (T1–T16 + extras); `pnpm --filter @pilotage/api test -- "modules/calendar"` → **32/32 pass** (23 new + the 9 pre-existing `calendar.access.spec.ts` cases, unbroken by the controller refactor); `pnpm typecheck` → **13/13, exit 0**, both diff-carrying packages **cache-miss and executed fresh** (`@pilotage/api` `19609675f8b089da`, `@pilotage/web` `90b039ea86b75fbb`) rather than replayed | **The control asked nothing and named nothing, and the year it chose was a guess.** "Importer les fériés (France)" was one `onClick` firing `seedFrenchHolidays()` with no argument; the handler then guessed `body.year` → the active academic year's `startDate.getFullYear()` → `new Date().getFullYear()`, and wrote **22 `CalendarEvent` rows** (11 holidays × two civil years) at `visibility: 'all'` — visible to every parent — each stamped with `activeAcademicYearId` whatever date it carried. Measured on `voltaire-demo`: **13 of the 22 rows were attached to an academic year that does not contain their own date.** The label named neither the count nor the years. **Four halves, each load-bearing.** (1) `confirm: true` is required **server-side** — the refusal throws before any read *and* before any write, so a `curl` is subject to it and the gate is not the dialog. (2) `year` is **required**: the whole fallback cascade is **deleted, not kept**, because the fallback *was* the stale-year defect and a 400 beats 22 rows in the wrong year; the `@IsInt() @Min() @Max()` bounds also close the `PF-51` family on this DTO (`{year:'abc'}`, `{year:1e9}` → 400 instead of an `Invalid Date` then an opaque 500), deliberately **without** `@Type(() => Number)`, which would have made `{"year":"2026"}` silently acceptable — the inverse of the intent. (3) `dryRun: true` returns the **same plan object from the same code path** that writes, and commits nothing, so the confirmation's counts *cannot* be a second implementation of the scope — **DNC-06 made structurally impossible rather than re-read**; the FE never enumerates a holiday and never re-implements the computus. (4) The handler moved out of the controller into a new `CalendarSeedService` (the `snapshot-ops.service` / `AdminRemediationService` precedent, `PrismaService`-only, provided directly beside `StudentAccessService`): **one `$transaction`, one `createMany`, one `AuditLog` row written inside it** — written even at `created: 0` — with a **derived** `actorRole` (a `super_admin` audits as `super_admin`, not the hard-coded `'school_admin'` of ~20 other sites), `portal`, and IP/UA sanitised **before** the transaction opens, so audit hygiene can never roll back a valid import. **Two adjacent defects fixed on the way past.** The existence probe was `where: { schoolId, title, startsAt }` with **no `tenantId`** — a `PF-11`-family cross-tenant read — and T7 is a real one-sided-fix trap rather than a tautology: it fails if only one of the two reads is scoped, and suppression is decided on an exact `(title, startsAt)` pair `Set`, never an `IN` cross-product. And each row's `academicYearId` is now the year **containing its own date**, or `null`. **Proven in both directions where it matters:** T9a/T9b execute the rollback claim against a buffering Prisma double rather than asserting it, T14 shows `dryRun` writes nothing at all, T16 pins the 22 inserts as a single `createMany` on the tx client, T15 pins the computus against four real French dates. **Two blockers were caught by the verify panel and fixed before land**, one of which is itself a gate gap (**`PF-95`**): a **raw NUL byte** as `pairKey`'s separator made the slice's core service a **binary** file to git (`Bin 0 -> 13165 bytes`, "Binary file not shown" in the PR, invisible to `git diff --check`, skipped by ripgrep) — replaced by `|`, the repo's 7-site composite-key delimiter, collision-free here because the titles come from a fixed 11-entry table; and an `import/order` warning that put `apps/web` at 35 against a ceiling of 34, reordered rather than re-baselined (**34/34**, ceiling held in both directions). **Not claimed — in full in `docs/spec/features/v3-e06/PROGRESS.md`:** the concurrent-double-click race is **narrowed, not eliminated** (no unique constraint, no `skipDuplicates`, `Serializable` deliberately not introduced — two interleaved applies can still write 44 rows; the durable fix is the G-MIGRATION slice deferred in story §3 D6, whose first task is a pre-existing-duplicates survey); **no browser rendered the drawer**, so every FE and a11y claim is static, including a WCAG 4.1.3 gap where the `role="status"` scope region is unmounted at the instant the counts arrive; **the controller boundary has no executed test** — all 23 cases call the service directly, and mutating `confirm: body.confirm === true` to `confirm: true` leaves the whole suite green while restoring the audited behaviour *plus* an audit row claiming consent (attempted as a mutation this run and reverted; `calendar.controller.ts:298` verified restored); **`AuditLog.ipAddress` records the reverse proxy, not the operator** (no `trust proxy` anywhere, and the sole caller is a Next server action, so the stored value is the web container's address and `userAgent` is `null`) — kept and **declared** rather than fixed, because blanket XFF trust would make an append-only governance column client-forgeable, which is worse than blank; **`PF-31` advanced on one handler of ~20**; **`PF-51` fixed on this DTO only**; **historical rows not repaired**; **AC-8 unexecuted** (`ci-gate.sh` and `test-ratchet.js` need a build, and agents may not build). **G-MIGRATION does not trigger:** no `schema.prisma` change, stated rather than omitted (story §3 D6)  **EXECUTED against the live local Postgres by the routine (run 22), not only against doubles — this is the half the sprint could not reach.** The slice's own suite proves the transaction claim with a *buffering Prisma double* (T9a/T9b); the routine then re-proved it on the real database. A throwaway drill instantiated the real `CalendarSeedService` with the real `PrismaClient` against `pilotage_postgres` (port 5433), on two throwaway tenants it created and deleted: **26/26 assertions, `REAL-DB DRILL: PASS`**. What that upgrades from asserted to executed: the no-`confirm` refusal leaves **0 events and 0 audit rows in Postgres** (not a mock call count); a genuine `ROLLBACK` — forced by handing the service an unsanitised `ipAddress` (`not-an-ip, 10.0.0.1`), so the `@db.Inet` cast fails **after** `createMany` has already run inside the transaction — leaves **`events=0 audit=0`**, which simultaneously proves *why* `sanitiseInetOrNull` exists; the first apply writes **22 rows + exactly 1 audit row** carrying `actorRole=super_admin` (derived, not the hard-coded literal), `portal=admin`, `ip=203.0.113.9`, `ua=pf29-drill/1.0` and `after.created=22`/`after.years=[2031,2032]`; a **second apply creates 0 while the table still holds 22** (idempotence measured in the database, not inferred), and still writes its own audit row (**2**); and G-TENANT is proven the hard way — a **second tenant holding all 22 identical `(title, startsAt)` pairs** still gets its own 22 created, with **0 cross tenant/school rows**, which is exactly the one-sided-fix trap. Per-date attachment was verified as an **invariant** rather than by spot-check: across the 22 rows, **0 are attached to an academic year that does not contain their own date**, and the distribution is real (`2030-2031` → 6, `2031-2032` → 9, `null` → 7, for the summer gap and the uncovered second year). **A first version of that assertion failed and the code was right:** the routine had asserted *no row on the earlier year*, but `2030-2031` legitimately contains the January–May 2031 holidays — the assertion was re-specified to the real invariant, and it is recorded here because the failure was the routine's, not the slice's. **`AC-8` is no longer unexecuted:** `bash scripts/ci-gate.sh` → **`GATE: PASS`**, all 19 stages, one `pnpm build` (8/8 tasks, 10 m 13 s), boot 229 routes. **Still not claimed, and the drill does not change it:** the drill also calls the **service**, so the controller boundary (`confirm: body.confirm === true`) remains without executed coverage exactly as stated above, and no browser rendered the drawer. **Residue removed:** the drill's tenants cascade-deleted, and its 6 orphan `audit_log` rows deleted by hand — see **`PF-96`**, which that cleanup discovered. | G-AUDIT, G-TENANT, G-DNC |
| VAL-03 migration + backup/restore rehearsal | V3-E02 | **S-E02-3** | `closed` *(local stack; the hosted half is R-01, still open)* | `apps/api/src/shared/quality/restore-drill-gate.spec.ts` — **81/81** (**331/331** across 10 suites in `src/shared/quality`, was 250/250 across 9) — three of those tests were added by the routine after adversarial review, see below | **The rehearsal was executed, not asserted — which is the entire finding.** `node scripts/restore-drill.js` against the LOCAL Docker stack: `RESTORE DRILL: PASS`, **exit 0**, **55 base tables / 30 non-empty / 13 550 rows** dumped with `pg_dump -Fc` inside `pilotage_postgres`, restored into a scratch database created `TEMPLATE template0`, and verified on **row counts AND per-table content checksums AND schema** (enum labels, columns, indexes, PK/UNIQUE/FK) — **dump 2284 ms · restore 8499 ms · verify 6190 ms · total 34 357 ms** (the routine's re-run against the fully-seeded database; the sprint's own earlier execution measured 1242/7488/4001/22 737 ms against a smaller dataset, and the committed baseline carries the later figures because it must describe the database the drill actually faces), recorded in `scripts/restore-drill-baseline.json` by `--update` from that passing run, every entry carrying a written reason. **Divergence proven in both directions against the real database**, through a seam that is monotone in the failing direction (`--inject-fault-sql` can never yield `ok` and can never write a baseline): deleting one `student` row → `row_count_divergence`, exit 1, naming `student — rowCount: source 2463, restored 2462` plus the three tables the FK cascade reached; mutating one `school.name` → `checksum_divergence`, exit 1, **same row count**, which is precisely the divergence a counter-only check misses. **Refusal paths executed (DNC-08):** bogus source → `unreachable_source` (the word *skip* appears **0** times in the whole output); a database with no `_prisma_migrations` → `unbaselined_ledger`; a non-existent container → `tooling_unavailable`, naming **both** routes tried; `--update` from a failed run → *refusing to write the baseline*, exit 1, **no file written**. **Three defects were found in this slice by its OWN adversarial review and fixed by the routine before merge — that is why this row is trustworthy rather than a defect in it.** (1) `evaluateDrill({})` returned `ok`, `exit 0`, `baselineWritable: true`: every check is written `if (x === false)`, so an input that OMITS a field skips the check that would have used it. `null` was handled; `{}` was not — and `{}` is the dangerous half, because a crashed or half-built run hands you an object, not a null. **That is DNC-08 committed by the function whose sole purpose is enforcing DNC-08.** A verdict of `ok` now requires positive evidence from all seven stages and names every missing one. (2) The ledger check asserted `applied_steps_count > 0` — but `prisma migrate resolve --applied`, **this repository's own documented baselining path** (`infra/docker/migrate-entrypoint.sh` §2, `docs/runbooks/baseline-hosted-database.md` §2), writes that column as its DDL default of 0. The drill would have reported a false `pending_migration` at exactly the moment an operator most needed reassurance. Removed; `finished_at` + not-rolled-back + checksum are what establish "applied". (3) `inventory_drift` made `--update` refuse, so the first legitimate migration adding a table would have turned the drill permanently red with its own documented remedy unreachable — it now permits a write, and a run that drifted AND diverged still refuses, which is its own test. **Not closed:** the drill is deliberately **not** a `ci-gate.sh` stage (ADR-025 D1 — it needs a live Postgres and that gate needs no services; the guard asserts that absence in the negative), and it has **never** run against the hosted database — nor should it, since there is no production (Step −1). `docs/runbooks/backup-restore-drill.md` §7 lists the nine things it does not prove; the sharpest is that a demo-seed dataset gives a duration on this hardware, not a restore time at real volume, so the recorded SLO is a floor and not a forecast | G-MIGRATION |
| VAL-10 running build SHA / schema version | V3-E02 | S-E02-1 → **S-E02-6** | `closed` | `apps/api/src/shared/release/release-manifest.spec.ts` — **19/19 passed locally** (`pnpm exec jest src/shared/release`; 29/29 with the S-E02-1 migration specs) | **The gate is executed, not asserted — in both directions.** The comparison is now between two *independent* sources: `GIT_SHA` baked into the image at build time (`ARG`→`ENV` in all three Dockerfiles + compose `build.args`) and `EXPECTED_GIT_SHA` injected at deploy time. `GET /version` publishes `release.verdict` ∈ {`match`,`drift`,`dirty`,`unstamped`,`unverified`}; the boot preflight refuses a non-servable verdict in production; `scripts/release-gate.sh` re-checks **independently** and `deploy-prod.sh` fails the deploy on a bad verdict and refuses a dirty tree outright. **Negative path executed against the live stack:** run against the actually-running local API (up 43 h), the gate **failed with exit 1** and named the cause — that container predates `/version`, i.e. a real drifted artefact. **Positive + every other path executed** against a synthetic manifest: `match`→0, `drift`/`dirty`/`unverified`/`unstamped`/missing-field→1, and a manifest *claiming* `match` against a foreign SHA →1 (the gate does not delegate its verdict to the artefact it judges). No bypass flag exists (DNC-10), locked by a test trying three plausible flag names. `S-E02-1` found the exposure half was itself inert — recorded as `PF-68` | G-MIGRATION |
| PF-68 release manifest inert: SHA never stamped, `/version` unroutable | V3-E02 | S-E02-6 → **S-E02-10** | `closed` | `apps/api/src/shared/release/release-surface.spec.ts` — **19/19**; `apps/worker/src/shared/release/version-server.spec.ts` — **8/8** (a real socket, really queried); `release-manifest.spec.ts` — 19/19 (38/38 across `src/shared/release`) | **The whole finding is closed; the residual half was the larger one.** *Run 7 (S-E02-6)* fixed the two original causes: `GIT_SHA`/`BUILD_SHA` was set by *nothing* in the repo (now baked at build time in all three Dockerfiles), and `/version` fell through to Next.js because it is excluded from Nest's `api/v1` prefix while nginx proxies only `/api/` (now an exact-match, rate-limited `location = /version`). *Run 12 (S-E02-10)* closes the rest. **The residual was measured against the previous gate, not argued:** `git show main:scripts/release-gate.sh` was run against synthetic deployments and returned **exit 0** for (a) a deployment where only the API answers — worker and web never interrogated — and (b) an API at the right commit sitting on a database that was **never baselined** (`migrations.status: unbaselined`, i.e. PF-03's exact live state). The gate *read* `schemaVersion` only to print it, and never read `migrations.status` at all. The new gate returns **exit 1** on both. **What landed:** the comparator moved to `packages/contracts/src/release/` — one copy, since three copies of a version comparator drift and a drifted copy turns green the third of the deployment it is not looking at; the worker gained a ~60-line `node:http` server serving `GET /version{,/worker}` and the **same** production refusal as the api (it writes real data from real queues); web gained a `force-dynamic` route at `/version/web`; `EXPECTED_GIT_SHA` is now injected into all three services; nginx routes all three by exact match; `release-gate.sh` checks 4 things instead of 1. **Executed in every direction** (see the run log): clean → 0; worker drift → 1; web manifest absent → 1 *(unreachable is a failure, never a skip)*; a proxy answering the api manifest on the worker path → 1 *(the `app` field is why)*; `unbaselined` → 1; a database clean about its **own** older migration → 1; an artefact **claiming** `match` at a foreign SHA → 1. **Run against the actually-running local stack: 4/4 failed** — all three containers are 2 days old and predate their manifests, which is R-05 live locally, reported rather than worked around. **Not claimed:** the gate has still never run against the *hosted* deployment (it cannot until a deploy carries these manifests), and it proves *which* artefact runs, not that it works | G-MIGRATION |
| PF-58 V3 substrate uncommitted; routine unrunnable | V3-E02 | S-E02-0 | `closed` | n/a — docs-only | Branch `ci/2026-08-02-v3-substrate-landing` **pushed to `origin`** (from `b665887`): 4 audits + 17 planning docs + 40 evidence files. Verified — file set identical to source worktree `youthful-chaum-6aad5c` and a content superset of it; `routine/` blobs byte-identical to the installed `SKILL.md` and `routine-lock.sh` (`cmp` against `git show` output). **PR [#170](https://github.com/Tanimou/projet-scolaire-claude/pull/170) is now open** — `gh pr create` succeeded on the 2026-08-02 19:xx run (the earlier classifier denial did not recur), state `MERGEABLE`/`UNSTABLE`. **Merged** — PR #170 landed as `81c6e15` on `main`; verified by the 2026-08-02 V3 run, which fast-forwarded the main checkout onto it and read all six Step-1 files from tracked content. `UNSTABLE` was caused by `PF-59`, not by this diff | G-DNC |
| PF-62 teacher grading REST surface unmounted in production | V3-E02 (found) → V3-E07/E08 (owns) | S-E02-2 | `closed` | `apps/api/src/modules/grades/grades.module.spec.ts` — **2/2 passing** (was 2/2 failing for 7 weeks) | **Live P0, fixed.** `controllers: [AssessmentsController, GradesController]` restored to `GradesModule` with the imports the two controllers' DI needs (`AuthModule`, `SchoolStructureModule`, `TeachingModule`; `NotificationsModule` is `@Global`), matching the pre-`3341ed0` shape. Verified on production **before** the fix: `/api/v1/assessments` and `/api/v1/grades/gradebook/x` both returned **404** while the mounted control `/api/v1/notifications` returned **401** and an unmatched path returned 404 — so the routes genuinely did not exist. API `tsc --noEmit` exit 0 and `nest build` pass after the change. **Residual risk stated honestly:** the guard asserts on module *source*, so it proves registration but not DI resolution at boot; DI is argued from the identical shape in `TeachingModule` (same `UserSyncService` + `TeacherProfileService` injection, boots today) rather than from an executed boot. A boot-level route-table assertion is `PF-67`. Deploy is an operator action — the fix reaches production only on the next rebuild | G-PORTAL |
| PF-67 *(widened, run 10)* — **no gate boots either Nest application**, so a type-correct change that breaks DI ships green | V3-E02 | **S-E02-9** | `closed` | `scripts/boot-check.js` — executed, positive **and** two negative paths; `apps/api/src/shared/quality/boot-gate.spec.ts` — **15/15** (`pnpm exec jest src/shared/quality` → 61/61 with the two lint guards) | **Something now starts the app, and it was proven against both known triggers rather than asserted.** `scripts/boot-check.js` constructs the real module graph with `Test.createTestingModule({imports:[AppModule]}).compile()` for **every** Nest application, and compares the API's route table — read off the **booted container**, not off module source — against `scripts/boot-route-baseline.json`. Wired as stage 7 of `ci-gate.sh` and into `ci.yml`'s build job (a guard asserts both, and asserts it runs *after* the build, since it reads `dist/`). **The stated blocker was verified, not assumed:** a probe spec importing `AppModule` under ts-jest dies parsing `jose@6`'s `export` (`AppModule → AlertsModule → AuthModule → JwtStrategy → jwks-rsa → jose`), which is why the check runs outside jest under plain Node — that loads `jose` natively rather than mocking away the thing we want to prove loads. **It runs against `dist/` deliberately:** R-24 is a defect in *emitted* metadata, so judging a re-compilation of the source would judge a different build than the one deployed. **Negative path 1 (R-24, DI break):** patching the emitted `design:paramtypes` of `analytics.service.js` to `[Object, Object, Object]` — the exact output run 10 measured from the `consistent-type-imports` autofix — makes the gate **exit 1** with *"Nest can't resolve dependencies of the AnalyticsService (?, Object, Object)"*. **Negative path 2 (PF-62, unmounted controller):** emptying `grades.module.js`'s `controllers:` array makes it **exit 1** naming all **13** routes that were 404 in production for seven weeks — and note the app still *booted* (38 controllers), so the compile half alone would have missed it; the two halves catch different defects. Also exercised: an app deleted from the baseline → exit 1; a missing `dist/` → failure, not a skip; `--update` from a partial run → refused. **Positive path:** api 42 modules / 40 controllers / 228 routes, worker 23 modules, `BOOT CHECK: PASS`. **No bypass flag** (DNC-10), locked by a test. **Residual, stated plainly:** `.compile()` is called, never `.init()` — that is what lets the check run with no Postgres or Redis, and it means the evidence is "the application can be constructed", not "it can serve traffic". A wrong connection string at runtime is still invisible to it. And like every gate here, it binds only as far as `ci-gate.sh` is actually run, while `PF-59` keeps the hosted runner dark. *Original run-10 report retained:* **second, independent trigger found — the finding is broader than "module guards read source".** `S-E02-8` measured it: applying `eslint --fix` for `@typescript-eslint/consistent-type-imports` to `apps/api/src/modules/analytics/analytics.service.ts` rewrote the three constructor-injected services to `import type`, and the emitted JS went from `__metadata("design:paramtypes", [PrismaService, GradesService, RemediationService])` with 3 `require()` calls to **`[Object, Object, Object]` with 0** — Nest could not resolve `AnalyticsService` at boot. **Every gate stayed green on the broken file:** `tsc --noEmit -p tsconfig.json` returned **exit 0** (executed), `nest build` succeeds, ESLint is *satisfied* (it produced the change), and the module-wiring guards read module source and never see a constructor. So the class is not "the wiring guards are textual" but "**nothing starts the app**". The rule is now off in the two packages that inherit `emitDecoratorMetadata`, and a guard ties that exemption to the tsconfig flag in both directions — but that defends the one trigger we found, and any other type-correct DI break still ships. Closing this needs a boot-time assertion (route-table snapshot or `Test.createTestingModule().compile()` in an ESM-capable jest project) | G-PORTAL |
| PF-72 `pnpm build` reports success while emitting an **empty** worker `dist/` | V3-E02 | S-E02-9 | `closed` | `apps/api/src/shared/quality/boot-gate.spec.ts` — describe *"a build that deletes its output directory must not be incremental"*, 3/3 | **Found by turning the boot gate on, and fixed in the same run.** `apps/worker/nest-cli.json` sets `deleteOutDir: true` while the worker inherited `incremental: true` from `@pilotage/tsconfig/base.json`: the build deletes `dist/`, then asks a compiler holding a `.tsbuildinfo` that says everything is already emitted to emit, and it emits nothing at exit 0. **Measured in both directions, changing one thing:** stale build-info present → `nest build` **exit 0, 0 files**; remove *only* the build-info and rerun → **exit 0, 53 files including `main.js`**. After the fix, two consecutive builds each emit 53 files — the second is precisely the case that used to emit nothing. `apps/api` was never affected because it already carried an explicit `incremental: false`, which made the override look stylistic rather than load-bearing. **Why it stayed invisible:** `pnpm build` exits 0, and turbo's `build` task declares `outputs: ["dist/**", …]`, so the *empty* dist was cached as a successful output and replayed on later cache hits; `.tsbuildinfo` is gitignored, so a clean clone reproduces it as soon as anything builds twice. The guard reads the value through `tsc --showConfig` rather than parsing the app's tsconfig, because the setting is **inherited** — and because regex-parsing JSONC is how run 10's guard nearly passed vacuously. Negative path executed: restoring `incremental: true` turns the guard red (1 failed / 14 passed), restoring `false` turns it green. **Not claimed:** a hosted deploy was probably never hit, since Docker builds from a fresh checkout with no build-info — but nothing in the repository *guaranteed* that, and a warm cache or reused workspace would have shipped a worker image with no `main.js` | G-DNC |
| PF-74 nothing asserts the Next.js build emitted anything, and `/version/web`'s `force-dynamic` is documented but unenforced | V3-E02 | **S-E02-11** | `closed` | `apps/api/src/shared/quality/web-artifact-gate.spec.ts` — **12/12** (`pnpm exec jest src/shared/quality` → **73/73** with the three other quality guards) | **This is `R-25`'s residual half given a matrix row so its closure is traceable — not a new discovery.** It was recorded on `R-25` as "`web` uncovered" and as item 2 of the epic's "Next slice" before this run started. **Measured, not assumed:** `apps/web/.next` was moved aside in its entirety and `node scripts/boot-check.js` re-run — **`BOOT CHECK: PASS`, exit 0** — and `grep -rn '\.next' scripts/` returned **nothing**, so no stage of `ci-gate.sh` read the directory at all. `boot-check.js` structurally cannot cover web: it discovers `apps/*` containing `src/app.module.ts`, and Next.js has no module graph to construct. turbo's `build` task declares `outputs: ["dist/**", ".next/**", …]`, so the exact caching rule that replayed `PF-72`'s empty `dist/` applies to `.next/` verbatim. **What landed:** `scripts/web-artifact-check.js` + `scripts/web-route-baseline.json` (108 routes), stage 8 of `ci-gate.sh` and a step in `ci.yml`'s build job, asserting `BUILD_ID`, the route inventory, an emitted `route.js`/`page.js` per manifest entry, non-empty `.next/static`, the standalone entrypoint `Dockerfile.web` runs as its `CMD`, and that `mustBeDynamic` routes were not prerendered. That last assertion is the sharp one: `/version/web` is the web third of the release gate (`S-E02-10`, **R-05**) and its source says `force-dynamic` is mandatory, yet **nothing enforced it** — deleting one line left the route present, the build green and the gate answering 200 with a build-time constant. `output: 'standalone'` is read from `.next/required-server-files.json` (Next's own record of the config it ran with), the same discipline as `tsc --showConfig` in `boot-gate.spec.ts`. **Executed in 9 directions:** clean → 0; `.next` deleted → 1 *(a missing build is a failure, never a skip)*; page dropped from the manifest → 1; manifest route with no emitted file → 1; `/version/web` prerendered → 1; standalone entrypoint absent → 1; `.next/static` absent → 1; app removed from the baseline → 1 *(no escape by omission)*; `--update` from a broken artefact → **refused**. The guard spec was exercised in the negative too — emptying `mustBeDynamic` plus deleting the source directive → 2 failed/10 passed; replacing the stage with `true` in **both** `ci-gate.sh` and `ci.yml` → 2 failed/10 passed. **Not claimed:** the check does not start Next.js, so web is proven *produced and complete*, not *correct* — the asymmetry with the two booted Nest apps is why `R-25` stays `mitigated`. And the causal step "removing `force-dynamic` causes prerendering" is **inferred**, not executed: confirming it needs a second `next build`, and all four route handlers in this build are already dynamic, so there is no in-build control case | G-DNC |
| PF-73 `engines.node >=20.0.0` blesses a Node version on which the API cannot start | V3-E02 | **S-E02-12** | `closed` | `apps/api/src/shared/quality/runtime-engines-gate.spec.ts` — **18/18** (`pnpm exec jest src/shared/quality` → **91/91** with the four other quality guards) | **The declaration is now arithmetic that runs, and the finding's own prescription turned out to be wrong in both directions.** `scripts/runtime-engines-check.js` reads every installed package's `engines.node` from pnpm's virtual store — **671 of 1294 declare one** — and asserts, with `semver.subset`, that no Node version blessed by the root declaration is refused by any of them. **Executed against the unfixed repository first:** `RUNTIME ENGINES CHECK: FAIL`, exit 1, and it named **35** contradicting dependencies where the finding named one (`@eslint/*`, `@typescript-eslint/*`, `sharp`, `glob`, `lru-cache`, `rimraf`, `minimatch`, `jwks-rsa`, `eslint-visitor-keys`, …). **The correction that matters:** PF-73 proposed *«Fix is one line (`>=22.12.0`)»*. Measured, that fix is wrong **both ways** — it **excludes** Node 20.19.x, which every installed dependency accepts (require(ESM) was backported there, and `jwks-rsa@4.0.1` says so itself: `^20.19.0 \|\| ^22.12.0 \|\| >= 23.0.0`), and it **blesses** 22.12.x and 23.x, which `eslint-visitor-keys@5.0.1` refuses (`^20.19.0 \|\| ^22.13.0 \|\| >=24`). The versions the whole installed set accepts are **20.19.x · 22.13.x · ≥24**; what the repo now *declares* is the narrower `^22.13.1 \|\| >=24.0.0`, because `engines` is a **support** statement — 22.13.1 is what the three Dockerfiles ship, ≥24 is what local development runs — and 20.19 is compatible-but-untested. Both corrections are locked as tests, so they survive the finding being re-read. **Positive path executed:** exit 0, *"is a subset of all 671 installed engines.node ranges"*, *"all 5 Node pins agree on 22.13.1"*. Wired as **stage 0** of `ci-gate.sh` and a step in `ci.yml`'s install job, with a guard asserting both so they cannot drift (S-E02-2 AC-4). No bypass flag and no `--update` (DNC-10) — the reviewed record is `engines` in `package.json`, so widening support always appears in the diff. **Not claimed:** the boot failure below 20.19 was **not executed against a Node 20 runtime** — that needs a second runtime the gate does not have. The packaging was read directly (`jose@6.2.3` `"type":"module"`, `main: ./dist/webapi/index.js` whose first line is `export {…}`; `jwks-rsa/src/utils.js:1` `const jose = require('jose')`), and the failure stays **inferred** from the require(ESM) support matrix. What is executed is the arithmetic over declared ranges — which is what a declaration *is* | — |
| PF-75 `engines.pnpm >=8.0.0` blesses a pnpm major that never produced this lockfile | V3-E02 | **S-E02-12** *(discovered and closed in the same run)* | `closed` | `apps/api/src/shared/quality/runtime-engines-gate.spec.ts` — *"fails when engines.pnpm blesses a major that never produced this lockfile"* | **Second defect in the same field, which PF-73 never mentioned.** The root declared `pnpm: ">=8.0.0"` beside `packageManager: pnpm@9.12.3`, while `pnpm-lock.yaml` carries `lockfileVersion: '9.0'` — pnpm 8 writes lockfile 6.0. So the repository declared support for a package manager that cannot have produced its own lockfile. Corrected to `>=9.0.0`, and the rule is now checked rather than remembered: the floor of `engines.pnpm` must sit inside the major of the `packageManager` pin, and `ci.yml`'s `PNPM_VERSION` must equal that pin. Both negative paths executed via the pure evaluator | — |
| PF-76 `.nvmrc` and `ci.yml` pin a **floating** Node major that includes the un-bootable window | V3-E02 | **S-E02-12** *(discovered and closed in the same run)* | `closed` | `apps/api/src/shared/quality/runtime-engines-gate.spec.ts` — *"fails on a floating major pin, even though it resolves safely today"* + *"pins one concrete Node version in .nvmrc, every Dockerfile and ci.yml"* | **PF-73 called itself latent because "everything runs 22". It was latent for a weaker reason than that.** `.nvmrc` said `22` and `ci.yml` said `'22'` — a **floating** major whose declared meaning is 22.0.0–22.99.x, i.e. it includes 22.0–22.11, the window in which the API provably cannot boot. It is safe only because nvm and `actions/setup-node` happen to resolve a bare major to the newest release; a statement that is true by accident is the same class of defect one level down. Both are now pinned to **22.13.1**, equal to the three `ARG NODE_VERSION` defaults, and the check requires every pin to be concrete, in range, and in agreement — Dockerfiles discovered from the filesystem, not from a list, so a fourth one is covered without anyone remembering | — |
| PF-60 sprint workflow silently overrides the routine's story selection | V3-E02 | S-E02-2 (fixed in flight) | `closed` | `node --check` clean; no remaining `intake.epic \|\| ARG_EPIC` derivation | **Operator override now wins.** `mode`/`epic`/`slice` resolve as `ARG_* || intake.*` (was `intake.* || ARG_*`, and `mode` ignored `ARG_MODE` entirely); the two downstream re-derivations at the slice-context and result sites were switched to the resolved `slice` so they cannot diverge; a disagreement between intake and an explicit override now emits a `log()` warning naming both instead of resolving silently; the intake prompt now states the override is authoritative rather than "honor unless clearly unsafe". Fixed **in this run** because the routine's Step 3 calls this workflow — leaving it would make V3's selection rule decorative for the next run too. *(Original report: on the 2026-08-02 run the routine selected `V3-E02 / S-E02-1` and the workflow began implementing `V3-E06 / S-E06-2` instead — caught by inspecting `journal.jsonl` and stopped before anything was written.)* | G-DNC |
| PF-61 duplicate `R-17` id in `risk-register.md` | V3-E02 | — *(substrate defect — fixed in this PR)* | `closed` | n/a — docs | Two distinct risks shared the id `R-17`: the CI-billing risk (added with PF-59) and the market-decision risk referenced by `open-decisions.md` D-04 and `roadmap.md`. Cross-references were therefore ambiguous. Resolved this run by renumbering the **newer** CI risk to **R-21** and leaving the market risk as `R-17`, so every pre-existing reference stays correct | G-DNC |
| PF-104 a job that fails by *stalling* reaches no counter | V3-E02 *(follow-up)* | **S-E02-18** | `closed` | `apps/worker/src/shared/observability/queue-depth.collector.spec.ts` *(new)* + `queue-metrics.spec.ts` — **63/63** across the three worker observability suites; `apps/api/src/shared/quality/observability-gate.spec.ts` — **71/71** (33 when `S-E02-13` wrote it); `scripts/observability-check.js` — executed, control **and** negative | **Proven by killing a worker, not by a stub — and this is the first executed proof of a `stalled` event anywhere in this programme.** `@OnWorkerEvent('stalled')` on the three processors now feeds `pilotage_queue_stalled_total{queue}` through one write implementation (`observeJobStalled`, DNC-01), and the counter is deliberately **not** zero-seeded so « No data » reads as « it never happened ». **The live drill, on the rebuilt local container:** `pilotage_postgres` paused, one `exports` job enqueued, the processor logged « `[audit_csv] … — start` » and blocked on its first Prisma write, `getJobCounts` confirmed `active: 1`, then `docker kill -s SIGKILL pilotage_worker` — a real death mid-job, leaving the job in `active` with a lock nobody would renew. Postgres unpaused, worker restarted, and **55 s later** the exposition carried `pilotage_queue_stalled_total{queue="exports",app="worker"} 1`. **The documented nuance held in the same run**, which is why it is trustworthy: `stalledCount` was 1, not `> maxStalledCount`, so BullMQ returned the job to `wait`, the worker re-ran it at 05:14:45 and it failed normally — the exposition therefore shows `pilotage_queue_stalled_total 1` *and* `pilotage_queue_jobs_total{outcome="failed_terminal"} 1` as **two separate facts that do not add up**, exactly as the counter's docblock and panel 9's description say. **G-TENANT discharged against the live artefact:** the probe payload carried `tenantId="probe-tenant-stall"`, `exportJobId="nonexistent-stall-probe-id"` and `requestedBy="routine-run-26"`, and the job name `stall-probe` is not allow-listed — `grep -c` for each of the four in the 14 721-byte exposition returns **0**, and the name rendered as the `<other>` sentinel. `observeJobStalled` does not take a job id **as a parameter**, so labelling by id would require changing a signature in review. **Then ingested, which no prior observability row can claim:** `--profile obs` started, all four Prometheus targets `up`, and panel 9's own expression `sum by (queue) (rate(pilotage_queue_stalled_total[5m]))` resolved against the real TSDB. **Not claimed:** the counter cannot distinguish « returned to wait » from « moved to failed » — the event does not carry it, which is precisely why this is a separate family and not a fourth `outcome`; and a job that stalls *past* `maxStalledCount` is still never counted by `pilotage_queue_jobs_total`, by BullMQ's design. Original finding text follows. — Raised by `S-E02-17`’s verify panel, from BullMQ’s **emit sites** rather than its documentation: in `bullmq@5.76.8` the worker’s only `emit('failed', …)` is `handleFailed`, i.e. the processor threw. `moveStalledJobsToWait` moves a job past `maxStalledCount` straight into the `failed` set and emits **`stalled` alone**. So an OOM-killed, SIGKILLed or lock-losing worker leaves `pilotage_queue_jobs_total` flat at zero while `pilotage_queue_depth{state="failed"}` climbs — the green-silent-false shape this epic exists to close, and it under-reports exactly when the system is broken. Fix: `@OnWorkerEvent('stalled')` on the three processors, as a fourth outcome or a `pilotage_queue_stalled_total{queue}` **not** labelled by `job` (only a jobId is available there) | G-DNC |
| PF-105 check 9’s rule 6 is a tautology — it cannot detect the defect it was written to close | V3-E02 *(follow-up)* | **S-E02-18** | `closed` | `apps/worker/src/shared/observability/queue-depth.collector.spec.ts` *(new)* + `queue-metrics.spec.ts` — **63/63** across the three worker observability suites; `apps/api/src/shared/quality/observability-gate.spec.ts` — **71/71** (33 when `S-E02-13` wrote it); `scripts/observability-check.js` — executed, control **and** negative | **The tautology is gone, and the proof is the script exiting 1 on a real artefact rather than a fixture.** `rendered` is no longer scraped from the zero-seeded failure counter: `readInstrumentedQueues` now drives the **real** `registerQueueDepthSources` with a synthetic non-zero source and a **real** `observeJobCompleted` per declared queue (allow-listed job name only — driving an unlisted one would make the `<other>` sentinel the proof), then takes the **intersection** of the queues carrying a `pilotage_queue_depth` sample *and* a `pilotage_queue_jobs_total` sample, matched on exact family name followed by `{` rather than by prefix — `pilotage_queue_depth_collection_failures_total` starts with `pilotage_queue_depth`, and counting it as the depth family is exactly how this check was vacuous before. **Executed both ways against `apps/worker/dist`:** unmodified → `OBSERVABILITY CHECK: PASS`, **exit 0**; a fourth queue `drift-probe-queue` added to the built `INSTRUMENTED_QUEUES` → **exit 1** with *« Declared with NO write path: [drift-probe-queue] »*, i.e. the missing queue is **named**, plus the independent check-5 problem beside it. Artefact restored; the check returned to exit 0. The `readInstrumentedQueues` header claim that it confirms the declaration against « what the process really publishes » is corrected in the same pass. Original finding text follows. — `scripts/observability-check.js` requires `declared ≡ rendered`, but in the gate’s own process the only queue-labelled samples in existence are the ones `queue-metrics.ts` zero-seeds **from `INSTRUMENTED_QUEUES`**. `rendered` is therefore derived from `declared`, and the comparison is `A ≡ A`: a declared queue that no write path ever touches passes unchanged. Rules 3/4/5 (api vs worker vs declared, read off the resolved `BullQueue_*` tokens) are real and unaffected — **this is the half that finally compares the two queue-name constant blocks, and it works**. What fails is the half claiming to confirm the declaration against “what the process really publishes”. **This is the routine’s own recurring defect — a guard that cannot fail — for the fourth time (`PF-83`, `PF-97`, `PF-103`, here)**, and the reason `S-E02-17` was not auto-merged | G-DNC |
| PF-106 nothing executes the queue instrumentation’s wiring | V3-E02 *(follow-up)* | **S-E02-18** | `closed` | `apps/worker/src/shared/observability/queue-depth.collector.spec.ts` *(new)* + `queue-metrics.spec.ts` — **63/63** across the three worker observability suites; `apps/api/src/shared/quality/observability-gate.spec.ts` — **71/71** (33 when `S-E02-13` wrote it); `scripts/observability-check.js` — executed, control **and** negative | **The wiring is executed, and both negatives were run rather than described.** `queue-depth.collector.spec.ts` boots a real `Test.createTestingModule` over `getQueueToken(...)` stubs, `await moduleRef.init()`, and asserts a **rendered** `pilotage_queue_depth` sample equals the **stub's** number — not a helper's return value; the per-processor cases invoke the decorated `@OnWorkerEvent` methods and assert the rendered `queue="…"` label, with one assertion over the decorator metadata so a misspelled event name (`'complete'`, which BullMQ never emits) turns it red; and one case asserts `AppModule.imports` contains `ObservabilityModule`. **Confirmed live on the rebuilt container**, which is the part a spec cannot give: `QueueDepthCollector` logged « Profondeur de file instrumentée pour 3 file(s) : exports, notifications-email, imports » and the exposition carried `pilotage_queue_depth_sources_bound{queue=…} 1` for **all three** queues — the seam itself observed in the real process, not in a testing module. Before the rebuild the running container exposed **0** samples of that family and **0** of `pilotage_queue_stalled_total`, against 24 depth samples: the measurement that makes the after meaningful. Original finding text follows. — `grep -rn 'OnWorkerEvent' --include=*.spec.ts apps/` returns **zero** hits and `QueueDepthCollector.onModuleInit` has no reference outside its own module: all 39 `S-E02-17` tests call the helpers **directly**, proving the helpers and never the seam. Dropping `ObservabilityModule` from `app.module.ts`, drifting one `@InjectQueue` token, or misspelling an event name ships green on every gate. **Re-scoped by run 24’s runtime evidence, and the re-scope matters:** the routine rebuilt the worker and drove a real failing job through it, so the seam is proven **live** — `QueueDepthCollector` bound 3 queues, 24 depth samples rendered, and `pilotage_queue_jobs_total{outcome="failed_terminal"}` went to 1. The finding is therefore **not** “the capability may be dead”; it is “the capability is live, proven once by hand, and **nothing re-proves it**”. Fix: a `queue-depth.collector.spec.ts` over `getQueueToken` stubs with `await moduleRef.init()`, plus per-processor `onCompleted`/`onFailed` cases, shown red once with `ObservabilityModule` removed (R-26) | G-DNC |
| PF-107 the series that makes a Redis outage legible is plotted on no panel | V3-E02 *(follow-up)* | **S-E02-18** | `closed` | `apps/worker/src/shared/observability/queue-depth.collector.spec.ts` *(new)* + `queue-metrics.spec.ts` — **63/63** across the three worker observability suites; `apps/api/src/shared/quality/observability-gate.spec.ts` — **71/71** (33 when `S-E02-13` wrote it); `scripts/observability-check.js` — executed, control **and** negative | **Plotted, and then actually queried.** Panel 5 gains target `B`, `sum by (queue) (rate(pilotage_queue_depth_collection_failures_total[5m]))`, legend `{{queue}} · échecs de collecte`, on the **right** axis with unit `ops` because the two series share neither unit nor order of magnitude — and with **no** `thresholds.steps`, since what « too deep » means stays a product decision (`PF-56`). **Ingested, not asserted:** with `--profile obs` running and all four scrape targets `up`, the expression resolved against the real Prometheus and returned a series per queue (`exports`, `notifications-email`, `imports`, all `0` — an honest zero, because the counter *is* zero-seeded). Panel 5's description was rewritten in the same pass so it now routes the on-call to a series the panel actually draws, and additionally names `pilotage_queue_depth_sources_bound` as the way to tell « never bound » from « bound and failing » — DNC-06 applied to the sentence that created the finding. Original finding text follows. — `pilotage_queue_depth_collection_failures_total` exists precisely so a swallowed collector error is observable, and panel 5’s own description routes the on-call to it **by name** — yet no panel queries it, and check 6 validates dashboard→registry only, never registry→dashboard, so no gate can see the gap. Depth degrades to **stale**, never zero (deliberate and correct), so a total Redis outage renders as flat, plausible lines with nothing naming it. Fix: one target on panel 5 | G-TRUTH |
| PF-108 a gauge describing a shared resource is summed across replicas | V3-E02 *(follow-up)* | **S-E02-18** | `closed` | `apps/worker/src/shared/observability/queue-depth.collector.spec.ts` *(new)* + `queue-metrics.spec.ts` — **63/63** across the three worker observability suites; `apps/api/src/shared/quality/observability-gate.spec.ts` — **71/71** (33 when `S-E02-13` wrote it); `scripts/observability-check.js` — executed, control **and** negative | **`max by`, and the arithmetic was demonstrated on the real TSDB rather than argued.** At one replica `sum` and `max` agree, so a single-instance check would have been vacuous; the same query with a second instance label injected — `… (pilotage_queue_depth or label_replace(pilotage_queue_depth, "instance", "worker-replica-2:4001", "instance", ".*"))` — returns `{queue="exports",state="failed"} = **4** ` under `sum by` and `= **2** ` under `max by`, against a real depth of **2**. Panels 6–8 are untouched: they `sum` a `rate()` of a counter, which *is* additive. **The rule generalises, and it was proven red:** new check **10** fails any dashboard expression that aggregates a **gauge-typed** family with `sum()`, resolved through the declared `# TYPE` lines rather than by naming `pilotage_queue_depth` — a rule that named the metric would be a fix wearing a gate's clothes. Executed: reverting panel 5's expression to the pre-slice `sum by (queue, state) (pilotage_queue_depth)` produced **exit 1** naming the panel, the metric and the remedy; restoring it returned **exit 0**. A missing or empty type map is a **failure, not a skip** (DNC-08), and an unparseable `sum(` is a problem rather than a silently accepted expression. **Discovered while proving it, and recorded rather than glossed: `PF-113`** — `--scale worker=2`, the reproduction this finding's own text prescribes, is **refused** by this compose file, because 16 of its 21 services declare `container_name`. The defect stands for any replica-bearing deployment; the stated way to reproduce it does not exist here. Original finding text follows. — The depth panel uses `sum by (queue, state) (pilotage_queue_depth)`. Depth is a property of the **queue**, not of the process, so every replica reports the same absolute number and Prometheus keeps them apart by `instance` — `docker compose up --scale worker=2` then **doubles every line**, with no code change and no gate noticing. This is `ADR-028` D1’s own “one number, two sources” defect arriving one dimension over. Panels 6–8 are correct as written (`sum` of `rate()` over counters *is* additive). Fix: `max by (queue, state)`, plus a line saying why the gauge aggregates with `max` while the counters beside it aggregate with `sum` | G-TRUTH |
| PF-109 a `DNC-06` guard is defeated by a French plural, in the file it inspects | V3-E02 *(follow-up)* | **S-E02-18** | `closed` | `apps/worker/src/shared/observability/queue-depth.collector.spec.ts` *(new)* + `queue-metrics.spec.ts` — **63/63** across the three worker observability suites; `apps/api/src/shared/quality/observability-gate.spec.ts` — **71/71** (33 when `S-E02-13` wrote it); `scripts/observability-check.js` — executed, control **and** negative | **Both halves fixed, and the guard now has a failing case — which it never had.** T17b matches by **stem** (one case-insensitive alternation over `dlq`, `dead-letter`/`dead_letter`, `lettre(s) morte(s)`, `file(s) morte(s)` and `mise au rebut`) and still scans the **raw** file rather than narrowing to panel fields, so the guard is strictly stronger, not narrower; JSON escape sequences are flattened to whitespace first, because inside a JSON string a newline is the two characters `\` and `n` and « lettres\nmortes » would otherwise walk straight through. The predicate is a **single named function** used by both the assertion over the real dashboard and the companion case that feeds it a synthetic panel title and asserts it fires — two inlined copies of a regex is DNC-01 at the test layer, where the guard and its proof drift apart and the proof keeps passing. The offending sentence in `pilotage-slo.json`'s `__comment` was **reworded, not exempted**: it still says BullMQ has no dedicated abandoned-job queue and that a job exhausting its attempts stays in the `failed` set. No gate was weakened to make this land. Original finding text follows. — The guard forbids dead-letter vocabulary in the dashboard but lists only singular terms (`lettre morte`, `file morte`), while the dashboard already contains « une file de lettres mortes ». Verified: all five forbidden terms report `false` against the current file. Unlike the `DNC-10` guard a hundred lines below it, this one has **no companion “the guard can actually fail” case**, which is why the gap was invisible. Fix: match stems, and add the companion case | G-DNC |
| PF-110 `ADR-028` takes a number already reserved for a different decision | V3-E02 *(follow-up)* | **S-E02-18** | `closed` | n/a — a register reconciliation, verified by reading both files | **The register is reconciled and the file keeps its number.** `architecture-impact.md` §4 now records that ADR-025…028 were consumed by `V3-E02` slices and that the reserved decisions renumber from **ADR-032**, and `ADR-028`'s own header carries a `Numbering` note pointing at that reconciliation. The existing file is **not** renumbered — history is preserved, which is the whole point of a register. This closes the bookkeeping half of the `R-26` shape landing on the ADR index itself: the next reader of §4 finds four occupied numbers *and* a written rule for what happened to them. Original finding text follows. — `architecture-impact.md` §4 reserves **ADR-028** for *“Audit in-transaction, chain genesis, and the accepted pre-V3 gap”* (epic **V3-E04**, blocking). `S-E02-17` takes the number for an observability decision and leaves §4 untouched. `ADR-025`/`026`/`027` collided with the same table in earlier slices, which is what makes this instance matter: the next reader finds four reserved numbers all occupied by other subjects and **no written rule for which register wins**. Fix: record in §4 that 025–028 were consumed by `V3-E02` slices and renumber the reserved decisions from ADR-032 | G-DNC |
| PF-114 check 10 rejects the correct replica-de-duplication idiom | V3-E02 *(follow-up)* | **S-E02-19** | `closed` | `apps/api/src/shared/quality/observability-gate.spec.ts` *(`T20`…`T20g`, `T21`…`T21g` — 33 new cases)* + driven end-to-end through the real `node scripts/observability-check.js` against a throw-away dashboard fixture | **Closed, but not by the fix the finding proposed — and that difference is the finding's own lesson.** The row asked for a shield keyed on an operator list (`max`/`min`/`avg`/`topk`/`bottomk`); `S-E02-19` keyed it on the **grouping modifier** instead (`unshieldedGaugeReferences` + the exported `REPLICA_LABELS`), because an operator name says nothing about whether the replica dimension survived. Measured before and after on eleven expressions: `sum by (queue) (max by (queue, state) (pilotage_queue_depth))` PROBLEM → clean; `sum by (queue) (pilotage_queue_depth)` PROBLEM → PROBLEM; `… max by (queue, state, instance) (…)` PROBLEM → PROBLEM; `… max_over_time(…[5m])` PROBLEM → PROBLEM. The end-to-end negative put both the correct and incorrect expression in one fixture file and got **exit 1, exactly one problem**, naming only the wrong one. **Residual — and it is the operator half this row got wrong:** `topk`/`bottomk` were left in `ACROSS_SERIES_AGGREGATIONS` and shield although they select rather than collapse → **`PF-116`** | G-DNC |
| PF-115 three gaps inside `S-E02-18`'s own mechanism | V3-E02 *(follow-up)* | **S-E02-19** | `closed` | `apps/api/src/shared/quality/observability-gate.spec.ts` *(`T22`…`T24f`)* + `apps/worker/src/shared/observability/queue-metrics.spec.ts` *(`T-STALL-6`)* + three negative paths driven through the real script | **All three closed, each proven by a driven negative rather than by a new assertion.** (a) a 14-entry reason enum, each naming the file it actually read — collector moved aside: *« could not be read from …/queue-metrics.js »* (the wrong file, this row verbatim) → *« the built queue-depth collector is absent (…/queue-depth.collector.js) »*; a module that throws → *« could not be required … — collector exploded »*. (b) the restore is **verified**, not trusted: `registryResidue()` re-renders the registry after the `finally`; with the `finally` deliberately blanked the real script exits 1 `INSTRUMENTATION UNREADABLE (registry-not-restored)` and enumerates **all nine** residues. (c) `T-STALL-6` now drives the cuid through `observeJobStalled`'s single parameter and asserts it collapses to `<other>`; making `queueLabel` a pass-through reddens it, where `HEAD`'s version stayed green — which is what made it vacuous. **Residual on (b):** the `pilotage_queue_jobs_total` branch cannot match on the throw path → **`PF-118`** | G-DNC · G-TENANT |
| PF-116 the replica shield accepts `topk`/`bottomk`, which select series rather than collapse them | V3-E02 *(follow-up)* | **S-E02-19** *(raised by the land pass, closed in the same run)* | `closed` | `apps/api/src/shared/quality/observability-gate.spec.ts` — **167/167** (`pnpm --filter @pilotage/api test -- observability-gate`, 2 suites, was 145); the 19-case shielding matrix is driven through `evaluateObservability`, the real entry point | **Closed at the routine’s Step 5, and the finding was WIDER than the land pass recorded — that widening is the result worth keeping.** The panel named `topk`/`bottomk`. Driving all thirteen operators through the real evaluator found **five** fail-open shapes, not two: `topk` and `bottomk` (SELECTORS — they return k of the ORIGINAL series with every label, `instance` included), plus `count`, `count_values`, `stddev` and `stdvar` (aggregations that are not idempotent over replicas: `count` returns N, so the outer `sum` counts processes; `stddev`/`stdvar` return 0 across identical replicas, which is not the resource’s value at all). The root cause is one conflation: **parsing an operator and trusting it are different questions**, and `ACROSS_SERIES_AGGREGATIONS` was being asked both. The fix separates them — every operator is still parsed and consumed (or the walk mis-reads the nesting), and only `REPLICA_IDEMPOTENT_AGGREGATIONS` = {`max`, `min`, `avg`, `group`, `quantile`} may SHIELD. Fail-closed by construction: an operator absent from that set shields nothing, so a PromQL operator added to the language later is unsafe until someone deliberately proves otherwise, and a test pins both the subset relation and the seven names that must stay out. **Executed in both directions, which is the only reason this row is trustworthy:** with the shielding set widened back to every parsed operator, **9 of the new cases go red** and are named individually (`topk`, `bottomk`, `count`, `count_values`, `stddev`, `stdvar`, the PF-117 case, `T22`, `T24`); with the fix restored, **167/167**. Full `scripts/ci-gate.sh`: **`GATE: PASS`, exit 0, 17/17 stages**, with `test-ratchet` reporting **no drift** on api *and* worker (the `PF-80` check). **Not claimed:** the walk still reasons about grouping modifiers syntactically, so a `by (...)` naming a replica label this repository has never used is judged by `REPLICA_LABELS` alone; and one case — `sum by (queue) (sum by (queue, state) (…))` — is caught by a second, incidental route (`sumAggregationArguments` yields the inner `sum`’s argument separately), so it stays green under the probe. It is recorded as passing in both states rather than presented as a proof. *The land pass’s original text follows.* **A regression in gate power introduced by `S-E02-19`, not a pre-existing hole — which is why it outranks a new epic.** `topk(3, x)`/`bottomk(k, x)` return their input series with **every original label, `instance` included**: they select, they do not aggregate. Both are in `ACROSS_SERIES_AGGREGATIONS` (`:409`) and, having no grouping modifier, hit `groupingRemovesReplicas(null) → true` (`:441`), so `sum by (queue) (topk(3, pilotage_queue_depth))` — which still double-counts every replica, i.e. `PF-107`/`PF-108` — is shielded. Measured: `HEAD` `metricNamesInExpr` → `['pilotage_queue_depth']` → PROBLEM; now `unshieldedGaugeReferences` → `{families: [], unscannable: false}` → **accepted**. Same for `bottomk(1, …)` and `topk by (queue) (3, …)`. Raised independently by all three escalation-panel lenses and then re-driven by the land pass rather than accepted on their word (`R-30`). Fix must be narrow — `quantile`/`count_values`/`group` genuinely collapse. Latent: 0 uses in `pilotage-slo.json`. Priority **P2** | G-DNC · G-TRUTH |
| PF-117 an unterminated quote blanks the expression tail with no signal | V3-E02 *(follow-up)* | **S-E02-19** *(raised by the land pass, closed in the same run)* | `closed` | `apps/api/src/shared/quality/observability-gate.spec.ts` — cases `T23`, `T24` and the `foo{a="b} + sum by (queue) (pilotage_queue_depth)` matrix row, all red before the fix | **Closed at the routine’s Step 5. The fix is four lines; the reason it matters is that this was the fail-OPEN direction.** Every residual this epic had recorded before was fail-closed — a gate too strict, which is annoying and self-announcing. This one made a blocking stage go GREEN on an expression `HEAD` reddened, which is the defect `S-E02-18` spent a whole slice closing, reintroduced by the slice after it. `scanQuotedSpans` is now the single scanner: it returns `{ masked, unterminated }`, and both callers route `unterminated` into their existing PROBLEM path (`unbalanced` in `sumAggregationArguments`, `unscannable` in `unshieldedGaugeReferences`). **It is a separate function rather than a changed return type on `maskQuotedSpans`** — that keeps the string contract `T21g` pins, while the mask and the failure signal come from ONE scan and cannot drift apart, which two scanners would eventually do. Verified in both directions: with the routing removed, `sumAggregationArguments('foo{a="b} + sum by (queue) (pilotage_queue_depth)')` returns `{ args: [], unbalanced: false }` — indistinguishable from a panel containing no `sum` at all — and three cases go red; with it restored, `{ args: [], unbalanced: true }` and **167/167**. A *terminated* quote is still masked and still silent (`pilotage_queue_depth{tenant="a"}` stays accepted), so the fix does not trade one false red for another. Full `scripts/ci-gate.sh`: **`GATE: PASS`, exit 0, 17/17 stages**, with `test-ratchet` reporting **no drift** on api *and* worker (the `PF-80` check). *The land pass’s original text follows.* **A `DNC-08` violation introduced by the AC-7 masking step, in a diff that restates the rule it breaks at `:381`.** `maskQuotedSpans` (`:349`) opens on `"`, `'` or `` ` `` and, if no closing delimiter follows, emits the remainder as spaces and **returns normally** — no `unbalanced`, no `unscannable`. Both callers then see no `sum` token and no metric reference and report nothing. Measured: `sumAggregationArguments('foo{a="b} + sum by (queue) (pilotage_queue_depth)')` → `HEAD` `{args:['pilotage_queue_depth']}` → PROBLEM; now `{args:[]}` → **silent accept**. Wider than PromQL suggests, because a bare `'` and a bare `` ` `` are treated as openers. Fix ≈ 4 lines: return `{ masked, unterminated }` and route it into the existing PROBLEM path. Priority **P2** | G-DNC |
| PF-118 `registryResidue`'s outcome branch cannot match on the throw path | V3-E02 *(follow-up)* | **S-E02-19** *(raised by the land pass, closed in the same run)* | `closed` | covered by the existing `AC-10` restore cases; `pnpm --filter @pilotage/api test -- observability-gate` → **167/167** | **Closed at the routine’s Step 5 by moving two lines, and the ordering is the whole point.** `drivenOutcomes.push({ queue, job })` now runs **before** `observeJobCompleted(…)`, not after. `observeJobCompleted` can throw mid-write; a tuple pushed afterwards would then be missing from `drivenOutcomes` for a sample that may already exist, so `registryResidue` would not look for the residue this drive is **most** likely to have left — on exactly the path the catch-and-still-verify design exists to cover. What the residue check needs is the INTENT to write; whether the write finished is precisely the unknown it is there to resolve. Full `scripts/ci-gate.sh`: **`GATE: PASS`, exit 0, 17/17 stages**, with `test-ratchet` reporting **no drift** on api *and* worker (the `PF-80` check). **Not claimed:** no test drives a throwing `observeJobCompleted`, because forcing that throw means mutating the built worker module the check is reading — the fix is an ordering argument, verified by reading the path, and it is recorded as such rather than dressed up as an executed negative. The two branches that DO fire on the throw path (depth sentinel, bound depth sources) were already covered and remain so. *The land pass’s original text follows.* `drivenOutcomes` is populated only inside the `try`, so a drive that throws on the first queue leaves it empty and the `pilotage_queue_jobs_total` residue branch with nothing to look for — on exactly the path the catch-and-still-verify design exists to cover. **`AC-10` is not defeated**: the depth sentinel and `pilotage_queue_depth_sources_bound` branches still fire there and a leaked registry is still reported ahead of the drive error. What is weaker than stated is the claim *"three things, one per thing the drive writes"* (`DNC-06`). Fix: record each label tuple **before** the write that produces it. Priority **P3** | G-DNC |

| PF-138 the re-armable `adminLogins` branch lost the `portal: 'admin'` scope its own card claims | V3-E04 | **S-E04-4** *(raised by the escalation panel, closed in the same run — run 32)* | `closed` | `apps/api/src/shared/quality/audit-vocabulary-gate.spec.ts` — the source-shape assertion on `where: { tenantId, portal: 'admin', action: { in: loginActions } }`; `audit-provenance-gate.spec.ts` — the `S-E04-1` occurrence case **flipped back** to length 1 with both shape assertions (`contains('where')`, `not.toContain('auditLog')`) restored | **Raised and closed inside one run, and the interesting part is the gate, not the line.** `S-E04-4` correctly stopped issuing the `adminLogins` query — no call site writes a login action, so the card could only ever read 0 (DNC-09) — and reported `null` instead. But the branch it left behind for the day login *is* instrumented had dropped the `portal: 'admin'` filter the deleted query carried, behind a card labelled « CONNEXIONS ADMIN ». Dormant today (`AUDIT_LOGIN_ACTIONS` is empty), and its own comment says adding a code is the whole change — so the first slice to instrument login would have made an RGPD governance card silently over-report teacher, parent and student sessions. **The reason it is closable rather than deferrable** is that `S-E04-1` stated its rule over the literal's *shape* (a `where:` filter, never a write) and wrote down the flip-back protocol in the spec itself; `S-E04-4` inverted the case to length 0 when the filter vanished, and restoring the scope meant executing that protocol rather than inventing one. Both directions are now pinned: the provenance gate allows exactly **one** `portal: 'admin'`, only as a filter, and the vocabulary gate asserts the filter is the one on the login branch. Because the branch is unreachable, the assertion is on the source — stated plainly rather than dressed as behavioural evidence | G-AUDIT, G-AUTHZ |
| PF-143 the frozen legacy alias table was derived from the seed and never checked against live data | V3-E04 | **S-E04-4** *(raised by the gate, closed in the same run — run 32)* | `closed` | `select distinct action, resource_type from audit_log` executed against the running `pilotage_postgres`, filtered against the 5-action / 8-resource-type frozen table — **zero** unaliased values on either axis | **A measurement that had not been taken, taken.** `LEGACY_AUDIT_ACTION_ALIASES` (5) and `LEGACY_AUDIT_RESOURCE_TYPE_ALIASES` (8) were derived from the *pre-fix seed*, while the rewritten KPI predicates now depend on that table being exhaustive: any pre-V3 French value outside it resolves `unknown`, renders « Code non répertorié » instead of « Format hérité », and — for actions — silently leaves the `criticalChanges` count. Run 32 ran the query against the target stack (Step −1: the local Docker stack **is** the target). Live distinct sets: actions `{Création 19, Validation 10, Mise à jour 9, Suppression 9, Export 7}` plus canonical `coefficient.upsert 5`; resource types `{Professeur 13, Évaluation 9, Classe 9, Note 8, Inscription 7, Élève 6, Résultats 1, Année scolaire 1}` plus canonical `subject_coefficient 5`. The set-difference against the frozen table is **empty on both axes** — the table is exhaustive on live data, not merely plausible. Two corroborations fell out: `criticalChanges` moves **9 → 18** (`Suppression` + `Mise à jour`, both declared critical) where the old predicate matched one of its four listed strings; and the old predicate's `'Révision'` and `'revise'` are confirmed to match **nothing in either vocabulary**, which is why dropping them with no alias was correct rather than a gap. **Honest residual, and it is small:** this is evidence about the target environment. A hypothetical deployed tenant carrying a French value outside the 13 would still resolve `unknown` — but under Step −1 there is no such tenant, and the `unknown` path is DNC-08-correct anyway (the value stays visible, verbatim, marked). No operator errand is owed | G-TRUTH |
| PF-135 nothing executed `/admin/audit`, the one route that had already 500'd for every admin | V3-E04 | **S-E04-4** *(raised by the gate, closed in the same run — run 32)* | `closed` | `apps/web/tests/e2e/journeys/admin-audit-vocabulary.spec.ts` — **4/4 passed**, authenticated as `school_admin` against the local stack with a freshly rebuilt `pilotage_api` | **Raised as the reason not to auto-merge, then discharged by executing it.** Every other web claim in `S-E04-4` was pure-function or textual, on the route `PF-14` measured at HTTP 500 for every authenticated admin three commits earlier — and this diff introduced a pattern the page had never used: a React **element** as `SelectOption.label`, built in a server component and handed across the RSC boundary into a `'use client'` filter. Executed: `GET /admin/audit` → **200**, « Journal d'audit » heading visible (the `PF-14` tripwire, re-armed); « Non instrumenté » rendered rather than a fabricated `0` (AC-3 / DNC-09); « Format hérité » rendered on legacy rows, value unchanged (AC-4 / DNC-08); the facet listbox opens and its options render French labels with no raw machine code standing in as a label (AC-2, the RSC-element claim). **Getting there cost three environment repairs and each is recorded rather than absorbed:** `PF-144` (the harness's own selector, fixed here), a rebuild of `pilotage_api` — whose `dist/` contained **0** occurrences of `auditLoginActionCodes` before and **3** after, so `adminLogins` really was being served by pre-`S-E04-4` code — and running the new bundle on `:3000` because `PF-126` blocks the `web` image build while the Keycloak clients only accept that origin. **Two of this spec's assertions were red before they were right, and both times the TEST was wrong, not the page** (`R-30`): the facet is derived from *observed* rows, so a declared-but-unwritten code like `calendar_event` cannot appear; and `subject_coefficient` is absent from this admin's facet because those rows belong to **another tenant** — a red that was G-TENANT working. Neither was resolved by changing product code. **Residual:** three of the four `authenticate *` setups still fail on `otp_required`, so this is executed on **admin only** — `PF-145` | G-TRUTH, G-PORTAL |
| PF-144 an ambiguous selector had killed the entire authenticated e2e layer, silently | V3-E02 *(gate power)* | **S-E04-4** *(found while discharging `PF-135`, fixed in the same run — run 32)* | `closed` | `apps/web/tests/e2e/auth.setup.ts` — `authenticate admin` **passes**; before the fix it died on a strict-mode violation with the two matching elements printed | **The authenticated e2e layer — `ADR-023`, `E10-S1`, four portals — had not run for an unknown number of runs, and nothing said so.** `auth.setup.ts` used `getByLabel('Mot de passe')`; the login form later gained a « Afficher le mot de passe » show/hide button whose `aria-label` also contains that phrase, so the locator began resolving to **two** elements — the password input and the toggle — and Playwright's strict mode failed every `authenticate *` setup. Because the authenticated projects declare `dependencies: ['setup']`, every `@journey` and authenticated `@a11y` spec then failed or skipped with it. **Nothing caught it because `ci-gate.sh` does not run Playwright**, and because the harness's own reachability guard converts an unclear state into a green skip (`PF-146`). Fixed by scoping the query by ROLE — `getByRole('textbox', { name: … })` for both fields — which cannot be re-broken by adding another control with an overlapping accessible name. This is why `apps/web/tests/e2e` had zero audit references and, more importantly, why the absence read as a gap in coverage rather than as a dead gate | G-DNC, G-PORTAL |

## Closed by run 33 — `S-E04-5` (KPI scope, day-inclusive window, tenant timezone)

| Finding | Epic | Story | Status | Test | Evidence | Gate |
|---|---|---|---|---|---|---|
| PF-32 audit end-date drop, 3/4 KPIs wrong | V3-E04 | **S-E04-4** *(canonical vocabulary — **shipped, run 32**)*, **S-E04-5** *(KPI scope + `to` filter — open)* | `closed` — **all four halves; (a) end-date and (b) KPI scope by `S-E04-5` (run 33)** | `apps/api/src/shared/quality/audit-vocabulary-gate.spec.ts` (AST-driven, bidirectional); `apps/worker/.../audit-csv.generator.spec.ts`; `analytics.service.spec.ts` | **Half (d) closed by `S-E04-4` (run 32), and the closing measurement corrected the finding's own numbers.** One declaration now exists — `packages/contracts/src/audit/` — carrying `AUDIT_RESOURCE_TYPES`, `AUDIT_ACTIONS` (each code with its French label and `critical`/`export` flags), `AUDIT_PORTALS` (**four**), and a **frozen** `LEGACY_AUDIT_*_ALIASES` table. All three consumers read it and hold no local copy: the web map (`RESOURCE_TYPE_LABELS`/`PORTAL_LABELS` **deleted**), the API KPI predicates, and the worker CSV generator that every prior reading missed. **The written set was extracted by a TypeScript AST walker rather than transcribed, and it falsified the hand-count in this row:** the run brief's own inventory of 27 actions was short by roughly a third across five literal shapes — the real figure is 42+, and the `asc`/`desc` trap (Prisma `orderBy`, present in both object and array form) is excluded by construction and asserted absent. Completeness is checked in **both** directions as set equality, which is what removes the 7 dead keys and keeps them removed. **`criticalChanges` measured 9 → 18** on live local data: it had been matching only the French `Suppression` out of four listed strings, and now matches the declared critical set plus the declared legacy aliases. **`adminLogins` was the DNC-09 case and is not fixed by a better predicate** — no call site writes any login action, so the card could only ever read 0; it now reports `null` («Non instrumenté») and issues **no query at all**, because `{ in: [] }` is a card that can only read 0. **`PF-143` discharged the same run against the target stack:** `select distinct action, resource_type from audit_log` returned **zero** values outside the frozen 5-action / 8-resource-type alias table, so the table is exhaustive on live data rather than merely derived from the pre-fix seed. **Not closed, and the residual is most of the row:** (a) `lte: new Date(to)` still drops the selected day; (b) all four KPIs still ignore every user filter; the `null`-portal facet gap of (c) is `PF-123`. All three are `S-E04-5`. Ten findings were raised closing this half — `PF-134`…`PF-143`, rows below. *(Original run-28 measurement retained:)* **Reproduces, and is materially bigger than the finding as written — run 28's main result.** (a) End-date confirmed: `analytics.service.ts:3251` does `lte: new Date(to)`, so `to=2026-08-08` becomes `2026-08-08T00:00:00Z` and the whole selected day is dropped (`M-21`). (b) All four KPIs (`:3310-3326`) are computed on a `where` that **ignores every user filter** — `from`, `to`, `action`, `resourceType`, `portal`, `actorId` — so three all-time counters sit beside a filtered table and silently disagree with it (`M-22`); the finding says *three of four are wrong*, and the measured defect is that **all four have the wrong scope**. (c) `adminLogins` measured **0** and is **structurally always 0**: no call site in the API writes a login audit row, so « CONNEXIONS ADMIN — Sessions ouvertes » can never read anything else (`M-20`). (d) **The vocabulary split, newly measured and in no audit:** the 54 live rows carry **French display strings in the structural columns** — `resource_type` ∈ {`Professeur`, `Note`, `Classe`, `Élève`, `Inscription`, `Évaluation`, `Résultats`, `Année scolaire`}, `action` ∈ {`Création`, `Mise à jour`, `Suppression`, `Validation`, `Export`} — while the 28 API sites write **machine codes** (`role`, `role.create`), **20** distinct (`M-11`), and `AuditPageFilters.tsx`'s `RESOURCE_TYPE_LABELS` declares **13** keys of which **7** are written by nobody, leaving **14** written codes unlabelled and an intersection with *what is actually in the database* of **0** (`M-12`…`M-16`). `criticalChanges` straddles both vocabularies — `['delete','Suppression','Révision','revise']` — and measured **9** by matching only the French `Suppression`, while `delete`, `Révision` and `revise` match **nothing in either** (`M-18`); `sensitiveExports` measured **7** (`M-19`). A **third** consumer exists and was missed by every prior reading: `apps/worker/src/modules/exports/generators/audit-csv.generator.ts:16` exports `action` **raw** (`M-31`). This makes the fix `G-TRUTH` **and** `G-PORTAL` work — one vocabulary declared in `packages/contracts`, labels derived from it, and the 54 legacy French rows migrated or rendered honestly as legacy, **never silently** **CLOSED by `S-E04-5` (run 33) — halves (a) and (b), the last two open.** Measured on the live local database, not asserted: with two rows inserted at 23:30 and 00:30 **Europe/Paris** on 2026-08-09, a filter `from = to = 2026-08-09` returns **0** rows under the old predicate (`gte`/`lte: new Date(ymd)` = UTC midnight) and **2** under the new one (`gte` = local midnight, `lt` = next local midnight, exclusive). Note the old code dropped **both** ends: `from` took UTC midnight too, i.e. 02:00 Paris in summer, so the first two hours of the first selected day vanished as well — correcting only `to` would have been the same lie moved two hours. (b) all four KPIs are now computed over the table's own `where` plus exactly one predicate each, combined with `AND: [...]` so the user's own `action` filter is not overwritten; `eventsInRange` is **not a fifth count** but `total` itself, so `kpis.eventsInRange.value === total` holds **by construction** — including under concurrent appends — rather than by assertion. `G-MIGRATION` discharged for the first time in this epic: `apps/api/prisma/migrations/20260809120000_tenant_timezone/` is a hand-reviewed expand-only `ALTER TABLE ... ADD COLUMN ... NOT NULL DEFAULT 'Europe/Paris'` with a non-volatile default (no table rewrite on PG15), a stated rollback and no contract phase; `prisma migrate deploy` applied it to the local stack and the ledger reads `0_baseline` + `20260809120000_tenant_timezone`, both finished and not rolled back; the gate's `schema drift` stage rebuilt both migrations into a scratch database and reported no drift. The resolved zone is read from `Tenant.timezone` by primary key, **echoed** in `filters.timezone`, and a caller-supplied `timezone` is proven to change neither the echo nor the boundary. `adminLogins` and `today` are gone — the card that could only read 0 no longer exists (DNC-09) — replaced by « Acteurs distincts », which counts actors via `groupBy` and excludes the actor-less system row rather than inventing a phantom actor. Tests: `apps/api/src/modules/analytics/audit-kpis.spec.ts` (754 lines) with explicit **negative controls** — restoring `lte: new Date(to)` drops the exact boundary row, restoring `gte: new Date(from)` drops the 00:30 Paris row, and giving a KPI `{ tenantId }` again changes its value on the fixture. DST is handled by incrementing the **civil date** and re-reading the zone offset at that instant, never by adding 86 400 000 ms — asserted on 2026-03-29 (23 h) and 2026-10-25 (25 h) in Paris. Full gate `GATE: PASS`. **Residual is not PF-32's:** the null-portal facet gap of half (c) is `PF-123`, whose read half this slice also closed | G-AUDIT, G-TRUTH |
| PF-134 **P1** — a **fourth** audit vocabulary survives in `AuditTable.tsx`, and it is already wrong on real data | V3-E04 | **S-E04-5** *(carry it)* | `closed` — `S-E04-5` (run 33) | — *(none — the predicate is covered by no test)* | `pickActionTone` / `pickActionIcon` are inline substring matchers (`création\|publish\|approve`, `suppression\|delete`, …) sitting next to the declaration that was supposed to replace them. **Already wrong, not latent:** `coefficient.upsert` and `grade.unflag` are declared `critical: true`, are counted by the « MODIFICATIONS CRITIQUES » card, and contain none of the tokens the predicate looks for — so they paint `neutral`. The card says a critical change happened; the row for it looks unremarkable. The new seed makes it reachable, because STEP 13 now writes canonical codes where it previously wrote French strings that *did* match. This is the single highest-value follow-up: parse `pickActionTone` with the AST walker the gate spec already imports, assert every `AUDIT_CRITICAL_ACTIONS` code reaches a `danger`/`warning` literal, positive-control against `LEGACY_AUDIT_CRITICAL_ALIASES`. The fix that follows is small and in keeping with `ADR-037` — give `AUDIT_ACTIONS` a declared tone and delete the last inline vocabulary **CLOSED by `S-E04-5` (run 33) — and there were TWO copies, not one.** Intake measured a **fifth** vocabulary the finding had not named: `pickActionTone` was declared in `AuditDetailDrawer.tsx:40` as well as `AuditTable.tsx:57`, so fixing only the file this row names would have left the drawer painting `neutral` the very rows the card counts as critical. Both are deleted; both call sites now read `auditActionTone()` from `@pilotage/contracts/audit`, and the tone is a **declared** property of `AUDIT_ACTIONS` (default derivation critical → `danger`, export → `info`, else `neutral`) rather than a substring match. `grep -rn pickActionTone apps/web/src packages/` returns only a comment recording the removal. `coefficient.upsert` and `grade.unflag` — the two this row names as already wrong — now reach a `danger` tone, so the card and the row agree | G-TRUTH, G-DNC |
| PF-137 **P2** — the epic's own `openapi.yaml` and `data-model.md` D-23 now disagree with the shipped code | V3-E04 | **S-E04-5** | `closed` — `S-E04-5` (run 33) | — | `AuditKpis` specifies `adminLogins` **removed** and replaced by `distinctActors` in four `{value, scope}` envelopes; `S-E04-4` ships `adminLogins: number \| null`. Both are defensible — the story mandated the interim shape — but a contract file disagreeing with the code, **inside an epic whose thesis is that no surface may assert what it cannot justify**, is the defect at the doc layer. Same file, same problem for `sensitiveExports`: the contract defines it structurally (`resourceType = 'export_job'`), the code defines it by action, and the two genuinely disagree on the seeded legacy fixture row. Reconcile when `S-E04-5` moves the KPIs to `{value, scope}` **CLOSED by `S-E04-5` (run 33).** `contracts/openapi.yaml` `AuditKpis` and `data-model.md` D-23 now describe the shape actually shipped: four `{value, scope, label}` envelopes keyed `eventsInRange` / `criticalChanges` / `sensitiveExports` / `distinctActors`, with `adminLogins` and `today` removed. The `sensitiveExports` disagreement is resolved **by a written ruling rather than by silently picking one**: the KPI is defined **by action** (`AUDIT_EXPORT_ACTIONS`), not structurally by `resourceType = 'export_job'`, and the contract now says so — an export job row is the *artefact*, the action is the *event*, and the card counts events | G-TRUTH |
| PF-139 **P2** — a 15-character KPI value in a `font-mono text-3xl` slot inside `overflow-hidden`, and an outage that renders as a claim | V3-E04 | **S-E04-5** | `closed` — `S-E04-5` (run 33) | — | `KpiCard` renders `value` at `font-mono text-3xl font-bold` inside `overflow-hidden`, with `min-w-0` on the *label* group only. « Non instrumenté » has a min-content width of ~11 mono chars at 30 px and cannot break mid-word; at the page's own `xl:grid-cols-4` the card's inner width is smaller than that, so the value or the « CONNEXIONS ADMIN » label is **clipped, not ellipsised**, and it worsens at 200 % zoom (SC 1.4.10 / 1.4.4). Every other string value in the codebase is short (`28/35`, `92%`). Second, same card: `page.tsx`'s `safe()` swallows every `ApiError` to `null` and the fallback hard-codes `adminLogins: null` — **the same sentinel** that means "not instrumented" — so an analytics outage renders as an affirmative claim about the audit log. Fix: `value="—"` with the phrase in the wrapping sub-label, and a `kpisUnavailable` flag distinct from the instrumentation gap **CLOSED by `S-E04-5` (run 33).** Both halves. The card no longer clips: the long string moved out of the `font-mono text-3xl` value slot, which now renders `—` for an unreadable measure, with the sentence carried in the wrapping sub-label. And the outage/instrumentation collapse is gone — `apps/web/src/app/admin/audit/audit-kpi-state.ts` derives **three** states that are never conflated: `measured` (a number, including a truthful `0`, visually indistinguishable from any other measured value), `not-instrumented` (« Non instrumenté »), and `unavailable` (« — » + « Indisponible », *« la mesure n'a pas pu être lue. Le chiffre affiché serait une invention. »*). This mattered more after this slice than before it: with the `adminLogins` card removed, the row's only honest witness to an outage would have disappeared and a failure would have rendered as **four** confident zeros. The derivation is a pure exported module with no `'use client'` precisely so it is testable without a render engine — `apps/web` still has no component unit runner (`PF-135`), and a rule verifiable only on screen would not be verified at all (DNC-08) | G-TRUTH |
| PF-161 **P3** *(CI ratchet)* | V3-E04 | `S-E04-6` | **`closed` 2026-08-09 (run 34)** — predicted by the reviewer lens, then **fixed in the land pass before the gate reached the stage**, so the ratchet never went red | `node scripts/lint-ratchet.js` — the stage that would have fired; the full gate's `lint:warnings (ratchet)` line is the evidence, and the import block now reads `school-structure/{academic-years,school-context,subjects}` → `schools/schools.controller` → `students/student-access.service` | `apps/api/src/shared/audit/provenance-callsites.spec.ts:23` — the new `SchoolsController` import is out of alphabetical order. `eslint-plugin-import` 2.32's `alphabetize` compares path segments: at index 3, `'schools'` vs `'school-structure'` compares `s` (115) against `-` (45), so `../../modules/school-structure/...` must sort **before** `../../modules/schools/...`. That is a 10th ESLint warning in `apps/api` against `scripts/lint-warning-baseline.json`'s pinned ceiling of **9** (all nine currently `@typescript-eslint/no-unused-vars`), and `scripts/lint-ratchet.js` enforces the ceiling in **both** directions — so it exits 1. Every other new or moved import in the diff was checked and is correctly ordered; `consistent-type-imports` is off for `apps/api` via `@pilotage/eslint-config/decorator-metadata`, so no other new warning is introduced. Fix: move the line below the three `school-structure/*` imports and above `students/student-access.service` | G-DNC |
| PF-162 **P2** — the seam made « in the same transaction » a type, and left « the code is declared » a convention | V3-E04 | **`S-E04-7`** | **`CLOSED` by `S-E04-7`** — `AuditWriteInput.action`/`.resourceType` are now `AuditActionCode`/`AuditResourceTypeCode`. **The blocking prerequisite the finding did not know about, measured and fixed in the same edit:** both vocabulary tables were declared `: readonly Entry[] = [ … ] as const`, and **the annotation wins** — `(typeof X)[number]['code']` resolved to `string`, so the obvious fix would have shipped green and inert, a named type forbidding nothing. Both are now `as const satisfies readonly Entry[]`. Proven in BOTH directions by `write-audit.spec.ts`'s `@ts-expect-error` controls (undeclared action, undeclared resource type, and a LEGACY French alias — `ADR-037` D4 enforced) plus a positive control: a widening back to `string` makes the directives unused and turns typecheck RED. `ADR-035` **D14**. Recorded as raised: | — *(the AST gate covers the written set; nothing covers the parameter type)* | `apps/api/src/shared/audit/write-audit.ts:99,101` — `AuditWriteInput.action` and `.resourceType` are typed **`string`**, not the declared unions, although `apps/api` already depends on `@pilotage/contracts` (`package.json:50`) and `audit-vocabulary-gate.spec.ts` imports `AUDIT_ACTIONS` directly. So `ADR-035` `D1` (in-transaction) is a **compile-time** invariant while `D6` (declared vocabulary) rests on a Jest AST walker plus an unenforceable convention — `D6.2` makes « call `writeAudit` with an inline object literal » part of the contract with nothing but a comment behind it. A future author who passes a pre-built variable gets a green typecheck and a shrunken written set, whose documented remedy is then to *delete the label*. The amplifier is `D2`: with fail-closed shipped, a typo'd code or a non-uuid `resourceId` no longer logs badly, it **takes down role assignment**. Fix: type them `AuditActionCode` / `AuditResourceTypeCode` from `@pilotage/contracts/audit` — it costs nothing here and converts `D6` from convention to invariant, matching `D1`. Deliberately not taken in `S-E04-6`: the panel named it a judgement call, and the diff was already at the reviewable ceiling | G-AUDIT, G-DNC |
| PF-122 a fourth provenance decision site inside the walk root, invisible to the gate that claims there is one | V3-E04 | **S-E04-7** *(owner)* | **`CLOSED` by `S-E04-7`** — the `actor: 'parent' | 'admin'` parameter is DELETED. `child-claims.service.ts#audit` now takes an `AuditProvenance` derived by `deriveAuditProvenance(jwt, extractAuditClientHints(req))` at BOTH controllers, above the transaction (S-E06-6's ordering rule — `AuditLog.ipAddress` is `@db.Inet`). The site was NOT converted as-is: routing `actorRole: 'admin'` — a value no Keycloak realm issues — through the canonical seam would have laundered a known-wrong provenance into the one place the repo points at as authoritative. Asserted by `child-claims.service.spec.ts` (`school_admin`, and explicitly **not** `admin`) and, in the negative, by a G-AUTHZ assertion that the three `guardianships.approve` decorators are byte-identical: **who may approve or reject is unchanged** (ADR-015). Original record: | — *(none — and the new gate cannot reach it: all four matchers pass over the shape by construction)* | `apps/api/src/modules/child-claims/child-claims.service.ts:722-729` decides both fields itself: `actor: 'parent' \| 'admin' = 'parent'` → `actorRole: actor`, `portal: actor`, driven with the literal `'admin'` at `:522`/`:609` (`guardianship.claim_approved` / `claim_rejected`) from `admin-child-claims.controller.ts` handlers that **already carry `@CurrentJwt() jwt`** — the same Pattern-B seam `S-E04-1` installed twice, not the JWT-less design work that justifies deferring `PF-121`. A `super_admin` approving a claim is audited **`actorRole: 'admin'`, which is not a Keycloak realm role** and which `ROLE_PRECEDENCE`/`ROLE_PORTAL` can never produce, so `/admin/audit`'s role facet carries a fifth orphan token and `S-E04-4` will read two vocabularies out of one column. **Root cause is a mis-measurement, not code:** `spec.md:22` / `M-6` counted this file among *"the 20 that already take `actorRole` from arguments"* — from a **call-site literal**, which is not the JWT. Raised by the escalation panel checking `AC-1` against the walk root instead of against the diff; re-verified by reading the file (`R-30`) | G-AUDIT |
| PF-155 **P2** | V3-E04 | **`S-E04-10`** *(re-pointed at land — `S-E04-7` is in flight as PR #208, OPEN and awaiting a human ruling on PF-163, and carried none of these four)* | **`closed` 2026-08-09 (run 36)** | `schools.controller.spec.ts` exercises `update` only with a name change, so **neither** status branch is covered | **`CLOSED` by `S-E04-10`** — `status` was removed from `UpdateSchoolDto` (the class is now `export`ed so the refusal is provable at the pipe itself). With `main.ts`'s `whitelist: true, forbidNonWhitelisted: true`, `PATCH /schools/:id { status: … }` is a **400**, not a stripped field — the distinction is the whole safety of the fix, because a silent strip would have turned "close the school" into a 200 that closes nothing. `DELETE /schools/:id` is unchanged and its students/academic-years refusal was deliberately **not** duplicated into `PATCH`: a second guarded door is still a second door. **Re-measured, not assumed:** `grep -rn "api/v1/schools" apps/web/src` returns five call sites and none is a `PATCH` of status, so no shipped client breaks. **Accepted capability loss, registered as `D-11` in `open-decisions.md`:** the same removal deletes the only **reopen** path; it had no action code, no endpoint, no UI and no test, and no `school.reopen` was invented here. `schools.controller.ts:78` / `:203` — `UpdateSchoolDto` exposes `status?: SchoolStatus` and `SchoolStatus` is exactly `active \| closed`, so `PATCH /schools/:id { status: 'closed' }` closes a school through `update()` — **bypassing `DELETE`'s students / academic-years refusal** — and is filed as `school.update`. Re-opening has no code at all. Both codes are `critical`, so the « Modifications critiques » count is unaffected; the **attribution** is wrong, and an auditor filtering on « Fermeture d'un établissement » gets an empty result for closures made this way. The bypass is pre-existing; the *fidelity* gap is created by `S-E04-6`, because that is the slice declaring `school.close` to be THE closure code. Fix: choose the action from the observed transition inside the same transaction, or drop `status` from the DTO. The missing students/academic-years guard on the PATCH path is the separate half | G-AUDIT, G-TRUTH |
| PF-157 **P3** | V3-E04 | **`S-E04-10`** *(re-pointed at land — `S-E04-7` is in flight as PR #208, OPEN and awaiting a human ruling on PF-163, and carried none of these four)* | **`closed` 2026-08-09 (run 36)** | — | **`CLOSED` by `S-E04-10`** — correctness moved **inside** the transaction: `tx.userRole.updateMany({ where: { id, revokedAt: null, userProfile: { tenantId } } })`, so PostgreSQL re-evaluates the predicate under the row lock and the loser reports `count = 0`, returns the winner's state and writes **no** row. `updateMany` returns a count, so a `findUniqueOrThrow` re-read is mandatory (it deliberately carries no `include` — the shipped response shape does not drift). The `count === 0` branch is an early **`return` above** `writeAudit`, never an `if` wrapping it (`ADR-035` D1 / the `audit-write-check.js` ratchet). The pre-transaction guard is **kept and relabelled a fast path** that decides nothing. **No DB backstop added, and that is `AC-13`:** the partial unique index `(user_profile_id, role_id) WHERE revoked_at IS NULL` is a schema change, deferred, and it guards a *different* race (two concurrent `assignRole` calls). **Two residuals carried, both named in `PROGRESS.md` § `S-E04-10`:** the `userProfile: { tenantId }` relation filter is the only one in an `apps/api` `updateMany` and the unit tests mock `updateMany`, so a two-step Prisma plan would leave them green — settle with `DEBUG="prisma:query"`; and `count === 0` does not verify its own claim (`if (after.revokedAt === null) throw new ConflictException(...)` would make the one silent-open path loud). `users.service.ts:137` (and `:89` for the sibling in `assignRole`) — the `revokedAt !== null` idempotency guard `S-E04-6` introduced is a **TOCTOU**: the read happens before `$transaction` opens, so two concurrent `DELETE /users/roles/:id` calls both pass it, both update, and both write a `critical` `role.revoke` for one revocation — with `revokedAt` holding the retry's clock, which is the exact defect the guard was added to close. No database backstop: `@@unique([userProfileId, roleId, schoolId])` cannot deduplicate because `schoolId` is written `null` and PostgreSQL treats NULLs as distinct in a unique index. In-scope fix: `tx.userRole.updateMany({ where: { id, revokedAt: null }, data: { revokedAt: now } })` inside the transaction, branch on `count === 0`, re-read for the response. The partial unique index `(user_profile_id, role_id) WHERE revoked_at IS NULL` is a schema change and is deferred | G-AUDIT |
| PF-158 **P3** | V3-E04 | **`S-E04-10`** *(re-pointed at land — `S-E04-7` is in flight as PR #208, OPEN and awaiting a human ruling on PF-163, and carried none of these four)* | **`closed` 2026-08-09 (run 36)** | the two no-op cases belong beside the existing AC-14 role tests | **`CLOSED` by `S-E04-10` for the sequential case; the concurrent case is closed for `role.revoke` only** — three pre-transaction no-op guards were added, so a form resubmit or client retry writes no `critical` row: `isSchoolUpdateNoOp(body, school)` (field-by-field, **not** body-emptiness — the case is a resubmitted *populated* form), `body.status === enrollment.status && !(isEnding && !endedAt)` and `status === 'dropped' && endedAt !== null`. Ordering is load-bearing and asserted: **tenant guard → no-op check → `$transaction`**, so a foreign-tenant no-op is still 404 and no cross-tenant read oracle is created. Each early return reproduces the shipped response shape (`parseAddress` normalisation at `schools`; the same `classSection` `include` added to `enrollments.update`'s pre-read). **Stated residual:** the three new guards read *before* their transaction and the write inside is unconditional, so two genuinely overlapping requests still both commit — today's behaviour, not a regression, forced by `AC-8`'s shape rule. **Second residual:** `DELETE /schools/:id` still has no no-op guard, so a second `DELETE` on an already-`closed` empty school writes a second `school.close` row with identical `before`/`after` status. Both carried in `PROGRESS.md` § `S-E04-10`. `schools.controller.ts:203`, `enrollments.controller.ts:288` — the no-op rule is applied **inconsistently across four families in one diff**. `assignRole`/`revokeRole` skip the row when nothing changed; `school.update` (whose DTO is entirely optional, so `PATCH {}` validates and runs `tx.school.update({data:{}})`), `enrollment.status_change` and `enrollment.cancel` on an already-`dropped` enrollment each write a `critical` row with byte-identical `before`/`after`. A double-clicked control or a client retry therefore inflates « Modifications critiques » with non-events — the KPI `S-E04-4` and `S-E04-5` spent two slices making honest — and the auditor reads a governance event that did not happen | G-TRUTH, G-AUDIT |
| PF-159 **P3** | V3-E04 | **`S-E04-10`** *(re-pointed at land — `S-E04-7` is in flight as PR #208, OPEN and awaiting a human ruling on PF-163, and carried none of these four)* | **`closed` 2026-08-09 (run 36)** | — | **`CLOSED` by `S-E04-10`** — `address` is recorded in `school.update`'s `before` **and** `after`, through a new `auditAddress()` helper rather than raw Prisma JSON, so the drawer renders a real diff on the one field for which the row is the sole surviving copy of the previous value. **The raw-value fallback is the load-bearing part:** `parseAddress` returns `null` for an *absent* address **and** for an *invalid* one, so applying it naively to `before` would have made the trail assert an address was **created** where one already existed — a falsification manufactured by the fix. `status` is **kept** on both sides as declared context (it is identical by construction since `PF-155`), with the reason written into the code rather than left to be guessed. `schools.controller.ts:208` — `school.update`'s `before`/`after` record `name`, `timezone`, `locale` and `status`, and **omit `address`** — the fifth and only structurally interesting field `update()` can change, including erasing it via `Prisma.DbNull`. An address-only PATCH therefore writes a `critical` row whose rendered diff is identical in every recorded field: `AuditDetailDrawer.tsx` shows a critical modification with an empty diff, on the one field where the audit row is the sole surviving record of the previous value. `parseAddress` is already in the file at `:58` | G-AUDIT |

## Closed by run 37 — `S-E04-9` (the invite path stops trading an account for an audit row)

| Finding | Epic | Story | Status | Test | Evidence | Gate |
|---|---|---|---|---|---|---|
| PF-163 **P1** — the invite handler is now fail-closed on its local half and fail-open on its remote half, and nothing compensates | V3-E04 | **`S-E04-9`** *(run 37 — the human ruling was not needed: the two resolutions were the finding's own, and the routine took the one that does not trade a guarantee away)* | `closed` — resolution **(a) COMPENSATE**; **(b) declined** | `apps/api/src/modules/identity/invite.controller.spec.ts` — **NEW, and it is the artefact the finding said was missing**. Its fake `$transaction` **stages then commits**: writes land in `staged`, are promoted to `committed` only when the callback resolves, and are discarded on throw — so *"nothing persisted"* is **observed**, not asserted. Cases (ii)/(iii) prove the rollback deletes the identity, (iv) proves a failed compensation names the orphan, (vi) proves a profile-**less** seeded realm account is **not** adoptable, (vii) proves the refusal issues **no** local lookup and therefore leaks no existence bit | **CLOSED by compensation, not by baselining — and the declined option is the point.** (b) (drop `invite` out of the sweep, baseline it `best-effort-post-commit`, move to 9 + 18 = 27) was mechanical and was **refused**: it narrows the trigger from *profile or role or audit fails* to *profile or role fails*, leaving the escalation reachable through any profile-create failure — pre-existing, not introduced by `S-E04-7` — while handing back the fail-closed guarantee `S-E04-7` had just bought at this exact site. **`invite` stays in the sweep; the arithmetic stays 10 + 17 = 27; `audit-write-baseline.json` and `audit-write-gate.spec.ts` are untouched.** The fix: `KeycloakAdminService.deleteUser` (one method; **404 is a success**, because a compensation that manufactures a phantom orphan is worse than one that is idempotent), invoked from a `.catch()` on rollback, **non-masking** — the original error is re-thrown unchanged. When the delete itself fails, `Logger.error` records it and the admin gets a French sentence **naming the orphaned `kcUserId`** (`actions.ts` renders `body.message` raw, so the id must be *in the string*). **`.catch()` + a private `persistInvitedProfile` is forced, not taste:** `audit-write-check.js:265-274` (`insideTryBlock`) walks **every** AST ancestor of a `writeAudit` call, so a `try` anywhere above it turns rule B red — the routine verified this by reading the walker, not by trusting the comment. `writeAudit` is byte-for-byte unchanged. **`ADR-035` gains D15–D17** and rules a three-way `D15` numbering collision (`S-E04-9` takes D15–D17, the carried doc correction D18, `S-E04-8` reserves D19+). **AC-4 (the repair path) was WITHDRAWN mid-slice and its premise falsified — verified independently by the routine:** profile creation is **lazy** (`ensureUser`), so a profile-less realm identity is normal by design, not an orphan; `realm-export.json` ships three of them with **0** matching `seed-demo.ts` rows; and `ADR-004` puts every tenant in one realm — so adoption would have let any `users.write` holder in any tenant overwrite a password, grant a realm role and **permanently** bind that identity to their tenant (`authProviderId` is globally `@unique`, `schema.prisma:838`). The finding's row offers its two halves as *and/or*, so it closes on the stronger one. **Residual, stated not hidden: `PF-167`** — the SMTP-failure branch still orphans, deliberately. Original finding text follows. `apps/api/src/modules/identity/invite.controller.ts:138` — `S-E04-7` wraps steps 5–7 (profile + optional custom role + audit) in one `$transaction`, correctly keeping every Keycloak call outside it. But steps 3–4 **already** created the Keycloak user and sent the activation email, and neither is reversible. `ADR-035` D2 makes an audit-insert failure fatal, so a rollback now leaves an **enabled Keycloak identity holding `school_admin`/`teacher` with no `UserProfile`**. Step 1 (`:87-92`) then refuses every retry with « Un utilisateur existe déjà avec l'email … Il peut se connecter directement » — advice that *completes* the failure instead of fixing it: on first login `UserSyncService.ensureUser` (`shared/auth/user-sync.service.ts:38-56`) finds no profile and self-provisions one under `DEMO_TENANT_SLUG`, `status: active`, with permissions from `REALM_ROLE_PERMISSIONS` rather than the profile. **The invitee lands in the wrong tenant with admin powers** — the `ADR-002` invariant — and `customRoleSlug` is silently lost. Before `S-E04-7` the same fault cost one audit row; after it, it costs the account. Trigger is a DB fault, not attacker-controlled, so this is availability-triggered, not exploitable on demand. `invite` is also **not** one of the story's declared Group-A/B sites (Group B requires *no external side effect*), so the trade was never stated. **Two acceptable resolutions, both cheap:** (a) compensate — delete or disable the freshly created `kcUserId` when the transaction aborts, and/or let step 1 repair a Keycloak account that has no local profile; or (b) drop `invite` back out of the sweep and baseline it under `best-effort-post-commit`, moving the arithmetic to 9 + 18 = 27 in `scripts/audit-write-baseline.json` `$doc` and `audit-write-gate.spec.ts:577`/`:584`/`:612-621`. Either way the missing `invite.controller.spec.ts` (fake `$transaction` that **stages then commits**, so a callback throw is *observably* not persisted) is what would have caught it | G-AUDIT, G-TENANT |

## Closed by run 38 — `S-E04-11` (the regulator-facing audit export: frozen columns, neutralised cells, a named answer for a bad timezone)

| Finding | Epic | Story | Status | Test | Evidence | Gate |
|---|---|---|---|---|---|---|
| PF-140 **P1** — the regulator-facing CSV changed bytes with no AC asking, and the escaping is one column from injectable | V3-E04 | **`S-E04-11`** *(run 38 — batched with `PF-149`)* | `closed` — all three halves | `apps/worker/.../audit-csv.generator.spec.ts` — extended, not replaced: the **full header is pinned as an exact string** *and* the frozen 0..9 prefix is pinned **separately**, so *"you moved a column"* and *"you added a column"* are two different failures. Injection cases prove `=cmd\|' /C calc'!A0` is emitted inert **and** that « Évaluation » and every other non-triggering value is byte-for-byte unchanged | **CLOSED, and the shape of the fix is the point: a slice that fixed a silent column move by performing another one would have fixed nothing.** (i) Indices **0..9 are frozen**; `action_vocabulary` and `resource_type_vocabulary` are **appended** at 10..11. (ii) The collapsed `vocabulary` column is **KEPT deliberately** — it is `weakerVocabulary(action, resource_type)` and therefore fully **derivable** from the two appended per-axis columns (`unknown` > `legacy` > `canonical`), so it is redundant rather than a third axis and the three can never disagree; **removing it would have moved `resource_id`/`ip_address` a second time, i.e. committed `PF-140` (i) inside the fix for `PF-140` (i).** Its retirement is a follow-up needing a versioned, announced format change. (iii) `csvEscape` force-quotes **and** prefixes a single apostrophe (the OWASP form) when a cell's first character is `=`, `+`, `-`, `@`, tab or CR, and `\r` joins the quoting predicate. The transform is **additive and reversible** (strip exactly one leading `'`) — nothing is ever dropped from a regulator's file — and **uniform across every column rather than a per-column allowlist**, which is precisely what would drift the day `audit_log.user_agent` joins the export. Apostrophe, not the other standard mitigation (a leading tab), because a tab is *itself* a trigger, so one pass suffices and the neutralisation never recurses. **The BOM is untouched** — it is deliberate and documented, and it is *why* (iii) matters rather than a defect. **The false comment is gone:** `:94-95` claimed the `*_label` columns were *"appended beside them, never interleaved"*, which the header contradicted; the replacement states the rule that is actually true and now enforced, and preserves the one thing the old comment got right (label adjacency, asserted at `audit-vocabulary-gate.spec.ts:1215`). **`G-DNC`/DNC-06 checked, not assumed:** `quickstart.md:212` is a `grep` command and `openapi.yaml:596` says only that `action` is exported **raw** — still true at index 3 — so neither doc enumerates columns and appending two falsifies neither. **Residual, recorded: `PF-168` (P1)** — the *same* injection defect is **live** in `apps/web/src/lib/csv.ts`, which this slice does not touch. Original finding text follows. Three things about the DPO export. (i) `action_label`, `resource_type_label` and `vocabulary` are inserted **mid-header**, not appended, and a UTF-8 **BOM** is now prepended — any index-based or `created_at`-keyed downstream parser breaks, silently. (ii) The single `vocabulary` column is `weakerVocabulary(action, resourceType)`, collapsing two axes, so a regulator cannot tell **which** value was unclassified — while the file already carries per-axis label columns. (iii) **Latent, not live:** the BOM promotes the file from "text" to "the document French Excel opens as a spreadsheet", and `csvEscape` only quotes on `[",\n]` — it neutralises no leading `=`/`+`/`-`/`@`/tab and no bare `\r`. No exported column is reachable free text **today**, but `audit_log.user_agent` is a raw client header one column away, and `S-E04-3` just started populating it. Fix: append `action_vocabulary` / `resource_type_vocabulary` at the end, and neutralise leading characters **before** any slice widens the export | G-TRUTH |
| PF-168 **P1** — the CSV-injection defect `S-E04-11` closed as *latent* in the worker was **live** in `apps/web`, on two portals, over operator-entered free text | V3-E04 → **V3-E05** | **`S-E05-1`** *(2026-08-10)* | `closed` | `apps/worker/src/modules/exports/generators/audit-csv.generator.spec.ts` — T-1 (the pre-existing regression block) passes **unchanged**, which is the proof the predicate was relocated and not altered; T-2 drives the shared `neutraliseCsvCell` over every trigger plus reversibility and the no-recursion invariant; T-3 pins the **web** dialect composition including `Martin; Dupont` and the `+33` phone delta. `apps/api/src/shared/quality/csv-escape-gate.spec.ts` drives the ratchet in **both** directions — each of rules A–E shown red independently, the DNC-08 ladder run against a scratch tree, and a real fourth copy written into a `.tsx` file turning the real script red then green again | **CLOSED. One predicate, one address, and a gate rather than a review holding it there.** `neutraliseCsvCell` / `CSV_INJECTION_TRIGGERS` / `CSV_NEUTRALISER` now exist exactly once, in `packages/contracts/src/security/csv-injection.ts` (pure, import-free, no class and no `instanceof` — so a spec resolving `src/` and a runtime resolving the CJS `dist/` cannot disagree). `apps/web/src/lib/csv.ts` and the worker generator both consume it and keep their own **dialect**; the private copy in `AlertsExportButton.tsx` and the two `escapeCell` copies on the parent portal are **deleted**. Decision recorded as **`ADR-037` D8**. **Two corrections to this finding’s own text, on the record (`R-30`), because a later run reads and believes it:** (1) the count was wrong in the direction that mattered — it says the real number of `csvEscape` copies is **2**; measured at `8e52da0` it was **3**, and **5** counting the two `escapeCell` copies, which the finding’s own grep for the *name* `csvEscape` could not see. That blind spot is exactly why the ratchet’s rule A matches on **shape** (a body that tests a `"`-bearing character class **and** performs the RFC-4180 quote-doubling) as well as on name, and why its walk root includes **`.tsx`** — three of the five copies lived in components, so a `.ts`-only walk would have certified the very surface the finding is about (`S-E06-5` at a third address). (2) The finding calls copy (b), `AlertsExportButton.tsx`, **« already correct »**; it was correct about the *neutralisation* and **not** about the *force-quoting* — it prefixed the apostrophe and then tested the ORIGINAL quote regex, so `=1+1` was emitted `'=1+1` **unquoted**. It now emits `"'=1+1"`. That is an intended behaviour delta, stated rather than absorbed. **A second intended delta, named so it is found here and not in a support ticket:** a guardian phone `+33 6 12 34 56 78` now exports as `"'+33 6 12 34 56 78"` and reads as text in Excel — `ADR-037` D7’s stated trade, *uniform across every column, never a per-column allowlist*, because an allowlist is what drifts the day a new free-text column joins the export. Numeric cells are **not** affected: `csvFixed1` values are passed to `csvRow` without going through `csvEscape`. **The ratchet:** `scripts/csv-escape-check.js`, wired as `ci-gate.sh` stage 0d-bis **and** as its own step in `.github/workflows/ci.yml`’s lint job (ci.yml re-lists stages individually; it does not call `ci-gate.sh`), **outside every `--quick` guard**. No baseline file, no `--update`, no environment variable — the ceiling is two escapers and a flag that raises it would be the off switch. **What is NOT claimed, stated plainly:** no browser downloaded a file, no spreadsheet was opened, and **no web unit test exists** — `apps/web` has Playwright only (`PF-129`/`PF-133`). The web half is evidenced by the gate’s executed rule C (an import **and** a call, read off the real file), rule A (the deletion **measured**, not asserted), T-3, and `pnpm typecheck`. **Residual, deliberately left open: `PF-169`** — the `;`+CRLF vs `,`+LF dialect contradiction, which needs a versioned, announced format change and is named in code and in `ADR-037` D8 (ii). Original finding text follows. `apps/web/src/lib/csv.ts:17` is a second, independent `csvEscape` whose predicate is `/[",;\n\r]/` — it quotes and neutralises nothing — and the module prepends `CSV_BOM`, so the artefact opens in Excel as a spreadsheet. Four live export buttons use it: `admin/guardians` (exports `lastName`, `firstName`, `email`, `phone`, **`profession`** — arbitrary operator text), `admin/enrollments`, `teacher/reports`, `teacher/students`. Where the audit CSV had no reachable free-text column, this one does, now | G-TRUTH, G-PORTAL, G-DNC |
| PF-149 **P1** *(latent — see reachability)* | V3-E04 | **`S-E04-11`** *(run 38 — batched with `PF-140` on the audit-export seam)* | `closed` — handled on **both** call sites, with **no** fallback introduced anywhere | `apps/api/src/modules/analytics/audit-kpis.spec.ts` (API: the 503 + `TENANT_TIMEZONE_UNUSABLE` discriminator) · `apps/worker/src/modules/exports/exports.processor.spec.ts` **NEW** + `audit-csv.generator.spec.ts` (worker: terminal job failure naming the zone) — both extended, neither replaced | **CLOSED — and the fix is wider than the finding asked, for a reason the routine verified rather than accepted.** The finding named `assertKnownTimezone` as the throw site; the sprint found that guarding **only** that call would protect the case that *cannot fire today* and miss the one that *can*. The `DEFAULT_AUDIT_TIMEZONE` branch never reaches that assert, and both `zonedYmd` (`window.ts:222`) and `resolveAuditWindow` (`:323`) assert **again** downstream — so under a small-ICU runtime it is the **default** zone that fails, for **every tenant at once**. The `try` therefore spans the whole window resolution on both sides. API (`analytics.service.ts`): `ServiceUnavailableException` (**503**, not the story's specified 500 — a deliberate, argued divergence: the service is unavailable *until configuration is fixed*) carrying the stable code `TENANT_TIMEZONE_UNUSABLE`. Worker (`audit-csv.generator.ts`): `UnrecoverableError` from `bullmq`, so a **configuration** fault is not retried three times into three identical failures — **routine-verified end to end**: `bullmq@5.28.1` exports it, it is an `Error` subclass, its `.name` is `'UnrecoverableError'`, and `queue-metrics.ts`'s `isUnrecoverable` matches on **name** (not class identity), grading it `failed_terminal` on attempt 1. The generator deliberately does **not** log — `GenerateArgs` carries no logger and `exports.processor.ts:112-124` already logs and persists `errorMessage` — so the thrown French sentence is **UI copy**, read verbatim in `/admin/exports`, and it names the offending zone first. `isUnknownTimezoneError` checks `instanceof` **OR** `name`, because `packages/contracts` ships CJS with a **git-ignored** `dist/` that `apps/api` and `apps/worker` each resolve separately — two class objects would make a bare `instanceof` dead code while a self-constructing test still passed. **No fallback to the server zone exists on any path**, and ABSENT (no row / empty column) stays distinct from UNUSABLE. **Residual, recorded not hidden: `PF-171`** — `School.timezone` (`schema.prisma:280`) is a second, independent column with the same exposure, and **neither** is validated on write, so the finding's "validate on write" half is only partly discharged. Original finding text follows. `assertKnownTimezone` throws `UnknownTimezoneError` when `Intl` accepts a zone but resolves it to `UTC` (the small-ICU trap). Failing closed is the right design — a silent fallback to the server's zone is the exact defect `S-E04-5` removes — but **the error is caught nowhere**: `grep -rn UnknownTimezoneError apps/api/src apps/worker/src` returns only a *comment* at `analytics.service.ts:3426`. So an unusable `Tenant.timezone` would surface as an unhandled **500 on `GET /api/v1/analytics/audit`**, and would kill every `audit_csv` job at `audit-csv.generator.ts:68`. **Reachability, measured by the routine rather than inherited from the panel — this is why it is latent, not live:** `tenant.timezone` is `TEXT NOT NULL DEFAULT 'Europe/Paris'`, both live tenants read `Europe/Paris`, **no writer for the column exists in this diff or anywhere in `apps/api/src`**, and full ICU is present on both sides (`process.versions.icu` = **76.1** inside `pilotage_api`; host resolves `Europe/Paris` correctly). It therefore cannot fire today. It becomes live the moment anything writes the column — an admin settings screen, an import, a seed — because **nothing validates the value on write**. Fix has two halves and both are small: validate on write (`isKnownTimezone` already exists and is exported), and map the error to a deliberate HTTP response instead of an unhandled throw. Do **not** "fix" it by reintroducing a fallback | G-TRUTH, G-DNC |
| PF-156 **P1** vertical privilege escalation — a `roles.assign` holder could self-grant any role's whole permission set | V3-E05 | **`S-E05-2`** | `closed` | `apps/api/src/shared/auth/privilege-ceiling.spec.ts` + `modules/identity/{roles.controller,users.service,invite.controller}.spec.ts` — **79/79 green**, executed by the routine on the landed tree | **Closed by one pure, total, fail-closed predicate, and the fail-before half was produced by mutation rather than asserted.** `exceedsGrantor(grantorSet, requested)` (`shared/auth/privilege-ceiling.ts`) returns the codes the grantor does not hold; `assertWithinCeiling` throws the French 403 `{message, required, missing}` in `permissions.guard.ts`'s own shape. Wired into **four** grant paths — `roles.controller.create()`, `roles.controller.update()` (only when `permissionCodes` is present; a rename stays exempt, commented as deliberate), `users.service.assignRole()` and `invite.controller`'s custom-role grant — every one **before** its `$transaction` opens, so a refusal writes no `Role`, no `UserRole` and **no audit row**, matching the posture the two pre-existing cross-tenant guards already had. **Fail-before evidence, measured by the routine, not by the sprint:** replacing the predicate body with `return []` turns **20 of 79 tests red across all four suites**; reverted, tree verified byte-identical to `HEAD`. **Fail-closed is structural:** an absent or empty grantor set denies *everything* (live, not theoretical — `user-sync.service.ts:67` resolves an unrecognised realm role to `[]`), an unknown code is not held therefore it exceeds, and a non-array `requested` denies as one opaque entry. `super_admin` is unaffected **structurally rather than by exemption** — its realm role already carries the whole catalogue and the string `super_admin` appears nowhere in the module; an `if (roles.includes(…)) return []` would be a bypass wearing a role name. **DNC-10:** no env read, no `SKIP_*`/`ALLOW_*`, no dev escape; both exports have arity **2** and a test asserts it, so an options bag cannot appear later without turning red. **The blanket `isSystem` refusal this finding's own text proposed was deliberately NOT implemented**, and `ADR-015`'s `S-E05-2` amendment records the corrected reasoning: the ceiling already refuses every seeded `isSystem` role for a non-`super_admin` grantor, so the ban is pure redundancy — the routine's own briefing argued it was "redundant where right and breaking where wrong", and that argument is measurably wrong. **One routine claim was falsified by the implementer and the correction is kept:** the brief asserted a `school_admin` could POST the *seeded* `super_admin` role id; `grep -rn super_admin apps/api/prisma/` returns **zero** matches — `seed.ts` seeds only `school_admin`, `teacher`, `parent`, `student`. The real chain is worse and does reproduce: mint a custom role carrying all **89** codes, self-assign it, re-authenticate — `effectivePermissions` unions custom-role permissions with no ceiling. Both halves of that chain are now refused | G-AUTHZ |
| PF-166 **P2** — the one **public** account-creation path wrote **no audit row at all**, and created the account in four non-atomic steps with no compensation | V3-E04 | **`S-E05-11`** *(2026-08-12)* | `closed` | `apps/api/src/modules/identity/register.controller.spec.ts` — (a) the happy path writes exactly ONE audit row with `action: 'user.register'`, the tenant resolved inside the transaction, `actorId === resourceId ===` the created profile id, and the provenance pair actually extracted from the request; (b) a `userProfile.create` failure rolls back **and** calls `keycloak.deleteUser` exactly once with the id THIS request created; (c) a `writeAudit` failure does the same, which is what makes fail-closed real here rather than asserted — no account survives an unaudited registration; (d) when the compensating delete itself throws, the thrown message **contains the orphan `kcUserId`** and the original failure is preserved as `cause` | **CLOSED.** `POST /auth/register-parent` now performs its two LOCAL writes (`tenant.upsert`, `userProfile.create`) plus one `writeAudit` inside a single `this.prisma.$transaction`, extracted into `persistRegisteredParent` — a separate method because rule B of `scripts/audit-write-check.js` reddens on a `try` anywhere among a `writeAudit` call's AST ancestors, function boundaries included. Both Keycloak calls stay OUTSIDE and before it (an HTTP round-trip inside an interactive transaction is how a 5 s timeout becomes an incident), and a `.catch()` at the call site deletes the identity this request created when the transaction rolls back — the `S-E04-9` compensation at its last address. One vocabulary entry added, `{ code: 'user.register', label: 'Auto-inscription d’un parent' }`, deliberately **not** `critical: true` so public signup traffic cannot drown the counter that exists to surface `role.grant` / `role.revoke`. Provenance goes through the canonical `deriveAuditProvenance` with a synthesised payload whose every field states what this request **created** (`sub` = `kcUserId`, the literal `sub` every future token for this account carries; `realm_access.roles` = the one realm role granted at step 4) rather than guessing at a caller — routed through the mapper because `audit-provenance-gate.spec.ts` G-3 forbids any second file under `apps/api/src` deciding an actor role from realm roles. `after` is an explicit allow-list, never a spread: `RegisterParentDto` carries a plaintext `password`, and this row is rendered on `/admin/audit` **and** exported by `audit-csv.generator.ts`. **Two deltas a caller can observe, stated rather than absorbed:** (1) a `P2002` on `userProfile.create` now returns the **same 409** as the pre-existing-identity branch instead of a raw-Prisma 500 — required, because the compensation means the identity no longer survives to make the retry 409 on its own, and it removes a driver leak that was also a user-enumeration oracle on an endpoint `PF-46` flags as unthrottled; (2) fail-closed on a **public funnel** means `audit_log` unavailable ⇒ no parent can register, each attempt spending a `createUser` + `setUserPassword` + `deleteUser` round-trip — taken deliberately (an unaudited account is worse than a deferred signup) and recorded in the `S-E05-11` amendment to `ADR-035`. The `demo` tenant upsert keeps its shape and its owner (`PF-165`), with its new cost written down: it is now a contended unconditional write holding a row lock on one tenant row for the transaction's duration | G-AUDIT, G-TENANT, G-DNC |
| TOOL-06 Seven `run_stage` calls omit their timeout, so every code-change gate fails on a diff it never examined | V3-E02 | `ci/2026-08-12-v3-a-gate-unbounded-stages` | `closed` | `apps/api/src/shared/quality/csv-escape-gate.spec.ts` — *every `run_stage` call in ci-gate.sh declares a numeric timeout (TOOL-06)* | **Reproduced before the change, on a clean worktree**: `bash scripts/ci-gate.sh` printed seven `timeout: invalid time interval ‘<stage name>’` banners and seven `✗`, then `GATE: PASS (nothing to gate)` — the `exit 0` at the "nothing to gate" branch fires *before* the summary, which is why docs-only runs never showed it and code runs always failed. The banner read `▶ node`, not the stage name, because with the timeout omitted `$limit` held the name and `$2` held the command — that is why the output was read for 39 runs and understood by nobody. **The seven calls were a pre-`#214` stage list left in place above the tiers the rewrite introduced**, so they ran unconditionally before the dispatcher. Excised. Six of the seven had working counterparts in a tier and lost no coverage; **`csv escapers` had no other call site anywhere**, so `#215`'s blocking ratchet had never executed once — it is now wired into TIER 1, which every PR runs. `run_stage` now **refuses a non-numeric timeout and exits 64** instead of filing a 125 as an ordinary stage failure: measured, `run_stage "runtime engines" node -e …` prints the usage error and the following line is never reached, while `run_stage 30 "probe stage" …` still passes. Post-change, each of the eleven check scripts is referenced **exactly once** in `ci-gate.sh` (was: seven duplicated) | G-DNC |
| TOOL-07 Three gate meta-tests were red on `main`, and two more could not fail, because `#214` moved the anchors they read | V3-E02 | `ci/2026-08-12-v3-a-gate-unbounded-stages` | `closed` | `boot-gate.spec.ts`, `web-artifact-gate.spec.ts`, `link-integrity-gate.spec.ts`, `audit-write-gate.spec.ts`, `csv-escape-gate.spec.ts` | Discovered while closing `TOOL-06` and **measured on `origin/main` before any edit**, by evaluating each anchor against the shipped file. `#214` gave every stage a timeout argument and replaced the `${QUICK}` flag with `$MODE`; five specs still read the old shapes. **Red, not vacuous** — each asserts `expect(buildAt).toBeGreaterThan(-1)` on `indexOf('run_stage "build"')`, which has returned `-1` since `#214`: `boot-gate.spec.ts:164`, `web-artifact-gate.spec.ts:132`, `link-integrity-gate.spec.ts:1611` (which also does `lastIndexOf('if [ "${QUICK}" -eq 0 ]')`, likewise `-1`). **Vacuous, not red** — `audit-write-gate.spec.ts:937` and `csv-escape-gate.spec.ts:735` walk brace depth looking for a line containing `QUICK` to prove their stage is not inside a skip guard; finding no such line they concluded "not inside a guard" and passed *wherever the stage was wired, including nowhere* — `PF-105`'s family. **Why nobody saw three red tests:** `ci-gate.sh` runs the api ratchet with `--skip src/shared/quality/` unless the diff matches `^(scripts/|\.github/|infra/|apps/api/src/shared/quality/)`, and `#220`, `#221` and `#222` touched none of those. All five anchors re-pointed at the shapes the file actually has (`/run_stage\s+\d+\s+"build"/`, `if [ "$MODE" = full ]`), and both depth scanners now assert the guard string exists before trusting their own result, so they cannot go vacuous the same way twice | G-DNC |
| TOOL-08 `prisma generate` ran BEFORE `schema drift` in the live tier — the AC-6 ordering held only in dead code | V3-E02 | `ci/2026-08-12-v3-a-gate-unbounded-stages` | `closed` | `apps/api/src/shared/quality/schema-drift-gate.spec.ts` — *ci-gate.sh runs it BEFORE `prisma generate`, in the tier every PR runs (AC-6)* | Discovered while closing `TOOL-06`. `S-E02-11` AC-6 requires the migration ledger to be refused **before** a client is generated, because `prisma generate` will happily generate a client for a schema no migration produces and lint, typecheck, build and boot then validate against that fiction while `migrate-entrypoint.sh` runs `migrate deploy` only — `db push`'s failure mode through the front door. In the tiers `#214` introduced, the two stages sat the other way round. It went unseen because the meta-test anchored on the literal `run_stage "prisma generate"`, which matches **only** the dead pre-`#214` line, where the order did hold: the test was reading a stage that exited 125 without running. Live tier reordered (drift, then generate); the assertion re-anchored on `/run_stage\s+\d+\s+"prisma generate"/` and given a companion that fails if either script is ever wired at two call sites again | G-MIGRATION, G-DNC |
| TOOL-10 **P1** — `test:api (ratchet)` cannot finish on a machine without a reachable Postgres, and the gate reports it as "TIMED OUT", which invites raising a bound that would not fix it | V3-E02 | `ci/2026-08-12-v3-a-tool10-drift-preflight` | `closed` | `apps/api/src/shared/quality/schema-drift-gate.spec.ts` — *"the CLI against a refused address concludes fast, names the ladder, and keeps its verdict"* (AC-2/3/4/5), *"an INDETERMINATE probe descends the real ladder — only a refusal may stop it"* (AC-P16), *"every `docker` invocation is bounded, and the extractor proves it found them"* (AC-6); `apps/api/src/shared/quality/test-ratchet.spec.ts` — the killed path is distinguished from a startup failure | **Measured by the routine, both directions, not only by the sprint agents.** Premise re-verified on this tree before any code was written: `node scripts/schema-drift-check.js` was killed at **>4 minutes** still on its first `SELECT 1` (last line printed `▶ prisma CLI : …`), i.e. worse than run 43's 89 745 ms; `timeout 150 docker port pilotage_postgres 5432/tcp` neither returned nor was killed, and `ps -W` listed **nine** orphaned `/c/Program Files/Docker/…/docker` processes dated **Aug 10** — bash `timeout` does not kill the docker CLI on this platform, so route C's cost was *unbounded*, not 90 s. Fix is the preflight the finding called for, never a raised bound (`ci-gate.sh` is byte-identical; `2bd1a25` already tried that once). After: **825 ms**, `rc=1`, `SCHEMA DRIFT CHECK: FAIL — tooling_unavailable`, output still naming `prisma db execute`, `psql` and `pilotage_postgres` (DNC-08 intact, AC-4 preserved). Second belt measured end to end: an `indeterminate` probe (`ENOTFOUND`, 248 ms) correctly **descends** the ladder and concludes in **5841 ms**, printing `docker did not answer within 5000 ms and was killed` — the `spawnSync` bound is real where bash `timeout` was not. The `!pre.open` mutant the review panel built, which leaves all 123 other cases green while making the gate permanently red on any machine whose probe cannot answer in 2 s, is now caught: **0** occurrences of the short-circuit narration on correct code vs **2** under the mutant, with the verdict identical in both — which is precisely why nothing else could see it | G-DNC |
| TOOL-14 **P1** — `#223`'s excision took the schema-drift stage's anti-drift note and its ADR filename out of `ci-gate.sh` with the dead block, turning two meta-tests red **on `main`**, unseen | V3-E02 | `ci/2026-08-12-v3-a-tool10-drift-preflight` | `closed` | `apps/api/src/shared/quality/schema-drift-gate.spec.ts` — *"both files carry the anti-drift note the sibling stages carry"* (AC-5, `:701`) and *"the new stage names its decision record, and that record exists"* (AC-15, `:722`) | **Found by the gate, on the first run whose full api ratchet has ever finished.** `TOOL-06` closed by excising the dead pre-`#214` stage block; that block also carried `S-E02-11` AC-5's `Kept in step with .github/workflows/ci.yml — the two must not drift (S-E02-2 AC-4)` and AC-15's `docs/adr/ADR-027-schema-drift-gate-needs-a-database.md`. Both went with it. `#223` could not see the two reds it created because **its own `test:api (ratchet)` timed out at 2400 s on `TOOL-10`** before reporting — the same stage list, the same run. This run's gate finished in **347 s** and named them immediately: `test-ratchet[api]: 2414/2433 passed · 2 NEW test failure(s) — not in the baseline`. **Proven pre-existing rather than assumed:** both test bodies and *every file they read* (`ci-gate.sh`, `ci.yml`, both ADRs) are byte-identical to `origin/main` under `git diff origin/main --stat`, so the inputs that produce the failure are `main`'s. Repair is **comments only** — `git diff origin/main -- scripts/ci-gate.sh` has **0** non-comment changed lines; no stage, no bound, no command moved, and `2bd1a25`'s raise-the-timeout mistake is not repeated. One trap worth recording: AC-5 requires the note **within 1400 characters of the invocation** and matches the literal `must not drift (S-E02-2 AC-4)`, so restoring it as a *wrapped* comment kept it red twice — once for distance, once because the line break split the substring. A note that reads correctly to a human and matches nothing is exactly the `TOOL-07` failure mode; measured with `node -e` on the shipped file until `at - noteAt` was 49, not 3057 | G-DNC |
| TOOL-06 `ci-gate.sh` — 7 stages pass a stage NAME where `timeout` expects a duration | — | — | `closed-by-other-work` | run 41 gate logs, before and after the rebase | **Observed red, then fixed by #223 mid-run — and run 41 re-verified rather than inheriting the excuse.** The first gate, against `db2473b`, printed `GATE: FAIL (7 stage(s))`, all seven `timeout: invalid time interval '<stage name>'`, emitted before the gate banner and therefore not diff-dependent. `545879f` (#223, track a) landed the fix while `S-E05-7` was in flight. That branch was **rebased on `origin/main` and re-gated**, producing **`GATE: PASS (fast)`** — the first genuinely reachable pass, with the `csv escapers` stage (one of the seven that had never once executed) green. Closed by #223's work; recorded here because run 41 is where it was independently confirmed fixed on a real diff |
| TOOL-15 **P1** — `csv-escape-gate.spec.ts` AC-7 writes a probe file into the REAL working tree and then asserts a repo-wide scan is green, so the merge gate is non-deterministic under parallel jest | V3-E02 | **`TOOL-15`** — `docs/spec/features/v3-e02/stories/TOOL-15.md` *(the design call was taken rather than deferred a fourth time: option (a), a scratch-tree copy — `ADR-039`, `open-decisions.md` `D-13`)* | **`closed`** — 2026-08-13 (run 47, `ci/2026-08-13-v3-e02-tool15-hermetic`), citing `ADR-039`. **The parked objection was answered by not needing a flag:** every check script computes `REPO_ROOT = resolve(__dirname, '..')`, so a copy under `<scratch>/scripts/` spawned with `cwd: scratch` roots itself in the scratch tree with **no interface change** — the `argv` whitelists and the DNC-10 assertions are untouched. Both relocated cases assert a **green control first**, and that ordering is the whole difficulty: `csv-escape-check.js` has six preflights and a **one-way** rule D (a `SANCTIONED`/`EXCLUDED` row matching nothing is itself red), `audit-write-check.js` a vacuity floor of **12** `writeAudit` sites — so a naive scratch tree goes red for a *preflight* reason and `expect(status).toBe(1)` would pass while proving nothing about the rule. Then RED naming the rule **and** the file, then green again. **Executed, not only typechecked:** `csv-escape-gate` + `audit-write-gate` **133/133**, `link-integrity-gate` + `walk-read-gate` **203/203**, the new ratchet **18/18**, and `git status` clean after every run | `apps/api/src/shared/quality/csv-escape-gate.spec.ts:492-522` — *"a deliberately-added fourth copy, in a .tsx file"* (AC-7) | **Two gate runs on this branch produced two DIFFERENT failure sets, which is the finding.** Run 1: `2414/2433 passed`, the 2 new failures being `TOOL-14`'s AC-5/AC-15, and AC-7 **green**. Run 2, after `TOOL-14` was repaired: `2415/2433 passed`, AC-5/AC-15 gone as expected, and **AC-7 newly red** — a case neither run's diff touched (`git diff origin/main --stat` on `csv-escape-gate.spec.ts` and `scripts/csv-escape-check.js` is **empty**). Standalone it **passes**: `jest csv-escape-gate.spec.ts -t "deliberately-added fourth copy"` → `1 passed`. The mechanism is visible in the test body: it `writeFileSync`s `apps/web/src/lib/__csv_escape_probe.tsx` into the shared checkout, spawns `csv-escape-check.js` over `REPO_ROOT` expecting RED, removes it, and spawns again expecting **exit 0 for the whole repository**. Under the ratchet's parallel workers that second assertion is a claim about a tree other workers are concurrently mutating, so it is a race in both directions — and the dangerous direction is not the false red seen here but the false green, where the probe is deleted by the `afterEach` of a retried run before the RED spawn reads it. **Why it is not repaired here:** the hermetic fix is to scan a copied tree rather than `REPO_ROOT`, and `csv-escape-check.js` deliberately exposes no root parameter — its own header records that *"a flag that lets a caller choose what is compared is a bypass flag wearing a different hat"* (`link-integrity-gate.spec.ts`'s lesson). Choosing between a scratch-tree copy, serialising this one spec, and weakening AC-7 to a rule-scoped assertion is a contract decision. **Consequence to state plainly: until it is settled, `GATE: PASS` on a gate-machinery diff is not reproducible, and a red on this case is not evidence about the diff that met it** | G-DNC |
| TOOL-18 **P1** — the probe-file race also lives in the CHECK SCRIPTS, which `TOOL-17` deliberately did not repair, so the api suite still fails non-reproducibly | V3-E02 | **`TOOL-15`** — `docs/spec/features/v3-e02/stories/TOOL-15.md` *(one defect at two addresses; this half is §4/AC-5)* | **`closed`** — 2026-08-13 (run 47, `ci/2026-08-13-v3-e02-tool15-hermetic`), citing `ADR-039` D4. **Fixed by removing the race AND by making the script legible — never by tolerating it, which the row below explicitly forbids.** `scanApp` now converts an unreadable walked file into a **structural failure** that reaches `main()`'s existing FAIL block: `DNC-08 — <repo-relative path> is unreadable: <errno>`, `LINK INTEGRITY CHECK: FAIL` **printed**, exit non-zero, the application **not** classified from a truncated corpus, and the partial pass **never memoised** into `SCAN_CACHE`. The collector is a parameter only `main()` passes, so `extractLiteralLinks`/`extractTemplateLinks` — the exports the guard spec drives in process — **still throw the original error object, unwrapped**: no tolerance exists on any in-process path. **Shown able to fail, and the ordering was the trap:** `main()` reaches `readInventory` *before* it scans, so a scratch tree with no `.next` satisfies "non-zero + a verdict line was printed" without executing the new code at all — the red-proof therefore builds a tree the real script rates **PASS**, asserts that PASS (`2 routes · 1 literal targets · 1 alive`), and only then makes one walked file unreadable, asserting the failure **names the path and the errno**. Unreadability is driven by a `--require` shim that throws `ENOENT` for one chosen absolute path inside the scratch tree (`chmod` is a no-op on Windows; withholding the file does not work because an absent file is simply not walked) | `apps/api/src/shared/quality/link-integrity-gate.spec.ts:1878-1908` — *"the CLI verdict is the classifier verdict — no gap between them"*; `apps/api/src/shared/quality/csv-escape-gate.spec.ts:492-522` AC-7 | **`TOOL-17` closed the five SPEC-side walkers and this row is the half it left standing — measured, not predicted.** Run 46 ran `scripts/ci-gate.sh` (no flags) twice on the same commit with a clean tree. **Run 1: `GATE: FAIL (1 stage)`** — `test-ratchet[api]: 2514/2532 passed · 13 failing · 11 known-failing`, the 2 NEW failures being `link-integrity-gate.spec.ts::the CLI verdict is the classifier verdict` and `csv-escape-gate.spec.ts::AC-7`. **Run 2: `GATE: PASS (fast)`** — `test-ratchet[api]: 2516/2532 passed · 11 failing · 11 known-failing`, **no drift**. **The denominator is 2532 in BOTH runs**, so nothing stopped running and exactly two tests flipped: this is the race, not a corpus change, and `TOOL-13`'s skipped-count ratchet would have said so had its baseline been populated (it prints `INACTIVE` — the operator `--update` from a complete run is still outstanding). Both specs pass **228/228 standalone**, the same signature `audit-vocabulary-gate.spec.ts` showed at 73/73 in run 45. **The mechanism, read from the code rather than inferred:** `scripts/link-integrity-check.js:1060-1061` is `for (const file of walkSources(root)) { const source = readFileSync(file, 'utf8'); … }` — a bare read over `apps/web/src` (its source set, `:110`) with **no `try`** anywhere on the path, and `apps/web/src/lib/__csv_escape_probe.tsx` is planted and deleted inside that very root by `csv-escape-gate.spec.ts:493`. So the CLI dies with an uncaught `ENOENT` and prints a stack trace instead of its verdict line. `link-integrity-gate.spec.ts:1899-1900` then `spawnSync`s that CLI and asserts `cli.status === (evaluation.problems.length > 0 ? 1 : 0)`, where `evaluation` came from an **in-process** `classifyAll` that read the tree **at a different instant** — so the two halves of the assertion sample the tree twice and a concurrent worker mutates it in between. `:1903`'s `expect(output).toMatch(/LINK INTEGRITY CHECK: (PASS\|FAIL)/)` fails for the same reason. **Note the asymmetry with the other two scripts:** `scripts/audit-write-check.js` and `scripts/csv-escape-check.js` at least convert an unreadable walked file into `fail(['DNC-08 — <path> is unreadable: …'])`; `link-integrity-check.js` does not even do that — it crashes. **Why `TOOL-17`'s helper is deliberately NOT the fix here, and this must not be "fixed" by reaching for it:** `scripts/lib/walk-read.js`'s docblock argues the divergence explicitly — a check script **is** the verdict, it runs once and alone, and an input it cannot read is a verdict it cannot pronounce, so tolerating the vanish there would be `DNC-08` proper (an unclassifiable state reported as a PASS). A spec is different because its verdict is carried by floors it asserts afterwards. Routing the check scripts through the tolerant seam would therefore make the merge gate quieter and weaker in one move — the exact trade `TOOL-17` was fenced off from taking. **The correct repair is hermetic writers (`TOOL-15`): stop planting probes in the shared checkout at all.** That is an open design call, and this row is the second independent piece of evidence that it has to be taken. **Consequence, restated because it is the operational one:** `GATE: PASS` on a gate-machinery diff is still not reproducible, `AUTO-LAND`'s `green` still cannot be discharged from a single gate run for this class of diff, and the standing habit — run the gate **twice**, read the `✗` stage names, and treat two disagreeing results as evidence the red is not yours — remains load-bearing. Tracked with `TOOL-15` (hermetic writers) and `TOOL-16(b)` (the end-to-end non-determinism this explains a second cause of) | G-DNC |

## Closed by run 50 — `TOOL-23` / `TOOL-24` (the drift gate reaches the PostgreSQL that IS running)

| Finding | Epic | Story | Status | Test | Evidence | Gate |
|---|---|---|---|---|---|---|
| TOOL-23 **P1** — the PostgreSQL client is resolved only through the inherited `PATH`, so a host where PostgreSQL is installed reports route B `ENOENT` and the gate declares itself toolless | V3-E02 | **`TOOL-23-24`** — `docs/spec/features/v3-e02/stories/TOOL-23-24.md` | `closed` — 2026-08-13 (run 50, `ci/2026-08-13-v3-e02-tool23-psql-discovery`) | `apps/api/src/shared/quality/postgres-client-path-gate.spec.ts` (new) + `schema-drift-gate.spec.ts` (extended) — **PASS/PASS, 146 passed · 5 skipped · 151 total**, measured by the routine | **Executed end-to-end, not asserted.** `scripts/lib/postgres-client-path.js` resolves `psql`/`pg_dump`/`pg_restore` through one deterministic filesystem ladder — inherited `PATH` first (so nothing that worked before changes), then well-known install roots highest-major-first, then unavailable exactly as before — and **both** `schema-drift-check.js` and `restore-drill.js` consume it, so the sameness `TOOL-22` made structural for the ADDRESS is now structural for the CLIENT. **The measurement that closes it:** `node scripts/schema-drift-check.js` went from `FAIL — tooling_unavailable` to **`SCHEMA DRIFT CHECK: PASS — ok: 2 migration(s) built 55 tables and the datamodel adds nothing`**, exit **0** (read directly, not through a pipe — `R-23`), in 25.7 s, printing `server reachable at 127.0.0.1:5432`, `scratch built : 55 base tables, 2 ledger row(s)`, `migrate diff … exit 0 (in-sync)` and `scratch database … dropped`. **This is the first end-to-end execution of the schema-drift gate in this programme** — the ledger is now proven to reproduce `schema.prisma` rather than asserted to. Preconditions re-measured by the routine before any edit: `127.0.0.1:5432` accepts in **3 ms**; `C:\Program Files\PostgreSQL\15\bin\psql.exe` **exists** and its directory **is** in the persisted Windows user PATH, but no running process inherited it; and that binary answered `pilotage\|PostgreSQL 15.3` at exit 0. **No new env var and no new CLI flag** (`DNC-10`) — discovery is deterministic from the filesystem, so no caller can substitute the client a gate reaches its verdict through. `PGPASSWORD` still travels in the child environment, never argv (`ADR-025` D6). The three comments that recorded *"there is no host `psql` here"* as a **fact about this host** (`schema-drift-check.js:296`, `:1014`, `restore-drill.js:1207`) were false and are corrected — the same shape as `TOOL-22`: a property that mattered held by a comment and by nothing else | G-MIGRATION *(vacuously — no schema change)*, G-DNC |
| TOOL-24 **P1** — the check asserted `serverReachable = false` from a CLIENT failure and printed *"no PostgreSQL server answered"* while a server was answering in 3 ms | V3-E02 | **`TOOL-23-24`** *(same seam)* | `closed` — 2026-08-13 (run 50) | `schema-drift-gate.spec.ts` — the verdict cases; **both directions executed by the routine against the real script** | `check()`'s reachability catch set `state.serverReachable = false` **unconditionally** and only then inspected `error.routeFailure` — which means *"no CLIENT could run the query"* and carries no information about the server. The script already owned the instrument that separates them (the three-state `TCP_PROBE_SOURCE` preflight), and we knew it had not answered `refused`, because `query()` only descends to route B when it has not. **So the run held the evidence and printed the opposite conclusion.** Now the failure text names which instrument measured what: a route failure is reported as a statement about the CLIENT, and `unreachable_server` fires only when the preflight itself measured `refused`. **`AC-5` — the direction that must stay red — was executed, not argued:** with `DATABASE_URL` pointed at the genuinely-dead `127.0.0.1:5433`, the run still printed `✗ no PostgreSQL server answered at the resolved address. Start the local stack …` **byte-identically** and exited **1**. This removes a CONFLATION; it adds no tolerance (`DNC-08`). **Cost of the defect, stated plainly:** this sentence is why `S-E01-2b` (RLS), `VAL-03` and `TOOL-13`'s drift half were parked for **eight consecutive runs**, and why each of those runs was aimed at a wedged Docker engine (`TOOL-19`) instead of at a client binary that was installed the whole time | G-DNC |
| TOOL-25 **P1** — the drift gate's OWN spec pinned a dead port, so its five end-to-end cases skipped on a correctly-configured checkout and the suite still reported green | V3-E02 *(gate power)* | **`TOOL-25`** — `docs/spec/features/v3-e02/stories/TOOL-25.md` (implemented run 51) | **`closed`** — 2026-08-13 (run 51, `ci/2026-08-13-v3-e02-tool25`) | `apps/api/src/shared/quality/schema-drift-gate.spec.ts` — **135 passed / 135 total, 0 skipped** (baseline `5 skipped · 119 passed · 124 total`); anti-recurrence ratchet in `apps/api/src/shared/quality/default-database-url-gate.spec.ts` | **Closed by execution, and the closing measurement was taken by the ROUTINE — the sprint explicitly declined it** (agents do not run jest, GUARDRAILS §4) and wrote *"AC-5, the slice's own closing measurement, is UNTAKEN"* into `bmad/roadmap.md`, recommending a hold. The routine took it from the **main checkout** and it passed, so that hold reason is discharged by measurement rather than by assertion. **Before → after, same command (`npx jest src/shared/quality/schema-drift-gate.spec.ts` from `apps/api`):** `5 skipped, 119 passed, 124 total` in 27.1 s → **`135 passed, 135 total`, 0 skipped** in 143.5 s. All five formerly-skipped cases RAN and PASSED, by name: *"the unmodified repository PASSES — the gate is not red on correct code (AC-2)"* (57.1 s), *"leaves no scratch database behind (AC-11)"* (1.1 s), *"the ledger really builds the schema the datamodel describes (AC-2)"* (4.3 s), *"a datamodel the ledger does not build FAILS, naming the drifted object (AC-1)"* (3.5 s), *"a migration that does not execute on PostgreSQL FAILS (AC-3)"* (13.3 s). **This is the first execution of that block anywhere in this programme** — `G-MIGRATION` is now discharged by a real `CREATE DATABASE` / `prisma migrate deploy` / `migrate diff` / `DROP DATABASE` journey against the live PostgreSQL, not asserted. **AC-11 was verified INDEPENDENTLY of the spec that asserts it**, because a self-asserted cleanup claim is exactly the shape this finding is about: `select datname from pg_database where datname like 'schema_drift%'` returned **`(none)`** after the run. ⚠️ **CORRECTION — the finding's recorded CAUSE was wrong, and the routine propagated it.** This row previously read *"one gate's rule forces another gate's spec onto a dead port … two gates are individually correct and jointly produce a false green"*, and run 50's `NEXT.md` said the same. **There was only ever ONE gate; the second was a comment.** Rule **A6 has never been able to read a `*.spec.ts` file**: `scripts/production-artefact-check.js:105` defines `EXCLUDED_FILE = /\.(spec|test)\.(ts|tsx|js|jsx|mjs|cjs)$/` and `:319` applies it inside `walk()`, *before any rule runs*. Measured independently by the routine: the scanner's own banner reports **597** files across the three scan roots, the same walk without the spec exclusion yields **701** — **104 spec files A6 structurally cannot see**, including the one whose comment blamed it. The address was simply diverging in silence, and a comment asserting a constraint that did not exist is what carried the divergence through `TOOL-22`, `TOOL-23` **and** `TOOL-24`. **The routine wrote that false premise into the sprint's own brief verbatim**; the sprint measured it instead of obeying it and was right — `feedback-verify-the-brief-you-wrote`, third payout. **Consequence for the repair, and it is not cosmetic:** "A6 by construction" could never have been the anti-recurrence ratchet, so it is **not** what shipped. The ratchet is a **consumer enumeration** in `default-database-url-gate.spec.ts` — three entries (`scripts/schema-drift-check.js`, `scripts/restore-drill.js`, `schema-drift-gate.spec.ts`), each call-site-anchored on **comment-blanked source** so a mere header mention cannot satisfy it, with a non-vacuity case pinning the list at length 3. **The fix itself:** the literal is deleted; `RESOLVED_BASE_URL = defaultDatabaseUrl()` is loaded from `scripts/lib/default-database-url.js` by computed require (the `ratchet-core.js` precedent), deliberately **unguarded** so a vanished resolver goes red at LOAD rather than falling back to a literal of its own. AC-3 honoured `TOOL-21`'s lesson: the two `expect(url.port).toBe('5433')` assertions were **replaced, not deleted**, by the invariance they actually defended — `buildScratchUrl()` / `deriveMaintenanceUrl()` preserve the base's host **and** port. Fixtures whose meaning is value-shaped moved to fixed synthetic DSNs (`OTHER_SERVER_URL`, `REDACTION_SAMPLE_URL`) so no assertion's meaning depends on this host's `.env`. **A guard was TIGHTENED in the same slice, because enabling the block made it necessary:** `describeWithDb` now requires the target to be **reachable AND loopback**, classified on the TCP preflight *before* the credentialed probe, with six deterministic injected cases including a positive control and a TEST-NET-1 non-vacuity probe. Previously the destructive block was bounded by **name only** (`schema_drift_%`). **Gate, run TWICE on the code tree, byte-identical:** `GATE: PASS (fast)` both times (verdict line read, never `$?` of a pipeline — `R-23`), `test-ratchet[api] 2635/2646 · 11 failing · 11 known-failing` and `test-ratchet[worker] 293/300 · 7 · 7` in both. **No excess failure**, which independently confirms `TOOL-21` is genuinely repaired on `main` (#240) — the condition that forced runs 46–48 to ship unmerged. api denominator **2585 → 2646**. `node scripts/production-artefact-check.js` → `✓ no development artefact in production-facing source`, with **no A6 exclusion added and `production-artefact-check.js` not edited** (AC-2). Build verified by **artefact** (`apps/api/dist/main.js` mtime), not by exit code. **Not closed by this row: the CLASS.** The skipped-count ratchet `TOOL-13` built for exactly this failure mode still prints `⚠ this baseline records no skipped counts. The skip ratchet is INACTIVE` for **both** apps, so it could not have caught `TOOL-25` and cannot catch the next one. `TOOL-25` was found by a human reading jest's tail. Arming remains an operator step — `node scripts/test-ratchet.js api --update` and `… worker --update` from a COMPLETE run. Five residuals raised as **`TOOL-26`**. | G-MIGRATION, G-DNC
| PF-217 `lesson_entry.classSessionId` is written from the request body with no ownership check | V3-E01 | **S-E01-1e** — closed 2026-08-15 (run 61); `ADR-049` applied to a second module | `closed` | `apps/api/src/modules/lessons/lessons-scope-ownership.spec.ts` — **36/36 pass** (`npx jest src/modules/lessons`, exit 0), the `AC-6 / PF-217` block driving the foreign id, the legitimate own-tenant id and the NULL id | **`PF-204`'s shape, found in the module this slice converts, and RLS does not close it — the reason is structural.** PostgreSQL evaluates referential integrity **outside** row security, so `tenant_isolation`'s `WITH CHECK` on `lesson_entry` sees only `lesson_entry.tenant_id`: a **foreign** `classSessionId` inserts cleanly under a perfectly correct policy. The column is mono-column, nullable **and `@unique`**, which makes it worse than the calendar case in a way worth naming — a foreign id is not only a cross-tenant *reference* but a one-shot **squat**, because once tenant A holds B's `class_session` id, B can never link its own lesson entry to its own session. Repaired by the `ADR-049 §D1` pattern: a `findFirst({ where: { id, tenantId } })` **inside the scope, before the write**, with the probe ordered ahead of the create so the `@unique` collision cannot become a `P2002` existence oracle. Both negative controls hold: a legitimate own-tenant id and an absent/NULL id still succeed — an optional FK was not turned into a required one | G-TENANT |
| PF-199 identity and context resolution CANNOT run inside the scope it resolves | V3-E01 | **S-E01-1e** — settled 2026-08-15 (run 61); `ADR-051 §D2–§D3` | `closed` | `scripts/tenant-adversarial-check.js` — **executed, exit 0**, against real PostgreSQL: *"the bootstrap allow-list is TWO-LAYERED and STATEMENT-RATCHETED — 5 identity file(s) declaring 21 statement(s), each with its own reason, compared for SET EQUALITY in both directions"* | **Settled as a NAMED ALLOW-LIST, never as a widened grant** — a grant that makes these statements work also makes every unconverted call site work silently, which was the whole failure mode. The enumeration gains a mandatory `kind`: **`surface`** (a whole-tree property, true of every statement by construction — boot globs, `apps/worker/src/**`) and **`bootstrap`** (identity/context resolution, where every statement is named with its **own** reason). The `bootstrap` kind is ratcheted by **set equality in both directions**, using the same matcher that produces the coverage arithmetic: an unlisted statement fails, a **dead** entry fails, a reasonless entry fails. Keys are `(glob, model.verb)`, never `file:line` — line keys drift on reflow and then get "repaired" by an `--update` mode, which is a knob wearing a different hat. **No ratio floor** (`DNC-10`). The `PF-199` set was recorded as 3 files / 11 statements; it is **4 files**, because `teacher-profile.service.ts` resolves `teacherProfileId` and **writes while doing so** — measured at **7** sites, not the 6 predicted. That entry *increases* the enumerated term, which is arithmetically indistinguishable from a manufactured green, so it is justified on the measurement and its before/after is reported explicitly | G-TENANT, G-DNC |
