## PF-166
status: closed
layer: L0
row: | PF-166 **P2** — the one **public** account-creation path wrote **no audit row at all**, and created the account in four non-atomic steps with no compensation | V3-E04 | **`S-E05-11`** *(2026-08-12)* | `closed` | `apps/api/src/modules/identity/register.controller.spec.ts` — (a) the happy path writes exactly ONE audit row with `action: 'user.register'`, the tenant resolved inside the transaction, `actorId === resourceId ===` the created profile id, and the provenance pair actually extracted from the request; (b) a `userProfile.create` failure rolls back **and** calls `keycloak.deleteUser` exactly once with the id THIS request created; (c) a `writeAudit` failure does the same, which is what makes fail-closed real here rather than asserted — no account survives an unaudited registration; (d) when the compensating delete itself throws, the thrown message **contains the orphan `kcUserId`** and the original failure is preserved as `cause` | **CLOSED.** `POST /auth/register-parent` now performs its two LOCAL writes (`tenant.upsert`, `userProfile.create`) plus one `writeAudit` inside a single `this.prisma.$transaction`, extracted into `persistRegisteredParent` — a separate method because rule B of `scripts/audit-write-check.js` reddens on a `try` anywhere among a `writeAudit` call's AST ancestors, function boundaries included. Both Keycloak calls stay OUTSIDE and before it (an HTTP round-trip inside an interactive transaction is how a 5 s timeout becomes an incident), and a `.catch()` at the call site deletes the identity this request created when the transaction rolls back — the `S-E04-9` compensation at its last address. One vocabulary entry added, `{ code: 'user.register', label: 'Auto-inscription d’un parent' }`, deliberately **not** `critical: true` so public signup traffic cannot drown the counter that exists to surface `role.grant` / `role.revoke`. Provenance goes through the canonical `deriveAuditProvenance` with a synthesised payload whose every field states what this request **created** (`sub` = `kcUserId`, the literal `sub` every future token for this account carries; `realm_access.roles` = the one realm role granted at step 4) rather than guessing at a caller — routed through the mapper because `audit-provenance-gate.spec.ts` G-3 forbids any second file under `apps/api/src` deciding an actor role from realm roles. `after` is an explicit allow-list, never a spread: `RegisterParentDto` carries a plaintext `password`, and this row is rendered on `/admin/audit` **and** exported by `audit-csv.generator.ts`. **Two deltas a caller can observe, stated rather than absorbed:** (1) a `P2002` on `userProfile.create` now returns the **same 409** as the pre-existing-identity branch instead of a raw-Prisma 500 — required, because the compensation means the identity no longer survives to make the retry 409 on its own, and it removes a driver leak that was also a user-enumeration oracle on an endpoint `PF-46` flags as unthrottled; (2) fail-closed on a **public funnel** means `audit_log` unavailable ⇒ no parent can register, each attempt spending a `createUser` + `setUserPassword` + `deleteUser` round-trip — taken deliberately (an unaudited account is worse than a deferred signup) and recorded in the `S-E05-11` amendment to `ADR-035`. The `demo` tenant upsert keeps its shape and its owner (`PF-165`), with its new cost written down: it is now a contended unconditional write holding a row lock on one tenant row for the transaction's duration | G-AUDIT, G-TENANT, G-DNC |

## PF-129
status: open
layer: L0
row: | PF-129 **P1** *(was `TECH_DEBT`/latent in `audit-findings-index.md`; **this slice makes it LIVE** — escalated, not newly discovered)* — a third `apps/web` server-side fetch to the API bypasses the one provenance helper, and the invariant that forbids it is enforced nowhere | V3-E04 → **V3-E06** *(fix is `apps/web` — **track c's seam**)* | — *(story to be written by track c)* | `open` — **verified by the routine at land of `S-E05-11`**, by reading the single caller rather than by trusting the brief | — *(none — `apps/web` has no component unit runner, `PF-129`/`PF-133`)* | **`PF-129`'s own text predicted this exact event:** *« Latent today — `RegisterController` writes no audit row — which is exactly what makes it dangerous: the day parent self-registration becomes an audited event, the request arrives with no `x-pilotage-*` header, branch 2 fires, and the row records the web container's egress address with no test red anywhere. »* `S-E05-11` is that day. The routine's own briefing for this slice asserted the opposite — « this endpoint is public so the request genuinely IS the operator's browser » — and the implementing agent **falsified it by measurement** rather than obeying it: `apps/web/src/app/parent/register/actions.ts:1,25-29` is `'use server'` and issues a bare `fetch` carrying only `Content-Type` / `Accept`, so it never calls `clientProvenanceHeaders`. Consequence, now shipping: `extractAuditClientHints` takes branch 2, so in production (`TRUST_PROXY_HOPS=2`) the chain is shorter than N and `ipAddress` is honestly **null**, while on the local `--profile app` topology (N = 0) it is the **web container's** egress address — one constant value shared by every registrant, which is `PF-31` reconstituted — and `userAgent` is `undici`'s, not the browser's. **The API side is already correct and needs no change**: the seam never invents, and the day `apps/web` forwards the headers this row starts recording the real operator with no edit to `register.controller.ts`. Fix stays as `PF-129` always stated it: route `registerParentAction` through `clientProvenanceHeaders` (or through `api()`), **and** add the missing web-side quality spec over the invariant — any `fetch(` whose URL contains `API_URL` under `apps/web/src` must be preceded by `clientProvenanceHeaders` in the same lexical scope — with the three current seams as positive controls. Raised to **P1** because it is no longer latent: an audited write is recording a wrong-or-null provenance today | G-AUDIT, G-PORTAL, G-DNC |

## TOOL-06
status: open
layer: L0
row: | TOOL-06 **P0** ⬆ *(was P1 — raised on measurement, not on opinion)* — seven `run_stage` calls omit the timeout argument, so they exit 125 before running; **the consequence is that `ci-gate.sh` can never print `GATE: PASS` for any diff, in any track** | V3-E04 *(routine/gate)* | — *(recorded, deliberately **not** storified this run — `scripts/` is shared AND matches `GATE_MACHINERY`)* | `open` — **measured by run 40 on a clean, passing tree** | `scripts/ci-gate.sh:102,118,142,165,198,230,235` | `run_stage` is `<timeout_seconds> <name> <command...>` (`:47`), and seven call sites pass no timeout, so `timeout` receives the stage **label** as its interval: `timeout: invalid time interval 'runtime engines'`, `'production artefacts (string scan)'`, `'compose invocation …'`, `'audit writes …'`, `'csv escapers …'`, `'schema drift …'`, `'prisma generate'`. They run **unconditionally, before the `── ci-gate (fast) ──` header**, so **every** invocation ends `GATE: FAIL (7 stage(s))` no matter how clean the tree. Run 40 measured this on a tree where every stage that actually executed passed: `production artefacts ✓ · audit writes ✓ · prisma generate ✓ · typecheck ✓ · lint ✓ (0 errors) · test:api 1021/1032 no drift ✓ · test:worker 293/300 no drift ✓`. **This makes AUTO-LAND's `GATE: PASS` condition unsatisfiable repo-wide**, which is a routine-stopping defect rather than a nuisance: every track must now leave every PR open and flagged, and the flag no longer distinguishes a real gap from this one. **Two corrections to `TOOL-06`'s own text (`R-30`), because the next run reads it and believes it:** (1) *"Six sibling calls are broken too, but have working duplicates lower down"* is **imprecise in the direction that matters** — only **four** have duplicates that run on a normal PR (`production artefacts :285`, `audit writes :291`, `prisma generate :300`, `schema drift :305`); `runtime engines :338` and `compose invocation :339` duplicate **only inside the `--full` block**, so on a fast run they do not execute at all. (2) `csv escapers` has **no duplicate anywhere at either flag level** — `scripts/csv-escape-check.js`, the ratchet `S-E05-1` built to hold `PF-168` closed, has therefore **never executed in CI**, which is what `TOOL-06` says and is now confirmed by exhaustive enumeration of the eleven `run_stage` sites rather than by reading one. Probable origin: `#216` (*"give the test stages a bound the api suite can actually finish inside"*) introduced the timeout parameter and re-signed only the lower call sites. Fix is mechanical — give the seven calls a bound — but it belongs to a **gate-owned** PR: `scripts/` matches `GATE_MACHINERY` (`:322`), so any diff touching it escalates `test:api` to the 2400 s whole-suite stage that `TOOL-04` proves cannot finish on this machine. Fix `TOOL-04` and `TOOL-06` together, or fix `TOOL-06` and accept one flagged PR | G-DNC |

## TOOL-07
status: open
layer: L0
row: | TOOL-07 **P1** *(routine, not product)* — a run's **detached heartbeat loop outlives its session**, so the staleness reaper can never free the track it was protecting | V3-E04 *(routine/gate)* | — *(recorded, deliberately **not** storified this run — routine triage)* | `open` — **measured on this run, which is the instance** | `$HOME/.claude/scheduled-tasks/daily-improvement-v3/routine-lock.sh` — no test; the evidence is `state/runs.log` plus this worktree's reflog | Step 3 of the task file prescribes `while :; do … heartbeat <track>; sleep 900; done` in the background, and Step 7 says to stop it. **Nothing enforces Step 7 when the session does not reach it.** This run's session was suspended for ~22 h after its sprint returned `landed: true`; the detached loop kept refreshing track b's claim the whole time, so `STALE_MIN=90` could never fire and the track was pinned indefinitely — `state/runs.log` records `GATE=BUSY all tracks claimed or blocked` at 08:40, 08:59, 09:00 (×3) and 09:07 on 2026-08-12, i.e. **six consecutive ticks lost across all tracks**, and a human-side cleanup at 09:34 had to commit the stranded working tree to `salvage/2026-08-12-v3-b-E04-register-atomicity` and release the claim by hand. The heartbeat is therefore an **anti-safety device**: it converts "this session died" from a self-healing condition into one requiring manual intervention, and it does so most reliably in exactly the case the reaper exists for. Note the asymmetry — a heartbeat that is too *slow* costs one reclaimed track, while one that outlives its session costs **every** subsequent tick. Fix directions, cheapest first: (i) have the loop write its own PID into the claim and have `heartbeat` refuse when the *session* process is gone; (ii) bound the loop (`for i in $(seq 1 12)`) so it expires on its own — a run longer than the bound re-arms it at a checkpoint; (iii) drop the background loop entirely and heartbeat from the run's own tool calls, which is what **this** run did after re-claiming, and it cost nothing. **Registered under `TOOL-` per the triage rule: the defect is in the routine's instrument, not in the product** | G-DNC |
