# Ledger updates — `ci/2026-08-11-v3-b-e05-s2` (run 39, track b, `S-E05-2`)

## PF-156
status: closed
layer: L0
row: | PF-156 **P1** vertical privilege escalation — a `roles.assign` holder could self-grant any role's whole permission set | V3-E05 | **`S-E05-2`** | `closed` | `apps/api/src/shared/auth/privilege-ceiling.spec.ts` + `modules/identity/{roles.controller,users.service,invite.controller}.spec.ts` — **79/79 green**, executed by the routine on the landed tree | **Closed by one pure, total, fail-closed predicate, and the fail-before half was produced by mutation rather than asserted.** `exceedsGrantor(grantorSet, requested)` (`shared/auth/privilege-ceiling.ts`) returns the codes the grantor does not hold; `assertWithinCeiling` throws the French 403 `{message, required, missing}` in `permissions.guard.ts`'s own shape. Wired into **four** grant paths — `roles.controller.create()`, `roles.controller.update()` (only when `permissionCodes` is present; a rename stays exempt, commented as deliberate), `users.service.assignRole()` and `invite.controller`'s custom-role grant — every one **before** its `$transaction` opens, so a refusal writes no `Role`, no `UserRole` and **no audit row**, matching the posture the two pre-existing cross-tenant guards already had. **Fail-before evidence, measured by the routine, not by the sprint:** replacing the predicate body with `return []` turns **20 of 79 tests red across all four suites**; reverted, tree verified byte-identical to `HEAD`. **Fail-closed is structural:** an absent or empty grantor set denies *everything* (live, not theoretical — `user-sync.service.ts:67` resolves an unrecognised realm role to `[]`), an unknown code is not held therefore it exceeds, and a non-array `requested` denies as one opaque entry. `super_admin` is unaffected **structurally rather than by exemption** — its realm role already carries the whole catalogue and the string `super_admin` appears nowhere in the module; an `if (roles.includes(…)) return []` would be a bypass wearing a role name. **DNC-10:** no env read, no `SKIP_*`/`ALLOW_*`, no dev escape; both exports have arity **2** and a test asserts it, so an options bag cannot appear later without turning red. **The blanket `isSystem` refusal this finding's own text proposed was deliberately NOT implemented**, and `ADR-015`'s `S-E05-2` amendment records the corrected reasoning: the ceiling already refuses every seeded `isSystem` role for a non-`super_admin` grantor, so the ban is pure redundancy — the routine's own briefing argued it was "redundant where right and breaking where wrong", and that argument is measurably wrong. **One routine claim was falsified by the implementer and the correction is kept:** the brief asserted a `school_admin` could POST the *seeded* `super_admin` role id; `grep -rn super_admin apps/api/prisma/` returns **zero** matches — `seed.ts` seeds only `school_admin`, `teacher`, `parent`, `student`. The real chain is worse and does reproduce: mint a custom role carrying all **89** codes, self-assign it, re-authenticate — `effectivePermissions` unions custom-role permissions with no ceiling. Both halves of that chain are now refused | G-AUTHZ |

## PF-09
status: open
row: | PF-09 An administrator can mint a role carrying permissions they do **not** themselves hold | V3-E05 | **`S-E05-2`** *(narrowed)*, residual → **`S-E05-2b`** | `open` — **NARROWED, NOT CLOSED, and booked that way deliberately** | `roles.controller.spec.ts`, `invite.controller.spec.ts` — green on the three closed channels | **Three of four mint/rewrite channels are ceilinged (`create`, `update`, the invite custom-role grant); the fifth grant channel is not, and it reproduces the whole finding.** `POST /users/invite` is gated only on `users.write` and passes `realmRoles: [body.realmRole]` straight to Keycloak, with `@IsEnum(['school_admin','teacher','parent'])` as the only constraint — so a `school_admin` who is refused `customRoleSlug: 'teacher'` (403) gets **201** for `realmRole: 'teacher'`, and that identity logs in holding `grades.revise`, `grades.write`, `attendance.write`, `lessons.write`, `lessons.delete` and `exports.execute.teacher`, none of which the caller holds. Same escalation, one door further along. **Two independent verify lenses raised this against the sprint's own "closed" label and the routine agrees with them**: the ceiling compares *permission codes*, and a realm role is a permission set that never passes through the predicate. Closing it needs a **grantor-relative realm-role ladder** (provision at or below your own level), which is a delegation decision belonging to `ADR-015`'s model — see `PF-178` — not a patch. `ADR-035`'s new note claiming `PF-09` closed was corrected at land | G-AUTHZ |

## PF-178
status: open
row: | PF-178 **P1** *(product decision, raised by `S-E05-2`)* — a correct ceiling refuses a `school_admin` the `teacher`, `parent` and `student` roles, and nothing replaces that path | V3-E05 → **`ADR-015`** / `open-decisions.md` | — *(story to be written — `S-E05-2b`)* | `open` — **measured, then shipped fail-closed on purpose** | `users.service.spec.ts` asserts the refusal; no test asserts a replacement path, because none exists | **This is the ceiling working, and it is still a real product gap.** Measured against `permissions.constants.ts`: `school_admin` holds **75** codes; `teacher` (34) exceeds it by **6** (`grades.write`, `grades.revise`, `attendance.write`, `lessons.write`, `lessons.delete`, `exports.execute.teacher`), `parent` (20) by **3** (`guardianships.claim`, `exports.execute.parent`, `remediation.book`), `student` (7) by **5** (the five `*.read.self`). `seed.ts:216-220` materialises those into real `rolePermission` rows, so the DB roles genuinely carry them. Net: **a `school_admin` can now assign none of the four other seeded system roles** — `super_admin` correctly (that is `PF-156`'s point) and `teacher`/`parent`/`student` as collateral, on the admin onboarding path. **The ceiling was deliberately not weakened to preserve the old behaviour**, because the old behaviour *is* the P0: the same mechanism that let an admin hand out `teacher` let them hand themselves `grades.revise`. The answer is a delegation model — an explicit assignable-roles grant, or a grantor-relative realm-role ladder permitting `school_admin → teacher\|parent\|student` while refusing `teacher → school_admin` — which also closes `PF-09`'s residual. **Needs a human ruling before implementation**; recorded in `open-decisions.md` as `D-12` | G-AUTHZ |

## PF-174
status: open
row: | PF-174 **P1** — `/admin/users` swallows the new 403 in silence: the menu still offers every role and says nothing when the grant is refused | V3-E06 *(raised by `S-E05-2`; **track c seam**)* | — *(story to be written)* | `open` — raised by the UX lens, **confirmed against the source by the routine** | — *(none — `apps/web` has no component unit runner, `PF-129`/`PF-133`)* | `apps/web/src/app/admin/users/actions.ts:7-10` has **no `catch`**, and `apps/web/src/app/admin/users/UsersTable.tsx:30-39` is `try … finally` with no `catch` clause, so a rejected server action becomes an **unhandled rejection**: the spinner stops, the menu closes, `router.refresh()` never runs and **no message appears**. `UsersTable.tsx:115` maps *every* role into the assign menu (`:122` `onClick={() => assignRole(u.id, r.id)}`), so it still offers all four seeded roles, three of which now always fail (`PF-178`). The contrast case proves it is a local defect rather than a missing capability: `/admin/roles` renders the *identical* 403 correctly, because `admin/roles/actions.ts:24-31` catches `ApiError` — which is also why the API puts the exceeding codes **inside `body.message`** and not only in the structured `missing` array. **This is the visible half of a correct backend change, and it lands in another track's seam**, so it is registered rather than fixed: `apps/web/**` belongs to track c. Fix: catch `ApiError` in `users/actions.ts` as `roles/actions.ts` already does, surface `body.message`, and stop offering roles the caller cannot grant | G-PORTAL, G-DNC |

## PF-175
status: open
row: | PF-175 **P2** — grants that escalated **before** the ceiling existed pass it unconditionally, because the union that feeds the comparison includes the escalated role | V3-E05 | — *(recorded, **not** storified this run — routine triage)* | `open` — raised by the escalation panel, registered by the routine at land | — *(no test: the condition is data, not code)* | The ceiling compares against `effectivePermissions`, which unions **custom-role** permissions into the grantor's set (`user-sync.service.ts:80-84`). An actor who already holds a role minted through `PF-09` therefore holds those codes *effectively*, and can keep minting and assigning them — the new guard reads them as legitimately held. **This is inherent to a grantor-relative ceiling and is not a defect in it**; what is missing is the one-off detection sweep that would tell a real deployment whether the condition exists. Query: `SELECT r.id, r.slug, count(rp.*) FROM "role" r JOIN "role_permission" rp ON rp."roleId" = r.id WHERE r."isSystem" = false GROUP BY r.id HAVING count(rp.*) > 0;` — any code in those roles absent from `REALM_ROLE_PERMISSIONS.school_admin` is a grant `PF-09` already produced. **Not run this run**, because the local database is seed data and an empty result there would prove nothing about anywhere else. Empty ⇒ the closure label is earned and this downgrades to monitoring; non-empty ⇒ revoke in the same window | G-AUTHZ |

## PF-176
status: open
row: | PF-176 **P3** — an attempted privilege escalation is logged, and logged anonymously | V3-E05 → V3-E04 | — *(recorded, **not** storified this run — routine triage)* | `open` — declared by `S-E05-2` in code and in `ADR-015` rather than discovered after it | `privilege-ceiling.spec.ts` asserts the refusal, not the log | `privilege-ceiling.ts`'s `assertWithinCeiling` emits a `logger.warn` naming the exceeding codes and the set sizes — deliberately, because a refusal writes **no audit row** (nothing happened, and that is the house posture for a pre-transaction refusal) and an attempted escalation on a children's-data platform must not be silent. But the predicate has **no actor context by design**, so the line carries no `jwt.sub`, no `tenantId` and no client hints: it says an escalation was attempted and cannot say by whom. **Adding a third parameter was refused on purpose** — it is the seam through which an options bag, and later a bypass flag, would arrive (DNC-10). Fix direction: move the `warn` out of the predicate to the four call sites, where `jwt.sub`, `tenantId` and the provenance hints are already in hand | G-AUDIT |

## TOOL-06
status: open
row: | TOOL-06 **P1** *(gate, not product)* — seven `run_stage` calls omit their timeout argument, and for the brand-new CSV escaper gate that broken call is the **only** one, so `#215`'s blocking ratchet has never executed | V3-E02 *(routine/gate)* | — *(recorded, **not** storified this run — routine triage; the fix is one word × 7 lines, but it is `scripts/**`, a shared path, and it belongs to whoever owns the gate rewrite)* | `open` — measured by the routine at land of `S-E05-2`, by reading a docs-only gate log | `scripts/csv-escape-check.js` — **passes standalone, exit 0**; the same script through the gate's own invocation **exits 125 without running** | `scripts/ci-gate.sh:47` documents the helper as `run_stage <timeout_seconds> <name> <command...>`. Seven call sites — **`:102`, `:118`, `:142`, `:165`, `:198`, `:230`, `:235`** — pass only `<name>`, so `timeout` parses the stage *name* as its interval and dies immediately: `timeout: invalid time interval 'csv escapers (one neutraliser, two escapers)'`, exit **125**, printed as `✗`. They are pre-`#214` leftovers the gate rewrite orphaned without deleting, and they execute unconditionally at the top of every run. **Six of the seven are harmless noise** — `runtime engines`, `production artefacts`, `compose invocation`, `audit writes`, `schema drift` and `prisma generate` all have a correct, bounded call site further down (`:285`–`:339`), so those checks really do run and the broken copies are merely overwritten in the summary, which is exactly why nobody noticed. **`csv-escape-check.js` has ONE call site in the whole file, and it is the broken one (`:198`).** Measured: `grep -c csv-escape-check scripts/ci-gate.sh` → **1**. So the ratchet `S-E05-1` landed **one hour before this run**, built specifically to stop `PF-168`'s CSV-injection class from recurring and described in `#215` as blocking, **has never executed and cannot fail.** It is `PF-105`'s family — a check whose green means nothing — but sharper, because this one is not merely tautological, it is *unreachable*. **Why the verdict still says PASS:** the broken stages are not counted in the summary or the exit tally, so a docs-only run prints seven `✗` lines and then `GATE: PASS (docs-only, 5s)`. Fix: add the timeout argument to all seven, and give `csv escapers` a bounded call site in the tier-1 block beside `audit writes`. **Id caveat:** allocated after `TOOL-05` in the same run; per `TOOL-05` itself, re-check the register before citing it | G-DNC |

## TOOL-05
status: open
row: | TOOL-05 **P2** *(ledger, not product)* — finding-id allocation is a race, and with three concurrent tracks it is now a race that is regularly lost | V3-E02 *(routine/ledger)* | — *(recorded, **not** storified this run — routine triage)* | `open` — observed live by the routine at land of `S-E05-2` | n/a — observed from two merged PRs declaring the same id | Two runs on 2026-08-11 read the register's maximum as `PF-172` within the same window and both allocated **`PF-173`**: `S-E05-1` (track c, `#215`) and `S-E05-2` (track b, `#218`). Both merged, so `main` briefly declared **two** `PF-173`s; resolved by renumbering the later-landing one to **`PF-178`**, per the rule `PF-150`'s own note already states. **Distinct from `PF-150`:** that was two *registers* disagreeing about one number; this is two *writers* allocating the same number from a correctly-read register. The ledger inbox prevents exactly this for **rows** and does nothing for **ids** — `tracks.md` guarantees no two tracks edit the same code or the same ledger file, and guarantees nothing about an id space that is global, dense, and allocated by reading a maximum. **Frequency is the argument:** three tracks each raising findings collide whenever two land in the same window, which is now the normal case rather than the exception. Fix directions, cheapest first: a **per-track id stripe**; or `routine-lock.sh gate` handing out a reserved block with the track claim; or `v3-reconcile-ledger.js` **refusing to fold** a block whose id already exists and reporting it — the last turns a silent duplicate into a loud one without changing how ids are chosen | G-DNC |

## TOOL-04
status: open
row: | TOOL-04 **P1** *(gate, not product)* — the fast gate escalates to an api suite that cannot finish, and it escalates precisely when the diff improves a gate | V3-E02 *(routine/gate)* | — *(recorded, **not** storified this run — routine triage; and it is the routine's own instrument, not the product)* | `open` — measured by the routine across **four** ratchet runs at land of `S-E05-2` | `scripts/ci-gate.sh` + `scripts/test-ratchet.js api` — the stage is red, and red for a reason that has nothing to do with any diff | **`ci-gate.sh` picks between two api stages on `GATE_MACHINERY='^(scripts/\|\\.github/\|infra/\|apps/api/src/shared/quality/)'`: a match runs the WHOLE api suite bounded at 2400 s, a non-match runs `--skip src/shared/quality/` bounded at 1200 s. The whole suite does not complete on this machine — it BLOCKS rather than runs.** Measured: (1) contended, timed out at 2400 s — three `test-ratchet.js api` trees were alive at once, two orphaned for 7–8 h, which is `PF-90` live, and the routine enumerated and killed **24** orphaned processes; (2) uncontended, timed out at 2400 s again, 2877 s for the whole gate; (3) unbounded and uncontended, **still running at ~80 min** with the 7 jest workers holding only **7–40 s of CPU each** — blocked, not computing — killed; (4) `--skip src/shared/quality/` → **`1008/1019 passed · 11 failing · 11 known-failing (baseline)`, `no drift`, exit 0** in minutes. The difference between (3) and (4) is exactly `src/shared/quality/**`, whose members are the artefact- and Docker-dependent meta-gates (`boot-gate`, `web-artifact-gate`, `link-integrity-gate`, `schema-drift-gate` all failed in run 2) — and the fast gate deliberately **no longer builds**, so the escalated branch runs artefact-dependent specs in a mode that produces no artefact. **The sharp end is the escalation predicate, not the bound:** touching any file under `apps/api/src/shared/quality/` is exactly what a slice that STRENGTHENS a gate does, so the gate is hardest to pass precisely when the diff improves it — `S-E05-2` hit this because its sprint extended `audit-write-gate.spec.ts`. Third consecutive gate-bound issue after `#216` and `#217`, both of which raised the bound; the measurement above suggests the bound is not the variable. **The blocking spec was NOT isolated and is not guessed at.** Recorded under `TOOL-` per the triage rule | G-DNC |

## PF-177
status: open
row: | PF-177 **P3** — a duplicated permission code answers 400 while naming no code at all | V3-E05 | — *(recorded, **not** storified this run — routine triage)* | `open` — measured by the routine at land | `roles.controller.spec.ts` covers the ceiling, not this arithmetic | `roles.controller.ts` validates the catalogue by **length comparison** — `perms.length !== body.permissionCodes.length` — so `["students.read","students.read"]` fails (one row returned, two codes sent) and the derived `missing` array is computed by `filter`, which finds every code present and yields **`[]`**. The caller receives `400 Permissions inconnues` with an empty list of unknown permissions: a refusal that cannot say what it refused. Pre-existing, unrelated to the ceiling, and unchanged by it; recorded because `S-E05-2` put a second refusal beside it and the two must not be confused when debugging. Fix: compare de-duplicated sets, or resolve `missing` by set difference rather than by count | G-DNC |
