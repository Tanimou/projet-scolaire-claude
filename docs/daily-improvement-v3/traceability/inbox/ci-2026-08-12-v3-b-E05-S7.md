# Inbox — `ci/2026-08-12-v3-b-E05-S7` (track b, run 41)

Story `S-E05-7` — the public registration funnel gains an admission bound.

## PF-46
status: in-progress
layer: L0
row: | PF-46 public unthrottled registration — **throttling third CLOSED**, `emailVerified` third still open | V3-E05 | S-E05-7 | `in-progress` | `register-throttle.supertest.spec.ts` "429s, and `createUser` was called exactly TIER1_MAX times" · `public-endpoint-throttle.spec.ts` · `register-throttle.guard.spec.ts` (56/56 green, jest run observed) | `POST /auth/register-parent` now refuses above a two-tier fixed-window bound **before the pipes and therefore before every Keycloak call**. Proved over a booted Nest app, not over a hand-built limiter: the (TIER1_MAX+1)th request is 429 **and `keycloak.createUser.mock.calls.length` is unchanged**. **NOT closed** — see the narrowing note below |

**Why `in-progress` and not `closed`.** `PF-46` is a three-part row — *public*, *unthrottled*, *self-verifies email*.
This slice closes the **unthrottled** third only.

- **`public` is not a defect** — self-service parent registration is the product's intended one public path. No work owed.
- **`emailVerified: true` stays open, by decision, and is the reason this row does not close.** Flipping it to
  `false` + `requiredActions: ['VERIFY_EMAIL']` needs Keycloak **realm SMTP wiring**, which lives in `infra/**` —
  outside track b's seam (measured this run: `infra/docker-compose.yml` runs `maildev` but sets no `KC_SMTP_*`) — and
  it **changes the login funnel**, since the parent could no longer log in immediately after submitting. That is a
  product call, not an agent's. The tradeoff is already recorded at `register.controller.ts:153-156` and was left
  byte-for-byte untouched.

## PF-181
status: open
layer: L0
row: | PF-181 **P2** — the new 429 renders above a permanently disabled submit button, so a throttled parent reads "retry in a few minutes" and then cannot | V3-E06 | — *(recorded, deliberately not storified this run — routine triage)* | — | `apps/web/src/app/parent/register/ParentRegisterForm.tsx` — the refusal copy this slice ships (`REGISTRATION_THROTTLED_MESSAGE`) instructs the registrant to wait and retry, but the form's submit control does not re-enable after the failed submission, so the instruction is unfollowable in the UI. **`apps/web` = track c's seam**, not track b's — recorded rather than reached across. Surfaced by the escalation panel as `R-6` | G-PORTAL |

## PF-182
status: open
layer: L0
row: | PF-182 **P2** — the in-process bound is per-replica and pre-validation; the edge `limit_req` companion does not exist | V3-E05 | — *(recorded, not storified this run)* | — | Two residuals of `S-E05-7`, both **outside track b's seam** and both written into `ADR-038` (D2, D4) rather than left to be found. (a) The counters are **module-level and therefore per-process**: `infra/docker-compose*.yml` declares no `replicas` today, so the ceiling holds, but `--scale api=N` silently multiplies it by N with no test going red. (b) The guard runs **before the `ValidationPipe`**, so `curl -d '{}'` in a loop spends tier-2 budget while reaching **zero** Keycloak calls — paying the availability cost without the amplification benefit ever being at stake. The real fix for both is an edge `limit_req` in `infra/nginx/conf.d/pilotage.conf` (note: the strict 10 r/m `auth_zone` at `:142` matches the web **page** routes and has never covered `/api/v1/auth/*`, and the local `--profile app` topology has no nginx at all) | G-AUTHZ |

## TOOL-06
status: closed-by-other-work
layer: —
row: | TOOL-06 `ci-gate.sh` — 7 stages pass a stage NAME where `timeout` expects a duration | — | — | `closed-by-other-work` | run 41 gate logs, before and after the rebase | **Observed red, then fixed by #223 mid-run — and run 41 re-verified rather than inheriting the excuse.** The first gate, against `db2473b`, printed `GATE: FAIL (7 stage(s))`, all seven `timeout: invalid time interval '<stage name>'`, emitted before the gate banner and therefore not diff-dependent. `545879f` (#223, track a) landed the fix while `S-E05-7` was in flight. That branch was **rebased on `origin/main` and re-gated**, producing **`GATE: PASS (fast)`** — the first genuinely reachable pass, with the `csv escapers` stage (one of the seven that had never once executed) green. Closed by #223's work; recorded here because run 41 is where it was independently confirmed fixed on a real diff |
