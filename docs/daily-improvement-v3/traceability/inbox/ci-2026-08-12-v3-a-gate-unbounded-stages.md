# Ledger updates — `ci/2026-08-12-v3-a-gate-unbounded-stages` (track a, V3-E02 routine/gate)

> **Id-allocation caveat** (`TOOL-05`): `TOOL-07`, `TOOL-08` and `TOOL-09` were allocated by reading the maximum in
> `audit-findings-index.md` at 2026-08-12 09:5x, while track b held an active claim. Allocation is a race between
> concurrent tracks — re-check the registry before citing these numbers.

## TOOL-06
status: closed
layer: L0
row: | TOOL-06 Seven `run_stage` calls omit their timeout, so every code-change gate fails on a diff it never examined | V3-E02 | `ci/2026-08-12-v3-a-gate-unbounded-stages` | `closed` | `apps/api/src/shared/quality/csv-escape-gate.spec.ts` — *every `run_stage` call in ci-gate.sh declares a numeric timeout (TOOL-06)* | **Reproduced before the change, on a clean worktree**: `bash scripts/ci-gate.sh` printed seven `timeout: invalid time interval ‘<stage name>’` banners and seven `✗`, then `GATE: PASS (nothing to gate)` — the `exit 0` at the "nothing to gate" branch fires *before* the summary, which is why docs-only runs never showed it and code runs always failed. The banner read `▶ node`, not the stage name, because with the timeout omitted `$limit` held the name and `$2` held the command — that is why the output was read for 39 runs and understood by nobody. **The seven calls were a pre-`#214` stage list left in place above the tiers the rewrite introduced**, so they ran unconditionally before the dispatcher. Excised. Six of the seven had working counterparts in a tier and lost no coverage; **`csv escapers` had no other call site anywhere**, so `#215`'s blocking ratchet had never executed once — it is now wired into TIER 1, which every PR runs. `run_stage` now **refuses a non-numeric timeout and exits 64** instead of filing a 125 as an ordinary stage failure: measured, `run_stage "runtime engines" node -e …` prints the usage error and the following line is never reached, while `run_stage 30 "probe stage" …` still passes. Post-change, each of the eleven check scripts is referenced **exactly once** in `ci-gate.sh` (was: seven duplicated) | G-DNC |

## TOOL-07
status: closed
layer: L0
row: | TOOL-07 Three gate meta-tests were red on `main`, and two more could not fail, because `#214` moved the anchors they read | V3-E02 | `ci/2026-08-12-v3-a-gate-unbounded-stages` | `closed` | `boot-gate.spec.ts`, `web-artifact-gate.spec.ts`, `link-integrity-gate.spec.ts`, `audit-write-gate.spec.ts`, `csv-escape-gate.spec.ts` | Discovered while closing `TOOL-06` and **measured on `origin/main` before any edit**, by evaluating each anchor against the shipped file. `#214` gave every stage a timeout argument and replaced the `${QUICK}` flag with `$MODE`; five specs still read the old shapes. **Red, not vacuous** — each asserts `expect(buildAt).toBeGreaterThan(-1)` on `indexOf('run_stage "build"')`, which has returned `-1` since `#214`: `boot-gate.spec.ts:164`, `web-artifact-gate.spec.ts:132`, `link-integrity-gate.spec.ts:1611` (which also does `lastIndexOf('if [ "${QUICK}" -eq 0 ]')`, likewise `-1`). **Vacuous, not red** — `audit-write-gate.spec.ts:937` and `csv-escape-gate.spec.ts:735` walk brace depth looking for a line containing `QUICK` to prove their stage is not inside a skip guard; finding no such line they concluded "not inside a guard" and passed *wherever the stage was wired, including nowhere* — `PF-105`'s family. **Why nobody saw three red tests:** `ci-gate.sh` runs the api ratchet with `--skip src/shared/quality/` unless the diff matches `^(scripts/|\.github/|infra/|apps/api/src/shared/quality/)`, and `#220`, `#221` and `#222` touched none of those. All five anchors re-pointed at the shapes the file actually has (`/run_stage\s+\d+\s+"build"/`, `if [ "$MODE" = full ]`), and both depth scanners now assert the guard string exists before trusting their own result, so they cannot go vacuous the same way twice | G-DNC |

## TOOL-08
status: closed
layer: L0
row: | TOOL-08 `prisma generate` ran BEFORE `schema drift` in the live tier — the AC-6 ordering held only in dead code | V3-E02 | `ci/2026-08-12-v3-a-gate-unbounded-stages` | `closed` | `apps/api/src/shared/quality/schema-drift-gate.spec.ts` — *ci-gate.sh runs it BEFORE `prisma generate`, in the tier every PR runs (AC-6)* | Discovered while closing `TOOL-06`. `S-E02-11` AC-6 requires the migration ledger to be refused **before** a client is generated, because `prisma generate` will happily generate a client for a schema no migration produces and lint, typecheck, build and boot then validate against that fiction while `migrate-entrypoint.sh` runs `migrate deploy` only — `db push`'s failure mode through the front door. In the tiers `#214` introduced, the two stages sat the other way round. It went unseen because the meta-test anchored on the literal `run_stage "prisma generate"`, which matches **only** the dead pre-`#214` line, where the order did hold: the test was reading a stage that exited 125 without running. Live tier reordered (drift, then generate); the assertion re-anchored on `/run_stage\s+\d+\s+"prisma generate"/` and given a companion that fails if either script is ever wired at two call sites again | G-MIGRATION, G-DNC |

## TOOL-09
status: open
layer: L0
row: | TOOL-09 **P3** — `runtime engines` is source-only and ~2 s but runs only under `--full`, so no PR checks the runtime it validated nothing on | V3-E02 | — *(recorded, **not** storified — widening the fast tier is a contract decision, not a repair)* | `open` — measured at the close of `TOOL-06` | — | `scripts/runtime-engines-check.js` had two call sites: the broken pre-`#214` one (removed with `TOOL-06`) and one inside the `--full` branch. Deleting the broken one is coverage-neutral — it exited 125 — but it leaves the check running only in the sweep and before a release. Its own rationale argues it should run first of all (`engines.node` claimed `>=20.0.0` while the API cannot boot below 20.19/22.12; **35** installed dependencies contradicted the declared range), and unlike `compose invocation` — which genuinely belongs in `--full` because it runs `docker compose config` — it reads source only and costs ~2 s, so nothing technical keeps it out of TIER 1. **Not promoted here on purpose:** the fast tier's contract is documented in the routine as "the source-only scanners, no Docker, no database", and changing what every PR runs is a call for `open-decisions.md`, not a side effect of repairing a timeout | G-DNC |
