# Inbox — `ci/2026-08-12-v3-a-tool10-drift-preflight` (track a, run 44)

## TOOL-10
status: closed
layer: L0
row: | TOOL-10 **P1** — `test:api (ratchet)` cannot finish on a machine without a reachable Postgres, and the gate reports it as "TIMED OUT", which invites raising a bound that would not fix it | V3-E02 | `ci/2026-08-12-v3-a-tool10-drift-preflight` | `closed` | `apps/api/src/shared/quality/schema-drift-gate.spec.ts` — *"the CLI against a refused address concludes fast, names the ladder, and keeps its verdict"* (AC-2/3/4/5), *"an INDETERMINATE probe descends the real ladder — only a refusal may stop it"* (AC-P16), *"every `docker` invocation is bounded, and the extractor proves it found them"* (AC-6); `apps/api/src/shared/quality/test-ratchet.spec.ts` — the killed path is distinguished from a startup failure | **Measured by the routine, both directions, not only by the sprint agents.** Premise re-verified on this tree before any code was written: `node scripts/schema-drift-check.js` was killed at **>4 minutes** still on its first `SELECT 1` (last line printed `▶ prisma CLI : …`), i.e. worse than run 43's 89 745 ms; `timeout 150 docker port pilotage_postgres 5432/tcp` neither returned nor was killed, and `ps -W` listed **nine** orphaned `/c/Program Files/Docker/…/docker` processes dated **Aug 10** — bash `timeout` does not kill the docker CLI on this platform, so route C's cost was *unbounded*, not 90 s. Fix is the preflight the finding called for, never a raised bound (`ci-gate.sh` is byte-identical; `2bd1a25` already tried that once). After: **825 ms**, `rc=1`, `SCHEMA DRIFT CHECK: FAIL — tooling_unavailable`, output still naming `prisma db execute`, `psql` and `pilotage_postgres` (DNC-08 intact, AC-4 preserved). Second belt measured end to end: an `indeterminate` probe (`ENOTFOUND`, 248 ms) correctly **descends** the ladder and concludes in **5841 ms**, printing `docker did not answer within 5000 ms and was killed` — the `spawnSync` bound is real where bash `timeout` was not. The `!pre.open` mutant the review panel built, which leaves all 123 other cases green while making the gate permanently red on any machine whose probe cannot answer in 2 s, is now caught: **0** occurrences of the short-circuit narration on correct code vs **2** under the mutant, with the verdict identical in both — which is precisely why nothing else could see it | G-DNC |

## TOOL-14
status: closed
layer: L0
row: | TOOL-14 **P1** — `#223`'s excision took the schema-drift stage's anti-drift note and its ADR filename out of `ci-gate.sh` with the dead block, turning two meta-tests red **on `main`**, unseen | V3-E02 | `ci/2026-08-12-v3-a-tool10-drift-preflight` | `closed` | `apps/api/src/shared/quality/schema-drift-gate.spec.ts` — *"both files carry the anti-drift note the sibling stages carry"* (AC-5, `:701`) and *"the new stage names its decision record, and that record exists"* (AC-15, `:722`) | **Found by the gate, on the first run whose full api ratchet has ever finished.** `TOOL-06` closed by excising the dead pre-`#214` stage block; that block also carried `S-E02-11` AC-5's `Kept in step with .github/workflows/ci.yml — the two must not drift (S-E02-2 AC-4)` and AC-15's `docs/adr/ADR-027-schema-drift-gate-needs-a-database.md`. Both went with it. `#223` could not see the two reds it created because **its own `test:api (ratchet)` timed out at 2400 s on `TOOL-10`** before reporting — the same stage list, the same run. This run's gate finished in **347 s** and named them immediately: `test-ratchet[api]: 2414/2433 passed · 2 NEW test failure(s) — not in the baseline`. **Proven pre-existing rather than assumed:** both test bodies and *every file they read* (`ci-gate.sh`, `ci.yml`, both ADRs) are byte-identical to `origin/main` under `git diff origin/main --stat`, so the inputs that produce the failure are `main`'s. Repair is **comments only** — `git diff origin/main -- scripts/ci-gate.sh` has **0** non-comment changed lines; no stage, no bound, no command moved, and `2bd1a25`'s raise-the-timeout mistake is not repeated. One trap worth recording: AC-5 requires the note **within 1400 characters of the invocation** and matches the literal `must not drift (S-E02-2 AC-4)`, so restoring it as a *wrapped* comment kept it red twice — once for distance, once because the line break split the substring. A note that reads correctly to a human and matches nothing is exactly the `TOOL-07` failure mode; measured with `node -e` on the shipped file until `at - noteAt` was 49, not 3057 | G-DNC |

## TOOL-15
status: open
layer: L0
row: | TOOL-15 **P1** — `csv-escape-gate.spec.ts` AC-7 writes a probe file into the REAL working tree and then asserts a repo-wide scan is green, so the merge gate is non-deterministic under parallel jest | V3-E02 | — *(recorded with its measurement; the repair is a design call about how a repo-wide scanner is tested hermetically, and `scripts/csv-escape-check.js` forbids the obvious shortcut — `open-decisions.md`, not a side effect of this slice)* | `open` — measured 2026-08-12 across two consecutive full-ratchet runs on the same branch | `apps/api/src/shared/quality/csv-escape-gate.spec.ts:492-522` — *"a deliberately-added fourth copy, in a .tsx file"* (AC-7) | **Two gate runs on this branch produced two DIFFERENT failure sets, which is the finding.** Run 1: `2414/2433 passed`, the 2 new failures being `TOOL-14`'s AC-5/AC-15, and AC-7 **green**. Run 2, after `TOOL-14` was repaired: `2415/2433 passed`, AC-5/AC-15 gone as expected, and **AC-7 newly red** — a case neither run's diff touched (`git diff origin/main --stat` on `csv-escape-gate.spec.ts` and `scripts/csv-escape-check.js` is **empty**). Standalone it **passes**: `jest csv-escape-gate.spec.ts -t "deliberately-added fourth copy"` → `1 passed`. The mechanism is visible in the test body: it `writeFileSync`s `apps/web/src/lib/__csv_escape_probe.tsx` into the shared checkout, spawns `csv-escape-check.js` over `REPO_ROOT` expecting RED, removes it, and spawns again expecting **exit 0 for the whole repository**. Under the ratchet's parallel workers that second assertion is a claim about a tree other workers are concurrently mutating, so it is a race in both directions — and the dangerous direction is not the false red seen here but the false green, where the probe is deleted by the `afterEach` of a retried run before the RED spawn reads it. **Why it is not repaired here:** the hermetic fix is to scan a copied tree rather than `REPO_ROOT`, and `csv-escape-check.js` deliberately exposes no root parameter — its own header records that *"a flag that lets a caller choose what is compared is a bypass flag wearing a different hat"* (`link-integrity-gate.spec.ts`'s lesson). Choosing between a scratch-tree copy, serialising this one spec, and weakening AC-7 to a rule-scoped assertion is a contract decision. **Consequence to state plainly: until it is settled, `GATE: PASS` on a gate-machinery diff is not reproducible, and a red on this case is not evidence about the diff that met it** | G-DNC |

## TOOL-11
status: open
layer: L0
row: | TOOL-11 **P2** — `exec()`'s new cross-server guard throws, and `exec()` is reached from a `finally`, so a future caller could end a run with no verdict at all | V3-E02 | — *(recorded, **not** storified this run — routine triage: a discovered P2 is recorded and reviewed at layer close)* | `open` — found by the review panel while closing `TOOL-10` | — | `scripts/schema-drift-check.js`'s `exec()` gained a guard refusing a URL whose host:port differs from the one the routes were opened and probed for — correct in itself, and it exists so the memoised preflight can never answer for the wrong server. But its own docstring two lines above still reads *"Returns `{ ok, detail }` rather than throwing"*, and `exec()` is reached from `dropScratch()` ← `cleanup()` ← the `finally` of `check()`, and from the `SIGINT`/`SIGTERM`/`uncaughtException` handlers. A throw on that path escapes past `report(state)` and ends the run **with no verdict** — `DNC-08` committed by the anti-`DNC-08` machinery. **Unreachable today** and that was checked rather than assumed: `deriveMaintenanceUrl` and `buildScratchUrl` replace only the database path segment, and a spec case pins the latter to exactly that. Recorded rather than widened into this diff because the repair is a one-line change of shape (`return { ok: false, detail: … }`) that belongs with a case driving the cleanup path, not bolted onto a slice about latency | G-DNC |

## TOOL-12
status: open
layer: L0
row: | TOOL-12 **P2** — routes A and B carry no spawn bound, so the `indeterminate` branch — the one that most resembles a real CI stall — descends an unbounded ladder on any host that ships a PostgreSQL client | V3-E02 | — *(recorded, **not** storified this run — routine triage)* | `open` — found by the review panel while closing `TOOL-10` | — | `TOOL-10` bounded the two `docker` spawns and the `pnpm` fallback; `run('psql', …)` and `run(cli.command, …)` still pass no `timeoutMs`. Measured with the same paren-balancing extractor the spec uses: `run('docker'` → 2 sites, both bounded; `run('psql'` → 1 site, unbounded; `run(cli.command` → 1 site, unbounded. It does not bite **here** only because `psql` is `ENOENT` on this Windows host, which is why the blackhole run still finished in 7 603 ms — on `ubuntu-latest`, where `ci.yml` runs and a PostgreSQL client ships, `psql`'s default `connect_timeout` is unset and the OS TCP timeout is ~130 s. So the fast path (`refused`) is now bounded on every platform, and the `indeterminate` path is bounded only on this one. Fix direction: the same `options.timeoutMs` already in `run()`, applied to routes A and B with data-plane-sized numbers (see `DOCKER_EXEC_TIMEOUT_MS`'s rationale — a bound that kills a legitimate `CREATE DATABASE` is a false red, not a safeguard) | G-DNC |

## TOOL-13
status: open
layer: L0
row: | TOOL-13 **P1** — the drift gate's whole end-to-end block, including *"the unmodified repository PASSES"*, silently becomes `describe.skip` when no database answers, and the ratchet reports that as GREEN | V3-E02 | — *(recorded with its measurement; it needs a database to close, which this machine does not have — see `NEXT-a.md`)* | `open` — raised by the review panel on this slice, confirmed by reading `schema-drift-gate.spec.ts:829` and `scripts/test-ratchet.js` | `apps/api/src/shared/quality/schema-drift-gate.spec.ts` — `describeWithDb = reachable ? describe : describe.skip` | Pre-existing, **not introduced by `TOOL-10`**, and stated because the preflight now sits upstream of it: `probeServer()` at module scope decides whether the end-to-end block runs, and `test-ratchet.js` compares **failures against a baseline, never test counts**, so tests that stop existing are invisible to it. The consequence is the one direction a gate must never fail in: if the preflight ever answered `refused` on a healthy server, the cases that prove the gate is *not red on correct code* would vanish and the ratchet would report green. `TOOL-10` mitigates the specific risk (three states, only `ECONNREFUSED` short-circuits, and a real `net.createServer()` listener proves the positive `open` case) but does not remove the structural one. Fix direction: have the ratchet fail when a suite's *skipped* count rises above its baseline, so a disappearing case is a red like any other. **Closing it requires a reachable PostgreSQL on `127.0.0.1:5433`** — the same precondition `S-E01-2b` needs | G-DNC, G-MIGRATION |
