# Risk Register

Reviewed by the V3 routine at Step 1 and updated at Step 6. A `HIGH` risk with no accepted mitigation **blocks its
owning layer** from being declared complete (`roadmap.md` §10).

**Scoring:** Likelihood × Impact, each Low/Medium/High. Severity is the worse of the two, escalated one level when the
risk concerns children's data or money.

---

## 1. Open risks

| ID | Risk | L | I | Sev | Owning epic | Mitigation | Status |
|---|---|---|---|---|---|---|---|
| **R-01** | **A tenancy fix migrates hosted data incorrectly and is unrecoverable**, because there is no migration history and prod runs `db push --accept-data-loss` | M | H | **CRITICAL** | V3-E02 → V3-E01 | Sequence E02 before E01; require a timed restore rehearsal (VAL-03) before any tenancy migration; expand/contract only; no destructive column drops in the same release. **Partially delivered 2026-08-02 (S-E02-1):** the migration ledger exists, no executable path runs `db push`, and the expand/contract + rollback plan is written down (`docs/runbooks/baseline-hosted-database.md` §5–6). **VAL-03 delivered locally 2026-08-07 (S-E02-3):** the restore rehearsal is no longer a requirement written down, it is a script that has been run. `scripts/restore-drill.js` executed a real timed cycle against the LOCAL Docker stack — 55 base tables / 13 550 rows dumped, restored into a scratch database and verified on row counts, per-table content checksums and schema; dump 1242 ms, restore 7488 ms, verify 4001 ms, total 22 737 ms, recorded as a reviewed SLO baseline. The divergence paths were driven (a deleted row → `row_count_divergence`; a mutated column → `checksum_divergence` at an identical row count) and the refusal paths too (`unreachable_source`, `unbaselined_ledger`, `tooling_unavailable`, and `--update` refusing to write from a failed run). The same run also found and fixed **PF-84**, which is the sharpest thing to record here: the safe migrator that this mitigation depends on had **never been able to start** — its entrypoint was excluded from every image ever built — so between 2026-08-02 and 2026-08-06 the ledger said "delivered" about a mechanism that was inert. **Status stays `open`, deliberately.** The rehearsal has run against a 13 550-row seeded LOCAL database on one machine. Note what "open" now means here: under Step −1 the hosted VPS is an audit fixture, so this risk is no longer about a deployment that exists — it is about the deployment this repository DESCRIBES how to make. That is not a downgrade. R-01's whole premise is that the first tenancy migration in `V3-E01` is an unrecoverable bet, and `V3-E01` will be written against these same artefacts; and the drill proves neither production data volume, nor a cross-machine restore, nor a cross-PostgreSQL-version restore, nor PITR/WAL, nor retention, nor any offsite copy (`docs/runbooks/backup-restore-drill.md` §7). Calling this `mitigated` would claim the half that matters most | open |
| **R-02** | **Cross-tenant data exposure persists undetected** because the fix is applied at the application layer only and a later query forgets the predicate | M | H | **CRITICAL** | V3-E01 | Database-enforced RLS *plus* application predicate; two-tenant adversarial suite (VAL-02) runs in CI on every PR, not once — **currently inert, see R-21 / PF-59** | open |
| **R-03** | **Canonicalising queries changes numbers users already rely on**, and the change is read as a new bug | H | M | **HIGH** | V3-E03 | Publish a KPI definition per metric with scope + freshness; ship a reconciliation console showing old vs new; announce before switching | open |
| **R-04** | **The routine marks a feature complete because its UI renders**, repeating the audit's central error | M | H | **HIGH** | routine | G-TRUTH + G-PORTAL gates; A2 Appendix A classification required in every PR; `OP` demands execution *and* read-back | mitigated by design |
| **R-05** | **Hosted images and schema drift from `main` again**, so V3 fixes are audited but not running | H | H | **CRITICAL** | V3-E02 | Immutable build/schema manifest per release; VAL-10 records running SHA + schema version; deploy gate compares them. **Impact raised M→H on 2026-08-02: this risk has already materialised.** The local Docker image (built 2026-06-06) carries `controllers: [AssessmentsController, GradesController]` in `dist/modules/grades/grades.module.js`, yet **no ref in the repository** has contained that line since 2026-06-01 — the running artefact was built from an uncommitted working tree. The consequence was not cosmetic: local behaviour looked healthy while the same routes returned 404 in production for seven weeks (`PF-62`), so manual testing could not have caught it. S-E02-1's `/version` manifest exposes the running SHA but **nothing compares it to the expected one** — that missing comparison is the live half of this risk. **Mitigation delivered 2026-08-03 (S-E02-6):** the comparison now exists between two independent sources (`GIT_SHA` baked at build time vs `EXPECTED_GIT_SHA` injected at deploy time); a drifted, dirty or unstamped artefact refuses to serve in production and fails `scripts/release-gate.sh`, which `deploy-prod.sh` runs after the healthcheck; `deploy-prod.sh` refuses a dirty working tree, which is the exact root cause of the materialisation. Step 2 also found the manifest was **inert** as shipped — the SHA was never stamped by anything and `/version` was unroutable behind nginx (`PF-68`). **Widened and completed 2026-08-03 (`S-E02-10`):** the mitigation covered **one artefact out of three**. `worker` and `web` are built and deployed separately and already carried a baked `GIT_SHA` that nothing could read, so two thirds of the deployment could drift undetected — and the gate *printed* `schemaVersion` without ever comparing it, so an artefact at the right commit sitting on a **never-baselined** database passed. Both measured against the previous script, which returned **exit 0** on each; the current one returns **exit 1**. All three artefacts now publish a self-identifying manifest, `EXPECTED_GIT_SHA` reaches all three, and the schema is compared against the migrations *this checkout* ships rather than against the artefact's own opinion of itself. **Not yet `mitigated`:** the gate has never run against the *hosted* deployment, because that deployment predates the manifests — running it against the equivalent local stack today fails **4/4**, which is this risk still live. It downgrades on the first deploy that passes the gate | **materialised — mitigation shipped for all three artefacts, unproven on hosted** |
| **R-06** | **Finance is built on mutable balances** and reproduces Lakoli's KPI/ledger divergence (DNC-01) | M | H | **HIGH** | V3-E15 | Immutable ledger entries only; every dashboard total derived with drill-down; reconciliation test with partial/over/duplicate/reversed cases | open |
| **R-07** | **A gate is weakened to let a story land** when evidence is hard to produce | M | H | **HIGH** | routine | Step 6 STOP condition #5 makes gate-weakening a stop, not a judgement call; deferred evidence must name a tracking finding id | mitigated by design |
| **R-08** | **Parent/child linkage repair changes who can see a child**, causing either lost access or wrongful access | M | H | **HIGH** | V3-E03 | Effective-dated link model with explicit historical states; ABAC tests for both directions; no silent widening | open |
| **R-09** | **Attendance atomicity fix invalidates existing rates**, and historical reports change retroactively | M | M | MEDIUM | V3-E09 | Recompute is explicit and versioned; label affected periods; do not silently rewrite published artefacts | open |
| **R-10** | **Audit backfill is impossible** for historical rows (no IP/UA/hash ever written), leaving a permanent gap | H | M | MEDIUM | V3-E04 | Do not fabricate history; start the chain at a declared genesis row and document the pre-V3 gap explicitly | open |
| **R-11** | **RLS degrades query performance** on large tenants once enabled | M | M | MEDIUM | V3-E01 | Benchmark before/after on the largest seeded tenant; index tenant predicates; treat as a release criterion not an afterthought |
| **R-12** | **Removing the demo seed breaks the hosted demo** that stakeholders use | H | L | MEDIUM | V3-E02/E06 | Keep an explicit, opt-in, clearly-labelled demo tenant provisioned by a separate command — never by production startup. **Delivered 2026-08-03 (S-E02-4):** the seed is now opt-in (`deploy-prod.sh --seed`) and gated on two independent signals, but the demo is **preserved, not deleted** — `DEPLOY_ENV=demo` + `ALLOW_SEED=provision-demo-data` reprovisions it, and the allowed path was **executed** to prove the guard is not a blanket refusal (the script crosses the guard and fails on the DB connection instead). Runbook: `docs/runbooks/provision-demo-tenant.md` | mitigated |
| **R-13** | **Legal pages are published without legal review**, creating a worse liability than the current 404 | M | M | MEDIUM | V3-E06 | Ship a holding page that states the policy is being finalised and gives a contact route; do not invent policy text (D-08) | open |
| **R-14** | **The four-portal check becomes a checkbox** rather than a real verification | M | M | MEDIUM | routine | G-PORTAL requires naming what was observed per portal, not asserting "4/4" | mitigated by design |
| **R-15** | **Scope silently expands** as capability epics discover foundation defects | H | M | MEDIUM | routine | dependency-map §5: no back-edges into L0; discovered defects become new finding ids scheduled at their own layer | mitigated by design |
| **R-16** | **V2 and V3 run on the same tick** and contend for the checkout | M | H | **HIGH** | routine | V3's lock wrapper delegates to V2's script, so both share one `write.lock`; additionally, operator must disable the V2 schedule before enabling V3 | mitigated by design |
| **R-23** | **A gate stage is permanently red and the runs report around it.** Observed 2026-08-03: `ci-gate.sh`'s `lint` stage has failed since it was written (ESLint v9 flat-config missing everywhere, `PF-70`), 4 of its 13 tasks are green only from turbo cache predating the version bump, and runs 5, 6 and 7 each declared their change green while citing typecheck/ratchet/build and never naming stage 2 | **H** | H | **CRITICAL** | routine / V3-E02 | A run must report the gate's **verdict line**, not a selection of its stages: if `ci-gate.sh` prints `GATE: FAIL`, the PR body says so and names the failing stage even when the cause is pre-existing. A cached green is not an executed green — when a stage's result decides a merge, re-run the suspicious task without cache before citing it. **Instance closed 2026-08-03 (`S-E02-7`):** the `lint` stage now executes in all 13 turbo tasks and exits 0, proven with `--force` so nothing could report a stale ✓, and proven to still **fail** on a deliberate error probe. Step 2 found the risk had been *understated* — not 4 cached-green tasks but **7 of 8 packages exiting 2**, with the single green one (`apps/web`) passing only through the deprecated `next lint`. A committed guard (`lint-gate.spec.ts`, 26/26) now fails if a package loses its flat config, if a `.eslintrc*` returns, or if a script reverts to `next lint`. **Risk stays `open` for the general case:** the guard protects *this* stage, and nothing yet forces a run to report the other five. **Run 10 (2026-08-03) exercised the discipline voluntarily:** it reports `ci-gate.sh`'s verdict line and every stage, and added a seventh stage (`lint:warnings`) rather than leaving the warning count outside the gate. Still voluntary, still unenforced | open — instance closed, discipline still unenforced |
| **R-22** | **"CI is down" is read as "the gate cannot exist"**, so stories that are locally executable are parked as blocked. Observed: the 2026-08-02 run recorded `S-E02-2` as ⛔ blocked by `PF-59` when the suites were runnable all along on the machine the routine already uses — and the first execution found a seven-week-old production P0 (`PF-62`) | **H** | H | **CRITICAL** | routine | `PF-59` blocks the *hosted runner*, not the *gate*. Before marking any story blocked on CI, check whether its evidence can be produced in the run's one permitted build slot; `scripts/ci-gate.sh` exists precisely so that check is one command. A story is only CI-blocked if its evidence genuinely requires hosted infrastructure | open |
| **R-21** | **Gate evidence silently reverts to assertion** because no CI runner exists (`PF-59`: Actions account-locked for billing). Every mitigation in this register that says "runs in CI" — R-02 above in particular — is currently inert, and a run could record a gate as satisfied on the strength of a test that was written but never executed | **H** | H | **CRITICAL** | routine / V3-E02 | Until billing is restored: a gate whose evidence is a CI test must be recorded as `evidence: deferred — PF-59` and the PR left open, never merged as green on an unexecuted test. Where a test can be run locally in the one permitted build slot, run it and say so explicitly. Restoring Actions billing is the actual fix and is an owner action | open |
| **R-24** | **A mechanical fix satisfies every gate and breaks the runtime.** Observed 2026-08-03 (`S-E02-8`): `eslint --fix` for `consistent-type-imports` rewrites NestJS constructor-injected imports to `import type`, erasing the `require()` and reducing `design:paramtypes` to `[Object, Object, Object]`. The app cannot boot — and `tsc --noEmit` returns **exit 0** (executed on the broken file), `nest build` passes, ESLint is satisfied because it authored the change, and the module-wiring guards read source, not constructors. Repo-wide `--fix`, `--update`, codemods and formatter migrations are all this shape: large, mechanical, reviewed by skimming, and validated by gates that only prove the code *compiles* | **M** | **H** | **CRITICAL** | routine / V3-E02 | A mechanical change over more than a handful of files must be justified against **what the gates cannot see**, not against their green result. Where a rule or codemod is incompatible with a compiler flag, encode the incompatibility in config with the measurement in its header and a guard tying it to the flag (`decorator-metadata.js` + `lint-ratchet.spec.ts`) rather than relying on a reviewer remembering. The real fix is a booted-application assertion — that is `PF-67`. **Delivered 2026-08-03 (`S-E02-9`):** `scripts/boot-check.js` constructs the real module graph of every Nest application and compares the API's booted route table against a reviewed baseline; it is stage 7 of `ci-gate.sh` and a step in `ci.yml`'s build job. **Executed against this exact trigger:** patching the emitted `design:paramtypes` of `analytics.service.js` to `[Object, Object, Object]` — the measured output of the `consistent-type-imports` autofix — makes the gate exit 1 naming the unresolvable dependency. A mechanical change of this shape can no longer pass. **Residual, and it is why this is `mitigated` and not `closed`:** the check calls `.compile()`, not `.init()`, so it proves the application can be *constructed*, not that it can serve traffic; and a mechanical change can still break things no gate models at all (a codemod that silently alters query semantics would still pass). The register entry stays as the standing instruction to justify large mechanical changes against **what the gates cannot see** | **mitigated — booted-application assertion now exists and was proven against the known trigger** |
| **R-25** | **A build reports success while emitting nothing.** Observed 2026-08-03 (`PF-72`): `apps/worker` combined `nest-cli.json`'s `deleteOutDir: true` with an inherited `incremental: true`, so `nest build` deleted `dist/` and then emitted **0 files at exit 0**. turbo cached the empty `dist/**` as that build's output and replayed it. Every gate was green over an artefact that did not exist. The general shape is wider than one flag pair: **no gate asserted that a build produced anything**, so any future toolchain change that silently stops emitting would land the same way | M | **H** | **HIGH** | V3-E02 | The boot check is the structural mitigation: it loads `dist/` and fails loudly when the artefact is absent or unloadable, so an empty build can no longer be reported as success (executed — removing `apps/worker/dist` makes the gate fail rather than skip). The specific flag conflict is fenced by a guard asserting every app whose `nest-cli.json` deletes its outDir resolves `incremental: false`, read through `tsc --showConfig` so an inherited value cannot hide. **Web half delivered 2026-08-03 (`S-E02-11`):** the residual was measured before it was fixed — with `apps/web/.next` deleted **in its entirety**, `boot-check.js` returned **exit 0** and no stage of `ci-gate.sh` read `.next` at all, so the whole web build could vanish behind a green gate. `scripts/web-artifact-check.js` now asserts `BUILD_ID`, a reviewed route inventory, an emitted server file per manifest route, non-empty `.next/static`, the standalone entrypoint `Dockerfile.web` actually runs, and that routes declared `mustBeDynamic` were not prerendered — the last of which holds `/version/web`, the web third of the release gate (R-05), to reading the container instead of a frozen build-time SHA. Exercised in **9 directions** (1 pass, 8 distinct failures, including `--update` refusing a broken artefact). **Residual, stated plainly:** the check does not *start* Next.js — booting it needs a port, a database and a session — so web is proven **produced and complete**, not **correct**, where the two Nest apps are proven constructible. That asymmetry is why this stays `mitigated` and not `closed` | **mitigated — all three artefacts now have a build-output assertion; only the two Nest ones are actually started** |
| **R-26** | **A declared invariant is trusted because it is written down.** Named on 2026-08-03 (`S-E02-12`) because it is now the mechanism behind **five** findings in six runs, not a one-off: `PF-69` (a directory in no gate), `PF-70` (a gate stage that had never once passed), `PF-72` (`deleteOutDir` + inherited `incremental`, emitting 0 files at exit 0), `PF-74` (`force-dynamic` mandated in a file header and enforced nowhere) and `PF-73` (`engines.node >=20.0.0` on a stack that cannot boot below 20.19). In every case the statement was correct-looking, load-bearing, and read by nothing. Two sub-shapes are worth separating: a declaration that is simply **wrong**, and one that is **true only by accident** — `.nvmrc: 22` (`PF-76`) resolved safely purely because nvm picks the newest patch, while *declaring* a range in which the API cannot start | **H** | M | **HIGH** | routine / V3-E02 | When a declaration decides behaviour — a version range, a build flag, a config directive, a route contract — something must **read it and fail**. Two rules learned the hard way: (a) read the **resolved** value, not the source that produces it (`tsc --showConfig` for `incremental`, `.next/required-server-files.json` for `output`, the emitted `prerender-manifest.json` for `force-dynamic`, pnpm's virtual store for dependency `engines`) — the defect lives in the output; (b) prove the new check against a declaration **known to be wrong**, because a check written against a healthy repo can only ever demonstrate that the repo is healthy. `S-E02-12` makes its evaluation a pure function precisely so 11 wrong declarations can be driven through it. **The class is not closed:** these five instances are, and each of the remaining declarations in the repo is a candidate until something reads it | open — five instances closed, the class is standing |
| **R-26** *(eighth instance — run 17)* | see **R-26** above. `PF-79`: `apps/web` was covered by no observability surface at all, and the gate **exited 0 on that exact state** — the class in its plainest form. But the instance worth recording is the one found *while closing it*, because it is a sub-shape none of the previous seven had: **a declaration whose FORM is load-bearing while its meaning is correct.** `apps/web/src/instrumentation.ts` guarded its Node-only work with an early `return` on `NEXT_RUNTIME`. Semantically flawless — the behavioural tests pass with it, and at runtime it loads nothing on edge. But webpack judges **reachability, not execution**: an early return is not a boundary it can see, so the `import()` stayed statically reachable and Next's edge compiler resolved `prom-client` for a runtime with no `fs`, `v8` or `cluster`. `next build` failed with **16 module-not-found errors**. Only `if (NEXT_RUNTIME === 'nodejs') { await import(…) }` gives `DefinePlugin` a branch dead-code elimination can remove | — | — | — | V3-E02 | Instance closed by `S-E02-15`. The general rule this adds to R-26's two: **when a construct is correct in meaning but load-bearing in shape, the shape is what must be asserted** — the two behavioural tests here passed with both forms, so only a syntactic guard could tell them apart. Proven in the negative against the real spec: the early-return form yields **1 failed / 40 passed**, naming the forbidden pattern. **The class stays open** | instance closed |
| **R-26** *(ninth instance — run 18)* | see **R-26** above. `PF-83`: `boot-gate.spec.ts` proved "the boot check runs after the build" by comparing `indexOf()` positions over the **raw text** of `ci.yml` and `ci-gate.sh`. `S-E06-1` added a comment naming `scripts/boot-check.js` above the build step and the guard read the prose — `bootAt`(4409) `< buildAt`(5543) — going red on a workflow whose step order was never touched. The sub-shape is the inverse of every previous instance: not *a declaration nobody reads*, but **a guard that reads more than the runner does**. Its danger is the repair rather than the defect — rewording the comment turns the test green, leaves the guard breakable by the next comment, and teaches the next author to write prose that pleases a regex | — | — | — | V3-E02 | Instance closed by `S-E06-1`. The assertion now runs over **executable content** (`#` comments blanked, byte offsets preserved) — R-26 rule (a) turned on a guard instead of on a config. The third test case is the one that makes this a fix rather than a deletion: a genuinely misordered fixture must still fail, or "ignore the comments" quietly becomes "assert nothing". Adds a rule to R-26's three: **a guard must be checked against a false positive as well as a false negative** — every prior instance was proven against a declaration known to be *wrong*, and this one needed proving against a file known to be *right*. **The class stays open** | instance closed |
| **R-26** *(tenth instance — run 20)* | see **R-26** above. `PF-86`: `infra/docker-compose.yml`'s own header, `docs/DEPLOYMENT.md`'s profile table and the routine's `SKILL.md` Step −1 all documented how to start the local stack, and **all three commands started something else** — Compose resolves `.env` from the compose file's directory, so every port fell through to a `${VAR:-default}` nobody had chosen. The invariant *"the ports come from `.env`"* was written in four places and executed in one (`infra/pilotage.sh`). Sharper than the previous nine, and worth recording as such: this instance was not merely unverified, it was **internally self-contradicting inside one tracked file** — `KC_HOSTNAME` hard-coded `8180` while `ports:` defaulted to `8080`, so the api's expected token issuer was reachable on no port and login was broken by construction. Adds a rule to R-26's four: **a value that appears twice is a declaration that will eventually be false**, so the fix is not a better default but a single source and a refusal. Closed by `S-E02-16` / ADR-026 with `${VAR:?…}`, derived URLs, and `scripts/compose-invocation-check.js` as stage 0c. **The class stays open** — and note that this instance was found by *running the documented command* (run 19) rather than by a gate, which is the second time in six runs that reading the repository could not have found it | instance closed |
| **R-26** *(eleventh instance — run 20, found by the gate itself)* | see **R-26** above. `PF-89`: `docker compose --profile prod up -d` was documented in two places and was an **invalid Compose project** — `nginx` depends on `web`/`api`, which no active profile enabled, and Compose refuses the whole project rather than degrading it. Recorded as a separate instance because of *how* it was found: `scripts/compose-invocation-check.js` reported it on its **first execution**, before anyone had read that part of the file. That is the shape the R-26 mitigation is supposed to produce — a gate that generalises a rule finds the instances the audit missed — and it is the first time in the register that a newly written gate paid for itself inside the same run. Closed in that run | instance closed |
| **R-17** | **Capability work starts before the market decision**, building Ivorian-specific modules for an EU-shaped product (or vice versa) | M | H | **HIGH** | V3-E18 | D-04 is a hard STOP condition; L4 epics cannot be selected while D-04 is unresolved | open |
| **R-18** | **Provider integration is built against an unavailable sandbox**, producing untestable code | M | M | MEDIUM | V3-E16/E17 | Credential-required is a STOP condition; provider adapter behind an interface with a deterministic fake for tests | open |
| **R-19** | **Accessibility regressions accumulate** because WCAG is never actually run | H | M | MEDIUM | V3-E02 | Put the existing axe/Playwright assets into CI as a gate (VAL-01/VAL-08); nested-interactive-control lint rule | open |
| **R-20** | **The audit's own snapshot goes stale** and V3 fixes findings that no longer reproduce | M | L | LOW | routine | Step 2 requires re-confirming reproduction before implementing; non-reproducing findings close as `closed-by-other-work` | mitigated by design |
| **R-27** | **The single-writer guarantee lapses in the middle of a run.** `routine-lock.sh` reclaims a `write.lock` whose heartbeat is older than `STALE_MIN` (60 min). The routine asks for a heartbeat around the **build** (Step 4) and nowhere in **Step 3**, which has taken over an hour in every run since run 9. Observed on run 15, not theorised: the lock was taken at 20:03, a second V3 tick reaped it as stale at **21:09**, held it, and released it at 21:11 — so the run kept writing to the checkout for half an hour holding nothing, which is exactly the pile-up the lock exists to prevent. Re-acquiring the gate then ran the salvage path, which `git stash`ed every tracked modification and reset the branch to `origin/main`; the slice survived only because that salvage exists | **H** | **H** | **CRITICAL** | routine | Heartbeat inside Step 3, not only Step 4 — the phase that takes longest is the one currently uncovered. Treat `heartbeat` printing `no lock held` as a stop-and-diagnose signal rather than a warning: by the time it prints, the guarantee has already lapsed and another run may have written. Do **not** re-run `gate` to recover — it salvages and resets. Longer term the lock should be refreshed by the act of writing rather than by remembering to call a script, which is the same "declared invariant nobody reads" shape as **R-26**. Recorded as `PF-77`; the fix belongs to the routine's own files, which live outside this repository, so it is an operator action | open |
| **R-28** | **A security header is added, observed to be present, and is inert or actively breaking — because presence was checked and effect was not.** Named on 2026-08-07 (`S-E06-2`), and it is the sharpest thing run 20 found. The nonce-based CSP was correct by every reading: the policy string was right, `csp-check.js` drove the builder and passed, the guard spec passed 40/40, the full gate passed 14/14, and `curl` against the rebuilt stack returned a well-formed `content-security-policy` header with a fresh nonce. **All of that was true while the login page was dead.** `/admin/login` is statically prerendered, so Next served it from the build-time cache: `x-nextjs-cache: HIT`, **21 `<script>` tags, 0 carrying a nonce** — and `'strict-dynamic'` makes browsers ignore `'self'`, so every script on the page would be blocked. The generalisation is not "remember prerendering": it is that **a header's correctness is a property of the header *and the document it is served with*, and every cheap check only sees the header.** Dropping `'strict-dynamic'` does not escape it either — the 15 external scripts would load and the 6 inline RSC-payload scripts would still be blocked, so the page renders and does not respond, which is worse because it looks alive | M | **H** | **HIGH** | V3-E06 | Render on demand so the nonce can reach the document (`force-dynamic` at the root layout — the rule at one point, not enumerated per route, because the fifteenth static page added later would fail silently). Enforced by `scripts/csp-check.js`: **no prerendered document may contain a `<script>` tag**, proven in the negative against the real artefact, where it named all 14. The wider lesson for the next security-header slice: **check the artefact the header is attached to, not only the header** | open |
| **R-29** | **The routine can be running several full gates at once, and the log cannot tell you so.** Stopping a background task kills the task shell but not the process tree beneath it on Windows, so a stopped `scripts/ci-gate.sh` keeps running while its replacement starts. Measured on run 20: **five** live `ci-gate.sh` processes, one inside `pnpm build` and one inside `test-ratchet.js api`, all redirecting to and truncating the same file. What makes this a risk rather than an inefficiency is that the corruption is **silent and plausible** — the merged log reads like one coherent run, so a verdict assembled from it can report a stage that a different process ran, or a `✗ test:api` produced by a jest run that read a spec file mid-edit. It also quietly breaks the one-`pnpm build`-per-run budget. **The write lock cannot detect it**: every process belongs to the same run and holds the same lock, so the single-writer invariant is satisfied while the guarantee it exists to give is not — the same gap as **R-27**, from the opposite direction (R-27 is one run holding no lock; R-29 is one lock holding several runs) | **M** | **H** | **HIGH** | routine | After stopping a background gate, enumerate `Win32_Process` for `ci-gate.sh` and kill each tree explicitly before relaunching; verify zero remain. Take only the final, uncontended log as the verdict (**R-23**). Treat a stage list that reads back in an impossible order — a stage present in one read and absent in the next — as the signature, because that is how this was caught. Recorded as `PF-90`; the durable fix is in the harness's task handling, outside this repository, so it is an operator action | open |
| **R-26** *(sixth instance — run 15)* | see **R-26** above. `PF-56`'s observability profile was declared in `docker-compose.yml` with **all three** of its bind-mount sources absent from the repository, so Prometheus would have received a directory where it expects its config file, and no application registered a metric for it to scrape. Nothing had ever tried | — | — | — | V3-E02 | Instance closed by `S-E02-13`: `scripts/observability-check.js` reads the profile, and the general rule it enforces — *every relative bind-mount source in every compose file must exist* — is the one that would have caught it rather than a fix for these three paths. **The class stays open**, as R-26 itself says: each remaining declaration in the repo is a candidate until something reads it | instance closed |
| **R-26** *(seventh instance — run 16)* | see **R-26** above. `PF-78`: a `jaeger` collector and an `OTEL_EXPORTER_OTLP_ENDPOINT` were declared, and no application started an SDK. Measured while closing it, the declaration was **wider than the finding said** — the variable sat on the compose file's shared environment anchor, so it reached **five** services (`migrator`, `api`, `worker`, `web`, `seed`), three of which can never emit. The sub-shape this instance adds is the most dangerous one yet: **a declaration that is correct but arrives too late to mean anything.** OpenTelemetry patches modules as they load, so a SDK started after `require("./app.module")` is configured exactly right and patches nothing — no error, no log, no span. The gate's own emission probe reproduced it by accident, requiring `node:http` one line too early and reporting 0 spans | — | — | — | V3-E02 | Instance closed by `S-E02-14`: `scripts/tracing-check.js` reads the load order off the **emitted** `main.js` (R-26 rule (a) — the defect lives in the output), compares the set of services handed a collector against `TRACED_SERVICES` **in both directions**, and then does what no previous instance could: it **runs the thing**, spawning a child process that serves a real request and fails if no span comes out. That last step is the answer to the sub-shape — an order assertion can be satisfied while emission is still broken for some other reason, so the gate asserts the outcome as well as the condition. **The class stays open** | instance closed |

## 2. Risks explicitly accepted (for now)

| ID | Risk | Why accepted | Revisit when |
|---|---|---|---|
| **A-01** | Pre-V3 audit history has no IP/UA/hash chain | Cannot be reconstructed; fabricating it would be worse than the gap | Never — documented permanently |
| **A-02** | Lakoli's teacher workspace and sensitive-follow-up behaviour remain unvalidated | Blocked by role; not required to build our own | If a competitive claim depends on it |
| **A-03** | Orientation/Conformity depth is unknown beyond the shipped client | `IMPLEMENTED_BUT_GATED`; not on our roadmap until D-04 | D-04 resolves toward Côte d'Ivoire |
| **A-04** | Synthetic audit residue remains in both systems | Recoverable demo data; documented in both audits | Before any production cutover |
| **A-05** | 22 French holidays were written to Pilotage's stale active year by an unconfirmed control | Recoverable; reversing needs explicit authorisation | Fixed as part of PF-15 / PF-29 |

## 3. Risk-to-gate mapping

| Gate | Risks it exists to control |
|---|---|
| G-TENANT | R-02, R-08, R-11 |
| G-AUTHZ | R-02, R-08 |
| G-MIGRATION | R-01, R-05, R-12 |
| G-AUDIT | R-10 |
| G-TRUTH | R-03, R-04, R-06, R-09 |
| G-PORTAL | R-04, R-14 |
| G-DNC | R-06, and the whole `DNC` register |

## 4. Review cadence

- **Every run:** the routine reads this file and refuses to select a story whose owning epic has an open `CRITICAL`
  risk with no mitigation in `status: mitigated|accepted`.
- **Every layer completion:** full re-score.
- **On any new P0 finding:** add a risk row in the same commit.
