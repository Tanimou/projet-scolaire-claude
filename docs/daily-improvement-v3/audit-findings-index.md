# Audit Findings Index — the traceable backbone of Daily Improvement V3

Every V3 epic, story and acceptance criterion traces back to a row in this file. Nothing enters the roadmap without a
finding id; nothing is closed without evidence against the finding.

**Sources** (round-5 authoritative, 2026-08-02)
`A1` = `01_Lakoli_Platform_Audit.md` · `A2` = `02_Internal_Platform_Audit.md` · `A3` = `03_Comparative_Gap_Analysis.md`
`A4` = `04_Exploration_Coverage_Matrix.md` · `E1` = `audit-evidence/lakoli/lakoli_deep-workflow-browser-audit-07.md`
`E2` = `audit-evidence/internal/internal_hosted-multirole-browser-audit-04.md`

**Classification vocabulary** — deliberately *not* collapsed to "missing/present", per A2 Appendix A and A4 §1:

| Code | Meaning |
|---|---|
| `BROKEN_RUNTIME` | Route/action exists and fails at runtime |
| `BROKEN_TRUTH` | Multiple surfaces report mutually incompatible facts from one dataset |
| `BROKEN_SECURITY` | Reachable path violates tenancy, ABAC or privilege boundaries |
| `SOURCE_ONLY` | Implemented in repo; hosted API/schema cannot serve it |
| `BACKEND_ONLY` | Endpoint/model exists with no reachable product control |
| `UI_ONLY / MOCK` | Surface exists without dependable backend truth |
| `NOT_IMPLEMENTED` | Explicitly deferred or absent |
| `BLOCKED_BY_DEPENDENCY` | Needs a record, provider, credential or installed dependency |
| `MISSING_CAPABILITY` | Competitor-proven capability we do not have |
| `DO_NOT_COPY` | Lakoli behaviour we must consciously *not* reproduce |
| `TECH_DEBT` | Working code carrying quantified, tracked debt — added 2026-08-03 for `PF-71`, whose 996 lint warnings are neither a runtime failure nor a security hole, but must not be silenced or forgotten |
| `CLOSED` | The capability now exists, a gate **executes** it, and that gate has been shown to fail on the pre-fix state — added 2026-08-04 for `PF-79`. The row keeps its original wording so what was found stays legible; the closure note states what was built **and what it deliberately did not cover**. A finding is never marked closed on the strength of code alone |

**Work-type vocabulary** — drives which V3 layer a finding lands in:
`DEFECT` · `ARCH_PREREQ` (architectural prerequisite) · `CAPABILITY` (net-new) · `VALIDATION` (evidence, not code).

---

## 1. P0 — release-blocking (Pilotage)

| ID | Finding | Class | Type | Source | Layer | Epic |
|---|---|---|---|---|---|---|
| **PF-01** | New/unmapped authenticated profiles are attached to a hard-coded `demo` tenant (`UserSyncService`, public registration) | `BROKEN_SECURITY` | ARCH_PREREQ | A2 §11, §14; E2 | L0 | V3-E01 |
| **PF-02** | `withTenant` helper exists with **no call sites**; no `CREATE POLICY` / `ENABLE ROW LEVEL SECURITY` anywhere. The "repositories are RLS-isolated" comment is unsupported | `BROKEN_SECURITY` | ARCH_PREREQ | A2 §11, §14; E2 | L0 | V3-E01 |
| **PF-03** | Production startup runs `prisma db push --accept-data-loss`; repository has **no SQL migration history**; prod compose runs demo seed outside `NODE_ENV=production`. **Residual half closed 2026-08-07 by `S-E02-5`.** The ledger existed (`S-E02-1`) and the seed was guarded (`S-E02-4`), but nothing *kept* the ledger honest: editing `apps/api/prisma/schema.prisma` without writing a migration passed every gate — `ci-gate.sh` runs `prisma generate`, which produces a client for a schema no migration builds, and `migrate-entrypoint.sh` runs only `migrate deploy`, so the edit reaches no database ever. That is `db push`'s failure mode through the front door. `scripts/schema-drift-check.js` now builds a scratch database FROM the migrations and diffs **that database** against the datamodel, wired into both harnesses (`ADR-027`). Still **not** covered, stated rather than implied: it proves the ledger *reproduces* and *executes*, not that a migration is **safe** (a data-destroying `DROP COLUMN` passes), and it reads no **deployed** database — that half is `S-E02-1`'s residual plus `restore-drill.js`'s seam | `BROKEN_SECURITY` | ARCH_PREREQ | A2 §12, §14; E2 | L0 | V3-E02 |
| **PF-81** | **The hosted Keycloak master realm runs on `admin`/`admin`.** Distinct from `PF-54`, which is about *application code* falling back to a default — this is the *infrastructure declaration*, and it is why that fallback was never noticed. `infra/docker-compose.yml` sets `KEYCLOAK_ADMIN: admin` / `KEYCLOAK_ADMIN_PASSWORD: admin` on the `keycloak` service itself, and `infra/docker-compose.prod.yml` **does not override either** — verified on `main` by reading the `keycloak` service block of the prod overlay, which changes only hostname/proxy/healthcheck settings. Meanwhile the prod overlay declared `KEYCLOAK_URL` for the `api` service but *neither* admin variable, so the API resolved both through `?? 'admin'` and **matched**. The default was not latent: it was the live credential of the production identity provider, and the two defects concealed each other — the code's fallback was correct about the deployment, which is why nothing ever failed. `S-E06-1` removed the code fallback and made the deploy command refuse without an explicit declaration, which is what converts this from invisible to blocking; it deliberately **rotated nothing**. Resolution is an operator action in two ordered steps (rotate in Keycloak, *then* update `.env.prod`) and is a routine STOP condition (external credential) | `BROKEN_SECURITY` | DEFECT | Discovered by the V3 run of 2026-08-04 (run 18), verifying `S-E06-1`'s premise against `main` | L0 | V3-E06 *(operator)* |
| **PF-82** | **The production-artefact scan reads source, so the story's own acceptance criterion is proven one step short of where it was written.** `S-E06-1` AC-1 says *"none of the scanned strings appears in a production **build**"*; `scripts/production-artefact-check.js` scans `apps/{web,api,worker}/src`. For the four Maildev literals the two are equivalent. For the class this story actually inventories they are not: `NEXT_PUBLIC_*` values are **build-time inlined** by Next, so with the 17 Tier-B `?? 'http://localhost…'` fallbacks still present, an image built without those variables bakes `http://localhost:8180` into the client bundle — and the gate stays green, because the source it read is unchanged. Vendor code compiled into the bundle is likewise unscanned. The script's header is accurate about its scope (it says *"production-facing source"*), so this is an evidence gap, not a false claim; recorded rather than left implicit because the gap is exactly the class of defect the story exists to close. Closing it means a second scan pass over `apps/web/.next` and `apps/*/dist`, in the shape `web-artifact-check.js` already established | `TECH_DEBT` | VALIDATION | Discovered by the V3 run of 2026-08-04 (run 18) | L0 | V3-E06 |
| **PF-83** | **A guard that a comment could turn red.** `boot-gate.spec.ts` proved the boot check runs *after* the build by comparing `indexOf('scripts/boot-check.js')` against `indexOf('pnpm build')` on the **raw text** of `ci.yml` / `ci-gate.sh`. `S-E06-1` added a comment naming that script above the build step to explain why its new env block is safe, and the guard read the prose: `bootAt`(4409) `< buildAt`(5543), **1 NEW test failure** on a workflow whose step order was never touched. R-26's ninth instance and a new sub-shape — not a declaration nobody reads, but **a guard that reads more than the runner does**. The danger is the repair: the obvious fix is to reword the comment, which leaves the guard fragile and trains the next author to write prose that pleases a regex. Fixed in the same run by asserting against executable content (`#` comments blanked, byte offsets preserved) — R-26 rule (a) applied to a guard instead of to a config — with the false positive reproduced against the real files in both directions, plus a case proving a genuinely misordered file is still caught | `TECH_DEBT` | DEFECT | Discovered **and closed** by the V3 run of 2026-08-04 (run 18) | L0 | V3-E02 |
| **PF-84** | **The safe migrator has never been able to start.** `infra/docker-compose.yml`'s `migrator` service runs `command: ["sh", "/app/infra/docker/migrate-entrypoint.sh"]` — the entrypoint delivered by `S-E02-1` to replace `prisma db push --accept-data-loss`. `.dockerignore` excluded **`infra/docker`**, under the heading *"Existing Docker artefacts (avoid recursion)"*, so `COPY . .` in `Dockerfile.api`'s `build` stage could never copy it. Every start of that service, on every stack, in every build that has ever existed, ends in `sh: can't open '/app/infra/docker/migrate-entrypoint.sh': No such file or directory`, **exit 2**. Executed against the real image, not read: `ls /app/infra` inside the container lists `docker-compose.yml`, `keycloak/`, `nginx/`, `postgres/`, `pilotage.sh` — and **no `docker/`**, so the absence is structural rather than staleness. Blast radius: `infra/docker-compose.prod.yml` deliberately inherits the command *(«Aucune surcharge nécessaire ici»)*, and `scripts/deploy-prod.sh:182` carries the comment *"implies migrator (migrate deploy) completed successfully"* — which has never once been true. Per Step −1 the severity is **"the deployment description is wrong"**, not an incident: PF-03's mechanism was inert, so nothing applied migrations *and* nothing ran `db push` either. Fixed in the run that found it by deleting the `.dockerignore` line; a guard keeps it out | `BROKEN_RUNTIME` | DEFECT | Discovered **and closed** by the V3 run of 2026-08-06 (run 19), by running the migrator | L0 | V3-E02 |
| **PF-85** | **The sprint workflow honours the operator's story id and then briefs the agents on a different story.** `PF-60` made `mode` / `epic` / `slice` resolve as `ARG_* \|\| intake.*`, so the operator override wins — for those three fields. It left `intent` untouched, and `intent` is the field every downstream agent actually reads (`sliceCtx`: `INTENT: ${intake.intent}`). The identifiers are a label; the intent is the instruction body. Measured on run 19: the routine passed `epic=V3-E02`, `slice=S-E02-3` and a 6 000-character hint; intake returned `V3-E06` / `S-E06-2` with a 1 500-character intent describing CSP and branding sanitisation; `epicId` correctly resolved to `V3-E02` — and the UX and test-architect agents were briefed on `S-E06-2` and produced specs for it. The run was killed after two agents. This is `PF-60`'s second half, not a regression: the fix was partial and the partition it drew (identifiers vs body) is exactly where the leak was. Fixed in the same run — when `ARG_SLICE` is set, the operator's slice + hint **is** the intent, intake's prose is discarded rather than merged, the disagreement is logged, and `JSON.stringify(intake)` in the `epic-spec` branch was swapped for a resolved object so it cannot re-leak the same way | `TECH_DEBT` | DEFECT | Discovered **and closed** by the V3 run of 2026-08-06 (run 19) | L0 | V3-E02 *(routine)* |
| **PF-87** | **The operator override was never read, and a built-in method name is why nobody noticed.** `bmad/workflows/sprint.workflow.js` read `args.mode` / `args.epic` / `args.slice` directly, but `args` arrives as a **JSON string**. On a string, `args.mode` and `args.epic` are `undefined` — so `ARG_MODE` fell back to `'auto'`, `ARG_EPIC` to `''`, and the intake agent's own pick won every time — while **`args.slice` resolves to `String.prototype.slice`**, a function, therefore **truthy**. The single field carrying the story's identity became the literal text `function slice() { [native code] }`, and the sprint proceeded with a story identifier that was a native-code stub. Run 19 briefed **three consecutive sprints** on `V3-E06 / S-E06-2` while the routine had selected `V3-E02 / S-E02-3`; the diagnosis came from an agent's own report — *"the operator override arrived malformed: SLICE and INTENT were both `function slice() { [native code] }`"*. **This is almost certainly the true mechanism behind `PF-60`**, which was diagnosed as a precedence bug (`intake.* \|\| ARG_*`) and fixed as one: that fix was correct and necessary, but with `ARG_EPIC` resolving to `''` it could not have changed any outcome — which is exactly why the symptom returned. The general shape is worth keeping: **a missing value that collides with a built-in method name never reads as missing.** `args.worktree` was equally undefined and equally invisible, because its fallback happened to be the right path. Fixed in the run that found it: `args` is parsed when it is a string, only **own string** properties are accepted (an inherited function is never a story id), and the resolved values are `log()`ged on the first line of every run so the next occurrence is visible in one glance instead of three sprints | `TECH_DEBT` | DEFECT | Discovered **and closed** by the V3 run of 2026-08-06 (run 19) | L0 | V3-E02 *(routine)* |
| **PF-86** | **The documented way to start the stack starts a different stack.** Docker Compose resolves `.env` from the **project directory** — the compose file's directory, `infra/` — not from the invoker's cwd. `POSTGRES_PORT=5433` and `KEYCLOAK_PORT=8180` live in the **root** `.env`, and `infra/.env` does not exist. So `docker compose up -d` (the command in `infra/docker-compose.yml`'s own header) and `docker compose -f infra/docker-compose.yml …` (the command in the routine's `SKILL.md` Step −1) both resolve `${POSTGRES_PORT:-5432}` to the **default**, and silently bring Postgres and Keycloak up **with no published ports at all**. Only `infra/pilotage.sh` passes `--env-file`. Observed live on run 19: a compose invocation from `infra/` **recreated** the running Postgres without its `5433:5432` mapping, and every host-side `prisma migrate diff` then failed `P1001 Can't reach database server at 127.0.0.1:5433` until `--env-file .env` was added. Two commands that look identical produce two different stacks, and the difference is invisible until something outside a container tries to connect. Second, smaller instance in the same file: the `seed` service `depends_on: api`, which lives in profile `app`, so `--profile seed` alone fails with *"service seed depends on undefined service api"* — the documented seed invocation cannot work either. **Re-scoped 2026-08-07 (run 20) from `TECH_DEBT` to `BROKEN_RUNTIME`, on measurement rather than on judgement.** The original text closed with *"neither is a runtime defect in the application; both are defects in how the system is described to be run"*. That was wrong, and the counter-evidence is inside `infra/docker-compose.yml` alone — it needs no reference to any untracked file. `KC_HOSTNAME` and `KEYCLOAK_PUBLIC_URL` hard-coded `http://localhost:8180`, the api uses the latter as its **expected token issuer**, `keycloak.ports` defaulted to `8080`, and the web's `NEXT_PUBLIC_KEYCLOAK_URL` defaulted to `8080` too. So on the documented path Keycloak published on host 8080, announced an issuer reachable on no port, sent the browser to 8080, and the api rejected the resulting token for a wrong issuer: **login was broken by construction**, and appeared to work only because one machine's gitignored `.env` said `8180` on a code path where compose never read it. A third instance of the profile half — `--profile prod` — was found by the gate written to close this finding and is recorded separately as **`PF-89`**. Closed by `S-E02-16` / ADR-026: no published host port carries a default (compose refuses and names the remedy), no host port is written twice, every profile activates its own dependencies, and `.env.example` stops describing a third, incompatible stack | `BROKEN_RUNTIME` | DEFECT | Discovered by the V3 run of 2026-08-06 (run 19), by running the documented command; re-scoped by run 20, by measuring it | L0 | V3-E02 |
| **PF-88** | **`PATCH /schools/:id/branding` writes any school, in any tenant.** The route resolved its target with `prisma.school.findUnique({ where: { id } })` — the id came from the path and the caller's tenant was never consulted. `@RequiresPermission('branding.write')` answers *may this user write a branding*, not *whose*; so any holder of that permission in **any** instance could rewrite the branding of **any** school in the database. On its own that is a cross-tenant write. Composed with `PF-45` — which this run found to be stored XSS rather than CSS injection, because `red}</style><script>alert(1)</script>` is 37 characters and the DTO allowed 60 — it is a **cross-tenant stored XSS**: the attacker writes into a victim tenant's row, and the payload is served from a server-rendered `<style>` on every authenticated page of the victim's four portals. Not in the audit: A2 §11 recorded the injection sink and the disabled CSP, and stopped at the render side. Found at Step 2 by reading the write path the story was about to validate — which is the argument for Step 2 reading the implementation rather than the finding. Fixed in the same run: `update(tenantId, schoolId, patch)` with the constraint **inside** the query (`findFirst({ id, tenantId })`, not read-then-compare), 404 rather than 403 so the response does not confirm the id exists | `BROKEN_SECURITY` | DEFECT | Discovered **and closed** by the V3 run of 2026-08-07 (run 20), at Step 2 of `S-E06-2` | L0 | V3-E06 |
| **PF-89** | **`--profile prod` alone is an invalid Compose project, so the second documented profile command cannot run either.** Discovered by `scripts/compose-invocation-check.js` on its first execution, while closing `PF-86` — not by reading. `nginx` lives in profile `prod` and `depends_on` `web` and `api`, both of which live in profile `app`; Compose refuses the **entire** project in that situation rather than degrading it, so `docker compose --profile prod up -d` — the command in `infra/docker-compose.yml`'s own header and in `docs/DEPLOYMENT.md`'s profile table — died on *"service nginx depends on undefined service web"*. This is the same shape as the `seed`/`api` instance recorded inside `PF-86`, at a second address, and it is recorded separately because it was found by an **executed** gate rather than by an audit: it is the evidence that the rule generalises rather than describing one mistake. Two of the file's five profiles were unrunnable exactly as documented, and nothing in the repository could tell you so. Closed in the same run by giving `migrator`, `api`, `worker` and `web` the profiles that depend on them, and held thereafter by rule **C4** of the new gate | `TECH_DEBT` | DEFECT | Discovered by the V3 run of 2026-08-07 (run 20), by the gate written to close PF-86 | L0 | V3-E02 |
| **PF-90** | **Stopping a background task does not stop what it started, so the routine can run several full gates at once — against one log file.** Observed live on run 20, three times over. `bash scripts/ci-gate.sh` was launched as a background task and stopped when a lint error made its verdict void; the task was reported *"Successfully stopped"*, but on Windows the harness kills the task shell and **not the process tree beneath it**, so `ci-gate.sh` kept running. Relaunching produced a second, then a third. `Get-CimInstance Win32_Process` found **five** live `ci-gate.sh` processes, one inside `pnpm build`, another inside `test-ratchet.js api` — all three writing to, and truncating, the same redirected log. The consequence is not slowness, it is **evidence corruption**: the stage list read back in an impossible order (`▶ build` present at one moment and absent the next), a stage could be reported by a run other than the one being read, and the `test:api` failure the log showed was produced by a jest run reading a spec file that was being edited concurrently. A gate verdict assembled from that log is unusable, and nothing in the log says so. This is the same family as `PF-77` (the lock lapses mid-run) and `PF-80` (the workflow reports `landed: true` on a tree it did not check): **the routine's controls describe a single writer while its machinery permits several.** The lock does not help — all three processes are the same run, holding the same lock, so single-writer is satisfied and the guarantee still fails. Mitigation used on run 20, and the one to repeat: after stopping a background gate, enumerate `Win32_Process` for `ci-gate.sh` and kill the tree explicitly before relaunching, then treat only the final, uncontended log as the verdict (**R-23**) | `TECH_DEBT` | DEFECT | Discovered by the V3 run of 2026-08-07 (run 20), by being bitten | L0 | V3-E02 *(routine)* |
| **PF-91** | **Nine authenticated-flow routes are exempted by the middleware and emitted by nothing, so an invitation or password-reset email lands on a 404.** `apps/web/src/middleware.ts:37-49` lists `/admin/accept-invite`, `/admin/forgot-password`, `/admin/reset-password`, `/teacher/accept-invite`, `/teacher/forgot-password`, `/teacher/reset-password`, `/parent/forgot-password`, `/parent/reset-password` and `/parent/verify-email` as unauthenticated-reachable — and **none of the nine is in the emitted route manifest**. The exemption list is a promise the app cannot keep: the middleware carefully waves through a page that does not exist. Every one of them is on the *first* path a new user takes (accept the invite) or the *recovery* path (forgot the password), which is exactly where a 404 is least recoverable — there is no logged-in shell to go back to. Found by `scripts/link-integrity-check.js` on its first execution, while closing `PF-19`: the gate was written to catch a literal swallowed by a `[param]`, and the nine dead exemptions fell out of the same pass as ordinary dead targets. Inventoried in `scripts/link-integrity-baseline.json` with this id and **not fixed** — building nine auth surfaces is its own story, and it belongs to `V3-E05`, not to a navigation-hygiene slice | `BROKEN_RUNTIME` | DEFECT | Discovered by the V3 run of 2026-08-07 (run 21), by the gate written to close PF-19 | L0 | V3-E05 |
| **PF-92** | **`/parent/remediation` has no index route while `/admin/remediation` and `/teacher/remediation` both do.** The parent is the one role for whom the remediation loop was designed — E7's whole thesis is alert → diagnosis → bookable resource → measured improvement, and the parent is the actor who books. `/parent/remediation/[planId]` exists, so a parent who arrives from an alert deep-link is served; a parent who trims the URL, or follows any link to the section root, is not. Asymmetric on role in the direction that hurts the core user. Inventoried in the link-integrity baseline, not fixed | `BROKEN_RUNTIME` | DEFECT | Discovered by the V3 run of 2026-08-07 (run 21), by the gate written to close PF-19 | L0 | V3-E06 |
| **PF-93** | **The four bare portal roots — `/admin`, `/teacher`, `/parent`, `/student` — have no index route.** Every portal is a route *prefix* (ADR-003) with pages beneath it and nothing at the top, so the most guessable URL in each portal 404s. Discovered as a by-product of a deliberate design choice in the gate, and worth recording as such: `extractLiteralLinks` treats *any* literal string starting with `/` as a link, with no `href`/`Link` context, so the `PUBLIC_PREFIXES`/portal-prefix constants in `apps/web/src/middleware.ts` entered the inventory alongside real hrefs. Six of those seven prefix constants are **not** links (`/api/auth` and `/favicon` are prefixes by construction) — but four of them are also, independently, a real navigation gap. The false-positive shape and the true finding arrived in the same row, which is why the baseline rows say so explicitly rather than reading as clean debt. The extractor keeps the loose definition on purpose (it is what surfaced this); the follow-up is a `class: "prefix-constant"` tag so the ceiling can reach zero | `BROKEN_RUNTIME` | DEFECT | Discovered by the V3 run of 2026-08-07 (run 21), by the gate written to close PF-19 | L0 | V3-E06 |
| **PF-94** | **`/pricing` and `/contact` are dead links in the public landing footer.** `apps/web/src/app/page.tsx:771,788` — the unauthenticated marketing surface, the first page a prospective school sees, linking to two routes that were never built. Same family as `PF-38` (`/legal/*`) and `PF-39` (`/help`): the product advertises a completeness it does not have, on the surface where credibility is decided. Unlike the legal trio, these are **not** blocked on **D-08** — no policy text is required to write a contact page — so they are the cheapest row in the ceiling to retire. Inventoried, not fixed | `BROKEN_RUNTIME` | DEFECT | Discovered by the V3 run of 2026-08-07 (run 21), by the gate written to close PF-19 | L0 | V3-E06 |
| **PF-95** | **A raw control byte in a source file makes it binary to git, and every gate the routine owns is blind to it — including the hygiene gate, by construction.** `apps/api/src/modules/calendar/calendar-seed.service.ts` was written with a **literal U+0000** (offset 3055) as a composite-key separator. Confirmed consequences: `git diff --no-index /dev/null <file>` reported `Bin 0 -> 13165 bytes`, so the 333-line file carrying `S-E06-6`'s transaction, dedup key, audit row and tenant scoping would have reached a **mandated P1 human review** as *"Binary file not shown"*; `.gitattributes` declares `*.ts text eol=lf`, so git was told to run its text filter over content it simultaneously detected as binary, and neither EOL normalisation nor the whitespace scan applied; and `grep -rn "createMany"` over the module returned **only spec hits** — the real `tx.calendarEvent.createMany` was invisible without `-a`, so every future grep-based audit or ratchet over `apps/api` would have silently excluded the file. It would have been the **only** binary source file among **691** tracked `.ts`/`.tsx`. **The finding is not the byte; it is that nothing could have caught it.** `git diff --check` has no text to scan on a binary file, so the diff-hygiene gate cannot fail on this class *by construction*; a diff-reading reviewer sees a space; and neither `production-artefact-check.js` nor `link-integrity-check.js` covers source-byte hygiene. The class had already bitten `S-E06-2`, where a NUL in a comment was caught only because it happened to trip `no-control-regex` in a file eslint was already reading — i.e. by luck, at a second address. Found this run only because two verify-phase reviewers ran `file` / `git diff --no-index` / `grep` out of band. Fixed in `S-E06-6` (the separator is now `\|`, the repo's 7-site composite-key delimiter, collision-free against a fixed 11-entry title table) but the **gate is not built**: a ~10-line "tracked `*.ts`/`*.tsx` must contain no `\0`" check belongs in the `scripts/*-check.js` family wired into `ci-gate.sh` and `ci.yml`, and would land green today with a zero-entry baseline | `TECH_DEBT` | DEFECT | Discovered by the V3 run of 2026-08-07 (run 22), during `S-E06-6`'s verify phase; the defect instance is closed, the **gate is not** | L0 | V3-E06 |
| **PF-04** | One dataset yields mutually incompatible grade/assessment/enrollment/alert counts across admin, teacher, parent, student | `BROKEN_TRUTH` | ARCH_PREREQ | A2 §5.1, §5.3, App. D | L0 | V3-E03 |
| **PF-05** | Parent grades page returns **zero** for a child whose published grade is visible on dashboard, subjects, printable report and the student portal | `BROKEN_TRUTH` | DEFECT | A2 §7, §14; E2 | L0 | V3-E03 |
| **PF-06** | Attendance can **silently partially save**; downstream rates then consume corrupt completeness | `BROKEN_RUNTIME` | DEFECT | A2 App. C.3, App. G | L1 | V3-E09 |
| **PF-07** | Two attendance read endpoints have **no teacher ABAC** — any teacher reads any class's roster and student PII | `BROKEN_SECURITY` | DEFECT | A2 App. C.2, App. G | L0 | V3-E05 |
| **PF-08** | Custom roles are **global across tenants** — cross-tenant read, write and delete of another school's roles and permission sets | `BROKEN_SECURITY` | DEFECT | A2 App. C.3, App. G | L0 | V3-E05 |
| **PF-09** | An administrator can mint a role carrying permissions they do **not** themselves hold (privilege escalation) | `BROKEN_SECURITY` | DEFECT | A2 App. C.3, App. E | L0 | V3-E05 |
| **PF-10** | Coefficient-matrix save accepts **foreign-tenant identifiers**, re-weighting another tenant's averages | `BROKEN_SECURITY` | DEFECT | A2 App. B.1, C.3 | L0 | V3-E05 |
| **PF-11** | Notification fan-out **dedup query is not tenant-scoped** | `BROKEN_SECURITY` | DEFECT | A2 App. B.4, C.3 | L0 | V3-E05 |
| **PF-59** | GitHub Actions is **account-locked for billing**, so no CI job starts on any branch. Every check reports `failure` in ~3 s having executed zero steps. V3's premise — gates *executed*, not asserted — has no runner to execute them on; `VAL-01` cannot be satisfied and every gate needing a test run degrades to `evidence: deferred` | `BROKEN_RUNTIME` | ARCH_PREREQ | Discovered by the V3 run of 2026-08-02 | L0 | V3-E02 |
| **PF-62** | **The entire teacher grading REST surface is unmounted in production.** `3341ed0` (2026-06-01) deleted `controllers: [AssessmentsController, GradesController]` from `GradesModule` while exposing `GradesService` to the parent dashboard. Both controller files still exist and still compile; they are simply registered in **no NestJS module anywhere**, so `/api/v1/assessments/*` (create/edit evaluations) and `/api/v1/grades/*` (enter notes, batch, gradebook, flag, revise) return router-level 404. Teachers cannot create an assessment or enter a grade on the hosted deployment | `BROKEN_RUNTIME` | DEFECT | Discovered by the V3 run of 2026-08-02, by executing the suite for the first time | L1 | V3-E02 → V3-E07/E08 |

**PF-62 evidence — measured on the live deployment, not inferred.** Probing
`https://pilotage.srv861861.hstgr.cloud`, using an unmatched path and a mounted path as controls:

```
404  /api/v1/definitely-not-a-route     ← control: unmatched → 404 "Cannot GET …"
404  /api/v1/assessments                ← UNMOUNTED
404  /api/v1/grades/gradebook/x         ← UNMOUNTED
401  /api/v1/notifications              ← control: mounted → guard rejects
```

A mounted-but-unauthorised route answers `401`; these answer `404`, exactly like a route that does not exist.

**Why nobody saw it for seven weeks.** A regression guard for *precisely this defect*
(`apps/api/src/modules/grades/grades.module.spec.ts`) was written on 2026-06-10 (`d0c3841`) and has been **red ever
since** — because the suite had never been executed. This is the concrete cost of `PF-55`, and the reason `S-E02-2` is
not a hygiene story.

**A second, independent defect this exposed.** The local Docker image (built 2026-06-06) *does* carry
`controllers: [AssessmentsController, GradesController]` in its compiled `dist/modules/grades/grades.module.js`, yet
**no ref in the repository** has contained that line since 2026-06-01. The running artefact was therefore built from an
uncommitted working tree and diverges from `main`. Local behaviour stayed healthy while production was broken — which
is why manual testing never caught it either. This is the deploy-gate half of `VAL-10` / `R-05` (compare the running
SHA against the expected one) arguing for its own existence.

**PF-59 evidence.** The GitHub check-run annotation is explicit — no inference involved:

```
GET /repos/Tanimou/projet-scolaire-claude/check-runs/91540293327/annotations
[{"annotation_level":"failure","path":".github",
  "message":"The job was not started because your account is locked due to a billing issue."}]
```

Scope: repository-wide and content-independent. The unrelated Dependabot PR #169 (2026-07-28) fails identically,
as does every `main` run since. Job logs return `BlobNotFound` because no job ever produced any.
Resolution is an account/billing action by the owner — **not** a code change, so no story can close this.
It is a Step-6 *credential/decision-required* stop condition for any story whose gate evidence depends on CI.

## 2. P1 — blocks trustworthy operation

| ID | Finding | Class | Type | Source | Layer | Epic |
|---|---|---|---|---|---|---|
| **PF-60** | The BMAD sprint workflow **silently overrides the routine's story selection**. `bmad/workflows/sprint.workflow.js:138` resolves `epicId = (intake && intake.epic) \|\| ARG_EPIC`, so the intake agent's own pick beats the operator override in `args`; the prompt (line 125) only asks it to "honor it unless clearly unsafe". Observed live: the routine selected `S-E02-1` and the workflow started implementing `S-E06-2`. **V3's layer/priority selection rule becomes decorative — the V2 failure mode V3 exists to prevent** | `BROKEN_RUNTIME` | ARCH_PREREQ | Discovered by the V3 run of 2026-08-02 | L0 | V3-E02 |
| **PF-63** | **7 `AnalyticsService.adminDashboard` specs are red** — every one dies on `TypeError: Cannot read properties of undefined (reading 'getTime')`, i.e. the spec's academic-year fixture no longer supplies a date the service reads. The admin dashboard's cycle drill-down, teacher-coverage, grading-rate and student-teacher-ratio figures therefore have **no passing test behind them** — precisely the numbers `PF-04`/`PF-36` say disagree across portals | `BROKEN_TRUTH` | DEFECT | Discovered by the V3 run of 2026-08-02 (first execution of the suite) | L0 | V3-E03 |
| **PF-64** | **4 remediation specs are red on stale Prisma test doubles** — `this.prisma.booking.updateMany is not a function` (3) and a missing model on the catalogue query (1). The catalogue one asserts *tenant scoping*, so a **G-TENANT guard is currently not executing**; the transition ones cover the teacher booking state machine | `BROKEN_RUNTIME` | DEFECT | Discovered by the V3 run of 2026-08-02 | L1 | V3-E10 |
| **PF-65** | **5 `SnapshotRecomputeService.recomputeScope` specs are red**: the harness captures **no snapshot write at all** (`Cannot read properties of undefined`), so the assertions that snapshots are byte-identical to the live computation, and idempotent, never run. Attribution between a stale harness and a genuinely inert snapshot pipeline is **not yet established** — if it is the pipeline, this is the same defect as `PF-24` and the analytics layer has no G-TRUTH evidence at all | `BROKEN_TRUTH` | DEFECT | Discovered by the V3 run of 2026-08-02 | L0 | V3-E03 |
| **PF-66** | **2 parent-digest specs are red**: the rendered digest does not contain the absolutised recommended-action CTA the spec asserts, and the cron re-entrancy guard dies on `release is not a function`. The guard that stops a parent receiving **duplicate digests** is therefore unverified | `BROKEN_RUNTIME` | DEFECT | Discovered by the V3 run of 2026-08-02 | L1 | V3-E11 |
| **PF-67** | **Nothing boots either Nest application, so a type-correct change that breaks dependency injection ships green.** Originally raised (run 5) as "the module-wiring guards assert on module *source*, not on a booted route table" — `grades.module.spec.ts` reads the module as text because importing it pulls `AuthModule → JwtStrategy → jwks-rsa → jose` (pure ESM) into a CommonJS ts-jest runtime. **Widened run 10 by a second, independent trigger** (`S-E02-8`): `eslint --fix` for `@typescript-eslint/consistent-type-imports` rewrites constructor-injected imports to `import type`, erasing the runtime `require()` so TypeScript emits `design:paramtypes` as `[Object, Object, Object]`. Measured on `analytics.service.ts`: 3 relative `require()` → 0. `tsc --noEmit -p tsconfig.json` **executed on the broken file returns exit 0**, `nest build` passes, ESLint is satisfied because it authored the change, and the wiring guards never look at a constructor. So the class is not "the guards are textual" but "**nothing starts the app**". One trigger is now fenced (`packages/eslint-config/decorator-metadata.js` + a guard binding the exemption to `emitDecoratorMetadata` in both directions); the class is open. Closing it needs `Test.createTestingModule({imports:[AppModule]}).compile()` or a route-table snapshot in an ESM-capable jest project | `BROKEN_RUNTIME` | ARCH_PREREQ | Discovered by the V3 run of 2026-08-02 (run 5); widened run 10 | L0 | V3-E02 |
| **PF-68** | **The VAL-10 release manifest shipped inert**, for two independent reasons: (a) **nothing anywhere set `GIT_SHA`/`BUILD_SHA`** — no Dockerfile `ARG`, no compose `build.args`, no deploy export — so `buildSha()` returned `"unknown"` in every environment; (b) **`/version` was unroutable**: nginx proxies `/api/`, but `/version` is excluded from Nest's global `api/v1` prefix, so it fell through to `location /` → Next.js and an external caller got the *web app's* 404. Verified against the hosted deployment. Both fixed by `S-E02-6`; the **residual** open half is that worker/web carry a `GIT_SHA` but expose no HTTP manifest (a worker/web drift is undetected), and the gate publishes `schemaVersion` without comparing it to the checkout's latest shipped migration | `BROKEN_RUNTIME` | DEFECT | Discovered by the V3 run of 2026-08-03 | L0 | V3-E02 |
| **PF-69** | **`apps/api/prisma/` is a fourth TypeScript project that neither passes typecheck nor is gated by one.** The api's own `tsconfig.json` sets `include: ["src/**/*"]`, so `pnpm typecheck` — the gate `PF-55` declared green — never looks at the 7 seed scripts (~2 900 lines) that write the demo dataset. Running their own `prisma/tsconfig.json` fails: `seed-demo-parent.ts:53` and `seed-demo-teacher.ts:49` both do `(await res.json()).access_token` on a `unknown`-typed body (TS2571). **Verified pre-existing**, byte-identical to `HEAD`, not introduced by `S-E02-4`. The same directory is invisible to **lint** as well: the api's script is `eslint "{src,test}/**/*.ts"`, which excludes `prisma/`. Three halves: fix the two casts, add `prisma/tsconfig.json` to `ci-gate.sh`'s typecheck stage, and widen the api lint glob — so the directory that writes the entire demo dataset stops being invisible to both gates | `BROKEN_RUNTIME` | DEFECT | Discovered by the V3 run of 2026-08-03 | L0 | V3-E02 |
| **PF-70** | **The `lint` stage of `scripts/ci-gate.sh` is fictional, and has never passed.** ESLint was upgraded to **v9**, which requires a flat `eslint.config.js`, but **no package has one**: the repo ships `apps/*/.eslintrc*` (v8 format) and five workspace packages whose lint script is a bare `eslint .` with no config at all. Executed: `@pilotage/i18n` is a turbo **cache miss**, actually runs, and fails with *"ESLint couldn't find an eslint.config.(js\|mjs\|cjs) file"* — this is what makes `ci-gate.sh` print `GATE: FAIL`. `@pilotage/contracts` reported ✓ in the same run purely as a **cache hit**; running its `eslint .` directly reproduces the identical failure. So four of the lint tasks are green only from turbo cache predating the v9 bump, and the one that truly executes is red. **Consequence:** runs 5, 6 and 7 each declared their change green while citing typecheck/ratchet/build and never reporting that gate stage 2 was failing — the R-21 failure mode in a new shape, a gate stage that is *executed but permanently red* and quietly reported around. Fixing it is a repo-wide flat-config migration, deliberately **not** attempted inside `S-E02-4` | `BROKEN_RUNTIME` | ARCH_PREREQ | Discovered by the V3 run of 2026-08-03 | L0 | V3-E02 |
| **PF-71** | **996 lint warnings became visible the moment the lint gate started running** (`S-E02-7`, closing PF-70). Measured per package once every package exited 0: web 597, api 321, worker 49, ui 24, imports-core 5; contracts, design-tokens and i18n are clean. They are dominated by three rules — `import/order`, `@typescript-eslint/consistent-type-imports` and `@typescript-eslint/no-explicit-any` — and **952 of the 996 are auto-fixable** (`eslint --fix`). This is not new debt; it is debt that was never measurable because no package could load a config. Warnings do not fail a build, so the gate is genuinely green: this finding exists so the number is tracked and can be ratcheted down deliberately (a `--max-warnings` ceiling that only ever decreases) rather than discovered again later as a surprise. — **CORRECTED 2026-08-03 (run 10, `S-E02-8`), and the correction is the point: "auto-fixable" ≠ "safe to auto-fix."** 243 of the 952 are `consistent-type-imports` in `apps/api` and `apps/worker`, the only two packages inheriting `emitDecoratorMetadata`; the autofix rewrites constructor-injected imports to `import type`, which erases the runtime `require()` and collapses `design:paramtypes` to `[Object, …]`, so Nest cannot resolve the provider at boot. Measured on the emitted JS of `analytics.service.ts`. `tsc --noEmit` returns **exit 0** on the broken file and `nest build` passes, so a repo-wide `--fix` would have shipped an outage that every gate called green (see **R-24**, and `PF-67`, which this widens). **Closed:** 996 → 44 via `scripts/lint-ratchet.js`; the 243 were resolved by removing the rule where it is incorrect, not by "fixing" them | `TECH_DEBT` | DEFECT | Discovered by the V3 run of 2026-08-03 (run 9); corrected and closed run 10 | L0 | V3-E02 |
| **PF-72** | **`pnpm build` reports success while producing an empty worker artefact.** `apps/worker/nest-cli.json` sets `deleteOutDir: true`, and `apps/worker/tsconfig.json` inherited `incremental: true` from `@pilotage/tsconfig/base.json`. The two are silently destructive in combination: the build **deletes `dist/`**, then asks a compiler holding a `.tsbuildinfo` that says every file is already emitted to emit — and it emits **nothing**, exiting **0**. Measured during `S-E02-9`: with the stale build-info present, `nest build` produced **0 files, exit 0**; removing *only* the build-info and rerunning produced **53 files including `main.js`, exit 0**. `apps/api` is unaffected solely because it carries an explicit `incremental: false`, which until now looked like a stylistic difference between two app configs rather than a load-bearing setting. **Nothing in the repository noticed**: `pnpm build` exits 0, and turbo — whose `build` task declares `outputs: ["dist/**", …]` — caches the empty `dist/**` as that build's successful output and replays it on every later cache hit, so the emptiness is *propagated*. `.tsbuildinfo` is gitignored, so a clean clone reproduces it as soon as anything builds twice. A hosted deploy is protected only by Docker's fresh-checkout builds; any workflow reusing a workspace or a warm turbo cache can ship a worker image with no `main.js`. Found by the boot check the moment it tried to load an artefact instead of reading source — the same class as `PF-67`, one layer earlier: not "the app does not boot" but "**there is no app to boot, and every gate said fine**". Fixed in `S-E02-9` by pinning `incremental: false` in the worker, with a guard asserting that *every* app whose `nest-cli.json` deletes its outDir resolves `incremental: false` through `tsc --showConfig` | `BROKEN_RUNTIME` | DEFECT | Discovered by the V3 run of 2026-08-03 (run 11) | L0 | V3-E02 |
| **PF-73** | **`engines.node` blesses a Node version on which the API cannot start.** The root `package.json` declares `"node": ">=20.0.0"`, but `apps/api/src/shared/auth/jwt.strategy.ts` imports `jwks-rsa` at module top level, and `jwks-rsa@4`'s `src/utils.js` does a CommonJS `require('jose')` — and `jose@6.2.3` is **ESM-only** (`dist/webapi/index.js` begins with `export {…}`; measured directly while diagnosing `PF-67`). `require()` of an ESM module is unflagged only from Node **22.12**. So on Node 20, loading `AuthModule` — which is on the boot path via `AlertsModule` — throws before the application starts. In practice everything runs 22: `.nvmrc` says `22`, both Dockerfiles pin `22.13.1`, and CI sets `NODE_VERSION: '22'`. The defect is therefore **latent**, not live: it is a declared support range that is wrong, which will mislead the first contributor who honours it. **Stated limitation:** this was **not executed against a Node 20 runtime** — the ESM-only packaging of `jose` and the CJS `require` in `jwks-rsa` were both verified directly, and the failure on Node 20 is inferred from the documented require(ESM) support matrix rather than measured. Fix is one line (`>=22.12.0`), but it belongs with a deliberate engines review rather than being smuggled into an unrelated slice. **CORRECTED 2026-08-03 (run 14, `S-E02-12`) — the prescription above is wrong in both directions, and is left in place only so the correction is legible.** Measured against all **671** installed packages that declare `engines.node`: `>=22.12.0` **excludes** Node 20.19.x, which every dependency accepts (`require(esm)` was **backported to 20.19**, and `jwks-rsa@4.0.1` states exactly that itself — `^20.19.0 \|\| ^22.12.0 \|\| >= 23.0.0`), and it **blesses** 22.12.x and 23.x, which `eslint-visitor-keys@5.0.1` refuses (`^20.19.0 \|\| ^22.13.0 \|\| >=24`). The finding also understated the blast radius: run against the unfixed repository, the new gate named **35** contradicting dependencies, not one. The declared range is now `^22.13.1 \|\| >=24.0.0` — deliberately narrower than the compatible set, because `engines` is a *support* statement — and it is checked by `scripts/runtime-engines-check.js` rather than asserted | `TECH_DEBT` | DEFECT | Discovered by the V3 run of 2026-08-03 (run 11) | L0 | V3-E02 |
| **PF-74** | **Nothing asserted that the Next.js build emitted anything, and `/version/web`'s `force-dynamic` was documented but unenforced.** `R-25`'s residual half, given an id so its closure is traceable. *(Recorded in `traceability-matrix.md` and `risk-register.md` by run 13 but never added here — the omission was caught and repaired by run 14.)* Measured: with `apps/web/.next` moved aside in its entirety, `node scripts/boot-check.js` returned **exit 0**, and `grep -rn '\.next' scripts/` returned nothing — no stage of `ci-gate.sh` read the directory at all. turbo's `build` task declares `outputs: [".next/**", …]`, so the caching rule that replayed `PF-72`'s empty `dist/` applies verbatim. The sharp half: `/version/web` is the web third of the release gate (`S-E02-10`, **R-05**) and its own header states `force-dynamic` is mandatory, yet deleting that one line left the route present, the build green, and the gate answering 200 with a build-time constant | `TECH_DEBT` | DEFECT | Discovered by the V3 run of 2026-08-03 (run 13) | L0 | V3-E02 |
| **PF-75** | **`engines.pnpm` blesses a pnpm major that never produced this lockfile.** The root declared `"pnpm": ">=8.0.0"` beside `packageManager: "pnpm@9.12.3"`, while `pnpm-lock.yaml` carries `lockfileVersion: '9.0'` — pnpm 8 writes lockfile 6.0. The same shape as `PF-73`, in the same field: a support claim nothing ever checked. Now `>=9.0.0`, with the floor required to sit inside the `packageManager` pin's major, and `ci.yml`'s `PNPM_VERSION` required to equal that pin | `TECH_DEBT` | DEFECT | Discovered by the V3 run of 2026-08-03 (run 14) | L0 | V3-E02 |
| **PF-76** | **`.nvmrc` and `ci.yml` pinned a floating Node major that includes the window in which the API cannot boot.** `PF-73` called itself latent because "in practice everything runs 22"; it was latent for a weaker reason than that. A bare `22` *declares* 22.0.0–22.99.x, which includes 22.0–22.11 — the range where `jose@6` cannot be `require()`d and `AuthModule` cannot load. It was safe only because nvm and `actions/setup-node` happen to resolve a bare major to the newest release; a statement that is true by accident is the same defect one level down. Both now pin **22.13.1**, equal to the three `ARG NODE_VERSION` defaults, with the agreement checked | `TECH_DEBT` | DEFECT | Discovered by the V3 run of 2026-08-03 (run 14) | L0 | V3-E02 |
| **PF-77** | **The routine's single-writer lock expires mid-run, and a concurrent tick reaps it and resets the working tree.** `routine-lock.sh` reclaims a `write.lock` whose heartbeat is older than `STALE_MIN` (60 min). V3's Step 4 says "heartbeat while polling" the build, and nothing asks for a heartbeat during **Step 3**, which in every run since run 9 has taken well over an hour. Measured on run 15: the lock was acquired at 20:03 and, with no heartbeat since, a **second V3 tick at 21:08 reaped it as stale**, took it, and released it at 21:11 — so from 21:09 the run held no lock while still writing to the checkout, which is precisely the pile-up the lock exists to prevent. The consequence was nearly destructive: re-acquiring the gate ran the salvage path, which `git stash`ed every tracked modification and reset the branch to `origin/main`. The work survived **only** because that salvage stash exists; the untracked new files survived by not being tracked. Two defects, one mechanism: the routine's own protocol does not keep the lock alive long enough to cover the phase that takes longest, and `heartbeat` reporting `no lock held` is the only signal that the guarantee has already lapsed | `BROKEN_RUNTIME` | DEFECT | Discovered by the V3 run of 2026-08-03 (run 15) | L0 | V3-E02 *(routine)* |
| **PF-78** | **Tracing is configured end to end and emitted by nothing.** `infra/docker-compose.yml` declares a `jaeger` service and sets `OTEL_EXPORTER_OTLP_ENDPOINT: http://jaeger:4318` in the shared application environment, so every application receives a collector endpoint. No application imports an OpenTelemetry SDK or creates a span — `grep -rn 'opentelemetry\|otel' apps/*/src` returns nothing. The only `@opentelemetry/api` in the tree arrives transitively through `prom-client`. This is the trace third of `PF-56`, separated because it is a build rather than a gate: the collector is reachable, the exporter variable is set, and the absence is upstream of both. Left open deliberately by `S-E02-13` rather than half-built | `NOT_IMPLEMENTED` | CAPABILITY | Discovered by the V3 run of 2026-08-03 (run 15) | L0 | V3-E02 |
| **PF-79** | **`apps/web` is the one artefact no observability surface covers — no metrics, no traces.** Recorded as a residual by `S-E02-13` ("apps/web exposes no metrics") and made explicit by `S-E02-14`, which **removed** `OTEL_EXPORTER_OTLP_ENDPOINT` from the `web` service rather than leave a collector declared to an application that could never reach it. The removal must not be read as coverage: web is the artefact users actually touch, and today a slow or failing page is invisible to both Prometheus and Jaeger while api and worker are visible to both. Next.js instruments through its own `instrumentation.ts` `register()` hook and a Next-aware exporter, so it is a different build from the two Nest applications — separated rather than half-built, exactly as `S-E02-13` separated traces from metrics. **CLOSED 2026-08-04 by `S-E02-15`.** The gap was measured first, and the measurement is the point: `grep -rniE 'prom-client\|opentelemetry\|/metrics' apps/web/src` returned **0 hits** while `node scripts/observability-check.js` returned **exit 0** — the gate was green on the state this finding describes. `apps/web` now has a Next 15 `src/instrumentation.ts` `register()` hook (runtime-guarded, dynamic-import-only, total — it cannot throw, because a throw there is fatal to Next's bootstrap) that starts a tracer provider and serves Prometheus on a **dedicated internal `node:http` socket**, never a Next route: `infra/nginx/conf.d/pilotage.conf` `location /` proxies everything unmatched to the web upstream, so a route would be public the day it was added and the existing nginx rule would not see it. Same metric names as the API behind `app="web"`, so one dashboard compares three artefacts; every HTTP panel of `pilotage-slo.json` gained the `app` grouping it lacked, without which api and web series would have blended into one line. G-TENANT holds on **two** unauthenticated surfaces: traces through the single `withRedaction` from `@pilotage/contracts` (no second copy — a guard spec fails if this module defines its own `sanitize*`), and labels through a `routeLabelFromSpan` that reads only template attributes, because a span processor sees attributes *before* export redaction. Executed: the probe yields 1 span and a histogram **sample** labelled by route template, with the cuid and `tenantId` absent from both the export payload and the exposition; 213/213 across `src/shared/quality`. **Not covered, deliberately:** nothing was ingested (no Prometheus/Jaeger/booted Next — the routine forbids bringing the profile up), Next's own span emission is inferred from its built-in OpenTelemetry support rather than executed, span **events** (`exception.*`) remain unredacted, and `PF-56`'s queue-depth/DLQ third is untouched | `CLOSED` | CAPABILITY | Discovered by the V3 run of 2026-08-03 (run 16) · closed by run 17 | L0 | V3-E02 |
| **PF-80** | **The sprint workflow reports `landed: true` while a change it made breaks a test it never thought to run.** `S-E02-15` widened `TRACED_SERVICES` from two entries to three in `packages/contracts`. The sprint verified the specs it authored (`src/shared/quality`, all green) and returned `landed: true`; it never ran the api suite, where `apps/api/src/shared/tracing/tracing.spec.ts` pinned the old list and went red. `node scripts/test-ratchet.js api` caught it as **1 NEW test failure** in the routine’s own Step 4. The shape is general and not about this one spec: **a shared-contract edit has a blast radius the editing agent cannot see from the directory it is working in**, and the workflow’s readiness check is scoped to the files it touched rather than to the packages that import them. The ratchet is what stood between that and a merged regression — which is an argument for the routine always running the **full** gate before merging rather than a stage selection (**R-23**), and an argument for the workflow running the ratchet in its own readiness step | `TECH_DEBT` | DEFECT | Discovered by the V3 run of 2026-08-04 (run 17) | L0 | V3-E02 *(routine)* |
| **PF-12** | Parent child/enrollment state contradicts itself: dashboard/detail say active; children list, "My family" and claim panel say none | `BROKEN_TRUTH` | DEFECT | A2 §7, App. B.7 | L0 | V3-E03 |
| **PF-13** | Class gradebook links pass a **class-section id** where the page expects a **teaching-assignment id**; dashboard "create assessment" shares the broken URL | `BROKEN_RUNTIME` | DEFECT | A2 §6.1, App. B.6 | L1 | V3-E07 |
| **PF-14** | `/admin/audit` crashes (server/client boundary); `/admin/reports` is 404 | `BROKEN_RUNTIME` | DEFECT | A2 §5.6, App. B.5 | L0 | V3-E04 |
| **PF-15** | Active academic year is **2023–2024** on a 2026 audit date; 2026 events attach to it; printable report mixes 2026 generation with 2023–24 | `BROKEN_TRUTH` | DEFECT | A2 §5.2, App. D | L0 | V3-E03 |
| **PF-16** | Whole-school announcement estimated 191 accounts but broke down as 1 parent / 0 teachers / 0 admins / **190 "other"**; the student never received it; recipient roles blank | `BROKEN_TRUTH` | DEFECT | A2 §5.4, §8 | L1 | V3-E11 |
| **PF-17** | Hosted UI ships **development-only Maildev `localhost` instructions** and visible seed artefacts/author labels. **PARTIAL 2026-08-04 by `S-E06-1`.** The **UI half is done**: the four `http://localhost:1080` Maildev instructions are gone from `/admin/register`, `/teacher/register` and the two places in the admin invite form, replaced by the config-driven `apps/web/src/components/auth/ActivationHint.tsx` + `apps/web/src/lib/support-contact.ts`, and a hit is refused thereafter by the `scripts/production-artefact-check.js` ratchet. **Not closed:** the seed author labels **already written to the hosted database** are data, and deleting data on a live deployment is routine STOP condition #3 — operator work. The seed-leakage half of the finding (`KEYCLOAK_DEMO_PASSWORD` defaulting to `Demo!2024Pilotage` in `infra/docker-compose.prod.yml`, just outside `SCAN_ROOTS`) is **untouched by this slice** and must not be read as covered | `BROKEN_SECURITY` | DEFECT | A2 §4, §12 | L0 | V3-E06 |
| **PF-18** | Student identity client is **aliased to the parent client**; student password reset targets the parent client | `BROKEN_SECURITY` | DEFECT | A2 §8, §11 | L0 | V3-E01 |
| **PF-19** | `/admin/classes/new` is linked prominently but falls into the `[id]` dynamic route and crashes | `BROKEN_RUNTIME` | DEFECT | A2 §5.2, App. B.1 | L0 | V3-E06 |
| **PF-20** | Dashboard enrollment/alert totals disagree with their own queues and rule lists (28 pending vs empty queue; 4 alerts vs 0 rules) | `BROKEN_TRUTH` | DEFECT | A2 §5.1 | L0 | V3-E03 |
| **PF-21** | Student **date of birth is silently dropped** on create/read-back | `BROKEN_RUNTIME` | DEFECT | A2 §5.2, App. G | L1 | V3-E08 |
| **PF-22** | Editing a cahier-de-texte entry returns **400 every time** | `BROKEN_RUNTIME` | DEFECT | A2 App. B.6, C.3 | L1 | V3-E08 |
| **PF-23** | Calendar edit **destroys `cycle_scope`** and drops `academicYearId`; multi-month events are invisible outside their start month | `BROKEN_RUNTIME` | DEFECT | A2 App. B.3, C.3 | L1 | V3-E08 |
| **PF-24** | Snapshot recompute is enqueued but **no consumer exists in this codebase**; comments assert a worker that is not here; the hosted drain cannot mark a repeated scope `done` | `SOURCE_ONLY` | DEFECT | A2 App. C.3, C.4 | L0 | V3-E03 |
| **PF-25** | Wrong password is reported as **"MFA required"**; `mfaEnabled` is hard-coded `false` | `BROKEN_RUNTIME` | DEFECT | A2 App. C.3, G | L0 | V3-E05 |
| **PF-26** | Logout does not end the Keycloak session (no RP-initiated logout); middleware never inspects `session.error`, so dead sessions keep browsing; middleware declares 9 non-existent auth routes | `BROKEN_SECURITY` | DEFECT | A2 App. C.2, C.3 | L0 | V3-E05 |
| **PF-27** | Parent can **terminally close a school-created remediation plan**; no direct booking action | `BROKEN_SECURITY` | DEFECT | A2 §7, App. B.7 | L1 | V3-E10 |
| **PF-28** | Alert filters, exports and totals operate on a truncated **≤100-row per-status window**; announcement engagement truncates at 500; announcement list is unpaginated | `BROKEN_TRUTH` | DEFECT | A2 App. C.3 | L1 | V3-E10 |
| **PF-29** | Calendar "import French holidays" executes **immediately with no confirmation** and wrote 22 rows into the stale active year | `BROKEN_RUNTIME` | DEFECT | A2 §5.5; E2 · **closed** by `S-E06-6` (2026-08-07, run 22) — server-side `confirm`, required `year`, `dryRun` plan from the write path, one transaction + one audit row; the stale-year half was the *fallback cascade*, now deleted. 13 of 22 rows measured mis-attached on `voltaire-demo` before the fix; those rows are **not** repaired | L0 | V3-E06 |
| **PF-30** | `POST /grades/batch` is an **N+1 inside a transaction** and fabricates phantom revisions | `BROKEN_RUNTIME` | DEFECT | A2 App. C.3 | L1 | V3-E08 |
| **PF-31** | Role grant/revoke and school mutations write **no audit row** (ADR-015 mandates it); audit actor role is hard-coded `school_admin`; `ip_address`/`user_agent`/`hash`/`prev_hash` are **never written** | `BROKEN_SECURITY` | DEFECT | A2 App. B.5, C.2 | L0 | V3-E04 |
| **PF-32** | Audit `to` date filter silently drops the selected end day; **three of four audit KPIs are structurally wrong** | `BROKEN_TRUTH` | DEFECT | A2 App. B.5 | L0 | V3-E04 |
| **PF-33** | An existing class is **29/28 over capacity** although manual enrollment correctly rejects overflow — import/history bypasses the invariant | `BROKEN_TRUTH` | DEFECT | A2 §5.2, App. G | L2 | V3-E12 |
| **PF-34** | Teacher class messaging link is 404; the alternate composer computes **zero recipients** for a class with known families | `BROKEN_RUNTIME` | DEFECT | A2 §6.3 | L1 | V3-E11 |
| **PF-102** | **Post-authentication open redirect on all four portal login forms.** `apps/web/src/components/PortalLoginForm.tsx:76` reads `callbackUrl` straight off the query string (`params.get('callbackUrl') ?? DEFAULT_LANDING[accent]`) with **no same-origin validation**, and the credentials branch hands it to `router.push(callbackUrl)` at `:125`. Next's App Router performs a hard `window.location` navigation for a cross-origin href, so `/parent/login?callbackUrl=https://evil.example/` shows the genuine login page, authenticates the parent for real (session cookie set), then lands them off-site — the maximally convincing phishing sequence, on a platform whose users are parents of minors. Protocol-relative forms (`//evil.example`, `/\evil.example`) work the same way. **Only the credentials branch is affected:** the SSO branch (`:136`, `signIn(…, { callbackUrl })`) is covered by next-auth's default same-origin redirect guard, because `apps/web/src/auth.ts` declares no `redirect` callback override. **Pre-existing, not introduced by `S-E06-5`** — recorded here because that slice's AC-7 asserts *"the authed redirect target is the `PORTAL_LANDING` constant lookup, never derived from `searchParams`"*, which is true of the middleware's **emission** side only, while the **consumption** side of the same parameter is unguarded; and because the slice adds three new public, indexable entry points into this flow (`/contact`'s CTA, `/help`'s four login links, `PublicInfoPage`'s footer « Portails » column), so the surface from which such a link is reached grows. **CLOSED 2026-08-07 by `S-E05-12`:** `apps/web/src/lib/safe-callback-url.ts` — one pure, import-free, edge-safe `safeCallbackUrl(raw, fallback)` called **once, at the read site**, so both sinks (`router.push` and `signIn(…, { callbackUrl })`) inherit one same-origin binding by construction; `DEFAULT_LANDING` deleted onto `PORTAL_LANDING`. **The fix this row used to recommend was itself exploitable, and that is why the recommendation is gone rather than ticked.** The proposed `raw.startsWith('/') && raw[1] !== '/' && raw[1] !== '\\'` was executed against Node's WHATWG `URL` with base `https://pilotage.example/parent/login`: **four** inputs pass it and still navigate off-origin — `/<TAB>/evil.example`, `/<LF>/evil.example`, `/<CR>/evil.example` (all → `https://evil.example/`) and `/<CR><LF>/evil` (→ `https://evil/`) — because the parser removes tab/LF/CR from **anywhere** *before* deciding the origin, so the string is rewritten after the check has already passed it. The shipped rule is a five-clause allow-list adding **C0 rejected anywhere** (not at position 1: a rule whose safety depends on where the byte sits breaks the next time normalisation order changes) and performs **no normalisation at all**, so the bytes validated are the bytes navigated. Gate: `apps/api/src/shared/quality/open-redirect-gate.spec.ts` — a 37-row accept/reject matrix driven through a differential `new URL(result, base).origin` oracle, **fails-before proven two ways** (the pre-slice expression is reported as an offender by the same scan that walks the tree; the briefed rule fails on exactly R25/R26/R27), DNC-10 executed with six bypass variables set, and a whole-tree structural scan over 366 files / 55 sink call sites with **0 offenders**. **214/214**, `pnpm typecheck` **13/13 exit 0**. **What the closure does NOT cover, stated:** (a) next-auth's own `/api/auth/signin/*?callbackUrl=` is a **second public entrance** into the same flow — `middleware.ts:34` makes `/api/auth` public, `auth.ts` declares no `pages:` override, and its same-origin clamp compares against a **header-derived** `baseUrl` (`trustHost: true`; nginx `server_name _` + `proxy_set_header Host $host`). Recorded, deliberately not asserted — a gate that reddens on an improvement gets deleted — and it is the cache-poisoning class, not the one-click chain this row describes; (b) the regression scan is **lexical and single-file**, and it is blind to the inline read at the sink (`router.push(params.get('callbackUrl') ?? '/x')`), to two-argument `push(x, opts)`, to `signIn(…, { callbackUrl })` and to `packages/ui/src`; (c) the fix is **in code, not on the deployment** — nothing here rebuilds. All six residuals carry an owner in `docs/spec/features/v3-e05/PROGRESS.md` | `CLOSED` | DEFECT | Discovered by the V3 run of 2026-08-07 (run 23), by the verify panel on `S-E06-5` | L0 | V3-E05 |
| **PF-134** | A **fourth** audit vocabulary survived in `AuditTable.tsx`, and it was already wrong on real data: `pickActionTone` / `pickActionIcon` were inline substring matchers (`création\|publish\|approve`, `suppression\|delete`, …) sitting next to the declaration meant to replace them, so `coefficient.upsert` and `grade.unflag` — declared `critical: true` and counted by the « MODIFICATIONS CRITIQUES » card — were toned as ordinary rows. **CLOSED by `S-E04-5`** (run 33) | `CLOSED` | DEFECT | `traceability/CLOSED-L0.md` — carried by `S-E04-5`; registered here by `S-E04-10` | L0 | V3-E04 |
| **PF-140** | **The regulator-facing DPO CSV changed bytes with no acceptance criterion asking, and its escaping is one column from injectable.** `action_label`, `resource_type_label` and `vocabulary` are inserted **mid-header** rather than appended and a UTF-8 **BOM** is now prepended, so any index-based or `created_at`-keyed downstream parser breaks silently; the single `vocabulary` column collapses two axes (`weakerVocabulary(action, resourceType)`), so a regulator cannot tell **which** value was unclassified. The existing spec covers the labels, not the byte layout. **CLOSED 2026-08-10 (run 38, `S-E04-11`)** by freezing indices 0..9, **appending** the two per-axis vocabulary columns at 10..11, **keeping** the collapsed column (derivable, and removing it would have moved `resource_id`/`ip_address` a second time — committing the defect inside its own fix), and force-quoting + apostrophe-prefixing any cell that starts with `=`, `+`, `-`, `@`, tab or CR. The false comment claiming the labels were *"appended, never interleaved"* is gone | `CLOSED` | DEFECT | `traceability/CLOSED-L0.md` — raised by run 33's review of `S-E04-5`, closed by `S-E04-11` (run 38) | L0 | V3-E04 `S-E04-11` *(owner)* |
| **PF-145** | **Only `admin` can be authenticated by the e2e harness; teacher, parent and student all fail `otp_required`.** With `PF-144` fixed, three of the four `authenticate *` setups still fail, and **not** on a selector: `[auth][cause] CredentialsLoginError: otp_required`. So even a repaired harness can only ever cover **one of four portals** — precisely the coverage `G-PORTAL` asks for, and the reason `ADR-023` exists | `BROKEN_RUNTIME` | DEFECT | `traceability/OPEN.md` — measured from the auth log during run 32's `PF-135` discharge | L0 | V3-E05 |
| **PF-149** | **`UnknownTimezoneError` is thrown and caught nowhere** *(latent — see reachability)*. `assertKnownTimezone` fails closed when `Intl` accepts a zone but resolves it to `UTC` (the small-ICU trap), which is the right design — a silent fallback to the server's zone is the exact defect `S-E04-5` removes — but `grep -rn UnknownTimezoneError apps/api/src apps/worker/src` returns only a *comment*. An unusable `Tenant.timezone` would therefore surface as an unhandled 500 rather than as a named, actionable refusal. **CLOSED 2026-08-10 (run 38, `S-E04-11`)** on **both** call sites — a 503 carrying `TENANT_TIMEZONE_UNUSABLE` on the analytics read, and a `bullmq` `UnrecoverableError` naming the offending zone on the worker (graded `failed_terminal` on attempt 1, so a configuration fault is not retried into three identical failures). The `try` spans the **whole** window resolution, because the `DEFAULT_AUDIT_TIMEZONE` branch skips the assert while `zonedYmd`/`resolveAuditWindow` assert again downstream — under small-ICU it is the *default* zone that fails, for every tenant at once. No fallback introduced. **Residual: `PF-171`** — the *validate on write* half stays open, because no writer for either timezone column exists yet | `CLOSED` | DEFECT | `traceability/CLOSED-L0.md` — raised by run 33's verify panel, closed by `S-E04-11` (run 38) | L0 | V3-E04 `S-E04-11` *(owner)* |
| **PF-156** | **Vertical privilege escalation: any holder of `roles.assign` can self-grant any role in the global catalogue** *(pre-existing; now audited rather than silent)*. `users.service.ts` `assignRole` + `shared/auth/user-sync.service.ts:80-84` — the role lookup is unfiltered, no ceiling check compares the grantor's own permissions against the role being granted, and `isSystem` roles are not refused, so `POST /users/:ownProfileId/roles` with any role id attaches its entire permission set. **Deliberately NOT fixed inside an audit slice** (`ADR-015`): what `S-E04-6` changed is that the escalation is now LEGIBLE, because the row names the granted role | `BROKEN_SECURITY` | DEFECT | `traceability/OPEN.md` — raised by `S-E04-6`'s security lens (run 34) | L0 | V3-E05 / `ADR-015` |
| **PF-264** | **The permission catalogue grants `parent` the two reads that had no ABAC.** `apps/api/src/shared/auth/permissions.constants.ts:264-265` gives the `parent` realm role `class_sessions.read` **and** `attendance.read`. Combined with `PF-07`, that made `GET /class-sessions/:id`, `GET /class-sessions/:id/roster` and `GET /attendance/overview` readable **by any authenticated parent of the establishment** — full `Student` rows (`medicalNotes`, `address`, `notes`, `customFields`, `birthDate`, `email`, `phone`) for other families’ children, plus the establishment-wide recent-attendance feed. This is the *audience* half of `PF-07` and it **survives** `S-E05-5`: `AC-9` forbade touching the catalogue inside a hardening slice, and removing a grant is a four-portal decision touching Keycloak role sync (`PF-53` owns catalogue drift). After `S-E05-5` the grants are **inert on those four handlers**, which is why deferring is safe — and why forgetting is not: they remain a standing invitation on **any handler added later** under the same permission code | `BROKEN_SECURITY` | DEFECT | Raised by `S-E05-5` (run 71, 2026-08-23), the slice that gave the attendance READ paths the ABAC their WRITE paths already had | L0 | V3-E05 |
| **PF-269** | **The attendance roster payload is maximal — `S-E05-5` narrows WHO reads, never WHAT is returned.** `roster` and `sessionDetail` return `include: { student: true }`, i.e. the entire `Student` row — `medicalNotes`, `address`, `notes`, `customFields`, `birthDate`, `email`, `phone`, `gender`, `nationality` — for every child in the class, and `sessionDetail` does it **twice** (`enrollments{include:{student}}` *and* `attendanceRecords{include:{student}}`). After the ABAC lands, the *owning* subject-teacher still receives all of it on every attendance-taking page load. An attendance roster needs `{ id, firstName, lastName, externalRef, photoUrl }`. `GUARDRAILS.md` §1 (children’s data, minimal access) makes this non-negotiable and RGPD calls `medicalNotes` special-category. **Out of `S-E05-5` because it changes a response contract consumed by `apps/web/src/app/teacher/classes/[id]/attendance`.** The failure mode is not a leak on day 1 — it is that `PF-07` gets marked *closed* and the maximal payload loses its only advocate, which is why `AC-21` forbids `PF-07`’s row from reading `closed` without naming this id | `BROKEN_SECURITY` | DEFECT | Raised by `S-E05-5` (run 71, 2026-08-23), the slice that gave the attendance READ paths the ABAC their WRITE paths already had | L0 | V3-E05 |

## 3. P2 / P3 — correctness, safety and credibility

| ID | Finding | Class | Type | Source | Layer | Epic |
|---|---|---|---|---|---|---|
| **PF-35** | Parent attendance mixes a **different class** into the child's history without labelling and shows an invalid **−71.4-point trend** | `BROKEN_TRUTH` | DEFECT | A2 §7 | L1 | V3-E09 |
| **PF-36** | Teacher counts vary 43 / 46 / 48 and one class alternates 25 / 26 | `BROKEN_TRUTH` | DEFECT | A2 §6 | L0 | V3-E03 |
| **PF-37** | New lesson defaults to **Published** — parent-visible before review | `BROKEN_RUNTIME` | DEFECT | A2 §6.2 | L1 | V3-E08 |
| **PF-38** | Legal (`/legal/privacy`, `/terms`, `/cookies`), `/pricing`, `/contact`, `/help` all **404 while parent registration requires accepting them** | `NOT_IMPLEMENTED` | DEFECT | A2 §4; E2 | L0 | V3-E06 |
| **PF-39** | Teacher/parent profile and help links 404; teacher notification settings contain parent copy ("your child"); teacher "Import grades" points into the admin portal | `NOT_IMPLEMENTED` | DEFECT | A2 §6.3 | L0 | V3-E06 |
| **PF-40** | Async KPI counters expose contradictory interim values (parent calendar 14→36; credit 0↔99 analogue) | `UI_ONLY / MOCK` | DEFECT | A2 §14 | L0 | V3-E03 |
| **PF-41** | Guardians list has a hard **200-row ceiling**; filters and export operate on the truncated set | `BROKEN_TRUTH` | DEFECT | A2 App. B.2 | L2 | V3-E12 |
| **PF-42** | `/admin/students` "Niveau" filter is **wired to nothing**; the table **fabricates some metrics as facts**; search triggers a full server render per keystroke | `UI_ONLY / MOCK` | DEFECT | A2 App. B.2 | L1 | V3-E08 |
| **PF-43** | Conversation moderation is **write-only** — `reviewed`/`dismissed`/`blocked` are unreachable | `NOT_IMPLEMENTED` | DEFECT | A2 App. B.4 | L1 | V3-E11 |
| **PF-44** | Meeting request "Clôturer" **resolves** with different semantics — the UI reports a lie | `BROKEN_RUNTIME` | DEFECT | A2 App. B.4 | L1 | V3-E10 |
| **PF-45** | Helmet **CSP is explicitly disabled**; branding values are injected unvalidated into a server-rendered `<style>` | `BROKEN_SECURITY` | DEFECT | A2 §11, App. E | L0 | V3-E06 |
| **PF-46** | `POST /auth/register-parent` is **public, unthrottled and self-verifies email** | `BROKEN_SECURITY` | DEFECT | A2 App. C.3 | L0 | V3-E05 |
| **PF-47** | Enrollment **approval/rejection workflow is explicitly not implemented** | `NOT_IMPLEMENTED` | CAPABILITY | A2 App. B.2 | L2 | V3-E12 |
| **PF-48** | "Documents" / "Ressources" are two full sidebar features over a field **nothing writes**; direct upload deferred | `BACKEND_ONLY` | CAPABILITY | A2 §6.3, App. B.6 | L2 | V3-E13 |
| **PF-49** | `BEHAVIOR_ALERT` can be enabled but **can never fire**; rule bounds are not server-enforced and the UI minimum disagrees with the evaluator | `NOT_IMPLEMENTED` | DEFECT | A2 App. B.3 | L1 | V3-E10 |
| **PF-50** | `/admin/assignments` renders **290 rows unpaginated**; unread counts fetch every message; parent dashboard fans out per child | `UI_ONLY / MOCK` | DEFECT | A2 App. K.4 | L1 | V3-E03 |
| **PF-51** | `PATCH /cycles/grade-levels/:levelId` runs **zero validation** and mass-assigns straight into Prisma; several query params bypass validation; notification `kind` accepts arbitrary strings | `BROKEN_SECURITY` | DEFECT | A2 App. C.2, C.3 | L0 | V3-E05 |
| **PF-52** | `hasPermission()` exists and is **never used** — no client-side permission gating; `users.suspend` is granted with **no implementation**; role revocation is backend-only | `BACKEND_ONLY` | DEFECT | A2 App. C.3 | L0 | V3-E05 |
| **PF-53** | Invite flow and permission rewrites are **non-atomic** and leave orphan/partial state; 18 granted permission codes are absent from the role builder; 5 required codes unseeded | `BROKEN_RUNTIME` | DEFECT | A2 App. C.3 | L0 | V3-E05 |
| **PF-54** | Hard-coded credential fallbacks and development URLs ship in production-facing code. **PARTIAL 2026-08-04 by `S-E06-1` — deliberately not marked `CLOSED`.** The **code path** is fixed and gated: `?? 'admin'` (×2), `?? 'http://localhost:8180'` and `?? 'maildev'` are deleted from `KeycloakAdminService`/`JwtStrategy`/`KeycloakModule`; `apps/api/src/shared/config/config-preflight.ts` refuses startup from `main.ts` **before** `NestFactory.create`, naming every absent variable in one throw, with no `SKIP_*`/`ALLOW_*`/`NODE_ENV` bypass (DNC-10) and variable **names only** in the message; both compose files now declare the keys, prod with `${VAR:?}` so the deploy command refuses by name; recurrence is held by the one-way `scripts/production-artefact-check.js` ratchet wired into **both** `scripts/ci-gate.sh` and `.github/workflows/ci.yml`, whose spec runs it in both directions over a fixture (executed: 562 files, Tier A clean, Tier B 17/17 at baseline). **What is NOT closed, and is why the row stays open:** (1) the guard checks **presence, not strength** — an operator copying the template verbatim satisfies `${VAR:?}` and `assertRequiredConfig` with `admin`/`admin`; (2) the **live** hosted master credential is unrotated, and rotation is operator work on a running deployment; (3) the same diff **states the value in a public repository** (`.env.prod.example`), upgrading an inferable default into a declared one — an amplification of the disclosure, flagged by the escalation panel and not repaired in the slice; (4) `SCAN_ROOTS` is `apps/*/src` by design, so `.env*.example`, `infra/` and `packages/{ui,i18n}` remain outside the ratchet, and Tier-A rule A5 matches one line at a time (a Prettier-wrapped `??` fallback escapes it) | `BROKEN_SECURITY` | DEFECT | A2 App. C.3 | L0 | V3-E06 |
| **PF-55** | 50 spec files exist but **test/typecheck execution is blocked** by missing dependencies and generated artefacts; no CI evidence of pass | `BLOCKED_BY_DEPENDENCY` | VALIDATION | A2 §13 | L0 | V3-E02 |
| **PF-56** | Optional observability only; no traces, SLOs, alert delivery or restore exercise; landing-page availability/security claims unvalidated | `BLOCKED_BY_DEPENDENCY` | VALIDATION | A2 §13, §4 | L0 | V3-E02 |
| **PF-57** | Student portal has **no profile/settings** surface; help 404 | `NOT_IMPLEMENTED` | CAPABILITY | A2 §8 | L2 | V3-E06 |
| **PF-61** | `risk-register.md` carried **two different risks under the id `R-17`** — the CI-billing risk (added alongside PF-59) and the market-decision risk that `open-decisions.md` D-04 and `roadmap.md` already referenced. Any cross-reference to "R-17" was therefore ambiguous, including the routine's own Step-1 read | `BROKEN_TRUTH` | VALIDATION | Discovered by the V3 run of 2026-08-02 | L0 | V3-E02 |
| **PF-58** | The entire V3 substrate (4 audits, 17 planning docs, 40 evidence files) was authored **only as untracked files inside a throwaway git worktree** and never committed. `main` had none of it, so the routine's Step 1 — which reads `roadmap.md`, `traceability-matrix.md`, `dependency-map.md`, `risk-register.md`, `open-decisions.md` — could not execute at all. The in-repo routine copy had also drifted behind the installed `SKILL.md` | `BLOCKED_BY_DEPENDENCY` | ARCH_PREREQ | V3 run 2026-08-02 | L0 | V3-E02 |
| **PF-96** | **`AuditLog` has no foreign key to `Tenant`, so audit rows outlive the tenant they describe.** `schema.prisma:1227` declares `tenantId String @db.Uuid` with **no `@relation`**, unlike `School`, `UserProfile` and `AcademicYear`, which all cascade from `Tenant`. Measured in the live local Postgres: deleting two tenants cascaded away all 44 of their `calendar_event` rows and **left all 6 of their `audit_log` rows behind**. Plausibly deliberate — an append-only governance trail arguably *should* outlive its subject, and a cascade would let a tenant deletion erase its own audit history — but **undocumented**, so the shape is indistinguishable from a forgotten relation and invites a "fix" that would be a regression. Needs a stated policy (model comment + ADR + retention rule), not a reflex schema change | `TECH_DEBT` | DEFECT | Discovered by the V3 run of 2026-08-07 (run 22), while cleaning up `S-E06-6`'s real-database drill | L0 | V3-E04 |
| **PF-97** | **The link-integrity gate was blind to every template-literal href in the application — including the one in the shell's own user menu.** `LITERAL_LINK = /(['"])(\/[^'"`\n\r]*)\1/g` matches single- and double-quoted strings only, and its character class **excludes the backtick**, so `` `/${portal}/profile` `` was never a target. Measured: 18 template hrefs invisible, 6 of them portal-interpolated, and expanding `` `/${portal}/profile` `` over the interpolated variable's *declared* union gives **0 of 3 alive** — « Mon profil » 404'd on every authenticated page of the admin, teacher and parent portals while `scripts/link-integrity-check.js` reported `LINK INTEGRITY CHECK: PASS`. A green gate over the exact hole it was written to close, which is why it earns its own id rather than being folded into `PF-39`. **CLOSED 2026-08-07 by `S-E06-5`:** the extractor resolves a template href over the interpolated variable's declared union (three declaration forms, ≥2-member floor) and an interpolation it cannot resolve is **reported, never dropped** — `templateRows === expanded + shape + unparsed` is reconciled at runtime and an unrecognised kind is a structural failure. **What the closure does NOT cover, stated:** the `as const` + `(typeof X)[number]` declaration form is unresolvable (so `middleware.ts`'s `PortalId`-annotated login hrefs fall to the weak `/*/login` shape), a union declared in a sibling module is not resolved across files, a multi-hole template is expanded over the full cross-product with no cap, and the two out-of-scope `continue`s in `scanApp` discard 133 of 301 slash-leading templates **uncounted** — see `PF-103` | `CLOSED` | DEFECT | Discovered by the V3 run of 2026-08-07 (run 23), by measuring `S-E06-5`'s premise instead of reading it | L0 | V3-E06 |
| **PF-98** | **Three link families that only became visible once `PF-97` was closed, and that this routine may not fix.** (a) `/teacher/messaging` — the « Nouvelle annonce » CTA of the « Communiquer avec les familles » card (`apps/web/src/app/teacher/classes/[id]/page.tsx:903`), a route that was never emitted; deliberately **not** retargeted, because the nearest real surface `/teacher/messages/new` **ignores `classSectionId`** (`TeacherMessageComposer` seeds the class from `classes[0]`), so pointing the CTA there would silently compose an announcement for the **wrong class** — `DNC-06` at a new address. Fixing it means teaching the composer to honour the parameter. (b) `/admin/assessments/*` — the row link on the admin assessments list (`app/admin/assessments/page.tsx:184`); `/admin/assessments` is emitted, `/admin/assessments/[id]` is not, so the shape cannot resolve for **any** id. (c) `/admin/guardians/*` — four affordances built from a guardian id (`app/admin/guardians/page.tsx:192,238,239` including a `RowActions` `editHref`, plus `app/admin/enrollments/page.tsx:277`), same shape, same absence. All three are held by `scripts/link-integrity-baseline.json` (one `dead` row + two `deadShapes` rows) with a reason and this id — **queued, not silenced** | `BROKEN_RUNTIME` | DEFECT | Discovered by the V3 run of 2026-08-07 (run 23), by the extractor widening in `S-E06-5` | L0 | V3-E06 *(follow-up)* |
| **PF-99** | **No portal has a profile surface, and two have no settings surface either.** `S-E06-5` removed the three dead « Mon profil » entries from the authenticated shell (`TopbarUserMenu`), because the defect *"a navigation entry 404s"* is closable now while the capability *"a user can view and edit their own profile"* is a different theme and a different slice. Precisely scoped, because the loose reading is falsified by the tree: `/parent/settings` (`apps/web/src/app/parent/settings/page.tsx:133`) and `/teacher/settings` (`.../teacher/settings/page.tsx:162`) **do** exist and carry preference panels — what does not exist anywhere is a `/*/profile` route, and the **admin and student portals have no self-service surface at all** (`PORTAL_SETTINGS_HREF` in `apps/web/src/lib/portals.ts` is deliberately `Partial`, so "no surface → no menu entry" is the typed default). Overlaps `PF-57` (student portal breadth, L2) but is not the same row: this one covers all four portals and is about the *profile* capability | `NOT_IMPLEMENTED` | CAPABILITY | Recorded by `S-E06-5` (2026-08-07, run 23) as AC-4's queued half | L1 | V3-E06 *(follow-up)* |
| **PF-100** | **`apps/web/src/components/UserMenu.tsx` is an orphaned duplicate of `components/shell/TopbarUserMenu.tsx`.** Nothing imports it (verified by grep across `apps/web/src`), yet it carried its own copy of the same three dead `` `/${portal}/profile` `` hrefs, so the defect existed twice and could be "fixed" once. `S-E06-5` applied the same removal to both and marked the file, but did **not** delete it: it is currently the in-repo canary for the widened extractor's 3-member-union path, and deleting a component is a decision about the shell's history rather than a link fix. Either delete it, or make the guard spec's dependence on it explicit so the canary is not lost with the file | `TECH_DEBT` | DEFECT | Discovered by the V3 run of 2026-08-07 (run 23) | L1 | V3-E06 *(follow-up)* |
| **PF-101** | **`packages/contracts` declares three portals; `apps/web` re-declares four locally.** `S-E06-5` made `apps/web/src/lib/portals.ts` the single source of portal identity for the web app (`PORTAL_IDS`, `PORTAL_LANDING`, `PORTAL_REQUIRED_ROLES`, `PORTAL_SETTINGS_HREF`) and it is deliberately import-free so the Next **edge** middleware can consume it — which is exactly why it could not be put in `packages/contracts`, whose CJS `dist/` is shared with the api and worker runtimes. The residual is that the shared package's own `PORTALS` constant still knows only `admin` / `teacher` / `parent`, predating the student portal, so the monorepo has two portal vocabularies of different sizes. Recorded rather than fixed: reconciling them is a contracts change with api/worker blast radius | `BROKEN_TRUTH` | DEFECT | Recorded by `S-E06-5` (2026-08-07, run 23) | L1 | V3-E06 *(follow-up)* |
| **PF-112** | **The sprint workflow's readiness step cannot observe an ESLint error, so it returns `landed: true` on a tree whose web build is broken.** `S-E05-12` passed readiness on `pnpm typecheck` **13/13, exit 0** and its two targeted quality suites **214/214**; the very next `bash scripts/ci-gate.sh` returned **`GATE: FAIL (7 stage(s))`**. Neither typecheck nor jest can see a lint error, and in this repository that gap is not cosmetic: `no-control-regex` is an **error**, and `next build` runs ESLint — so a lint error does not warn, it **fails `@pilotage/web#build`**, and with no `.next` emitted the `web artefact` and `link integrity` stages fail behind it. One rule violation, four red stages, and every signal the implementing agent had was green. The general shape: *readiness verifies a strict subset of the merge gate, and the subset omits the stage a brand-new file is most likely to trip.* Second instance of the **`PF-80`** class, which is what makes it a class rather than an incident — different missing stage, though: `PF-80` wants `scripts/test-ratchet.js` wired into readiness, this wants `scripts/lint-ratchet.js`, which already fails on any ESLint error, so nothing new has to be written, only wired. Both live in `bmad/workflows/sprint.workflow.js`, the routine's own file, so no product slice can close either. Stated so it is not read as worse than it is: the merge gate caught it, **R-23** did its job, and nothing reached `main`. **Renumbered from `PF-104` to `PF-112` on 2026-08-08 by `S-E02-18`**: two different findings had been filed under `PF-104` — this one, and run 24's stalled-job finding two rows below. The stalled-job one keeps the id because `bmad/roadmap.md`, `traceability-matrix.md` row 114 and §5's narrative all already point at it; this row moves because nothing but `docs/spec/features/v3-e05/PROGRESS.md` referenced it. History is preserved, not deleted: the collision is recorded here rather than quietly overwritten, because an id that silently changes meaning is exactly the class of defect this index exists to catch | `TECH_DEBT` | DEFECT | Discovered by the V3 run of 2026-08-08 (run 24), by running the full gate on a slice whose readiness was green | L0 | V3-E02 *(routine)* |
| **PF-103** | **`scripts/link-integrity-check.js`'s hand-rolled lexer still mis-reads two shapes, and its anti-drop invariant is a tautology.** Three residuals of the `PF-97` widening, all **latent on this tree** (measured: 0 surviving `{/*` markers and 0 lost hrefs across 365 files) but live in a **blocking** CI stage, where a false red is the `R-30` trap. (a) `'}'` is a member of `REGEX_MAY_FOLLOW_PUNCTUATION` (`:415`), so in JSX the `/` of `{expr} />` is read as a regex opener and the scanner swallows to the next `/`: `stripCommentsPreservingLines('<X a={b} /> {/* <Link href="/admin/ghost" /> */}')` returns its input **unchanged** and the gate then emits `/admin/ghost` as a live target. The `S-E06-5` fix guarded only the adjacent `</` case (`regexMayStartAt` refuses when `src[i-1] === '<'`); this one is not adjacent. (b) `templateRows === expanded + shapeChecked + unparsed` (`:1264`) partitions **one array by `kind`** and therefore cannot fail on a drop — the real drops are the two `continue`s at `:1105` and `:1118`, which discard **133 of 301** slash-leading templates before any counter moves, so the printed *"168 interpolated"* is not the number of interpolated hrefs the gate saw. Same shape for `expansions.filter(isInternalRouteTarget)` (`:1117`), which can halve a row's coverage while it still reports `kind: 'expanded'`. (c) `keyof typeof C` (`:875`) has no minimum-member floor, so a spread or computed-key map silently **under-approximates** (measured: a 3-key map behind a spread resolves to one member). Fixes: drop `'}'` or refuse `skipRegex` when `src[i+1] === '>'`; count the drops as an `out-of-scope` kind and print it; make `objectLiteralKeys` return `null` on a spread/computed key or `< 2` literal keys; cap the cross-product and fall back to the shape check | `TECH_DEBT` | DEFECT | Discovered by the V3 run of 2026-08-07 (run 23), by the verify panel measuring the new lexer instead of reading it | L0 | V3-E06 *(follow-up)* |
| **PF-104** | **A job that fails by *stalling* reaches no counter.** In `bullmq@5.76.8` the worker's only `emit('failed', …)` is in `handleFailed` — i.e. the processor threw. `Worker.moveStalledJobsToWait` runs the Lua script that moves a job past `maxStalledCount` (default 1) straight into the `failed` set and emits **`'stalled'` alone**, with a bare jobId and no `'failed'` on any worker. So `pilotage_queue_jobs_total` and `pilotage_queue_job_duration_seconds` (`apps/worker/src/shared/observability/queue-metrics.ts:437`) silently miss the single most operationally important failure mode of a task executor: a worker OOM-killed, SIGKILLed, or losing its lock mid-job. Panel 6 (« Échecs — réintentables vs terminaux ») stays flat at zero while `pilotage_queue_depth{state="failed"}` climbs — the *green, silent, false* shape this epic exists to close — and the `failed_terminal` series `S-E02-17` AC-2 sells as "the question a DLQ panel really asks" under-reports precisely when the system is broken. Fix: `@OnWorkerEvent('stalled')` on the three processors, recorded either as a fourth outcome value or as a separate `pilotage_queue_stalled_total{queue}` (**not** labelled by `job` — only a jobId is available), and state in `queue-metrics.ts` that BullMQ's `failed` event does not cover stalled jobs so the next reader does not re-derive the gap. **CLOSED 2026-08-08 by `S-E02-18`:** `pilotage_queue_stalled_total{queue}` as a **separate family**, not a fourth `outcome` — the event carries a bare `jobId` and asserts no terminality, so folding it into the outcome counter would have claimed a job label it does not have and a finality BullMQ does not promise. `observeJobStalled(queue: string)` has **arity 1**, which makes the identifier *unrepresentable* rather than merely discouraged; the three `@OnWorkerEvent('stalled')` handlers are one line each, and `queue-metrics.ts` now states in prose that `failed` does not cover stalled. Driven, not asserted: `queue-depth.collector.spec.ts` C7/C9 call the handlers **off the real processor prototypes** with a cuid jobId and a `{data:{tenantId}}` receiver and assert the exposition contains neither, with an anti-vacuity `sample(...) === 2`. Plotted on a **new panel 9**. **Not covered, stated:** the counter is deliberately **not** zero-seeded (so "No data" reads as *never happened*), but both spec files `.reset()` it in `beforeEach`, so the two assertions claiming *"it is not zero-seeded"* pass either way — an R-26 vacuity carried to `S-E02-19`; and the deviation from AC-4 (a new panel 9 rather than a second target on panel 6) is recorded in `docs/spec/features/v3-e02/PROGRESS.md` | `CLOSED` | DEFECT | Discovered by the V3 run of 2026-08-07 (run 24), by the verify panel reading BullMQ's emit sites instead of its documentation · closed by run 26 | L0 | V3-E02 *(follow-up)* |
| **PF-105** | **Check 9's rule 6 is a tautology and cannot detect the defect it was written to close.** `scripts/observability-check.js:981` requires `declared ≡ rendered`, where `rendered` is scraped from the `queue="…"` labels of the worker exposition — but in the gate's own process the only queue-labelled samples in existence are the ones `queue-metrics.ts:304` zero-seeds at import (`for (const queue of INSTRUMENTED_QUEUES) queueDepthCollectionFailures.inc({ queue }, 0)`). `rendered` is therefore *derived from* `INSTRUMENTED_QUEUES` and equal to `declared` by construction. The stated purpose — *"a declaration nobody executes is the defect this check exists to close"* — is unmet: a queue listed in `INSTRUMENTED_QUEUES` that no write path ever touches renders its zero-seeded counter and passes rule 6 unchanged. T15c/T15d prove the *rule* against a hand-built fixture; nothing proves the real input can differ from `declared`. Rules 3/4/5 (api vs worker vs declared, read off the resolved `BullQueue_*` tokens) are real and unaffected. Fix: make `rendered` a value only a real write path can produce (drive a synthetic depth source + an allow-listed `observeJobCompleted` per declared queue before rendering, then require **both** `pilotage_queue_depth` and `pilotage_queue_jobs_total` to carry the label), or replace the half with a resolved read of the built `queue-depth.collector.js`; and correct the header claim that it confirms the declaration against "what the process really publishes". **CLOSED 2026-08-08 by `S-E02-18`, with one honest caveat.** Both halves of the recommended fix shipped: `readInstrumentedQueues()` drives a synthetic depth source **and** an allow-listed `observeJobCompleted` per queue before rendering, and `rendered` is now the **intersection** of `pilotage_queue_depth` ∩ `pilotage_queue_jobs_total` — neither of which is zero-seeded, so a label can only appear if a real write path produced it. The drive set itself is read off the **collector's own resolved `self:paramtypes`** (`readCollectorBoundQueues`), not off `INSTRUMENTED_QUEUES`, and *that* independence is what turns `A ≡ A` into a comparison. `queueLabelsOfFamily` matches families exactly, so the zero-seeded `…_depth_collection_failures_total` / `…_depth_sources_bound` cannot re-introduce the tautology. The header claim was corrected with it. **The caveat:** the rewritten rule has been executed only against **fixtures**. `node scripts/observability-check.js` reads `apps/worker/dist`, which on the landing checkout still holds `S-E02-17`'s build, so the real-input half of this closure is proven the moment the worker is rebuilt and not before — recorded rather than claimed. Two residuals carried to `S-E02-19`: `readInstrumentedQueues` mutates the shared prom-client registry and restores it in a `finally` **nothing asserts**, and `readCollectorBoundQueues` has no test and `return null`s on six causes that collapse into one message naming the wrong file | `CLOSED` | DEFECT | Discovered by the V3 run of 2026-08-07 (run 24), by the verify panel measuring the new gate's own input · closed by run 26 | L0 | V3-E02 *(follow-up)* |
| **PF-106** | **Nothing executes the queue instrumentation's wiring.** `grep -rn 'OnWorkerEvent' --include=*.spec.ts apps/` returns zero hits, and `QueueDepthCollector.onModuleInit` (`apps/worker/src/shared/observability/queue-depth.collector.ts`) has no reference outside its own module — all 39 `S-E02-17` tests call `observeJobCompleted` / `observeJobFailed` / `registerQueueDepthSources` **directly**, proving the helpers and never the seam. Every layer above is blind: check 6 reads `# TYPE` lines only (a registered-but-never-observed family passes), and check 9 cannot see it either (`PF-105`). So dropping `ObservabilityModule` from `app.module.ts`, drifting one `@InjectQueue` token, misspelling an event name (`'complete'`, which BullMQ never emits) or writing `observeJobCompleted(QUEUE_EXPORTS, job)` inside `ImportsProcessor` ships **green on every gate** while `pilotage_queue_depth` renders no samples at all and panels 6–8 read "No data" for ever — indistinguishable from an idle system, and the same "gauge reads 0 on a backlogged queue" failure the eight-state analysis was written to prevent, arriving through the wiring instead of the state list. Fix: a `queue-depth.collector.spec.ts` that builds a `Test.createTestingModule` over `getQueueToken(...)` stubs, `await moduleRef.init()`, asserts `queueDepthSourceCount() === INSTRUMENTED_QUEUES.length` and that a rendered `pilotage_queue_depth` sample equals the **stub's** number; plus per-processor `onCompleted`/`onFailed` cases asserting the rendered `queue="…"` label, one assertion over the `@OnWorkerEvent` metadata, and `AppModule`'s `imports` containing `ObservabilityModule`. Related residual: `onModuleInit` degrades to a **partial** binding silently — a queue failing the `typeof queue.getJobCounts === 'function'` filter is dropped with only a `logger.log` count nobody compares against `INSTRUMENTED_QUEUES.length`, while its zero-seeded failure counter still renders it as healthy. **CLOSED 2026-08-08 by `S-E02-18`:** `apps/worker/src/shared/observability/queue-depth.collector.spec.ts` (**new**, 427 lines) executes the seam instead of the helpers — the three `@OnWorkerEvent('stalled')` handlers are invoked **off the real processor prototypes**, the `ON_WORKER_EVENT_METADATA` key is guarded against a Nest rename **with an anti-vacuity assertion** (C8/C11b: the same extraction against a throw-away `@Injectable()` with no handler must return nothing, so the predicate is shown able to fail), and C11 reads `ObservabilityModule`'s **resolved** `imports` metadata so a dropped module is a red test rather than a silent "No data". **53/53 executed.** The `onModuleInit` partial-binding residual this row names is closed too, and by a metric rather than a log line: `pilotage_queue_depth_sources_bound{queue}` is written **only** inside `registerQueueDepthSources`, and the collector *reads* the shortfall through `unboundInstrumentedQueues()` rather than recomputing it — the obvious place to duplicate a subtraction, not duplicated. **Not covered, stated:** the new gauge is named by panel 5's prose but queried by no `expr` on any panel — `PF-107`'s exact shape, one metric over, recorded as a residual of this slice rather than silently inherited | `CLOSED` | DEFECT | Discovered by the V3 run of 2026-08-07 (run 24), by the verify panel and the edge hunter independently · closed by run 26 | L0 | V3-E02 *(follow-up)* |
| **PF-107** | **The series that makes a Redis outage legible is plotted nowhere.** `pilotage_queue_depth_collection_failures_total` was added by `S-E02-17` specifically so a swallowed collector error is observable rather than silent (ADR-028 D2 rule 4), and panel 5's own description in `infra/grafana/dashboards/pilotage-slo.json:79` routes the on-call to it by name — « cette asymétrie EST le signal de diagnostic, et `pilotage_queue_depth_collection_failures_total` la nomme ». No panel queries it: the four new `expr`s (lines 85/101/117/133) reference `pilotage_queue_depth`, `pilotage_queue_jobs_total` (×2) and `pilotage_queue_job_duration_seconds_bucket` only, and the metric appears in the file exclusively inside descriptions and a `__comment`. Check 6 validates dashboard→registry, never registry→dashboard, so no gate can see it. Depth degrades to **stale**, never zero (deliberate, and correct — an operator reading `0 waiting` during an outage is worse informed than one reading a stale number), which means the dashboard as shipped renders a total Redis outage as flat, plausible lines with nothing naming the outage — while the frozen queues are the ones carrying bulletins and guardian alert email. Fix: one JSON target on panel 5, `sum by (queue) (rate(pilotage_queue_depth_collection_failures_total[5m]))`, legend `{{queue}} · échecs de collecte` (no `thresholds.steps`, consistent with the deliberate no-threshold posture); or soften the description so it stops presenting an unplotted series as the diagnostic signal. **CLOSED 2026-08-08 by `S-E02-18`:** `pilotage_queue_depth_collection_failures_total` is now queried by a real target on the dashboard, so the series panel 5's description names is the series a reader can see. **The class is NOT closed, and the same slice reproduced it once:** `S-E02-18` adds `pilotage_queue_depth_sources_bound`, routes panel 5's rewritten description to it **by name** (« Une file dont aucune source n'est branchée se lit sur `pilotage_queue_depth_sources_bound` »), and plots it on no `expr` — verified by enumerating every target on panels 1–9. No gate can see that, because check 6 validates dashboard→registry and never registry→dashboard, which is why the fix here was a JSON target and not a gate. The remedy is one target — `max by (queue) (pilotage_queue_depth_sources_bound)`, and it **must** be `max`, not `sum`, or this slice's own new check 10 will (correctly) reject it — carried to `S-E02-19` | `CLOSED` | DEFECT | Discovered by the V3 run of 2026-08-07 (run 24), by the security and architecture panels independently · closed by run 26 | L0 | V3-E02 *(follow-up)* |
| **PF-108** | **A gauge describing a shared resource is summed across replicas.** `infra/grafana/dashboards/pilotage-slo.json:85` uses `sum by (queue, state) (pilotage_queue_depth)`. Depth is a property of the **queue**, not of the process — which is exactly why every worker replica reports the same absolute number for the same queue; Prometheus keeps the series apart by `instance`, and `sum` then adds them. `docker compose up --scale worker=2` doubles every line on the graph with no code change and no gate noticing: the same "one number, two sources → a graph that contradicts itself" defect `ADR-028` D1 is written to prevent, one dimension over. Panels 6–8 are correct as written — they `sum` `rate()` over counters, which *is* additive across replicas. Fix: `max by (queue, state) (pilotage_queue_depth)` (correct for one replica and for N) and a description line saying why the gauge is aggregated with `max` while the counters beside it are aggregated with `sum`. **CLOSED 2026-08-08 by `S-E02-18`, and generalised into a gate:** the dashboard moved to `max by`, and a **new blocking check 10** (`evaluateGaugeAggregation`) rejects any `sum()` over a family whose **declared `# TYPE`** is `gauge` — stated over the type, so it catches the *class* rather than this one line, and it fails **closed** on a missing/empty/non-object type map (pinned by T18f/T18g). `readExposedMetricNames` was widened **additively** (`exposedMetrics` stays a `string[]`, types land on a new field), because reshaping the existing field would have made check 6's `if (exposedSet.size === 0) break` a vacuous pass inside a blocking stage — that reasoning is written down and pinned by T18h. **Not covered, and it is the reason a follow-up exists:** check 10 walks `input.dashboards` only, so a future Prometheus **alerting/recording rule** that `sum()`s the gauge is outside it; and the rule as shipped **false-reds** on two correct inputs — a `sum` token inside a quoted label value, and the canonical `sum by (queue) (max by (queue, state) (pilotage_queue_depth))`, which is the very expression check 10's own message recommends. Both measured, both latent on this tree, both carried to `S-E02-19` | `CLOSED` | DEFECT | Discovered by the V3 run of 2026-08-07 (run 24) · closed by run 26 | L1 | V3-E02 *(follow-up)* |
| **PF-109** | **A `DNC-06` guard is defeated by a French plural, in the file it inspects.** T17b (`apps/api/src/shared/quality/observability-gate.spec.ts:846`) forbids dead-letter vocabulary in the real dashboard but lists only singular French terms (`'lettre morte'`, `'file morte'`), while `infra/grafana/dashboards/pilotage-slo.json:4` already contains « une file de lettres mortes » — verified: all five forbidden terms report `false` against the current file. Unlike the `DNC-10` guard a hundred lines below, T17b has **no companion "the guard can actually fail" case**, so the gap is invisible: the guard passes, the term is present, and nothing says so. Fix: match stems (`/dlq|dead[- ]letter|lettres?\s+morte|files?\s+morte|mise au rebut/i`) and add the companion case feeding a synthetic panel title through the same predicate and asserting it fires, mirroring the `DNC-10` pair already in the file. **CLOSED 2026-08-08 by `S-E02-18`:** T17b matches stems rather than exact singular terms, and — the half that makes it a closure rather than a patch — **T17b2 is the companion case**, feeding a synthetic panel title through the same predicate and asserting it fires. The guard was shown **red on the French plural that defeated it** before being shown green, which is the R-26 rule this row exists to enforce: a guard never shown red is a guard we hope works | `CLOSED` | DEFECT | Discovered by the V3 run of 2026-08-07 (run 24), by running the guard's own predicate against the file it guards · closed by run 26 | L1 | V3-E02 *(follow-up)* |
| **PF-110** | **`ADR-028` takes a number already reserved for a different decision, and the register is still not reconciled.** `docs/daily-improvement-v3/architecture-impact.md:80` (§4, *New ADRs required*) reserves **ADR-028** for *"Audit in-transaction, chain genesis, and the accepted pre-V3 gap"*, epic **V3-E04**, `Blocking: yes` — and `V3-E04` is one of the two immediate next epics. `S-E02-17` takes the number for an unrelated observability decision and leaves §4 untouched; `ADR-028`'s own header says *"Supersedes / narrows: nothing"*, so nothing in the file records the reassignment either. `ADR-025`/`026`/`027` collided with the same table in earlier slices, which is what makes this instance matter: the next agent to open §4 reads four reserved numbers, finds all four occupied by other subjects, and has **no written rule for which register wins** — the `R-26` shape ("a value that appears twice is a declaration that will eventually be false") landing on the ADR index itself. Fix (cheapest honest one): keep the file, add a line to §4 recording that 025–028 were consumed by `V3-E02` slices and that the reserved decisions renumber from ADR-032, plus a `Numbering` note in `ADR-028`'s header pointing at the reconciliation. **CLOSED 2026-08-08 by `S-E02-18`, and resolved the right way round:** the **shipped** ADR keeps its number and the **reservation** moves (`architecture-impact.md` §4 renumbers the unwritten decisions to `ADR-032`…`035`), because renumbering a shipped file would have silently repointed every existing citation. The actual fix is neither of those edits but the **written precedence rule** they now carry: `docs/adr/` is the register of record, the planning table is a wish list. `ADR-028` gained the `Numbering` header note. **Residual, and it is in the same paragraph:** the sentence six lines under the new rule still reads *"Superseding it with ADR-025 … is part of `V3-E01`'s definition of done"*, which under the rule just written points `ADR-002`'s supersession at the drill decision — carried to `S-E02-19` | `CLOSED` | DEFECT | Discovered by the V3 run of 2026-08-07 (run 24), by the drift reviewer · closed by run 26 | L1 | V3-E02 *(follow-up)* |
| **PF-111** | **A running container can be arbitrarily older than the code, and nothing on the local `up` path says so.** Measured at run 24’s Step 2: `pilotage_worker` was running an image built **2026-06-06** — two months older than `S-E02-10`/`S-E02-13`/`S-E02-14`/`S-E02-15` — whose `dist/` contained neither `shared/release/` nor `shared/observability/`. `127.0.0.1:4001` therefore refused connections and **0** metric series existed in the *running* stack, while four `traceability-matrix.md` rows read green: each had been proven against `dist/` on disk and against sockets the check scripts spawn themselves, which is sound evidence about the **build** and no evidence at all about the **deployment**. This is **not** a code defect — `EXPECTED_GIT_SHA` is unset, which by `S-E02-10`’s deliberate design yields verdict `unverified` and blocks nothing (the documented DNC-08/DNC-10 exemption in `main.ts`) — and the mechanism that would catch it therefore already exists and is simply not armed locally. It matters because **Step −1 of the V3 routine makes the local Docker stack the target**, which turns “it works locally” into a claim about containers; a two-month-old container makes that claim false in silence, and the next run reads its own gates as green against nothing. `R-05`’s class, and the reason Step 4b exists. Fix candidates, cheapest first: (a) set `EXPECTED_GIT_SHA` in the local `.env` so the existing release preflight refuses to boot a stale artefact; (b) a `ci-gate.sh` stage comparing each running container’s image creation time against `HEAD`’s commit time and failing on inversion. Note (a) is nearly free and (b) is the one that cannot be forgotten | `TECH_DEBT` | DEFECT | Discovered by the V3 run of 2026-08-07 (run 24), by the routine measuring the local stack before trusting it | L0 | V3-E02 *(follow-up)* |
| **PF-113** | **No service in this repository can be scaled, so the multi-replica behaviour `PF-108` describes cannot be reproduced by the command `PF-108` prescribes.** Measured while proving `PF-108`: `docker compose --env-file .env -f infra/docker-compose.yml up -d --scale worker=2 worker` is **refused** — *"The `worker` service is using the custom container name `pilotage_worker`. Docker requires each container to have a unique name. Remove the custom name to scale the service"* — and it is not specific to the worker: **16 of the file's 21 services declare `container_name`**, so `--scale` is unavailable for every one of them. Two consequences worth separating. (1) `PF-108` is still a **real** defect — a gauge summed across replicas is wrong arithmetic wherever replicas exist, and the prod overlay or any orchestrator would produce them — but its stated reproduction is not executable here, so its fix had to be demonstrated by injecting a second `instance` label into the PromQL (`sum` → 4, `max` → 2, real depth 2) rather than by running two workers. That is a weaker demonstration than running the real thing, and it is recorded as such rather than presented as equivalent. (2) More generally, **nothing in this repository has ever run with more than one instance of any application**, so every per-process assumption in the observability layer — process-local counters, the depth collector's one-source-per-queue binding, the release manifest's single socket — is untested against the shape a replica-bearing deployment would take. `container_name` is deliberate and useful locally (`docker logs pilotage_worker`), so the fix is not to delete it: it is to decide whether horizontal scaling is in scope, and if it is, to add an overlay without `container_name` on the two application services plus a check that exercises it. Priority **P3** — nothing is wrong today and the routine's target is a single-instance local stack (Step −1); the cost is that a whole class of defect is unreachable by any gate | `TECH_DEBT` | GAP | Discovered by the V3 run of 2026-08-08 (run 26), while executing `PF-108`'s own reproduction command | L0 | V3-E02 *(follow-up)* |
| **PF-114** | **Check 10 goes red on the one expression its own error message recommends.** `scripts/observability-check.js` flags any `sum()` whose argument names a gauge-typed family — but `sumAggregationArguments` returns the sum's **direct** argument without asking whether that argument has already collapsed the replica dimension, so `sum by (queue) (max by (queue, state) (pilotage_queue_depth))` is flagged. That expression is **correct PromQL** and is the canonical de-duplication idiom: `max` removes the per-`instance` duplication, `sum` then adds the states, which is precisely what check 10 tells the reader to do. Executed against the shipped evaluator on 2026-08-08: `max by (queue, state) (pilotage_queue_depth)` → ok; `sum by (queue, state) (pilotage_queue_depth)` → FLAGGED (correct); `sum by (queue) (max by (queue, state) (pilotage_queue_depth))` → **FLAGGED (wrong)**; `sum by (queue) (rate(pilotage_queue_depth_collection_failures_total[5m]))` → ok. **Not red today** — no dashboard expression uses the nested form — which is exactly why it is scheduled rather than hot-fixed: the failure is latent until someone writes the correct thing. It matters because a blocking stage that rejects correct work is the mirror of the defect `S-E02-18` closed, and `observability-check.js`'s own header says why it is equally corrosive: it teaches people to skip the gate. Fix: treat a `sum` argument as safe when its outermost operation is a replica-collapsing aggregation (`max`/`min`/`avg`/`topk`/`bottomk`) that does not group by `instance`, with a case for the nested idiom and a case proving the bare gauge is still rejected. **CLOSED 2026-08-08 by `S-E02-19` (run 27)** — but *not* by the fix this row proposed, and the difference is the whole lesson. The proposed fix keyed the shield on an operator list (`max`/`min`/`avg`/`topk`/`bottomk`); the slice keyed it on the **grouping modifier** instead (`unshieldedGaugeReferences` + `REPLICA_LABELS`), because an operator name says nothing about whether the replica dimension survived. Measured after the fix: the nested idiom is clean, the bare gauge is still flagged, `max by (queue, state, instance)` is still flagged, and `max_over_time` is still flagged. **Residual, and it is exactly the part of this row's proposed fix that was wrong:** `topk`/`bottomk` were nevertheless left in `ACROSS_SERIES_AGGREGATIONS`, and they *select* series rather than collapse them — so `sum by (queue) (topk(3, pilotage_queue_depth))` is now **accepted** where `HEAD` flagged it. Carried to `PF-116` | `CLOSED` | DEFECT | Discovered by the V3 run of 2026-08-08 (run 26) — raised by the sprint's verify panel and then confirmed by the routine driving the shipped evaluator, rather than accepted on the panel's word (`R-30`) · closed by run 27 | L0 | V3-E02 *(follow-up)* |
| **PF-115** | **Three gaps inside `S-E02-18`'s own mechanism, none of them red today.** (a) `readCollectorBoundQueues()` has **no test** and returns `null` on six distinct causes that collapse into one message naming the **wrong file** — it says `queue-metrics.js` when the unreadable artefact is `queue-depth.collector.js`, so the operator is sent to the wrong place at the moment the gate fails. (b) `readInstrumentedQueues()` now **writes to the shared prom-client registry** — it must, since driving a real write path is how `PF-105` was closed — and restores it in a `finally` that **nothing asserts**; a leak there fabricates queue traffic that every later `registry.metrics()` reader in the same process, including a future check, would treat as real. That is the "green and false" shape this epic exists to close, relocated into the mechanism written to close it. Mitigating and measured: the routine ran the script three times in one session (control, negative, control) and it returned `PASS`/`FAIL`/`PASS` with no residue, so the restore does work — it is simply not *guaranteed* by a test. (c) `T-STALL-6` declares a `JOB_ID` constant it never passes to anything, so its `not.toContain(JOB_ID)` cannot fail, and its comment claims otherwise; the genuine driven G-TENANT proof is case `C9` in `queue-depth.collector.spec.ts`, plus the live drill recorded on `PF-104`. Fix (a) with a per-cause message naming the file it actually read, (b) with a case that asserts the registry is byte-identical before and after, and (c) by driving the constant or deleting it. **CLOSED 2026-08-08 by `S-E02-19` (run 27), all three.** (a) both readers return a discriminated `{ok}` result over a **14-entry reason enum**, each entry naming the file it actually read; driven end-to-end with the collector moved aside — the message went from *« could not be read from …/queue-metrics.js »* (the wrong file, this row verbatim) to *« the built queue-depth collector is absent (…/queue-depth.collector.js) »*. (b) the restore is now **verified, not trusted**: a pure `registryResidue()` re-renders the registry after the `finally` and reports a leaked depth sentinel, a driven `(queue, job, outcome)` sample or a still-bound depth source **ahead of** any drive error; driven with the `finally` deliberately blanked → **exit 1 `INSTRUMENTATION UNREADABLE (registry-not-restored)`**, enumerating all nine residues. (c) `T-STALL-6` now drives the cuid through `observeJobStalled`'s single queue-name parameter and asserts it collapses to `<other>`, shown able to fail by making `queueLabel` a pass-through — which left `HEAD`'s version green, and is what made it vacuous. **Residual on (b):** `registryResidue`'s `pilotage_queue_jobs_total` branch cannot match on the *throw* path, because `drivenOutcomes` is populated only inside the `try` — carried to `PF-118` | `CLOSED` | DEFECT | Discovered by the V3 run of 2026-08-08 (run 26), by the sprint's verify panel against its own output · closed by run 27 | L0 | V3-E02 *(follow-up)* |
| **PF-116** **CLOSED 2026-08-08 by `S-E02-19` (run 27), verified by run 28 against the shipped code** — `scanQuotedSpans` returns `{masked, unterminated}` and both callers route it into their PROBLEM path (`scripts/observability-check.js:289`, `:295`, `:403`), and `topk`/`bottomk` are documented as SELECTORS held out of the shielding set (`:443`, `:449`, `:477`). **This status column read `TECH_DEBT` until run 28 while `traceability-matrix.md` already read `closed` — the two ledgers disagreed for a full run.** Corrected by reading the code rather than by picking a ledger (`R-30`). | **The replica shield accepts `topk`/`bottomk`, which SELECT series rather than collapse them — so the blocking stage stops catching the very defect `PF-108` named.** `S-E02-19` replaced check 10's one-level metric scan with a recursive shield walk, and keyed the shield on the **grouping modifier**, which is right. But `ACROSS_SERIES_AGGREGATIONS` (`scripts/observability-check.js:409`) still lists `topk` and `bottomk`, and `groupingRemovesReplicas(null, …)` (`:441`) returns `true` for an aggregation with no modifier — so their argument is marked shielded at `:558`. `topk(3, x)` and `bottomk(k, x)` are the two PromQL aggregation operators that return their input series **unchanged, every label included**: they select k elements, they do not aggregate. The `instance` dimension survives them, so `sum by (queue) (topk(3, pilotage_queue_depth))` still double-counts every replica — literally `PF-107`/`PF-108`. **Measured both directions on the landing checkout, by the land pass rather than taken from the panel (`R-30`)**: `HEAD` (`79921da`) `metricNamesInExpr('topk(3, pilotage_queue_depth)')` → `['pilotage_queue_depth']` → PROBLEM; after the slice `unshieldedGaugeReferences(…)` → `{families: [], unscannable: false}` → **accepted**. Same for `bottomk(1, …)` and `topk by (queue) (3, …)`. This is therefore a **regression in gate power introduced by the fix**, not a pre-existing hole — and it is AC-4's own stated trap (*"a fix that shields on the string `max` rather than on aggregation semantics"*) reappearing on an operator that IS in the list, which is why AC-4's `max_over_time` cases do not reach it. The fix must stay **narrow**: `quantile`, `count_values` and `group` with no modifier genuinely do collapse to one series per group. Cheapest honest fix: a `SELECTOR_AGGREGATIONS = new Set(['topk','bottomk'])` consulted at `:558` — `const shieldsHere = !SELECTOR_AGGREGATIONS.has(match[2]) && groupingRemovesReplicas(kind, labelList)` — so the operators are still *parsed and consumed* (the `unscannable`/`DNC-08` path must not change) and the gauge inside is reached by the residual scan. Plus three AC-4-shaped negatives, red pre-fix. **Latent today**: `grep -c 'topk\|bottomk' infra/grafana/dashboards/pilotage-slo.json` → 0 — which is the argument for closing it now rather than after someone writes a top-k panel | `CLOSED` | DEFECT | Discovered by the V3 run of 2026-08-08 (run 27) — raised independently by all three members of the escalation panel, then re-measured by the land pass driving both the `HEAD` and the landing evaluator | L0 | V3-E02 *(follow-up)* |
| **PF-117** **CLOSED 2026-08-08 by `S-E02-19` (run 27), verified by run 28 against the shipped code** — `scanQuotedSpans` returns `{masked, unterminated}` and both callers route it into their PROBLEM path (`scripts/observability-check.js:289`, `:295`, `:403`), and `topk`/`bottomk` are documented as SELECTORS held out of the shielding set (`:443`, `:449`, `:477`). **This status column read `TECH_DEBT` until run 28 while `traceability-matrix.md` already read `closed` — the two ledgers disagreed for a full run.** Corrected by reading the code rather than by picking a ledger (`R-30`). | **An unterminated quote blanks the rest of the expression, and the blocking stage passes vacuously.** `maskQuotedSpans` (`scripts/observability-check.js:349`) was added by `S-E02-19`'s AC-7 step so that a `sum` token inside a quoted label value stops being a false PROBLEM — correct, and it fixed two real false reds. It opens on `"`, `'` or `` ` `` and, if no closing delimiter follows, emits **every remaining character as a space and returns normally, with no error signal**. Both callers (`sumAggregationArguments`, `unshieldedGaugeReferences`) then see an expression with no `sum` token and no metric reference, report nothing, and check 10 passes. **Measured by the land pass**: `sumAggregationArguments('foo{a="b} + sum by (queue) (pilotage_queue_depth)')` → `HEAD` `{args: ['pilotage_queue_depth'], unbalanced: false}` → PROBLEM; after the slice `{args: [], unbalanced: false}` → **silent accept**. This is a fail-**open** in the direction `DNC-08` forbids, and **the same diff restates the rule it breaks**, at `:381`: *an expression the reader cannot parse must be a PROBLEM, never an accepted expression*. The function it was modelled on — `metricNamesInExpr`'s `.replace(/"[^"]*"/g, ' ')` — cannot swallow a tail, because it requires a closing quote. It is also wider than PromQL suggests: the mask treats a bare `'` and a bare `` ` `` as openers, so one stray apostrophe in a hand-edited `pilotage-slo.json` expression is enough. Fix ≈ 4 lines and matches the file's own posture: return `{ masked, unterminated }` and have both callers raise the existing PROBLEM (`unbalanced` / `unscannable`) when `unterminated` is true, plus one case pinning that an unterminated quote goes **red**, not green | `CLOSED` | DEFECT | Discovered by the V3 run of 2026-08-08 (run 27), by the escalation panel's security and architecture lenses against the diff's own new code, then re-measured by the land pass | L0 | V3-E02 *(follow-up)* |
| **PF-118** **CLOSED 2026-08-08 by `S-E02-19` (run 27), verified by run 28 against the shipped code** — `scanQuotedSpans` returns `{masked, unterminated}` and both callers route it into their PROBLEM path (`scripts/observability-check.js:289`, `:295`, `:403`), and `topk`/`bottomk` are documented as SELECTORS held out of the shielding set (`:443`, `:449`, `:477`). **This status column read `TECH_DEBT` until run 28 while `traceability-matrix.md` already read `closed` — the two ledgers disagreed for a full run.** Corrected by reading the code rather than by picking a ledger (`R-30`). | **`registryResidue`'s outcome branch cannot match on the throw path, so one third of `AC-10`'s claim is unproven there.** `readInstrumentedQueues` (`scripts/observability-check.js`) populates `drivenOutcomes` only inside the `try`. If `observeJobCompleted` throws on the first queue, `driven` is empty and the `pilotage_queue_jobs_total` residue branch has nothing to look for — so on exactly the path the catch-and-still-verify design exists to cover, the check degrades from three residue tests to two. `AC-10` is **not** defeated: the depth sentinel (`DRIVEN_DEPTH_SENTINEL`) and the `pilotage_queue_depth_sources_bound` branches still fire on that path, and a leaked registry is still reported ahead of the drive error. What is one third weaker than stated is the claim *"three things, one per thing the drive writes"* (`DNC-06` territory). Cheapest honest fix: record each `(queue, job, outcome)` label tuple **before** the write that produces it, so the residue predicate knows what to look for even when the write throws | `CLOSED` | GAP | Discovered by the V3 run of 2026-08-08 (run 27), by the escalation panel's test-architect lens reading the new verification path | L0 | V3-E02 *(follow-up)* |
| **PF-119** | **A real admin page is reachable from nowhere, while the navigation entry beside it points at a page that does not exist.** `apps/web/src/app/admin/analytics/page.tsx` exists and renders « Analytique des performances », and `grep analytics apps/web/src/components/shell/sidebar-items.ts` returns **zero** hits — so the page can only be reached by typing the URL. Two lines away, `sidebar-items.ts:175` publishes `{ key: 'reports', icon: BarChart3, label: 'Rapports', href: '/admin/reports' }`, and `apps/web/src/app/admin/reports` **does not exist** (`PF-14`'s reports half, already inventoried in `scripts/link-integrity-baseline.json`). The two share the `BarChart3` icon, which is the tell: an analytics surface was built, the sidebar slot was written against a *different* name, and neither side was reconciled. This is why `S-E04-2` must decide `/admin/reports`'s fate on evidence rather than by reflex — "implement `/admin/reports`" may well be the wrong half of `AC-5`, since the capability plausibly already exists at another address and the honest fix is to **point the existing entry at the existing page**, not to build a second one. Note the link gate cannot see this: it checks that published hrefs resolve, not that resolvable routes are published, so an orphaned route is invisible to it by construction — the same asymmetry `PF-93` exposed for portal roots | `TECH_DEBT` | DEFECT | Discovered by the V3 run of 2026-08-08 (run 28), by the `epic-spec` sprint reading the navigation while scoping `AC-5`, then re-verified by the land pass (`ls` + `grep`) | L0 | V3-E06 *(raised)* → V3-E04 `S-E04-2` *(decides)* |
| **PF-120** | **A spec-kit shipped two different measurement registers under one `M-n` namespace — `PF-110`'s shape, in the diff that denounces `PF-110`.** `docs/spec/features/v3-e04/data-model.md` §0 and `PROGRESS.md` § *What Step 2 measured* were written by different agents in the same sprint and each opened its own `M-1…M-n` register. They agree through `M-6` and diverge after: `M-7` means *"audit write sites outside `apps/api/src`"* (**2**, `packages/imports-core/src/engine.ts:197`/`:288`) in one file and *"sites hard-coding `portal: 'admin'`"* (**9**) in the other; `M-24` means *RLS occurrences* (**0**) in one and *"third vocabulary consumer"* in the other. **Every individual measurement was accurate** — the land pass independently re-verified nine of them, including all four collided ids — so nothing false was published; what was published was an **ambiguous citation key**, in a kit whose whole purpose is to be cited by eight downstream slices. The same kit's `data-model.md` §ADR section explicitly refuses to take `ADR-029` *"parce que c'est le premier fichier libre"* on the grounds that it would recreate `PF-110`; the identical error was then committed one section earlier, for measurement ids, unnoticed. **CLOSED in the same run**: `data-model.md` §0 is declared the register of record and carries the `Source` column; the three measurements only `PROGRESS.md` held were re-verified and promoted as `M-32`…`M-34`; `PROGRESS.md`'s table was renumbered onto the canonical ids and now names the collision rather than quietly overwriting it. **The general lesson, which outlives this kit:** an id namespace opened by more than one agent in one sprint needs an owner, exactly like an ADR number — the sprint has no mechanism for that today, which is the untouched half | `CLOSED` | DEFECT | Discovered by the V3 run of 2026-08-08 (run 28), by the routine cross-reading the sprint's own output at the land pass (`R-30` — the numbers were being verified, not trusted, and the collision surfaced because two files disagreed about what `M-7` meant) | L0 | V3-E04 |
| **PF-121** | **The two audit writes outside `apps/api` still hard-code both provenance fields, and no JWT exists on their call path.** `packages/imports-core/src/engine.ts:201-202` and `:292-293` each write `actorRole: 'school_admin'` + `portal: 'admin'` inside a `tx.auditLog.create` (four literal occurrences, two write sites — `M-5`/`M-7`). They are shared verbatim by the API and by `apps/worker/src/modules/imports/imports.processor.ts`, so on the worker path the row is written while draining a BullMQ job: **there is no authenticated caller in scope at all**, and `S-E04-1`'s `deriveAuditProvenance(jwt)` seam cannot be applied without first capturing the provenance at *enqueue* time and carrying it in the job payload. That is real design work (a payload field, a producer change on both enqueue paths, and a ruling on what a job-written row should say about a portal it never passed through), which is why `S-E04-1` **did not touch it**: `AC-2` is scoped to `apps/api/src` by the epic contract, and widening the fix in the slice that installs the seam would have coupled a mechanical collapse to an unresolved decision. **Consequence to read plainly: after `S-E04-1`, `import.apply` and `import.rollback` rows still say `school_admin`/`admin` whoever ran them** — the epic's claim of one derivation is true of `apps/api/src`, not of the monorepo. Note `packages/imports-core/dist/` is the runtime `main` and a build artefact: editing `src` without a build changes nothing at runtime, which is a second reason this does not belong in a no-build slice. Registered before it was needed, cited as owner by `S-E04-1`'s story §1.5 and by its guard's walk-root comment, so the exclusion is queued rather than silenced | `TECH_DEBT` | DEFECT | Discovered by the V3 run of 2026-08-08 (run 29), by `S-E04-1`'s Step-2 measurement widening the literal grep past `apps/api/src` — the same walk-root lesson `S-E06-5` recorded, applied to a workspace boundary instead of a directory | L0 | V3-E04 `S-E04-7` *(owner)* |
| **PF-122** | **A fourth actor-role decision site lives INSIDE the walk root, decides both provenance fields from a default parameter, and writes a value that is not a realm role — and the new blocking gate is structurally blind to it.** `apps/api/src/modules/child-claims/child-claims.service.ts:722-729`: `private async audit(tx, args, action, before, after, actor: 'parent' \| 'admin' = 'parent')` writes `actorRole: actor` and `portal: actor`, called with the hard-coded literal `'admin'` at `:522` (`guardianship.claim_approved`) and `:609` (`guardianship.claim_rejected`) — two of the most governance-sensitive writes in the product, both reached from `admin-child-claims.controller.ts` handlers that **already carry `@CurrentJwt() jwt`**, i.e. exactly the Pattern-B seam `S-E04-1` installed twice (imports, integrations), not the JWT-less design work that justified deferring `PF-121`. Three consequences: (1) a `super_admin` approving a claim is audited **`actorRole: 'admin'`, a string that is not a Keycloak realm role at all** (the four are `super_admin`/`school_admin`/`teacher`/`parent`) and that `ROLE_PRECEDENCE`/`ROLE_PORTAL` can never produce — so `/admin/audit`'s role facet carries a fifth orphan token no label map in the codebase knows, and `S-E04-4` will read **two incompatible actor vocabularies out of one column**; (2) the parent paths write `actorRole: 'parent'` from a **default parameter**, not from the token; (3) it passes **all four** matchers of `apps/api/src/shared/quality/audit-provenance-gate.spec.ts` by construction — `HARDCODED_ACTOR_ROLE` is pinned to the literal `'school_admin'`, `HARDCODED_PORTAL` requires a quote immediately after `portal:` (here it meets `actor`), the role-precedence matcher needs four role names in one bracketed literal, and the inline-portal matcher needs a `?` ternary. **The root cause is a mis-measurement, not an oversight in the code**: `docs/spec/features/v3-e04/spec.md:22` and `data-model.md` `M-6` classify this file among *"the other 20 that already take `actorRole` from arguments"* — taking it from a **call-site literal** is not deriving it from the JWT. That is the same class of mis-measurement `S-E06-5` recorded, arriving one layer up from where `S-E04-1` defended against it. **Not a regression** — pre-existing, unchanged by the slice; what the slice added is a **blocking** gate whose message reads *"exactly one file decides an actor role"* while this file does too, which is why `AC-1` and the two ledger claims were **narrowed** in the same land pass rather than ticked. Fix: thread `deriveAuditProvenance(jwt)` from the admin and parent controllers into `child-claims.service.audit()`, dropping the `actor: 'parent' \| 'admin'` parameter, and widen the gate from three known literals to a rule over the **column** (*every `auditLog.create` payload must reach `actorRole`/`portal` from a `derive*Provenance` call in the same lexical scope*) with this site as a positive control | `TECH_DEBT` | DEFECT | Discovered by the V3 run of 2026-08-08 (run 29), by the escalation panel's architecture lens checking `AC-1` against the walk root instead of against the diff — then re-verified by the land pass by reading the file (`R-30`), which confirmed the signature, both `'admin'` call sites and all four matcher blind spots | L0 | V3-E04 `S-E04-7` *(owner)* |
| **PF-123** | **The write the whole platform is about records no actor role at all.** `apps/api/src/modules/grades/assessments.controller.ts:290` writes `action: 'assessment.publish'`, `resourceType: 'assessment'` inside a `$transaction` with **no `actorRole` key and no `portal` key** — both columns `null`. Grade publication is the event the alert engine, the parent dashboard and every notification hang off, and its audit row cannot say who published. It is invisible to `S-E04-1`'s gate for a structural reason worth stating once: **a rule that bans literals cannot see an absence** — `HARDCODED_ACTOR_ROLE`/`HARDCODED_PORTAL` match values that are wrong, not keys that are missing, and `G-AUDIT`'s per-file check greps that the file still *contains* the substring `auditLog.create`. Compounding it on the read side: `analytics.service.ts:3358-3364` builds `/admin/audit`'s portal facet with `where: { portal: { not: null } }`, so a `null`-portal row is reachable by **no offered filter value at all** — no « sans portail » option exists — and `analytics.service.ts:3246` filters `portal` by exact match. So these rows are not merely under-attributed, they are **unreachable by any filtered review**. **Not a regression** (pre-existing; `S-E04-1` did not touch this file). Fix belongs with `PF-122` in the same column-level rule: assert that every `auditLog.create` payload under `apps/api/src` **binds** `actorRole` and `portal`, absence included, so a missing key is as red as a wrong literal — plus the read-side `null` facet `S-E04-2`/`S-E04-5` owe | `TECH_DEBT` | DEFECT | Discovered by the V3 run of 2026-08-08 (run 29), by the escalation panel's architecture lens enumerating what the new gate cannot see, then confirmed by the land pass with a `grep -n "actorRole\|portal"` over the file returning **zero** hits (`R-30`) | L0 | V3-E04 `S-E04-7` *(owner)* |
| **PF-124** | **The admin « Mot de passe oublié ? » link sends the operator to a port the stack does not serve.** Observed in the DOM of `http://localhost:3000/admin/login` while `S-E04-2` performed its authenticated render: the anchor's `href` is `http://localhost:8180/realms/pilotage-scolaire/login-actions/reset-credentials?client_id=portal-admin&redirect_uri=http%3A%2F%2Flocalhost%3A**3100**%2Fadmin%2Flogin` — while the web container publishes **`:3000`** (`WEB_PORT=3000`). The `redirect_uri` is therefore neither the origin the user is browsing nor, in all likelihood, a URI registered on the `portal-admin` client, so Keycloak either refuses it as an invalid redirect or returns the operator to a dead port after a successful reset. `:3100` is the **local `next dev`** port (`apps/web/package.json:7`), so this is build/runtime env drift between the dev workflow and the containerised one, of the same family as `PF-86`. **Not measured further** — `S-E04-2` did not follow the link, because doing so is `V3-E05`'s seam, not `V3-E04`'s, and the finding is about a URL that is wrong on its face | `TECH_DEBT` | DEFECT | Discovered by the V3 run of 2026-08-08 (run 30), incidentally, by reading the login page's accessibility tree during `S-E04-2`'s render step — i.e. by looking at the surface the routine had to pass through, not at the surface it was auditing | L0 | V3-E05 |
| **PF-125** | **Credentials login on the local stack takes ~30 s while the identity provider answers in ~1 s.** Measured during `S-E04-2`: `POST /api/auth/callback/credentials` stayed pending for roughly 30 seconds before returning `200`, whereas a direct Keycloak password grant from the host (`.../protocol/openid-connect/token`, same user, same client) returns in **1.15 s**. So ~29 s is spent inside the web container's own credentials callback, not waiting on Keycloak. **Recorded as a measurement, not a diagnosis** — the cause was not investigated and no hypothesis is offered here, because guessing one would be exactly the kind of plausible-but-unverified claim this kit exists to refuse. Worth an id because a 30-second login is a user-visible defect that no gate measures and that every future run will pay again when it needs an authenticated render | `TECH_DEBT` | DEFECT | Discovered by the V3 run of 2026-08-08 (run 30), by timing the login the render step depended on | L1 | V3-E05 |
| **PF-126** | **The `web` image cannot be rebuilt: `next build` fetches `Inter` from Google Fonts at build time, and the BuildKit stage cannot reach it.** `docker compose build web` ran for **571 s** and then failed in `[build 2/3]` with `src/app/layout.tsx — next/font error: Failed to fetch \`Inter\` from Google Fonts` → `Build failed because of webpack errors` → `ERR_PNPM_RECURSIVE_RUN_FIRST_FAIL @pilotage/web build`. **The obvious explanation is wrong and was checked before this was filed:** the host reaches `https://fonts.googleapis.com/css2?family=Inter` with **HTTP 200**, and so does a throwaway `docker run --rm alpine` container on the default bridge — so it is not "the machine is offline" and not a plain container-DNS fault; something about the BuildKit stage specifically fails to fetch. **Consequences, both of which bit this run.** (i) The image build has an **undeclared external dependency at build time**: a Google outage, a rate-limit, or an air-gapped build host makes the deployable artefact unbuildable, which is a supply-chain property of the Dockerfile, not of the network. (ii) It is the direct reason `S-E04-2`'s **second** render is `pending-verification` — the fix is in the source tree and gated, but the running container still serves the pre-fix bundle, so no run can visually confirm a `/admin/audit` fix until this is resolved or a sweep finds another route. Fix direction (not taken here, because self-hosting a font is a design decision with a visual consequence and no audit finding asks for it): vendor the font via `next/font/local` so the build is hermetic, per the same reasoning `ADR-027` applies to migrations — the build must reproduce from the repository | `TECH_DEBT` | DEFECT | Discovered by the V3 run of 2026-08-08 (run 30), by attempting the rationed rebuild `S-E04-2` needed for its second render, and then falsifying the first two explanations rather than filing the first one | L0 | V3-E02 |
| **PF-127** | **The link-integrity gate is one-directional by construction, and an orphaned page is invisible to it — permanently, not accidentally.** `scripts/link-integrity-check.js` answers *"does every published href resolve to an emitted route?"*. It never asks the converse, *"is every emitted route reachable from some navigation surface?"* — so while `/admin/analytics` existed with **zero** sidebar entries (`PF-119`), the gate was **green throughout**. `S-E04-2` closed that instance by repointing one entry; it did **not** close the class, and the next orphan will be just as invisible. This is the same asymmetry `PF-93` exposed for portal roots, now observed a second time, which is what promotes it from an anecdote to a gap worth an id. Fix direction: extend the existing ratchet with a **reverse pass** over the emitted route inventory (`scripts/web-route-baseline.json`, already produced by `web-artifact-check.js`), baselining the routes that are legitimately unlinked — deep links, `[param]` details, auth callbacks — so the ceiling may only shrink, exactly as `deadShapes` does today. Deliberately **not** built inside `S-E04-2`: it is a gate change of `V3-E02`'s family, and a size-S navigation slice is the wrong place to add a new blocking ratchet | `TECH_DEBT` | GAP | Discovered by the V3 run of 2026-08-08 (run 30), by asking why the gate had never flagged `PF-119` — i.e. by interrogating a **green** check rather than a red one | L0 | V3-E02 |
| **PF-128** | **`ADR-036`'s Context table named one `apps/web` server seam; there are two — and a one-seam fix would have blanked an entire class of audited write, forever and undetectably.** `apps/web` reaches the API server-side from `src/lib/api-client.ts` (server components, server actions) **and** from `src/app/api/proxy/[...path]/route.ts`, the route every **client** component's request passes through. `ADR-036`'s Context table, written by `S-E04-1`, listed only the first. Had `S-E04-3` implemented against the ADR as written, every client-component-driven audited write would have arrived at the API carrying no `x-pilotage-*` header, fallen to branch 2 of `extractAuditClientHints`, and recorded `req.ip` — **the web container's own address, identical for every actor forever**, which is `PF-31` precisely, reconstituted by the fix written to delete it. The reason this class of miss is worth an id rather than a line in a commit message is that **nothing would ever have reported it**: the resulting row is a plausible address, and the slice's own acceptance criterion (« two actions from two client addresses produce two different values ») would have been evidenced on the seam that *was* fixed. **CLOSED by `S-E04-3`**: one exported helper (`src/lib/client-provenance.ts`) is called by both seams, and the ADR's Context table is corrected in the same diff rather than left to be inherited (`R-30`). **What is NOT closed, and is `PF-129`:** there is a **third** server-side fetch, and no gate on the `apps/web` side asserts the invariant at all | `CLOSED` | DEFECT | Discovered by the V3 run of 2026-08-08 (run 31), by `S-E04-3`'s implementing agent grepping for server-side fetches to `API_URL` instead of implementing against the ADR's Context table — i.e. by measuring the document it had been handed | L0 | V3-E04 `S-E04-3` |
| **PF-129** | **A third `apps/web` server-side fetch to the API bypasses the one provenance helper, and the invariant that forbids it is enforced nowhere.** `apps/web/src/app/parent/register/actions.ts:25` builds its own `fetch(\`${API_URL}/api/v1/auth/register-parent\`, …)` with a hand-rolled header record and never calls `clientProvenanceHeaders`. This is `PF-128`'s failure mode one file over, and it survived the very slice that closed `PF-128` — because the guard that would catch it does not exist: `apps/api/src/shared/quality/audit-provenance-gate.spec.ts` walks `apps/api/src` only, and there is **no web-side gate**, so *"every server-side fetch to `API_URL` goes through `clientProvenanceHeaders`"* is stated in three docblocks and asserted by nothing. **Latent today** — `RegisterController` writes no audit row — which is exactly what makes it dangerous: the day parent self-registration becomes an audited event (an obvious governance candidate: account creation), the request arrives with no `x-pilotage-*` header, branch 2 fires, and the row records the **web container's** egress address with no test red anywhere. Fix: route `registerParentAction` through the helper (or through `api()`), **and** add a web-side quality spec stated over the invariant — any `fetch(` whose URL contains `API_URL` under `apps/web/src` must be preceded by `clientProvenanceHeaders` in the same lexical scope — with the three current seams as positive controls | `TECH_DEBT` | DEFECT | Discovered by the V3 run of 2026-08-08 (run 31), by the escalation panel's security lens falsifying the slice's own docblock claim that `apps/web` has exactly two server seams — one grep | L0 | V3-E04 `S-E04-7` *(owner)* |
| **PF-130** | **The three `x-pilotage-*` provenance headers are accepted from the public internet, so any authenticated actor can erase their own row from the append-only trail with one header.** `infra/nginx/conf.d/pilotage.conf:103` (`location /api/`) passes all client headers through unchanged — `client-hints.ts:98-100` records this as a *convenience* (« `infra/nginx` needs no edit »). Consequence: `curl -X PUT https://<host>/api/v1/subjects/coefficients/matrix -H 'Authorization: Bearer <their own token>' -H 'x-pilotage-forward-token: junk'` takes branch 1 of `extractAuditClientHints` (`forwarderIsClaiming` is true), fails `tokenMatches`, and returns `NO_CLIENT_HINTS` — so the row records `ip_address = NULL` **and** `user_agent = NULL` instead of the real address branch 2 would have captured. The row is **indistinguishable from an honest blank**, and nothing is logged. `ADR-036` D9 discloses the downgrade as "the safe direction", which is an argument about not storing a *false* value — it is not an argument that suppression is harmless in a forensic trail, and what removes detectability is the deliberate UI decision that the absence phrase « ne nomme aucune cause ». Second consequence: any leak of `AUDIT_FORWARD_TOKEN` becomes **internet-facing provenance forgery** rather than an internal-network-only risk. Fix: `proxy_set_header X-Pilotage-Client-Ip ""; …-User-Agent ""; …-Forward-Token "";` in every `location` proxying to `api_upstream` (the web container reaches the API on the internal `pilotage` network and does not traverse nginx, so its headers are unaffected), correct the `client-hints.ts` docblock which records the opposite posture, add a supertest case asserting an edge-stripped request falls to branch 2, and count refused-token requests so suppression is visible as an **event** even though the row stays blank | `TECH_DEBT` | DEFECT | Discovered by the V3 run of 2026-08-08 (run 31), by the escalation panel's security lens asking what an *insider* can do with the new headers, after clearing the outsider-forgery path | L0 | V3-E04 `S-E04-7` *(owner)* |
| **PF-131** | **The `N = 2` hop pin does not hold uniformly across the public surface: one `location` contributes no `X-Forwarded-For` entry.** `infra/nginx/conf.d/pilotage.conf:116` (`location /api/v1/notifications/stream`) sets **no** `X-Forwarded-For`, `X-Real-IP` or `Host`, unlike `location /api/` at `:103` and the three `/version` locations. Only Traefik contributes an entry on that path, so the real chain depth is **1** while the API is pinned at `TRUST_PROXY_HOPS=2`. The short-chain guard (`client-hints.ts:312-315`) fires only when `chain.length < trustedHops`, so a caller who **pads** the chain defeats it: sending `X-Forwarded-For: 9.9.9.9` makes Traefik produce `9.9.9.9, <real client>`, nginx passes it through untouched, and `proxyaddr` with `trust = 2` returns the caller-chosen `9.9.9.9` — the property `AC-2` asserts cannot happen. **Impact today is latent** (no `auditLog.create` is reachable under that prefix), but the uniformity of the pin **is** the security argument. Note the `ADR-036` D7-step-2 append records this location as measured-and-safe, having reasoned about the short-chain case and not about the padded one — so the ADR needs correcting alongside the config | `TECH_DEBT` | DEFECT | Discovered by the V3 run of 2026-08-08 (run 31), by the escalation panel's security lens enumerating **every** public path into the API rather than the one the slice's tests drive | L0 | V3-E04 |
| **PF-132** | **Both `.env` examples ship a literal `AUDIT_FORWARD_TOKEN` placeholder, and nothing refuses one — so the normal operator path publishes a governance trail an anonymous caller can write.** `.env.prod.example` ships `AUDIT_FORWARD_TOKEN=__CHANGE_ME__openssl_rand_hex_32__`; `.env.example` ships `change-me-audit-forward-token`. The token is **not** in `REQUIRED_ENV` (deliberately — absence is fail-safe), `assertRequiredConfig` checks presence only, `scripts/production-artefact-check.js` has no rule for it, and no test covers its **value**. The security argument written into `client-hints.ts` and `ADR-036` D9 — *"an absent or wrong token can only make provenance more null, never less"* — is true only while the token is **unknown**. D9 itself states the exposure: the web→api call hairpins through the public edge, so `x-pilotage-client-ip` / `-user-agent` are accepted from the internet and the token is the only thing between an outsider and a **forged** row. An operator who copies the example file therefore turns « null rather than wrong » into « plausible and wrong » — strictly worse than shipping the feature off, because it reinstates in practice the property `ADR-036` D1 refuses in writing, and it is the `PF-54` `?? 'admin'` shape a second time, arriving as a copied default instead of a code fallback. Fix belongs where the value is **read** (`apps/api/src/main.ts` → `configureAuditClientHints`): refuse to configure the seam when the token matches a shipped placeholder or is under ~32 bytes, log the refusal **by name**, leave provenance null — plus the placeholder strings as a `production-artefact-check.js` row so the ratchet is one-way. **PARTIAL 2026-08-08 by run 31's land pass — deliberately NOT `CLOSED`:** both example files now ship `AUDIT_FORWARD_TOKEN=` (**empty**, with the reasoning inline and `openssl rand -hex 32` as the instruction), which removes the hazard on the *copy-the-example* path — empty means no token is configured, so `extractAuditClientHints` returns `NO_CLIENT_HINTS` and provenance is null, the direction `D4` requires. The row stays **open** because the durable half is missing: nothing *refuses* a weak or published token at the read site, no `production-artefact-check.js` row ratchets the placeholder strings, and an operator who invents `hunter2` is exactly where this row started. Same shape as `PF-54`, which is `PARTIAL` for the same reason — the code path is fixed and the **strength** of the value is still unchecked | `TECH_DEBT` | DEFECT | Discovered by the V3 run of 2026-08-08 (run 31), by the escalation panel's architecture lens asking what the security argument depends on rather than whether it is stated — the answer being an assumption (*the token is secret*) that no artefact enforces | L0 | V3-E04 `S-E04-7` *(owner)* |
| **PF-133** | **A `'use client'` component imported a value from a server module, and nothing in the repository asserts that boundary — so the violation sat latent until an unrelated slice made it fatal.** `apps/web/src/components/notifications/NotificationListItem.tsx` (`'use client'`) imported the **value** `KIND_ICON` from `NotificationCenter.tsx`, a **server** component that imports `api` from `@/lib/api-client`. Importing a value across that boundary drags the entire server module into the browser graph. This was already true **before** `S-E04-3` and was silently tolerated — the server fetch helper was merely being bundled for the browser, useless but harmless. Adding `next/headers` (the one import that is *hard* server-only) turned it into `GATE: FAIL`: `next build` reported *"You're importing a component that needs \"next/headers\""* with the trace `api-client.ts → NotificationCenter.tsx → NotificationListItem.tsx`, and **three further gate stages went red as a consequence** (`web artefact`, `csp`, `link integrity` all only read `apps/web/.next/`, which the failed build never emitted) — one defect, four red stages, on a tree where `pnpm typecheck` was **13/13 green**. **Fixed by run 31 at the boundary, not at the import:** the client-safe vocabulary moved to `components/notifications/notification-model.ts` and the two `'use client'` files import from there **directly** — a re-export from `NotificationCenter` would not have worked, because the import **specifier** decides the graph, and `await import('next/headers')` would have gone green while leaving a client component depending on a server module, i.e. hidden the defect rather than closed it. **Why it keeps an id after being fixed:** the instance is closed, the class is not. `tsc` cannot see this invariant and `next build` only sees it once a hard server-only import exists, so any *other* `'use client'` file importing a value from a server module is still latent today and will surface as a mystery build break in whichever unrelated slice next touches a shared lib. Fix direction: a web-side quality spec over the invariant — no module reachable from a `'use client'` entry may import `@/lib/api-client`, `next/headers`, `@/auth` or `server-only` — with the three current server seams as positive controls; it is the natural companion to `PF-129`'s missing web-side gate and should be built with it. **A second lesson came from FIXING it, and it is the sharper one:** the first fix moved `type Portal` out of `NotificationCenter.tsx` along with the values. `scripts/link-integrity-check.js` resolves a template's union **from the file that carries the link**, and `NotificationCenter.tsx:240` carries `/${portal}/notifications`. With the alias gone `resolveDeclaredUnion` returned `null`, the template stopped expanding into three real routes and degraded to an unresolved shape — **and the `link integrity` stage still reported `PASS`**, because an unresolved shape is tolerated by design. Three routes silently stopped being checked, and the only red anywhere was one floor assertion in `link-integrity-gate.spec.ts` G-1. The correct fix is narrower than the first: only **values** cross the boundary harmfully — an `import type` is erased before bundling and creates no graph edge, which is exactly why `next build` named `NotificationListItem.tsx` (imports the value `KIND_ICON`) and **not** `NotificationsFilters.tsx`, which was already importing `type Portal` from the same server module without consequence. So the values moved and `type Portal` moved back. **A green stage is not evidence of coverage**; what caught this was a floor assertion pinned to a real declaration in a real file — the same shape as `S-E04-1`'s `AC-7` negative control, and the same asymmetry as `PF-127` | `TECH_DEBT` | DEFECT | Discovered by the V3 run of 2026-08-08 (run 31), by the routine running the **full** gate on the sprint's `landed: true` tree rather than trusting the sprint's own typecheck — the `PF-80` lesson executing | L0 | V3-E02 |
| **PF-135** | **Nothing had ever executed `/admin/audit` — the one route that had already 500'd for every admin.** Raised by the gate as the reason not to auto-merge `S-E04-4`, then discharged by executing it: every other web claim in that slice was pure-function or textual, on a route `PF-14` measured at HTTP 500 three commits earlier. **CLOSED in the same run (32)** by `apps/web/tests/e2e/journeys/admin-audit-vocabulary.spec.ts` — 4/4 authenticated as `school_admin` against the local stack | `CLOSED` | DEFECT | `traceability/CLOSED-L0.md` — raised and closed by `S-E04-4` (run 32); registered here by `S-E04-10` | L0 | V3-E04 |
| **PF-136** | **Two consumers disagree on how to merge a mixed-vocabulary row, and the marker is `aria-hidden`.** `AuditDetailDrawer.tsx` resolves `auditVocabularyExplanation(action) ?? auditVocabularyExplanation(resourceType)` — action wins — so a row with a **legacy** action and an **unknown** resource type prints « Enregistrée avant l'unification du vocabulaire », claiming historic provenance for a brand-new undeclared code. The table does the cautious thing (`describeNonCanonicalFields` picks `unknown` if any axis is unknown); two answers to one question is the error class `ADR-037` D4 exists to remove | `BROKEN_TRUTH` | DEFECT | `traceability/OPEN.md` — raised by run 33's escalation panel and a11y lens | L0 | V3-E04 `S-E04-7` *(owner)* |
| **PF-137** | **The epic's own `openapi.yaml` and `data-model.md` D-23 disagreed with the shipped code.** `AuditKpis` specified `adminLogins` removed and replaced by `distinctActors` in four `{value, scope}` envelopes; `S-E04-4` shipped `adminLogins: number \| null`. Both are defensible — the story mandated the interim shape — but a contract file disagreeing with the code, **inside an epic whose thesis is that no surface may assert what it cannot justify**, is the defect at the doc layer. **CLOSED by `S-E04-5`** (run 33) | `CLOSED` | DEFECT | `traceability/CLOSED-L0.md` — closed by `S-E04-5` (run 33); registered here by `S-E04-10` | L0 | V3-E04 |
| **PF-138** | **The re-armable `adminLogins` branch lost the `portal: 'admin'` scope its own card claims.** Raised by the escalation panel and **closed inside the same run (32)**. Evidence: the source-shape assertion on `where: { tenantId, portal: 'admin', action: { in: loginActions } }` in `audit-vocabulary-gate.spec.ts`, plus `audit-provenance-gate.spec.ts`'s `S-E04-1` occurrence case flipped back to length 1 with both shape assertions restored | `CLOSED` | DEFECT | `traceability/CLOSED-L0.md` — raised and closed by `S-E04-4` (run 32); registered here by `S-E04-10` | L0 | V3-E04 |
| **PF-139** | **A 15-character KPI value in a `font-mono text-3xl` slot inside `overflow-hidden` — an outage that renders as a claim.** `KpiCard` renders `value` at `font-mono text-3xl font-bold` inside `overflow-hidden` with `min-w-0` on the *label* group only. « Non instrumenté » has a min-content width of ~11 mono chars at 30 px and cannot break mid-word, so at the page's own `xl:grid-cols-4` the value or the « CONNEXIONS ADMIN » label is **clipped, not ellipsised** — the honest blank the epic spent a slice building, made unreadable by its container. **CLOSED by `S-E04-5`** (run 33) | `CLOSED` | DEFECT | `traceability/CLOSED-L0.md` — closed by `S-E04-5` (run 33); registered here by `S-E04-10` | L0 | V3-E04 |
| **PF-141** | **The audit table shows the label and drops the raw code, while its own filter still matches the raw column.** The action badge renders `action.label` and the raw stored code appears **nowhere in the row**, while the action filter is still a free-text `contains` match against the raw column: a user who reads « Suppression d'un rôle » and types it gets zero rows. It also contradicts the invariant the rest of the slice states explicitly — the drawer renders the raw code under the title *precisely* because « le libellé est posé au-dessus de la valeur réelle, il ne la remplace jamais » | `BROKEN_TRUTH` | DEFECT | `traceability/OPEN.md` — raised by run 33's review of `S-E04-5` | L0 | V3-E04 `S-E04-7` *(owner)* |
| **PF-142** | **No test in the repository verifies the built CJS `packages/contracts/dist` any more.** Both `apps/api/jest.config.js` and `apps/worker/jest.config.js` now map `@pilotage/contracts` to `packages/contracts/src`. The rationale is correct and documented — `test-ratchet.js` spawns jest with `cwd: appDir`, bypassing turbo's `test → ^build`, so specs would otherwise read a stale git-ignored `dist/` — but the consequence is that **nothing verifies the artefact Node actually loads**, while `audit-csv.generator.ts` value-imports the resolvers at **module load**. It sits directly against the guardrail that `packages/contracts` builds to CJS and must never revert to a source-only `main` | `TECH_DEBT` | GAP | `traceability/OPEN.md` — raised by run 33 | L0 | V3-E02 *(gate power, not product)* |
| **PF-143** | **The frozen legacy alias table had been derived from the seed and never checked against live data.** `LEGACY_AUDIT_ACTION_ALIASES` (5) and `LEGACY_AUDIT_RESOURCE_TYPE_ALIASES` (8) were derived from the *pre-fix seed* while the rewritten KPI predicate depends on them. **CLOSED in the same run (32)** by taking the measurement that had not been taken: `select distinct action, resource_type from audit_log` executed against the running `pilotage_postgres` and filtered against the frozen table — **zero** unaliased values on either axis | `CLOSED` | DEFECT | `traceability/CLOSED-L0.md` — raised and closed by `S-E04-4` (run 32); registered here by `S-E04-10` | L0 | V3-E04 |
| **PF-144** | **An ambiguous selector had killed the entire authenticated e2e layer, silently.** `auth.setup.ts` used `getByLabel('Mot de passe')`; the login form later gained a « Afficher » control matching the same label, so strict mode failed and `ADR-023`'s four-portal authenticated layer had not run for an unknown number of runs — with nothing saying so. **CLOSED in the same run (32)**, found while discharging `PF-135` | `CLOSED` | DEFECT | `traceability/CLOSED-L0.md` — raised and closed by `S-E04-4` (run 32); registered here by `S-E04-10` | L0 | V3-E02 *(gate power)* |
| **PF-146** | **The e2e reachability guard turns « cannot tell » into a green skip, and that is how `PF-144` hid.** `auth.setup.ts`'s `originReachable()` aborts its probe after **4 s** and treats the abort as « no stack booted → green skip, no false red ». Against `next dev` a cold route compiles in **5–17 s** (measured run 32: `/teacher/login` 5.9 s, `/student/login` 5.3 s, `/admin/dashboard` **16.6 s**), so a perfectly healthy stack reads as *down* and the whole authenticated layer skips green. Same asymmetry as `PF-127`: a green stage is not evidence of coverage | `TECH_DEBT` | GAP | `traceability/OPEN.md` — reproduced twice during run 32 | L0 | V3-E02 *(gate power)* |
| **PF-148** | **The audit page's focus indicator composites to 1.42:1, and `S-E04-5` adds three more controls that use it.** `focus-visible:outline-none` + `focus-visible:ring-blue-400/40` yields **#bfdbfd — 1.42:1** against the adjacent surface, well below SC 1.4.11's 3:1, so there is effectively **no perceivable keyboard focus state**. Recorded with its correction: the reviewing lens called this « a straight regression » on the two filter controls and that is **measured false** — the two pre-existing selects already carried it. The slice widens the surface; it did not create the ratio | `TECH_DEBT` | DEFECT | `traceability/OPEN.md` — raised by run 33's a11y lens *(no test — `apps/web` still has no component unit runner)* | L0 | V3-E04 *(or a V3-E02 a11y slice)* |
| **PF-150** | **The epic's remaining order is asserted in prose, in one story's report, and nowhere a run can read it.** The sequencing ruling that `S-E04-8` (hash chain) stays **last** — because a chain computed over provenance that is not yet true would be a cryptographically verifiable record of falsehoods — lives only in run reports and story prose. `dependency-map.md` does not encode it, so the ordering survives on the next run remembering it. Record it as an explicit `blockedBy` on `S-E04-8` rather than as a paragraph. **⚠️ Id collision, noted 2026-08-09 and deliberately NOT resolved here:** `docs/spec/features/v3-e04/PROGRESS.md:1353` uses `PF-150` for a different finding; renumbering belongs in both files at once or not at all | `TECH_DEBT` | GAP | `traceability/OPEN.md` — raised by run 33, re-pointed 2026-08-09 | L0 | V3-E04 `S-E04-7` *(owner)* |
| **PF-153** | **`assignRole`'s role lookup is unfiltered, and stays unfiltered — by decision** *(latent — becomes live with `ADR-013`)*. `users.service.ts:85` reads `prisma.role.findUnique({ where: { id: roleId } })` with no tenant predicate. `Role` has no `tenantId` (`schema.prisma:900`); tenancy runs `schoolId → School.tenantId` and `schoolId: null` means global, so adding a tenant filter to a global table would be a **visibility change dressed as a fix**. The gap that IS real: `users.service.spec.ts` has G-TENANT negatives for a foreign `userId` and a foreign `userRoleId` and **none for a foreign role** | `TECH_DEBT` | GAP | `traceability/OPEN.md` — raised by `S-E04-6` (run 34) | L0 | V3-E05 / `ADR-013` |
| **PF-154** | **`transfer`'s capacity pre-check runs outside the transaction, so a concurrent enrolment can over-fill the target section** *(pre-existing)*. `enrollments.controller.ts` reads `target._count.enrollments >= target.maxStudents` before `$transaction` opens, so two concurrent requests can both pass it and both create. Not introduced by `S-E04-6` and not widened by it; registered because that slice is what made the two writes atomic, and therefore what makes the remaining race legible rather than invisible | `BROKEN_TRUTH` | DEFECT | `traceability/OPEN.md` — raised by `S-E04-6`'s test-architect lens (run 34) | L0 | V3-E04 |
| **PF-155** | **`PATCH /schools/:id { status: 'closed' }` closed a school through `update()` — bypassing `DELETE`'s refusal, and filing the closure as `school.update`.** `UpdateSchoolDto` exposed `status?: SchoolStatus` (`active \| closed`), so a closure could skip the students / academic-years guard entirely. Both codes are `critical: true`, so « Modifications critiques » counted correctly: the **attribution** was wrong, and an auditor filtering « Fermeture d'un établissement » got an empty result for those closures. Re-opening had no code at all. **CLOSED by `S-E04-10`**: the property is removed, so the API's `forbidNonWhitelisted` turns the payload into a loud 400; the reopen capability it also removed is registered as an open capability gap rather than replaced by an invented `school.reopen` | `CLOSED` | DEFECT | `traceability/OPEN.md` — raised by `S-E04-6`'s edge lens (run 34) | L0 | V3-E04 `S-E04-10` |
| **PF-157** | **`revokeRole`'s idempotency guard was a TOCTOU, so two concurrent revocations both wrote a `critical` row for one revocation.** The `revokedAt !== null` read happens before `$transaction` opens; both callers passed it, both updated, and `revokedAt` ended up holding the retry's clock — the exact defect the guard was added to close. No database backstop exists: `@@unique([userProfileId, roleId, schoolId])` cannot deduplicate because `schoolId` is written `null` and PostgreSQL treats NULLs as distinct. **CLOSED by `S-E04-10`** with an in-transaction conditional `updateMany` on `revokedAt: null` plus a `count === 0` early return; the partial unique index `(user_profile_id, role_id) WHERE revoked_at IS NULL` remains **DEFERRED** and still owns the concurrent-`assignRole` race, which this fix does not touch | `CLOSED` | DEFECT | `traceability/OPEN.md` — raised by `S-E04-6`'s edge lens (run 34) | L0 | V3-E04 `S-E04-10` |
| **PF-158** | **The no-op rule was applied inconsistently across four families in one diff, so a double-click inflated « Modifications critiques ».** `assignRole`/`revokeRole` skipped the row when nothing changed; `school.update` (whose DTO is entirely optional, so `PATCH {}` validates and runs `tx.school.update({data:{}})`), `enrollment.status_change` and `enrollment.cancel` on an already-`dropped` enrollment each wrote a `critical` row with byte-identical `before`/`after` — and the last of the three also moved the original cancellation timestamp forward. **CLOSED by `S-E04-10`**: field-level no-op detection **before** `$transaction` at all three sites, always after the tenant guard, with every `writeAudit` left one unconditional inline-literal statement (`ADR-035` D1). DNC-01 is the rule at stake | `CLOSED` | DEFECT | `traceability/OPEN.md` — raised by `S-E04-6`'s edge lens (run 34) | L0 | V3-E04 `S-E04-10` |
| **PF-159** | **`school.update`'s `before`/`after` omitted `address` — the fifth and only structurally interesting field `update()` can change, including erasing it via `Prisma.DbNull`.** An address-only PATCH therefore wrote a `critical` row whose rendered diff was identical in every recorded field, so `AuditDetailDrawer.tsx` showed a critical modification with an **empty diff** — on the one field where the audit row is the sole surviving record of the previous value. **CLOSED by `S-E04-10`**: `address` is recorded on both sides through `parseAddress`, with a raw fallback when the stored value does not validate, so an invalid legacy address is never rendered as a fabricated creation | `CLOSED` | DEFECT | `traceability/OPEN.md` — raised by `S-E04-6`'s edge lens (run 34) | L0 | V3-E04 `S-E04-10` |
| **PF-160** | **`shared/audit/` stopped being framework-free.** `write-audit.ts` is the first **production** file there to import `@nestjs/common` (`provenance.ts` and `client-hints.ts` import only `node:*` and siblings), and it embeds user-facing French UI copy (`AUDIT_WRITE_FAILED_MESSAGE`) — justified, because `SchoolsManager.tsx:32,41,52` renders `res.error` verbatim inside a red inline banner, but it contradicts the story's own « Body: one `tx.auditLog.create`. Nothing else », and it is a convention change that landed without being named as one | `TECH_DEBT` | GAP | `traceability/OPEN.md` — raised by `S-E04-6`'s ADR-drift lens (run 34) | L0 | V3-E04 `S-E04-7` *(owner)* |
| **PF-161** | **A lint-ratchet import-order drift, predicted by the reviewer lens and fixed in the land pass before the stage could go red** *(CI ratchet)*. `provenance-callsites.spec.ts:23` — the new `SchoolsController` import broke the alphabetised block; it now reads `school-structure/{academic-years,school-context,subjects}` → `schools/schools.controller` → `students/student-access.service`. **CLOSED 2026-08-09 (run 34)**; kept as a row because a ratchet that never went red is precisely the near-miss the register exists to remember | `CLOSED` | DEFECT | `traceability/CLOSED-L0.md` — predicted and closed inside `S-E04-6` (run 34); registered here by `S-E04-10` | L0 | V3-E04 |
| **PF-163** | **The invite path can trade a real account for an audit row.** `invite.controller.ts` — `S-E04-7` wraps steps 5–7 in one `$transaction` and correctly keeps the Keycloak calls outside it, but steps 3–4 have already created the Keycloak user and sent the activation email, and neither is reversible. With `ADR-035` D2 making an audit-insert failure fatal, a rollback now leaves an **enabled Keycloak identity with no `UserProfile`**; the next login self-provisions it into `DEMO_TENANT_SLUG` with realm-derived permissions — the `ADR-002` invariant, broken. Trigger is a DB fault, not attacker-controlled. Two resolutions were stated in its `OPEN.md` row. **CLOSED 2026-08-10 (run 37) by resolution (a), COMPENSATE — not the mechanical (b).** (b) was declined because it only narrows the trigger from *profile or role or audit fails* to *profile or role fails*, leaving the escalation reachable through any profile-create failure, while handing back the fail-closed audit guarantee `S-E04-7` had just bought at this exact site. `S-E04-9` deletes the orphan on rollback instead, and names it to the admin when the delete itself fails. `invite` therefore **stays in the sweep** and the arithmetic stays **10 + 17 = 27** | `CLOSED` | DEFECT | `traceability/CLOSED-L0.md` — raised by `S-E04-7`'s verify panel (run 35), merged still-open in PR #208, closed by `S-E04-9` (run 37) | L0 | V3-E04 `S-E04-9` *(owner)* |
| **PF-164** | **A branded type re-opened one hop from the seam that introduced it.** Three private `audit()` forwarders declared `tx: Prisma.TransactionClient` rather than the branded `AuditTransactionClient` — and `Prisma.TransactionClient` is `Omit<PrismaClient, ITXClientDenyList>`, where `Omit` *removes* members without forbidding them, so a full `PrismaService` stays structurally assignable. `writeAudit(this.prisma, …)` is a type error; `this.audit(this.prisma, …)` compiled. **Code half applied by the routine at land (run 35)**; the **durable half is open**: `TRANSACTION_CLIENT_TYPE_NAMES` (`audit-write-check.js:276`) still whitelists the bare name, so a new unbranded forwarder still passes, and no `@ts-expect-error` proves the fix can go red. Narrowing is blocked on `remediation.controller.ts`'s baselined forwarders, which legitimately use the bare type | `TECH_DEBT` | GAP | `traceability/OPEN.md` — raised and half-applied in `S-E04-7` (run 35) | L0 | V3-E04 |
| **PF-168** | **✅ CLOSED by `S-E05-1` (2026-08-10) — `ADR-037` D8, ratchet `scripts/csv-escape-check.js`.** The neutraliser now exists once, in `packages/contracts/src/security/csv-injection.ts`; the web module and the worker generator consume it and keep only their own dialect; the private copies are deleted. **Two corrections to the text below, on the record (`R-30`), because the next run reads this file at Step 1 and believes it:** (1) *"the real count is **2**"* is **wrong** — measured at `8e52da0` there were **3** `csvEscape` declarations, and **5** distinct CSV escapers counting the two `escapeCell` copies on the parent portal. This finding grepped for the **name** `csvEscape`, which is precisely the blind spot that made the count wrong, and is why the ratchet's rule A matches on **shape** as well as name and walks **`.tsx`** as well as `.ts`. (2) The claim that `AlertsExportButton.tsx` was *already correct* holds for the **neutralisation** and **not** for the **force-quoting**: it prefixed the apostrophe then tested the original quote regex, so `=1+1` was emitted `'=1+1` **unquoted**. It now emits `"'=1+1"` — an intended behaviour delta. **Residual left open on purpose: `PF-169`** (the dialect half). **Not claimed:** no browser, no spreadsheet, and no web unit test — `apps/web` has Playwright only (`PF-129`/`PF-133`); the web half is evidenced by the gate's executed rules A and C. Original finding text follows. **The CSV-injection defect `S-E04-11` closed as *latent* in the worker is LIVE in `apps/web`, on two portals, over operator-entered free text.** `apps/web/src/lib/csv.ts:17` is a second, independent `csvEscape` whose predicate is `/[",;\n\r]/` — it quotes and neutralises nothing — and the module prepends `CSV_BOM`, so the artefact opens in Excel as a spreadsheet. Four live export buttons use it: `admin/guardians` (exports `lastName`, `firstName`, `email`, `phone`, **`profession`** — arbitrary operator text), `admin/enrollments`, `teacher/reports`, `teacher/students`. Where the audit CSV had no reachable free-text column, this one does, now. Found by verifying a review claim: the sprint reported "3× `csvEscape`"; the real count is **2**, and the second copy being unhardened is the finding | `BROKEN_SECURITY` | DEFECT | routine run 38, measured at land of `S-E04-11` | L0 | V3-E04 → V3-E05 |
| **PF-169** | **The two CSV writers disagree about what French Excel needs, and both cite French Excel as the reason.** `apps/web/src/lib/csv.ts:1-15` uses `;` + CRLF « for FR Excel »; `audit-csv.generator.ts` uses `,` + `\n` and cites the same authority for its BOM. Both cannot be right — and if the web module's premise holds, the DPO's regulator-facing export opens in French Excel as one column per row, defeating the very BOM added for that tool. Not fixed in `S-E04-11`: changing the delimiter rewrites every byte offset, which is the silent byte change that slice exists to stop. **STILL OPEN after `S-E05-1` (2026-08-10), deliberately — not an oversight.** That slice closed `PF-168` by sharing the *neutraliser* and pointedly **not** the *dialect*, and recorded the refusal in three places so it cannot be inherited silently: a code comment on `CSV_SEPARATOR` in `apps/web/src/lib/csv.ts`, the header of `packages/contracts/src/security/csv-injection.ts`, and **`ADR-037` D8 (ii)**. It also adds one measured argument this row did not have: folding the two quote sets into a single union regex would force-quote every worker cell containing a `;` — an `audit_log.user_agent` header, say — and so **silently rewrite the regulator's file** while claiming to unify it. Closing this row still requires the **versioned, announced** format change with a consumer census that `ADR-037` D6 defines | `BROKEN_TRUTH` | DEFECT | routine run 38, measured at land of `S-E04-11` | L0 | V3-E04 → V3-E05 |
| **PF-170** | **The audit export truncates at 50 000 rows and says nothing, so a partial file is indistinguishable from a complete one.** `take: 50_000` with `orderBy: createdAt desc` means what is dropped is the *oldest* end of the requested window — precisely what a retention question asks about — and nothing in the artefact, the `ExportJob` row or `/admin/exports` records that it happened. Same `G-TRUTH` property `S-E04-11` defends (a projection must label its scope) on the axis it did not touch | `BROKEN_TRUTH` | DEFECT | `S-E04-11` intake (run 38), registered at land | L0 | V3-E04 |
| **PF-171** | **`School.timezone` is a second unvalidated timezone column, and neither it nor `Tenant.timezone` is validated on write.** `schema.prisma:263` and `:280` are independent `String @default("Europe/Paris")` columns whose own schema comment states neither is derived from the other. `S-E04-11` discharged `PF-149`'s *map the error* half on both call sites and left *validate on write* partly done: `isKnownTimezone` is available, but no writer for either column exists in `apps/api/src`, so there was no path to guard without inventing a settings endpoint — which the slice correctly refused to do | `TECH_DEBT` | GAP | residual of `S-E04-11` (run 38), measured by the routine | L0 | V3-E04 → V3-E05 |
| **PF-172** | **The 503's stable discriminator is exported where its stated consumer cannot import it.** `analytics.service.ts` exports `TENANT_TIMEZONE_UNUSABLE` and its docblock says the purpose is to let `/admin/audit` distinguish « analytics is down » from « this tenant's timezone is misconfigured » — but `apps/web` cannot import from `apps/api/src`; the shared home is `packages/contracts`. A comment asserting an invariant the placement forbids: the `PF-164` shape at a new address. Compounding it, `/admin/audit`'s `safe()` collapses every `ApiError` into one amber strip, so a correctly-placed code would still have no consumer | `TECH_DEBT` | GAP | `S-E04-11` review panel (run 38), registered at land | L0 | V3-E04 |
| **PF-165** | **A Keycloak identity with no local profile is adopted into the `demo` tenant on first login, with permissions derived from its realm roles.** `user-sync.service.ts:38-56` — `ensureUser` creates the `UserProfile` lazily, and when it finds none it upserts the hard-coded `DEMO_TENANT_SLUG` and creates the profile there, `active`, while `effectivePermissions` unions `REALM_ROLE_PERMISSIONS` for every realm role on the token. **This is the amplifier that gave `PF-163` its severity, and it is reachable without `PF-163`:** creation is lazy, so every never-logged-in realm identity is profile-less by design; `realm-export.json` ships three of them (`admin@`/`teacher@`/`parent@pilotage.local`) with **0** matching `seed-demo.ts` rows; and `ADR-004` puts every tenant in one realm. Latent while `demo` is the only tenant; a live `ADR-002` breach the day a second exists. Until run 37 it existed only inside `PF-163`'s prose, so closing `PF-163` would have silently retired it | `BROKEN_SECURITY` | DEFECT | routine run 37 (Step 2), corroborated by `S-E04-9`'s panel | L0 | V3-E04 → V3-E05 |
| **PF-166** | **The one public account-creation path writes no audit row at all, and creates the account in four non-atomic steps with no compensation.** `register.controller.ts:65-126` — `POST /auth/register-parent` is unauthenticated and creates a Keycloak identity, sets a **permanent** password, upserts the `demo` tenant and creates a `UserProfile` with no `$transaction` and no compensating action, so a failure at the last step leaves exactly the orphan `PF-163` names, self-provisioned by `PF-165` on first login. Independently: it writes **no** `auditLog` row on any path, leaving a privileged mutation invisible to the audit trail the epic exists to complete. `PF-46` covers throttling and self-verified email at this endpoint — not atomicity, not the missing row | `BROKEN_AUDIT` | DEFECT | routine run 37 (Step 2) | L0 | V3-E04 |
| **PF-167** | **The invite path's SMTP-failure branch still leaves an enabled, profile-less identity — deliberately.** `invite.controller.ts:191-203` — when `sendExecuteActionsEmail` throws, the handler keeps the Keycloak user on purpose, because the message it returns (the temporary password is usable) would otherwise be false. Same orphan class as `PF-163`, different trigger, therefore a different remedy: a user-facing copy change on `ActivationHint.tsx`'s seam, not a compensation. Declared by `S-E04-9` in code and in `ADR-035` D15, and registered so `PF-163`'s closure cannot be read as *"invite can no longer orphan an identity"* | `BROKEN_AUDIT` | DEFECT | `S-E04-9` (run 37), stated at land | L0 | V3-E04 |
| **TOOL-03** | **The audit-write sweep counts existing audit writes, so a privileged mutation that writes none is invisible to it.** `scripts/audit-write-check.js` proves *every audit write goes through the seam*; it never proves *every privileged mutation writes one*. A site with zero writes contributes nothing to the 27-site population, is not a conversion candidate and needs no baseline row, so it passes every rule silently — `PF-166` is the live instance, and it was found by reading a controller, not by any gate | `TOOLING` | GAP | routine run 37 (Step 2) | L0 | V3-E04 *(routine/gate)* |
| **PF-173** | **The CSV ratchet enforces uniqueness of the escaper, not coverage of the surfaces — and one teacher-facing export line is still unescaped.** `scripts/csv-escape-check.js` rules A–E are all conditioned on an escaper *existing*: a surface that hand-joins user data, or calls `csvRow` (a bare `cells.join(CSV_SEPARATOR)`), declares no escaper and passes every rule while shipping exactly the defect `PF-168` names. The live instance is `apps/web/src/app/teacher/reports/_components/ExportReportButton.tsx:68`, `` lines.push(`Année;${academicYear?.name ?? ''}`) `` — tenant-authored text interpolated into a `.csv` a teacher opens, no escape, no quoting; Excel evaluates a formula in any column. **Pre-existing, not introduced by `S-E05-1`** — but that slice's header claimed the surface was covered, so the claim was narrowed at land and the gap registered here rather than left contradicted in a comment. Fix direction: the branded-type pattern this repo already shipped for the audit seam (`ADR-035`, `write-audit.ts`, `64f64dd`) — `csvEscape`/`csvFixed1` return a branded `CsvCell`, `csvRow`/`buildCsv` accept only `CsvCell[]` | `BROKEN_SECURITY` | GAP | `S-E05-1` security lens (run 39), measured and registered at land | L0 | V3-E05 |
| **PF-162** | **The seam made « in the same transaction » a type, and left « the code is declared » a convention.** `write-audit.ts:99,101` — `AuditWriteInput.action` and `.resourceType` are typed **`string`**, not the declared unions, although `apps/api` already depends on `@pilotage/contracts` and `audit-vocabulary-gate.spec.ts` imports `AUDIT_ACTIONS` directly. So an undeclared code is caught by an AST gate at test time rather than by `tsc` at the call site — the weaker of the two guarantees, on the very axis the epic is about | `TECH_DEBT` | GAP | `traceability/OPEN.md` — raised by `S-E04-6`'s escalation panel (run 34), recorded by the routine at land | L0 | V3-E04 `S-E04-7` *(owner)* |
| **PF-178** *(renumbered from `PF-173` 2026-08-11 — `S-E05-1` allocated `PF-173` concurrently and landed first, so this one moved; see `TOOL-05`)* | **A correct privilege ceiling refuses a `school_admin` the `teacher`, `parent` and `student` roles, and nothing replaces that path.** Measured against `permissions.constants.ts`: `school_admin` holds 75 codes; `teacher` (34) exceeds it by 6, `parent` (20) by 3, `student` (7) by 5 — and `seed.ts:216-220` materialises those into real `rolePermission` rows, so the DB roles genuinely carry them. Net effect of `S-E05-2`: a `school_admin` can assign **none** of the four other seeded system roles — `super_admin` correctly, `teacher`/`parent`/`student` as collateral, on the admin onboarding path. The ceiling was deliberately **not** weakened to preserve the old behaviour, because the old behaviour *is* the P0. The answer is a delegation model (an explicit assignable-roles grant, or a grantor-relative realm-role ladder), which also closes `PF-09`'s residual — a human ruling, tracked as `D-12` | `MISSING_CAPABILITY` | GAP | `S-E05-2` (run 39), measured and shipped fail-closed on purpose | L0 | V3-E05 → `ADR-015` |
| **PF-174** | **`/admin/users` swallows the new 403 in silence: the menu still offers every role and says nothing when the grant is refused.** `apps/web/src/app/admin/users/actions.ts:7-10` has no `catch` and `UsersTable.tsx:30-39` is `try … finally` with no `catch` clause, so a rejected server action is an unhandled rejection — spinner stops, menu closes, `router.refresh()` never runs, no message. `:115` maps every role into the assign menu, three of which now always fail (`PF-173`). The contrast case proves it is local rather than a missing capability: `/admin/roles` renders the identical 403, because `roles/actions.ts:24-31` catches `ApiError` — which is why the API puts the exceeding codes inside `body.message`, not only in `missing`. The visible half of a correct backend change, landing in **track c's** seam | `BROKEN_UX` | DEFECT | `S-E05-2` UX lens (run 39), confirmed against source by the routine | L0 | V3-E06 *(track c)* |
| **PF-175** | **Grants that escalated before the ceiling existed pass it unconditionally.** The ceiling compares against `effectivePermissions`, which unions custom-role permissions into the grantor's set (`user-sync.service.ts:80-84`), so an actor already holding a role minted through `PF-09` holds those codes *effectively* and can keep minting them. Inherent to a grantor-relative ceiling, not a defect in it; what is missing is the one-off detection sweep. Query recorded in the ledger row. Not run this run: the local database is seed data, and an empty result there would prove nothing about anywhere else | `BROKEN_SECURITY` | GAP | `S-E05-2` escalation panel (run 39), registered at land | L0 | V3-E05 |
| **PF-176** | **An attempted privilege escalation is logged, and logged anonymously.** `privilege-ceiling.ts`'s `assertWithinCeiling` emits a `logger.warn` naming the exceeding codes — deliberately, because a refusal writes no audit row (nothing happened) and an attempted escalation must not be silent. But the predicate has no actor context **by design**, so the line carries no `jwt.sub`, no `tenantId` and no client hints. A third parameter was refused on purpose: it is the seam through which an options bag, and later a bypass flag, would arrive (DNC-10). Fix: move the `warn` to the four call sites, which already hold the actor | `BROKEN_AUDIT` | GAP | declared by `S-E05-2` in code and `ADR-015` (run 39) | L0 | V3-E05 → V3-E04 |
| **TOOL-04** | **The fast gate escalates to an api suite that cannot finish, and it escalates precisely when the diff improves a gate.** `ci-gate.sh` runs the whole api suite (2400 s bound) when the diff matches `^(scripts/\|\.github/\|infra/\|apps/api/src/shared/quality/)`, and `--skip src/shared/quality/` (1200 s) otherwise. Measured over four runs: the full suite timed out contended, timed out uncontended, and was **still blocked at ~80 min** unbounded with its workers holding 7–40 s of CPU each; the skipped variant returned **no drift** in minutes. The difference is exactly `src/shared/quality/**` — the artefact- and Docker-dependent meta-gates — and the fast gate no longer builds. So the gate is hardest to pass exactly when a slice strengthens one of its own checks. Third gate-bound issue after `#216`/`#217`, both of which raised the bound; the bound does not appear to be the variable. Blocking spec not isolated, deliberately not guessed | `TECH_DEBT` | DEFECT | routine run 39, measured at land of `S-E05-2` | L0 | V3-E02 *(routine/gate)* |
| **TOOL-06** | **Seven `run_stage` calls omit their timeout argument, and for the new CSV escaper gate the broken call is the only one — so `#215`'s blocking ratchet has never executed.** `ci-gate.sh:47` documents `run_stage <timeout_seconds> <name> <command...>`; `:102`, `:118`, `:142`, `:165`, `:198`, `:230` and `:235` pass only `<name>`, so `timeout` reads the stage name as its interval and exits **125** before the command runs. They are pre-`#214` leftovers the rewrite orphaned. Six are harmless — those checks have a correct bounded call site at `:285`–`:339`, which is why the breakage went unseen. **`csv-escape-check.js` has exactly one call site (`:198`) and it is the broken one**: measured, the script passes standalone at exit 0 and exits 125 through the gate. The ratchet `S-E05-1` landed an hour earlier to stop `PF-168`'s class recurring, and described as blocking, cannot fail. `PF-105`'s family, but unreachable rather than merely tautological. The broken stages are excluded from the summary and the exit tally, so a docs-only run prints seven `✗` and then `GATE: PASS` | `TECH_DEBT` | DEFECT | routine run 39, measured at land of `S-E05-2` | L0 | V3-E02 *(routine/gate)* |
| **TOOL-05** | **Finding-id allocation is a race, and with three concurrent tracks it is now a race that is regularly lost.** Two runs on 2026-08-11 read the register's maximum as `PF-172` within the same window and both allocated **`PF-173`**: `S-E05-1` (track c, CSV ratchet coverage, `#215`) and `S-E05-2` (track b, the delegation gap, `#218`). Both merged, so `main` briefly declared **two** `PF-173`s — resolved by renumbering the later-landing one to `PF-178`, per the rule `PF-150`'s own note states (*renumber in both files at once or not at all*). **This is not the same defect as `PF-150`.** That one was two *registers* disagreeing about one number; this is two *writers* allocating the same number from a correctly-read register, which the ledger inbox was designed to prevent for **rows** and does not prevent for **ids**. `tracks.md` guarantees no two tracks edit the same code and no two edit `OPEN.md`; it guarantees nothing about the id space, which is global, dense and allocated by reading a maximum. The frequency is the argument: three tracks × a finding-raising run each is a collision whenever two land in the same window, and that is now the normal case rather than the exception. Fix directions, cheapest first: allocate ids from a **per-track stripe** (track a = `PF-x00…`, b = `PF-x33…`, c = `PF-x66…`), or have `routine-lock.sh gate` hand out a reserved id block with the track claim, or make `v3-reconcile-ledger.js` **refuse to fold** a block whose id already exists and report it — the last one turns a silent duplicate into a loud one without changing how ids are chosen | `TECH_DEBT` | DEFECT | routine run 39, observed live at land of `S-E05-2` | L0 | V3-E02 *(routine/ledger)* |
| **PF-177** | **A duplicated permission code answers 400 while naming no code at all.** `roles.controller.ts` validates the catalogue by length comparison (`perms.length !== body.permissionCodes.length`), so `["students.read","students.read"]` fails, while `missing` is computed by `filter` and finds every code present — yielding `[]`. The caller gets `Permissions inconnues` with an empty list of unknown permissions. Pre-existing and unchanged by the ceiling; recorded because `S-E05-2` put a second refusal beside it and the two must not be confused when debugging | `TECH_DEBT` | DEFECT | routine run 39, measured at land of `S-E05-2` | L0 | V3-E05 |
| **TOOL-07** | **The ledger reconciler writes its fold into a checkout nobody reads, and deletes the inbox file there — so folded findings never reach `origin/main`, and can never be re-folded.** `routine-lock.sh`'s `reconcile()` runs the script in `$REPO` and contains **zero git verbs**, while `v3-reconcile-ledger.js:111` `unlink`s each inbox file it consumes. Both effects are local to `$REPO`'s working tree: measured on 2026-08-12 as an uncommitted ` M OPEN.md` / ` M CLOSED-L0.md` / ` D inbox/ci-2026-08-11-v3-b-e05-s2.md`, mtime **2026-08-11 10:39**. Because the unlink is local too, the next tick finds nothing to fold and the rows are lost for good, while every track worktree branches from an `origin/main` whose `OPEN.md` never received them — nine rows, including **`PF-174` (P1)**. **Self-demonstrating:** Step 1 calls `OPEN.md` "the only selection input", and run 40 had to find its own P1 story in `NEXT-b.md` prose because it was not there. **This is `PF-58`'s class at the ledger** — "produced" conflated with "committed" — one level up from the planning artefacts that row is about | `TECH_DEBT` | DEFECT | routine run 40, measured in Step 1 of `S-E06-8` | L0 | V3-E02 |
| **PF-179** | **A 401 during a role write renders `NEXT_REDIRECT;…` to the admin instead of redirecting to login.** None of the three actions in `apps/web/src/app/admin/roles/actions.ts` re-throws the Next navigation signal, so the digest `redirect()` throws is caught by their blanket `catch`, returned as data and rendered as an inline error — the session-expired user is never sent to the login page. The inverse of `PF-174`'s failure (nothing shown when a refusal was due; here a framework internal shown when a navigation was due), and fixed on `/admin/users` by the same `apiResultFromError` these three predate | `TECH_DEBT` | DEFECT | routine run 40, raised by `S-E06-8`, confirmed at land | L0 | V3-E06 |
| **PF-180** | **The notification-preference actions discard the API's message and render `HTTP 403` for every refusal.** All three actions in `apps/web/src/app/admin/settings/preferences-actions.ts` collapse an `ApiError` to `` `HTTP ${err.status}` `` and never read `body.message`, so a permission refusal, a validation error and a proxy failure are indistinguishable. Not `PF-174`'s silence — a message is shown, carrying nothing actionable — on a surface reachable from three portals | `TECH_DEBT` | DEFECT | routine run 40, measured in Step 2 of `S-E06-8` | L0 | V3-E06 |
| **PF-185** | **Public parent registration still assigns tenancy from a module constant — the half of `PF-01` that cannot be closed by symmetry.** `apps/api/src/modules/identity/register.controller.ts:60` declares `const DEMO_TENANT_SLUG = 'demo'` and `:366-369` runs `tx.tenant.upsert({ where: { slug: 'demo' }, update: {}, create: { slug: 'demo', name: 'Demo Tenant' } })` **inside the registration transaction**, feeding the returned `tenant.id` to **both** the new `UserProfile` and the provenance audit row. `S-E01-1a` deleted the twin occurrence in `apps/api/src/shared/auth/user-sync.service.ts` — where a *login* was creating a tenant — and left this one measured rather than closing it by analogy. **The distinction is the finding.** In `ensureUser` the caller was authenticated and the correct answer was to refuse; here the caller is an anonymous member of the public who genuinely belongs to no school yet, so there is nothing to resolve *from*, and refusing would simply delete public parent registration. `RegisterParentDto` declares **no tenant field of any kind** and the comment at `:358-361` says so, which makes this a **missing-resolution** finding, not an injection and not a privilege escalation — no caller value influences tenancy today. **The fork, recorded so it is chosen once instead of re-argued:** (a) the registrant selects a school and tenancy follows the selection — needs a public school directory plus an anti-enumeration story on an already rate-limited surface (`ADR-038`, `PF-182`); (b) registration creates a **`pending`, tenant-less** profile an admin later attaches — needs `UserStatus.pending`, and `apps/api/prisma/schema.prisma:31-35` declares only `active|suspended|deleted`, so it is an enum change and carries `G-MIGRATION`; (c) tenancy arrives as a Keycloak claim — `S-E01-4`, **blocked on decision `D-02`**. Closing this is blocked on a product decision the routine does not take unilaterally. Until it is taken, `demo` remains a real, reachable tenant that a **public, unauthenticated** endpoint creates | `BROKEN_SECURITY` | DEFECT | Discovered by the V3 run of 2026-08-14 (run 53), by the slice that closed the `ensureUser` half of `PF-01`, grepping `DEMO_TENANT_SLUG` across `apps/api/src` after deleting its own occurrence | L0 | V3-E01 |
| **PF-186** | **Identity adoption runs on a bare email string — unscoped by tenant, with `email_verified` discarded and `status` never consulted.** `apps/api/src/shared/auth/user-sync.service.ts:177-199`: when the `authProviderId` lookup misses, `findMany({ where: { email }, take: 2 })` decides membership, and a single match with a null `authProviderId` is **bound to the subject and its `tenantId` becomes the tenancy of the request** for ~242 call sites. Three depths, one finding. **(a) Unscoped.** `email` is unique only *per tenant* (`schema.prisma:879`), so "exactly one match" is a fact about today's data, not about the schema; which tenant to adopt when an address exists in two establishments needs a discriminant, i.e. **`D-02`**. `S-E01-1a` made ambiguity **refuse** rather than pick arbitrarily, so this is a fail-closed placeholder over an unanswered question, not a live cross-tenant grant. **(b) One row is not consent** — and this third needs no decision at all: `payload.email_verified` is parsed in `jwt.strategy.ts:12` and never read, and `status` is consulted nowhere in the auth seam, so a `suspended` or soft-`deleted` profile resolves *and* can be bound to a fresh subject, while a soft-deleted duplicate counts toward the ambiguity test and refuses the legitimate holder. **(c) The refusal has no UI**: `UNPROVISIONED_USER_CODE` exists so the web can branch on a machine code, but `apps/web/src/lib/me.ts:34` catches 401 only and `AppShellRoot.tsx:86` awaits `fetchMe()` in a layout server component, so the 403 lands on the portal error boundary whose message Next redacts to a digest — `ACCOUNT_NOT_PROVISIONED` appears nowhere under `apps/web/src`. **Ledger note:** this id was cited from `ADR-043` and two source files *before* any row existed for it, which is `TOOL-07`'s class at the finding level — the row is added here at land so the citations resolve | `BROKEN_SECURITY` | DEFECT | Discovered by the V3 run of 2026-08-14 (run 53), by the slice that closed the `ensureUser` half of `PF-01` | L0 | V3-E01 |
| **TOOL-19** | **The Docker ENGINE is wedged, not the docker CLI — three runs bounded the wrong layer.** Runs 44, 45 and 46 each recorded `docker ps` hanging (>150 s, then >12 min, with bash `timeout` failing to kill it) and concluded the defect was the docker CLI on Windows; run 44 added `spawnSync`-level bounds on that basis. Measured a layer down this run: `//./pipe/docker_engine` **accepts a connection**, so the daemon is listening and the pipe exists, but `GET /v1.43/containers/json?all=1` returns **nothing in 15 000 ms and then `ECONNRESET`s**. The CLI is not slow — it is blocked on an engine that never answers, and **no client-side bound can repair that**; it can only make the client give up sooner. `dockerd` and `com.docker.service` are both resident, alongside **11 orphaned `docker` CLI processes dated Aug 10–12** and **5 `docker-ai` processes** (newest 2026-08-13 02:06), the latter notable because this project's standing configuration records `EnableDockerAI=false`. **Consequence:** Step −1 tells the routine to rebuild a stale local stack, and that instruction is currently **not executable on this host** — `docker compose build` cannot reach an engine that will not answer. Everything parked behind "settle the database" (`S-E01-2b` RLS, `TOOL-13`'s drift-gate half, and the live-PostgreSQL path of `TOOL-10`'s preflight, which has still never executed) stays blocked for an **8th consecutive run**. The unblocking action is an **operator restarting the Docker engine**, after which the 11 orphans should be reaped. Two cheap standing checks, so no future run re-measures the CLI: `127.0.0.1:5433` refuses in **5 ms**, and the named-pipe probe above distinguishes *no stack* from *no engine*. |
| **PF-265** | **`attendance.controller.ts` `assertOwnership` resolves identity with `teachers.ensureForUser` — an `upsert`, i.e. a WRITE, on a path that may end in a 403.** Exactly the defect `ADR-051 §D1` names and that `TeacherProfileService.findForUser` was built to remove. Three call sites, all write-shaped (`listSessions`, `openSession`, `batch`), all outside the blast radius of the read slice that recorded this. `S-E05-5` deliberately did **not** refactor them — doing so inside the diff that proves the new read seam would corrupt the evidence for both — and instead added `assertSessionOwnership` beside it on `findForUser`, so the file now carries the before and the after side by side with a docblock saying which is which | `BROKEN_SECURITY` | DEFECT | Raised by `S-E05-5` (run 71, 2026-08-23), the slice that gave the attendance READ paths the ABAC their WRITE paths already had | L0 | V3-E05 |
| **PF-266** | **The `parent` realm role short-circuits the teacher branch of `GET /attendance/students/:studentId`.** The handler reads `if (roles.includes(‘parent’)) { …guardianship… }` as a **terminal** branch, so a caller holding both `parent` and `teacher` — a teacher whose own child attends the school — is evaluated on guardianship alone and refused a pupil they legitimately teach. Pre-existing; frozen to the byte by `S-E05-5`’s `AC-4`; reachable by no first-party caller (only the three parent pages call this route). The honest fix is to evaluate all three predicates and **OR** them, impossible while `AC-4` demands byte-identity. **The inverse is the trap, and `ADR-061 §D7` records why the obvious reordering was refused:** `privileged → guardian → teacher` would silently **grant** a `school_admin`-who-is-also-a-parent cross-family access — today they are limited to their own children — so a slice narrowing four handlers would have widened a fifth on its way past. Two tests in `attendance-read-abac.spec.ts` pin the order and go red if it is "tidied" | `BROKEN_RUNTIME` | DEFECT | Raised by `S-E05-5` (run 71, 2026-08-23), the slice that gave the attendance READ paths the ABAC their WRITE paths already had | L0 | V3-E05 |
| **PF-267** | **`POST /attendance/:id/justify` is the file’s eighth handler, its only unguarded mutation, and carried a `DNC-06` docblock.** It performs a tenant check and nothing else — no `assertOwnership` — while its docblock claimed *"admin or teacher of the class"*: a guide promising deeper behaviour than the runtime delivers, **inside the file `S-E05-5` was changing**, which would have made that slice’s `G-DNC` evidence dishonest. The docblock is therefore corrected in-slice (comment only, zero blast radius) to describe what executes and to name this row; **the missing ownership check is a WRITE-handler change and stays out of the read slice.** Live exposure is bounded because `attendance.justify` is realm-granted to `school_admin` alone (`permissions.constants.ts:188`) and never to `teacher` — so the gap becomes reachable exactly through the custom-role path of `PF-268`. Cheapest honest fix: assert ownership on the record’s session, reusing `assertSessionReadable` | `BROKEN_SECURITY` | DEFECT | Raised by `S-E05-5` (run 71, 2026-08-23), the slice that gave the attendance READ paths the ABAC their WRITE paths already had | L0 | V3-E05 |
| **PF-268** | **Privilege is decided by realm-role NAME at ~19 sites, while `PermissionsGuard` honours custom roles.** `roles.includes(‘super_admin’) \|\| roles.includes(‘school_admin’)` is house convention across `announcements`, `calendar`, `grades`, `assessments`, `lessons` and `attendance` — but `PermissionsGuard` resolves through `UserSyncService.effectivePermissions`, which unions realm grants with **DB custom-role** grants (`ADR-013`, `ADR-047`: custom roles are global). A tenant-defined « vie scolaire »/CPE role holding `attendance.read` therefore **passes the guard and is refused by the handler**. After `S-E05-5`’s `AC-5`, `@RequiresPermission(‘attendance.read’)` on `GET /attendance/overview` no longer describes who may call it — a permission-catalogue truth defect of `PF-53`’s family, and the first **documented** instance. **Measured 2026-08-23 on the local stack: `select count(*) from role` → 0, and 0 custom grants of `attendance.read` / `class_sessions.read` / `attendance.justify` — live outage risk empirically NIL today**, but `roles.write`/`roles.assign` are `school_admin` grants, so it can become non-nil without a deploy. Honest fix: a dedicated `attendance.read.establishment` code, forbidden by `AC-9` and owned by `ADR-015` | `BROKEN_TRUTH` | DEFECT | Raised by `S-E05-5` (run 71, 2026-08-23), the slice that gave the attendance READ paths the ABAC their WRITE paths already had | L0 | V3-E05 |
| **PF-270** | **Three divergent copies of the same authorization predicate — "does this teacher teach this child?"** `messaging.service.ts:90` `isTeacherOfStudent` and `remediation.service.ts:912` (whose own docblock says it *"mirrors messaging.service.ts isTeacherOfStudent exactly"*) already duplicate it; `S-E05-5` makes it **three**, on a **fourth join key** (`teacherProfileId`, because `AC-6` forces the read-only `findForUser`, versus the two copies’ `teacherProfile: { userProfileId }`). The new copy also deliberately **omits** the other two’s `try/catch → false`, which `PF-248` already records as a defect (an infrastructure fault rendering as a genuine ABAC refusal, undiagnosable). Both divergences are declared in `ADR-061 §D1` rather than left to be discovered. **Consolidation is blocked on a prior decision:** remediation’s copy runs inside a `TenantScopeService` scope while messaging’s runs on the owner connection, so a shared helper must first decide the connection question. Fix direction: one `TeachingWall` seam taking a resolved client as a parameter, the two existing copies delegating, and `PF-248`’s catch-narrowing landing in the same pass | `BROKEN_SECURITY` | DEFECT | Raised by `S-E05-5` (run 71, 2026-08-23), the slice that gave the attendance READ paths the ABAC their WRITE paths already had | L0 | V3-E01 / V3-E05 |
| **PF-271** | **`class_session.teacher_profile_id` duplicates `teaching_assignment.teacher_profile_id` with no database constraint linking them.** `ADR-061 §D3` rules that attendance ownership is compared against the **denormalised** column — the same one `batch` already trusts for the WRITE — so that a drifted row cannot 403 the roster of a session the same teacher may legally write (the teacher workspace calls `class-sessions/open` then `:id/roster` on the same session). Measured, so the ADR asserts rather than hopes: the columns **cannot** diverge through the application today — `teaching-assignments.controller.ts:191` `update` carries only `weeklyHours`, `isMainTeacher`, `role`, and `openSession` writes both from one source. **Nothing in the database enforces the equality** — no FK, no CHECK. `S-E05-5` makes the column **authorization-bearing on four handlers**, so a future backfill, import or admin tool that writes one without the other becomes a silent ABAC divergence. `PF-241`’s family; fix direction: a CHECK/trigger, or drop the column onto the relation | `BROKEN_TRUTH` | DEFECT | Raised by `S-E05-5` (run 71, 2026-08-23), the slice that gave the attendance READ paths the ABAC their WRITE paths already had | L0 | V3-E05 |
| **PF-272** | **A same-tenant existence oracle, newly introduced on two routes.** After `S-E05-5`’s `AC-1`/`AC-2`, an **in-tenant** session id the caller does not own answers **403** while an unknown id answers **404**, so the pair distinguishes existence *within* the establishment (pre-existing on `studentAttendance`’s parent branch). The **cross-tenant** half is closed and tested — `ADR-048 §D9` / `ADR-061 §D4`: tenant and existence are always evaluated before ownership, so a foreign-tenant id is indistinguishable from a nonexistent one. Same-tenant, hence P3, and consistent with the `lessons` house pattern — which is why fixing it here **alone** would be worse than leaving it: two neighbouring modules would then disagree about what a 404 means | `BROKEN_SECURITY` | DEFECT | Raised by `S-E05-5` (run 71, 2026-08-23), the slice that gave the attendance READ paths the ABAC their WRITE paths already had | L0 | V3-E05 |
| **PF-273** | **`super_admin` has no entry in `REALM_ROLE_PERMISSIONS`, so every `super_admin` short-circuit is unreachable.** The map’s keys are `school_admin`, `teacher`, `parent`, `student`. A caller holding **only** `super_admin` resolves to an empty permission set and is refused by `PermissionsGuard` **before any handler runs**, which makes `roles.includes(‘super_admin’)` dead code at all ~19 sites that write it — `attendance.controller.ts`’s pre-existing `assertOwnership` included. Two readings, and the project must pick one: either the catalogue is missing its most privileged role (a real gap, latent until someone provisions a pure `super_admin`), or nineteen guards carry a branch that has never executed. `S-E05-5` keeps the short-circuit for symmetry with the house idiom and explicitly does **not** claim it as a tested audience | `BROKEN_TRUTH` | DEFECT | Raised by `S-E05-5` (run 71, 2026-08-23), the slice that gave the attendance READ paths the ABAC their WRITE paths already had | L0 | V3-E05 |
| **PF-274** | **The teacher attendance error banner has no `role="alert"`, so the new 403 is announced to nobody.** `apps/web/src/app/teacher/classes/[id]/attendance/AttendanceManager.tsx:124` renders the refusal banner with no `role="alert"` and no live region: a teacher using a screen reader hears **silence** and finds an empty table where the roster should be. WCAG 2.2 AA §4.1.3 (Status Messages). Pre-existing and **not caused by** `S-E05-5` — but that slice makes the banner reachable on a **new** path, because `actions.ts:28` funnels `ApiError.body.message` verbatim into it (which is also why the new refusal string is production copy and was written as such: « Vous ne pouvez consulter que les séances de vos affectations. »). One-line fix in a file the backend slice may not touch (`GUARDRAILS` §4, disjoint file sets) | `BROKEN_RUNTIME` | DEFECT | Raised by `S-E05-5` (run 71, 2026-08-23), the slice that gave the attendance READ paths the ABAC their WRITE paths already had, by its UX lens | L0 | V3-E06 |

> **Amended by `S-E05-5` (run 71, 2026-08-23): `PF-264` … `PF-274` are declared here in the same commit that raised
> them — eleven ids, two into §2 and nine into §3.** This is the `TOOL-01` lesson applied prospectively: a finding whose
> id is cited by `traceability/OPEN.md`, by `ADR-061` and by `attendance.controller.ts` itself, but declared nowhere,
> cannot own a baseline row, and `scripts/audit-write-check.js` resolves owner ids against **this** file. §7’s counting
> summary is again **deliberately left as it stands** and is therefore stale by a further 11, knowingly, on exactly the
> terms the two amendments above set. **Id-collision note, since parallel runs have produced one before (`PF-185`/
> `PF-186`, runs 53/54):** open PR **#263** claims `PF-256`…`PF-263` and `ADR-060`; `main`’s maximum at allocation time
> was `PF-255`; this run therefore allocated from **`PF-264`** upward and took **`ADR-061`**. Nothing in the claimed
> range is reused.

> **Amended by `S-E03-2` (run 81, 2026-08-25): `PF-335` … `PF-353` are declared here in the same commit that raised
> them — nineteen ids, and `ADR-071`.** Same `TOOL-01` reasoning as the amendment above, applied to a slice that was
> **about** the cost of unrecorded ids: `S-E03-4` (run 80) shipped `PF-327`…`PF-330` in production comments and in no
> ledger file, and those rows had to be back-filled at land. This run wrote its own at land instead of inheriting the
> same debt. Of the nineteen, five were declared by `ADR-071` at planning (`PF-335`…`PF-339`), three are cited from
> production source (`PF-340` at `lessons.controller.ts:410`, `PF-342` at `grades.controller.ts:414`, `PF-344` at
> `student-authz-locality-gate.spec.ts:117,548`), and **eleven were allocated at the land pass by MEANING** from the
> escalation panel and the six review lenses (`PF-341`, `PF-343`, `PF-345`…`PF-353`). §7's counting summary is again
> **deliberately left as it stands** and is therefore stale by a further 19, knowingly, on the terms the amendments
> above set. **Id-collision note — one occurred INSIDE this run, which is the useful part:** `ADR-071:17` allocated
> `PF-337` to *three permissions for one datum* while the DS review lane independently allocated `PF-337` to the
> `ErrorState`/`EmptyState` heading skip. Two agents, one run, one id — the `PF-185`/`PF-186` pattern of runs 53/54,
> reproduced **without** parallel runs. It was separated at land **by MEANING**, never by pattern-replace (ids are
> cited from production source, so a blind replace edits the wrong file): the heading skip became **`PF-345`**.
> Allocation was checked against `main`'s maximum (**`PF-334`**, `ADR-070`) **and** against open PRs; nothing in
> `PF-335`…`PF-353` is reused.

> **PF-58 is the class of failure the routine is least able to see.** The planning work was done well and lost anyway,
> because "produced" was conflated with "committed". Same conflation as A2 App. A's *UI exists ≠ feature delivered*.
> Mitigation now in force: the in-repo `routine/` copies must be byte-identical to the installed artefacts, so drift is
> a one-line `diff` (see `README.md` → *Where the routine lives*). Related: the known Workflow worktree-path bug, where
> sprint agents edit the main repo rather than their assigned worktree — here the failure ran in the opposite direction.

## 4. Capability gaps proven by Lakoli (`MISSING_CAPABILITY`)

Sequenced *after* L0/L1 by design — see `roadmap.md` §3 for why.

| ID | Capability | Lakoli evidence | Pilotage today | Layer | Epic |
|---|---|---|---|---|---|
| **LG-01** | Fee catalogue: 18 fee types, schedule, cycle/class/service scope, non-retroactive scope changes | A1 §5.3, App. A.2 | Absent | L3 | V3-E15 |
| **LG-02** | Receivable ledger with balance/status/adjustments; post-payment identity lock | A1 App. A.2 | Absent | L3 | V3-E15 |
| **LG-03** | Counter payment: 8 modes, receipt, cashier session, journal | A1 App. A.2 | Absent | L3 | V3-E16 |
| **LG-04** | Daily cash closing with theoretical-vs-counted variance and formal PV | A1 App. A.2 | Absent | L3 | V3-E16 |
| **LG-05** | Two-layer reconciliation (internal ledger + provider settlement) | A1 App. A.2 | Absent | L3 | V3-E16 |
| **LG-06** | Discounts/bursaries, 8 default criteria, targeted grant | A1 App. A.2 | Absent | L3 | V3-E15 |
| **LG-07** | Refund and typed mass-cancellation as dedicated operations | A1 App. A.2 | Absent | L3 | V3-E16 |
| **LG-08** | Forecast-vs-actual budget | A1 App. A.2 | Absent | L4 | V3-E18 |
| **LG-09** | Online payment providers, payment links, signed callbacks | A1 App. C.3 | Absent | L3 | V3-E16 |
| **LG-10** | SMS campaigns: mandatory simulation, 16 trigger events, wallet, delivery log, J+7/30/60/90 ladder | A1 App. A.4 | Absent | L4 | V3-E17 |
| **LG-11** | WhatsApp deep-link assistants | A1 App. A.4 | Absent | L4 | V3-E17 |
| **LG-12** | Unified admissions funnel: `preinscription_creee → paiement_demande → paiement_recu → dossier_complet → validee` + document conformity states | A1 App. A.1 | Enrollment requests exist; approval not implemented | L2 | V3-E12 |
| **LG-13** | Re-enrollment campaign: 6 steps, 4 independent state axes | A1 App. A.1 | Absent | L2 | V3-E14 |
| **LG-14** | End-of-year decisions: 7 outcomes, 6 non-computability codes, publish/freeze/hash/reopen with reason | A1 App. A.1 | Absent | L2 | V3-E14 |
| **LG-15** | 10+ official document generators, registry, branding, QR authenticity | A1 App. A.7 | Bulletins/exports only; reports 404 | L2 | V3-E13 |
| **LG-16** | Timetable: rooms, slots, conflicts, workload, print | A1 App. A.3 | Absent | L4 | V3-E18 |
| **LG-17** | Discipline: incident/measure/convocation lifecycles with Direction validation | A1 App. A.6 | Absent | L4 | V3-E18 |
| **LG-18** | Clubs/activities: 18 types, 16 domains, publication, printed recap | A1 App. A.6 | Absent | L4 | V3-E18 |
| **LG-19** | HR: staff registry, 9 departments, 6 contract types, contract lifecycle | A1 App. A.5 | Absent | L4 | V3-E18 |
| **LG-20** | Statutory payroll: 19 rubric codes, CNPS/CMU/IRPP, batch validation | A1 App. A.5 | Absent | L4 | V3-E18 |
| **LG-21** | Staff timekeeping: 6 event types, bulk four-eyes, monthly report | A1 App. A.5 | Absent | L4 | V3-E18 |
| **LG-22** | Cafeteria / transport / other services as subscription + charge primitives | A1 App. A.2 | Absent | L4 | V3-E18 |
| **LG-23** | Student document control: `a_controler / conforme / a_corriger` with reason | A1 App. A.1 | Empty; no writer | L2 | V3-E13 |
| **LG-24** | Guided tours (63) + 10-step onboarding checklist + 73 help articles | A1 App. A.7 | Thin guidance; help 404 | L2 | V3-E13 |
| **LG-25** | Tenant exit export: reason, name confirmation, idempotency key, SHA-256 manifest | A1 App. A.7 | Exports exist; no whole-tenant package | L2 | V3-E13 |
| **LG-26** | Sensitive follow-up behind **nominative, time- and domain-limited habilitation** | A1 App. A.6 | Absent | L4 | V3-E18 |
| **LG-27** | Bulk import: aliases, preview, 2 000-row cap, receivable side effect, double confirmation | A1 App. A.1 | Async design exists, **runtime unproven** | L2 | V3-E12 |
| **LG-28** | Period closure lifecycle with prechecks; reopen depublishes official results | A1 App. A.3 | Absent | L2 | V3-E14 |
| **LG-29** | Lesson book `draft → submitted → visa → correct` | A1 App. A.3 | Lessons default Published, edit broken | L1 | V3-E08 |

## 5. `DO_NOT_COPY` register — Lakoli behaviours V3 must consciously avoid

Sourced from A3 Appendix C and A1 §11. **V3's routine must fail a story that reproduces any of these.**

| ID | Lakoli behaviour | Why it must not be copied |
|---|---|---|
| **DNC-01** | Debt KPI (~5k) disagrees with detailed ledger (~8k) | Finance loses trust; build ledger invariants first |
| **DNC-02** | Future-dated attendance accepted | Official registers become impossible |
| **DNC-03** | Discipline event date drifts after save | Safeguarding chronology fails |
| **DNC-04** | Public and staff pre-enrollment are separate silos | Duplicate applicants and manual matching |
| **DNC-05** | HR → teacher assignment email deadlock | Cross-module onboarding blocks |
| **DNC-06** | Guides promise deeper behaviour than runtime delivers | Expectation debt |
| **DNC-07** | WhatsApp templates/state in `localStorage`; one hard-coded client template | No central audit or delivery truth; tenant leakage |
| **DNC-08** | AI health audit missed manually reproduced defects | Automation is a signal, never a release gate |
| **DNC-09** | Gated shipped modules labelled "coming soon" | Bundle/support/security ambiguity |
| **DNC-10** | Hard-coded demo-account billing bypass | Backdoor by string comparison |
| **DNC-11** | Refused applicant leaks into official document population | Admission-state/privacy boundary failure |
| **DNC-12** | "Irreversible" cash close contradicted by later mass-cancellation tooling | Accounting policy incoherence |

## 6. Validation obligations (not code — evidence)

| ID | Obligation | Source | Owner lens | Gate |
|---|---|---|---|---|
| **VAL-01** | Clean dependency install + full CI run (lint, typecheck, unit, integration, Playwright, a11y) — **blocked by `PF-59`** (no runner starts) | A2 §13, §16 | Engineering | G1 |
| **VAL-02** | Two-tenant adversarial suite on every read/write/export/job | A2 §16, App. E | Security | G0 |
| **VAL-03** | Migration upgrade/downgrade + backup/restore rehearsal, timed | A2 §16; A3 §8 Phase 0 | Operator | G1 |
| **VAL-04** | Production Keycloak client/redirect/audience review | A2 §16 | Security | G0 |
| **VAL-05** | Provider sandbox: delivery, retry, callback, outage, reconciliation | A2 §16; A3 §4.3 | Finance/Comms | G4/G6 |
| **VAL-06** | Import batch validation → apply → rollback with a synthetic batch | A2 §16; A4 §4.2 | Data migration | G6 |
| **VAL-07** | Custom-role create/edit/deny scenarios | A2 §16 | Security | G0 |
| **VAL-08** | Full WCAG keyboard / screen-reader / contrast audit | A2 §16 | Accessibility | G7 |
| **VAL-09** | Load, queue/DLQ, object-storage and observability/SLO tests | A2 §16 | Operations | G7 |
| **VAL-10** | Confirm which build SHA + Prisma schema version actually run in web/API/worker | A2 App. I Q1–Q2 | Operator | G1 |

## 7. Counting summary

| Bucket | Count |
|---|---|
| P0 defects/prerequisites | 12 |
| P1 | 24 *(+`PF-102`)* |
| P2/P3 | 43 *(+`PF-71`, +`PF-97`…`PF-101`, +`PF-103`, +`PF-104`…`PF-112`, +`PF-113`…`PF-115` run 26, +`PF-116`…`PF-118` run 27, +`PF-119`…`PF-120` run 28, +`PF-121`…`PF-123` run 29, +`PF-124`…`PF-127` run 30, +`PF-128`…`PF-133` run 31)* |
| Lakoli capability gaps | 29 |
| Do-not-copy rules | 12 |
| Validation obligations | 10 |
| **Total tracked items** | **149** *(+`PF-128`…`PF-133`, run 31 — one **closed on arrival** (`PF-128`, the second `apps/web` server seam `ADR-036`'s Context table did not name, found by measuring the document rather than implementing against it) and five open: a third seam with no gate over the invariant, provenance headers accepted from the public edge, one nginx `location` that breaks the uniformity of the `N = 2` pin, a shipped forward-token placeholder that turns « null rather than wrong » into « plausible and wrong » (`PF-132`, now `PARTIAL` — the example files ship empty, the read site still refuses nothing), and a server/client import boundary that **no artefact asserts** (`PF-133`, surfaced only because the routine ran the full gate on a tree the sprint had already reported `landed: true`); +`PF-124`…`PF-127`, run 30 — one incidental observation on a surface the run merely passed through, one timing measurement filed **without** a diagnosis, one build-reproducibility defect filed only after its two obvious explanations were falsified, and one gap found by interrogating a **green** gate; +`PF-72`, `PF-73`, `PF-74`, `PF-75`, `PF-76`, `PF-77`, `PF-78`, `PF-79`; +`PF-97`…`PF-103`, run 23; +`PF-104`…`PF-111`, run 24; +`PF-112`, the ninth finding that had been sharing `PF-104`'s id — the count said 8 while 9 rows existed; +`PF-113`…`PF-115`, run 26 — **rows existed since 2026-08-08 but were never counted**, corrected by run 27's land pass; +`PF-116`…`PF-118`, run 27; +`PF-119`, `PF-120`, run 28; +`PF-121`, `PF-122`, `PF-123`, run 29 — `PF-122`/`PF-123` are the two provenance decision sites **inside** the walk root that `S-E04-1`'s `AC-1` claimed did not exist, registered by the same land pass that narrowed the claim)* |

Delta since the 2026-08-02 baseline: **+13**. Four were discovered by V3 runs on 2026-08-02 —
`PF-58` (substrate not on `main`, §3), `PF-59` (Actions billing lock, §1),
`PF-60` (sprint workflow overrides story selection, §2) and `PF-61` (duplicate `R-17` id, §3).
The fifth, `PF-71`, was discovered on 2026-08-03 (run 9) by the act of making the lint gate executable: the 996
warnings it counts had existed all along and were simply unmeasurable while no package could load a config.
The sixth, `PF-72`, was discovered on 2026-08-03 (run 11) by the act of making the *boot* gate executable — the worker
had been building to an empty `dist/` behind a green `pnpm build`, and no gate that reads source could ever have seen
it. `PF-73` was found in the same run while verifying *why* the boot check cannot run inside jest. `PF-74` (run 13)
was `R-25`'s residual half at the web address. `PF-75` and `PF-76` were found on 2026-08-03 (run 14) by the act of
making the *runtime declaration* checkable: `engines.pnpm` blessed a pnpm major that never produced this lockfile, and
`.nvmrc`/`ci.yml` pinned a floating Node major that includes the window where the API cannot boot. `PF-78` was found on
2026-08-03 (run 15) the same way — making the *observability profile* startable exposed that a Jaeger collector and an
`OTEL_EXPORTER_OTLP_ENDPOINT` are declared for three applications, none of which emits a span. `PF-79` was found on
2026-08-03 (run 16) while **closing** `PF-78`: enumerating which services actually receive the OTLP endpoint showed it
was **five**, not three — the variable sat on the shared environment anchor, so the two one-shot jobs got it too — and
narrowing it to the two applications that emit left `apps/web` observable by nothing at all, which needed an id rather
than a silence. All follow the same pattern as `PF-62`: **turning a gate on is what finds the defect it was written
for.**

`PF-91`, `PF-92`, `PF-93` and `PF-94` (run 21, 2026-08-07) are the largest single instance of that pattern so far:
`scripts/link-integrity-check.js` was written to close **one** finding (`PF-19`, a link swallowed by a `[param]`), and
its first execution returned **24** dead internal targets where the story had measured 15 — nine of them auth routes
the middleware explicitly waves through. Nothing in the repository could previously answer *"which internal links point
at nothing?"*, so the debt was not hidden, it was **unmeasurable**. All four are inventoried in
`scripts/link-integrity-baseline.json` with an owning id and a reason; none is fixed.

`PF-77` is the exception to that pattern, and worth naming as such: it was not found by turning a gate on, it was found
by **being bitten**. The routine's own single-writer lock expired mid-run and a concurrent V3 tick reaped it, after
which recovering by re-running `gate` stashed and reset the working tree. It is the first finding in this register
whose subject is the routine's *own* machinery rather than the product, and it is `CRITICAL` (`R-27`) because the
guarantee it breaks — one writer at a time — is what every other run has silently depended on.

`PF-97` … `PF-103` (run 23, 2026-08-07) continue the run-21 pattern one layer up: `S-E06-5` set out to retire nine
static rows of that same baseline, measured the extractor first, and found the gate itself was blind to **every**
template-literal href in the application — so « Mon profil » had been 404-ing on every authenticated page of three
portals behind a `LINK INTEGRITY CHECK: PASS`. `PF-97` is that blindness and is **closed** by the slice; `PF-98`
(three link families that only became visible once it was closed), `PF-99` (the profile capability itself),
`PF-100` (an orphaned duplicate of the shell menu) and `PF-101` (two portal vocabularies of different sizes) are its
queued consequences; `PF-103` is the new lexer's own residual debt, found the same way — by measuring it rather than
reading it.

*(Bookkeeping note, run 23: all seven ids above were being **cited as owners before they existed** — `PF-98` was the
`finding` value on three live rows of `scripts/link-integrity-baseline.json`, and `scripts/link-integrity-check.js`
validates only the id's **shape** (`/^(PF|R|VAL|D)-\d+$/`), never its existence. So the baseline's own contract —
*"every entry carries the finding id that owns the fix, so nothing here is 'silenced' — it is queued"* — was false for
three real dead targets: they were silenced, by a green gate, one level up from the hole the slice existed to close.
Repaired inline here, the same way run 9 repaired `PF-61` and run 14 repaired `PF-74`. **The durable fix is to make the
gate resolve the id against this index instead of against a regex** — recorded in `PF-103`'s family and owed by the next
`V3-E06` follow-up. `PF-102` was added in the same pass: it is a pre-existing post-authentication open redirect the
verify panel found while checking `S-E06-5`'s AC-7 wording, and it had no id at all.)*

*(Bookkeeping note, run 14: `PF-74` was recorded in `traceability-matrix.md` and `risk-register.md` by run 13 but never
added to this index, so the register the routine reads at Step 2 was missing a finding the matrix already tracked.
Repaired inline, the same way run 9 repaired `PF-61`. The check this run built has no equivalent for the planning
substrate itself — a matrix row with no index entry is exactly the class of defect V3 keeps finding in code, and here
it is in the ledgers.)*

`PF-104` … `PF-110` (run 24, 2026-08-07) are the same pattern a third time, and the sharpest instance of it yet:
`S-E02-17` instrumented BullMQ and wired the check that watches the instrumentation into a **blocking** CI stage — and
three of the seven findings say that stage and its dashboard cannot see the failure they were written for. `PF-105`
(check 9's rule 6 compares a constant with labels rendered *from* that constant) and `PF-106` (no test executes a single
one of the six new `@OnWorkerEvent` handlers, nor the collector's `onModuleInit`) are the same defect class as `PF-83`,
`PF-97` and `PF-103`: **a guard that cannot fail**, arriving inside the diff written to close a guard that could not
fail. `PF-104` is the product half of it — a worker killed mid-job emits `stalled`, never `failed`, so the
terminal-failure series under-reports exactly when the system is broken. `PF-107`/`PF-108` are the dashboard's two
(the diagnostic series the panel description names is plotted nowhere; a shared-resource gauge is `sum`-med across
replicas), and `PF-109`/`PF-110` are bookkeeping debts with teeth — a `DNC-06` guard defeated by a French plural in the
file it inspects, and an ADR number taken from a block `architecture-impact.md` §4 had reserved for `V3-E04`. None is
fixed; all seven are owed by `S-E02-18`, which the roadmap now names as the next slice **ahead of** `PF-102`.

Every one of these appears in `traceability-matrix.md` with its epic, story, test and evidence slot.

*(Bookkeeping note, run 36 — `S-E04-10` / TOOL-01. The ledger had been citing **26** finding ids this register could
not resolve, which is what makes `scripts/audit-write-check.js` unable to bind a baseline: `PF-134`…`PF-146`,
`PF-148`…`PF-150`, `PF-153`…`PF-162`. They are appended above — 5 into §2, 21 into §3 — as a **register append only**:
no existing row is edited, nothing is renumbered, and §7's counting summary is deliberately **left as it stands**.
It is therefore **stale by construction and knowingly so**, which is recorded here rather than silently corrected:
P1 reads 24 and is now 29, P2/P3 reads 43 and is now 64, and **Total tracked items** reads 149 and is now **175**.
`grep -oE "PF-[0-9]+" audit-findings-index.md | sort -u | wc -l` returns **159** unique ids, up from 133. Two things
this append does NOT do. First, `PF-150`'s **id collision** is left in place: `traceability/OPEN.md` and
`docs/spec/features/v3-e04/PROGRESS.md:1353` use it for two different findings, and renumbering belongs in both files
at once or not at all. The row above is the `OPEN.md` one, and it says so. Second, the source instruction was
**falsified while executing it**: `PF-134`, `PF-135`, `PF-137`, `PF-138`, `PF-139`, `PF-143`, `PF-144` and `PF-161`
have **zero** rows in `traceability/OPEN.md` — they live in `traceability/CLOSED-L0.md`, having been closed by
`S-E04-4` and `S-E04-5` — so **8** rows are sourced from there and carry class `CLOSED`, exactly as `PF-120` and
`PF-128` already do, and 18 from `OPEN.md`. Three further open ids raised by `S-E04-5` — the run-33 residuals in the
147–152 range, held back from the list above — are in **neither** file, so no description exists to append and none
was invented. Their id tokens are deliberately **not written here either**, so that `grep -oE "PF-[0-9]+"` keeps
counting rows rather than prose: they stay unresolvable by id, and they are the next register debt, not this one.
**Amended by the routine at land: `PF-163` and `PF-164` are appended too, making 28 rather than 26.** They did not
exist when this slice was scoped — they were raised by `S-E04-7` and reached `OPEN.md` only when **PR #208 merged
mid-run**, which re-opened the very gap TOOL-01 had just closed. Appending them is the difference between the next
story being executable and being blocked: resolution **(b)** for `PF-163` writes a baseline row *owned by `PF-163`*,
and `scripts/audit-write-check.js` resolves owner ids against **this file**, so it would have refused the row. Unique
ids are therefore **161**, not 159, and §7's totals are stale by 28 rather than 26 — knowingly, on the same terms.)*

> **Amended again by the routine at land of `S-E04-9` (run 37): `PF-165`, `PF-166`, `PF-167` and `TOOL-03` are
> declared here in the same commit that raised them, making 32 appended ids and **165** unique.** This is the
> `TOOL-01` lesson applied prospectively rather than retroactively: run 36 had to append 28 ids *after* the register
> became the binding constraint on the mechanism it feeds, and paid a blocked story for it. A finding whose id is
> cited by the ledger but declared nowhere cannot own a baseline row, so declaring at raise-time — not at first use —
> is what keeps the next slice executable. `PF-163` is **closed** by this run and its row above is left in place with
> its class updated; closed ids stay declared, exactly as `PF-161` and `PF-120` already do. §7's totals are now stale
> by 32, knowingly, on the same terms.
