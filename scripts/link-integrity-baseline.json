{
  "$comment": "Dead internal link targets, read off the EMITTED Next.js route inventory (.next/app-path-routes-manifest.json) by scripts/link-integrity-check.js, never off the app/ directory tree. Regenerate deliberately with `node scripts/link-integrity-check.js --update`; the diff is meant to be reviewed. A dynamic capture can never appear here — it is always a failure and --update refuses to record one.",
  "$doc": [
    "Dead internal link targets tolerated until their owning finding is closed.",
    "This list may only ever SHRINK.",
    "Every entry carries the finding id that owns the fix, so nothing here is \"silenced\" — it is queued.",
    "An entry whose target resolves again, or that nothing links to any more, is a FAILURE: remove it."
  ],
  "dead": {
    "/admin": {
      "finding": "PF-93",
      "reason": "middleware.ts:130 `pathname.startsWith('/admin')` — a portal-prefix predicate, not an href. Inventoried rather than excluded because dropping middleware.ts would also hide the nine phantom auth routes (PF-91), which are real. It is ALSO a genuine navigation gap: the bare portal root has no index route, so typing it 404s instead of reaching /admin/dashboard. Owner: V3-E06."
    },
    "/admin/accept-invite": {
      "finding": "PF-91",
      "reason": "middleware.ts AUTH_ROUTES_BY_PORTAL exemption for a route that does not exist. Not a navigation link; an invitation email lands on a 404 and the exemption list makes it look handled. Owner: V3-E05."
    },
    "/admin/forgot-password": {
      "finding": "PF-91",
      "reason": "middleware.ts AUTH_ROUTES_BY_PORTAL exemption for a route that does not exist. Not a navigation link; a password-reset email lands on a 404 and the exemption list makes it look handled. Owner: V3-E05."
    },
    "/admin/reports": {
      "finding": "PF-14",
      "reason": "Sidebar item « Rapports » (components/shell/sidebar-items.ts:175). /teacher/reports exists; the admin page was never built. Owner: V3-E04."
    },
    "/admin/reset-password": {
      "finding": "PF-91",
      "reason": "middleware.ts AUTH_ROUTES_BY_PORTAL exemption for a route that does not exist. Not a navigation link; a password-reset email lands on a 404 and the exemption list makes it look handled. Owner: V3-E05."
    },
    "/api/auth": {
      "finding": "PF-93",
      "reason": "middleware.ts:35 PUBLIC_PREFIXES entry — the NextAuth namespace root, used with startsWith. /api/auth/[...nextauth] is a catch-all and needs at least one segment, so the bare namespace resolves to nothing by construction. Not user-facing and not a defect; inventoried because a static extractor cannot tell a prefix constant from an href. Owner: V3-E06."
    },
    "/contact": {
      "finding": "PF-94",
      "reason": "Public landing footer « Contact » (app/page.tsx:788), on the unauthenticated marketing surface. Same class as /help: linked from a shipped page, never built. Owner: V3-E06."
    },
    "/favicon": {
      "finding": "PF-93",
      "reason": "middleware.ts:35 PUBLIC_PREFIXES entry — an asset prefix for /favicon.ico, used with startsWith. Not a route and not a defect; inventoried because a static extractor cannot tell a prefix constant from an href. Owner: V3-E06."
    },
    "/help": {
      "finding": "PF-39",
      "reason": "« Centre d'aide » in components/shell/TopbarUserMenu.tsx:58, rendered for every role on every authenticated page. Owner: V3-E06 / S-E06-4."
    },
    "/legal": {
      "finding": "PF-38",
      "reason": "middleware.ts:35 PUBLIC_PREFIXES entry — the legal namespace root, used with startsWith. It resolves the day S-E06-4 ships the /legal/* pages; until then it is dead for the same reason they are. Blocked on decision D-08."
    },
    "/legal/cookies": {
      "finding": "PF-38",
      "reason": "Consent link (app/page.tsx:787). Owned by S-E06-4, blocked on decision D-08 — the routine may ship a holding page but may never author policy text (R-13)."
    },
    "/legal/privacy": {
      "finding": "PF-38",
      "reason": "Consent link (app/page.tsx:785, parent/register/ParentRegisterForm.tsx:176, components/AuthSplitLayout.tsx). Owned by S-E06-4, blocked on decision D-08 — the routine may ship a holding page but may never author policy text (R-13)."
    },
    "/legal/terms": {
      "finding": "PF-38",
      "reason": "Consent link (app/page.tsx:786, parent/register/ParentRegisterForm.tsx:172). Owned by S-E06-4, blocked on decision D-08 — the routine may ship a holding page but may never author policy text (R-13)."
    },
    "/parent": {
      "finding": "PF-93",
      "reason": "middleware.ts:132 `pathname.startsWith('/parent')` — a portal-prefix predicate, not an href. ALSO a genuine navigation gap: the bare portal root has no index route, so typing it 404s instead of reaching /parent/dashboard. Owner: V3-E06."
    },
    "/parent/forgot-password": {
      "finding": "PF-91",
      "reason": "middleware.ts AUTH_ROUTES_BY_PORTAL exemption for a route that does not exist. Not a navigation link; a password-reset email lands on a 404 and the exemption list makes it look handled. Owner: V3-E05."
    },
    "/parent/remediation": {
      "finding": "PF-92",
      "reason": "parent/dashboard/_components/RemediationProgressStrip.tsx:222,233. /parent/remediation/[planId] exists; /admin/remediation and /teacher/remediation both have an index — parent is the only portal without one. The portal-asymmetry shape G-PORTAL exists to catch. Owner: V3-E07 / V3-E06."
    },
    "/parent/reset-password": {
      "finding": "PF-91",
      "reason": "middleware.ts AUTH_ROUTES_BY_PORTAL exemption for a route that does not exist. Not a navigation link; a password-reset email lands on a 404 and the exemption list makes it look handled. Owner: V3-E05."
    },
    "/parent/verify-email": {
      "finding": "PF-91",
      "reason": "middleware.ts AUTH_ROUTES_BY_PORTAL exemption for a route that does not exist. Not a navigation link; a verification email lands on a 404 and the exemption list makes it look handled. Owner: V3-E05."
    },
    "/pricing": {
      "finding": "PF-94",
      "reason": "Public landing footer « Tarifs » (app/page.tsx:771), on the unauthenticated marketing surface. Same class as /help: linked from a shipped page, never built. Owner: V3-E06."
    },
    "/student": {
      "finding": "PF-93",
      "reason": "middleware.ts:133 `pathname.startsWith('/student')` — a portal-prefix predicate, not an href. ALSO a genuine navigation gap: the bare portal root has no index route, so typing it 404s instead of reaching /student/dashboard. Owner: V3-E06."
    },
    "/teacher": {
      "finding": "PF-93",
      "reason": "middleware.ts:131 `pathname.startsWith('/teacher')` — a portal-prefix predicate, not an href. ALSO a genuine navigation gap: the bare portal root has no index route, so typing it 404s instead of reaching /teacher/dashboard. Owner: V3-E06."
    },
    "/teacher/accept-invite": {
      "finding": "PF-91",
      "reason": "middleware.ts AUTH_ROUTES_BY_PORTAL exemption for a route that does not exist. Not a navigation link; an invitation email lands on a 404 and the exemption list makes it look handled. Owner: V3-E05."
    },
    "/teacher/forgot-password": {
      "finding": "PF-91",
      "reason": "middleware.ts AUTH_ROUTES_BY_PORTAL exemption for a route that does not exist. Not a navigation link; a password-reset email lands on a 404 and the exemption list makes it look handled. Owner: V3-E05."
    },
    "/teacher/reset-password": {
      "finding": "PF-91",
      "reason": "middleware.ts AUTH_ROUTES_BY_PORTAL exemption for a route that does not exist. Not a navigation link; a password-reset email lands on a 404 and the exemption list makes it look handled. Owner: V3-E05."
    }
  }
}
